# T350 換行契約（治本，鐵律20）：本專案把「位元」當契約——T274 等回歸測試會拿 index.html
# 的指定代碼區塊與 backups/ 的舊版逐字比對，一旦 Windows 的 autocrlf 把 LF 轉成 CRLF，
# 整套位元級測試立刻紅（已發生兩次：一次 git checkout 還原、一次 git worktree add 建車位）。
# 這裡明確宣告：所有文字檔在倉庫與工作區一律 LF，不受 core.autocrlf 影響。
*           text=auto eol=lf
*.html      text eol=lf
*.js        text eol=lf
*.json      text eol=lf
*.md        text eol=lf
*.py        text eol=lf
*.txt       text eol=lf
*.svg       text eol=lf
*.webmanifest text eol=lf

# 備份快照＝位元契約（T269 釘 manifest.pre-T269.json 的 SHA-256、T274 逐字比對舊版代碼區塊）：
# 一律不做任何換行轉換，checkout 出來必須與提交時逐位元相同。
backups/**  -text
attic/**    -text

# 二進位：絕不做任何轉換
*.png       binary
*.jpg       binary
*.ico       binary
*.wav       binary
*.mp3       binary
