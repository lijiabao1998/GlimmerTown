# T383 — 守衛債三條:尺寸表一致性/稅收窮舉/live 亂數 token 快照

狀態:**已完成(不 bump,直寫 master;PASS 3328→3350)。自審通過,破壞性五案全過。**
發卡:業主 loop 直令(2026-08-02,「先補一次守衛債…選三個低成本缺口」,GPT 順位 0)。
出卡+施工+自審:Claude(業主 loop 授權;依 T380a/T381 慣例附破壞性證明與披露)。
前置:master `87bb1e7`(v11.7,PASS 3328);三車位 clean ahead=0;receipt=match。
路徑:**純測試卡,不 bump、直寫 master**(T371/T371b/T370a 三條先例;bump 唯一理由=動 index.html/sw.js 執行期位元組,本卡零位元組)。

> **自審風險披露**:出卡/施工/覆核同為 Claude,業主 loop 明示授權。補償=每條守衛附
> 破壞性證明(注入→紅,判紅看 exit code+FAIL 行)＋「應綠」邊界案(證明不亂咬)。

---

## 一句話

把 ARCH §10 的三條「有規矩沒機器守」變成機器守:①五份多格尺寸表全鍵嚴格相等(§10.1);
②KNAME 133 鍵稅收分支執行期窮舉(§10.2,鐵律14);③live `buildSprites` 亂數 token
總數+行序 CRC 快照(§10.4,補 T272/T274 凍結快照的 live 缺口)。

## 偵查結論(四路工作流+親手覆驗,全部實測)

1. **ARCH §10.1 的「SZC/SZM 各缺 20 鍵」已過時**:T378 已補齊。現況五表
   (MSZ 18102+18103 / SZC 8630+8631 / SZB 8656-8658 / SZM×2 @18670,18692)全部 65 鍵、
   鍵值逐項相等(親手 python 覆驗 miss/extra/wrong 全空)。唯一凍結債:**k9 設計性不入任何表**
   (18102 註解;8698 `||1` 色環減半是另案視覺債,本卡不修只凍結)。
   字面量份數指紋:`const MSZ=`×1、SZC×1、SZB×1、SZM×2;`Object.assign(MSZ/SZC/SZB`各×1、SZM×0。
2. **稅收鏈** @12105-12245,四種守衛式樣混用(等值空守衛/等值帶稅/範圍/查表 LMCFG309);
   現況 133/133 全覆蓋;既有斷言只有一條字串 includes(test 4648)。漏鍵的必然落點=工業稅
   fallback `else{const eduIndMul=b.lv===3`(全檔唯一,親驗)。JOBSI 只有 0-3 ⇒ 漏鍵升 lv4=NaN。
3. **live buildSprites**(1432..`\nfunction wealthSpr(` 前,錨點全檔唯一):RAW 含註解 229 token
   其中 111 個只活在註解 ⇒ **必須剝註解**(六態狀態機+行數 canary);STRIPPED 實測
   118 token = randX62/ri28/randC20/stx8(工作流與親手 python 兩路一致)。
   **基線協定:總數與 seqCRC 以守衛自身 JS 抽取器產出為準**(python 參考值 CRC 與實作序有差,
   不得跨實作抄數字——4161/4224 教訓)。
4. 慣例:純測試卡不 bump(CHANGELOG 註「不 bump 版本;PASS X→Y」),直寫 master,
   驗收欄自律具名;verify.py 版本檢查只驗相等不驗變動,receipt 不受影響。

## 要改什麼

1. `test_fixde.js` 注入區(~230,T343 注入之後、eval 之前):
   - `inject343` 加稅 fallback 記錄器:`else{if(window.__taxFbk)window.__taxFbk.push(b.k);const eduIndMul=…`
     (閘控於 `__taxFbk`,未設時零行為差)。
   - IIFE 尾端追加 `window.__t383TaxCase`(仿 `__t343TickCase`:newWorld 固定種子、
     stub R/hasRoadNear/computePower/computeWater/disastersOn、直寫 133 鍵各一棟、tick 一次、
     還原、回傳 {kn,bad,money})。
2. `test_fixde.js` 尾端(FIX-D/FIX-E 完成標記之前)三個守衛塊:
   - **T383a 尺寸表**:regex 解析五表(T378 K1 先例),份數斷言 1/1/1/2,MSZ 鍵數≥65 棘輪,
     k9 不入表凍結,四副本對 MSZ 逐鍵 miss/extra/wrong 全零,訊息點名鍵。
   - **T383b 稅收窮舉**:`__t383TaxCase()`,斷言 kn≥133、bad(fallback 去 k3 後)為空、money 有限。
   - **T383c 亂數快照**:段界定→剝註解(行數 canary)→tokenize→斷言總數與 seqCRC 等於基線常數;
     基線由本卡實測釘死,後續合法改動同卡更新並在卡面記 delta(PASS 棘輪同款)。
3. `docs/ARCH.md` 同步(維護條款強制):§10.1 改寫為「已由 T383 機器守;殘餘=k9 不入表」、
   §10.2/§10.4 標記已收口、§9 不變量5 行號更新(18102/8630/8656/18670/18692)、
   §1 buildSprites 範圍與 §10.4 的 test 行號修正。
4. `docs/CHANGELOG.md` 一條+本卡狀態/施工記錄+loop 帳本。

## 允許觸碰

`test_fixde.js`(僅新增;不改既有斷言)、`docs/ARCH.md`(上列段落)、`docs/CHANGELOG.md`、
`docs/tasks/T383-*.md`、`docs/tasks/LOOP-自主迭代-202608.md`。

## 禁止觸碰

1. **`index.html`/`sw.js` 零位元組**(這是不 bump 的前提;若被迫動,升級走 bay+bump 重裁)。
2. verify.py 釘住的 harness 脊椎:唯一 `function assert(`、唯一 PASS producer、唯一
   `process.exit(0)`、DONE_CONTRACT 段、完成標記行。
3. 既有斷言與兩釘種子(seed301=4153/seed22=4550)一律不動。
4. 記錄器注入必須閘控(`window.__taxFbk` 未設=零行為差),不得影響其他測試段的模擬數值。
5. LF-only(CRLF=0);tools/ 不動。

## 不變量

1. 套件 0 FAIL,PASS 只增(3328→實測值入 CHANGELOG);verify ALL GREEN;工具鏈 54/54。
2. 三守衛落地當下全綠(現況實測即基線)。
3. `index.html` 位元組不變(git diff 空)⇒ 指紋/亂數流/存檔零影響,receipt 維持 match。

## 驗收清單(破壞性證明,複製整組檔案到測試目錄跑,防 fail-fast 遮蔽)

1. 尺寸表:副本 SZC 刪一鍵→紅並點名;SZM(foot) 改一值→紅並點名。
2. 稅收:副本刪 `else if(b.k>=124&&b.k<=133);` 行→紅並點名落入 fallback 的鍵群。
3. 亂數:副本 buildSprites 中段插一行 `const __z383=rand();`→紅(總數 119≠118)。
4. **應綠②案**:①副本只改 buildSprites 內某條含 `rand(` 字樣的註解文字→全綠
   (證明剝註解生效=T359「註解不得含 rand(」紀律自此對 live 守衛免疫);
   ②master 原樣→全綠。
5. 閘門:verify ALL GREEN、PASS≥3328 且 0 FAIL、`python -B -m unittest tools.test_toolchain`。

## 回滾

單一 commit 直寫 master,`git revert` 即回;零執行期影響。

---

## 施工記錄(Claude,2026-08-02)

### 閘門
`node test_fixde.js` **PASS 3328→3350 / 0 FAIL**;`verify.py` **ALL GREEN**(11.7 不動);
`tools.test_toolchain` **54/54 OK**;`git diff index.html` **空**(零位元組承諾兌現)。

### 破壞性五案(整倉複製到測試目錄;紅案判定=exit≠0 且首條 FAIL 含 T383)

| 案 | 注入 | 結果 |
|---|---|---|
| m1 | SZC 刪 k54(避開 T378/T364b 舊字面斷言=隔離本守衛) | **紅** `T383a SZC 缺 MSZ 的鍵…:k54` |
| m2 | SZM(foot) 113:4→113:3 | **紅** `T383a SZM(foot) 尺寸值與 MSZ 不一致:k113(3≠4)` |
| m3 | 刪 `else if(b.k>=124&&b.k<=133);` | **紅** `T383b …k124,k125,…,k133 落入工業稅 fallback` |
| m4 | buildSprites 中段插 `const __z383=rand();` | **紅** `T383c token 總數應為 118,實得 119` |
| m5 | buildSprites 內加含 `rand(/R()/ri(` 字樣的行尾註解 | **綠(正確)** — 剝註解生效實證 |

### 過程錯誤(誠實記錄)
第一輪證明環境自己壞:測試目錄只複製了 index/test/sw/docs/backups,套件還要讀
`tools/spr_families.json` 與 `atlas.html` → 四案全是 ENOENT 崩潰假紅(exit 1 無 FAIL 行)。
若當時只看 exit code 就會把「環境壞」當「守衛咬」收工 —— 正是 T343c 教訓的形狀。
重做=整倉複製(除 .git)+紅源驗明(首條 FAIL 須含 T383)。m3 的紅順帶揭露:合成城裡
k127 健康門檻不滿足時也靠 124-133 範圍守衛兜底,其專屬稅分支(12184)覆蓋範圍見卡面風險。

### 基線交叉驗證
T383c 基線由守衛同款 JS 抽取器實測(118/0x103ef29c),與獨立 python 實作逐值一致;
偵查工作流第三路自己的變體 CRC(0xa3244a74)不同=實作序差異,**證明基線不可跨實作抄數**,
協定已寫入守衛註解。
