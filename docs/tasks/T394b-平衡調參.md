# T394b — 平衡調參(二期 R3;T394a 報告建議 1-4 施行)

依據=T394a 平衡數據報告(12 種子×400 天+專精四臂實測)。四項調參,全部零亂數、
零存檔欄位變更、**零基線軌跡影響**(edu 費只在選 edu 後生效;v385 只在接單後讀;
門檻是 UI 閘;池目標是 cms 碼路徑常數——12 種子基線零接單零專精 ⇒ 兩釘必然恆等,套件可證)。

1. **專精解鎖 Lv6→Lv9**(報告建議1):Lv6 中位第 20 天,當時 taxI 0-10/日、運量觀光為零,
   四乘數乘在近零基數上=「永久決定做在資訊與收益最稀薄的時刻」。specPick386 門檻
   `rankIdx+1<6`→`<9`;面板與指南「城市等級 6 解鎖」兩處同步改 9(守衛釘「門檻與文案
   數字必須一致」=改門檻者順手改文案)。
2. **edu 固定日費改規模縮放**(建議2):`sq('edu',12,0)`→`sq('edu',eduFee394,0)`,
   `eduFee394=min(40,max(6,round(pop/100)))`——消除「早選必虧」(實測 11/12 種子 60 天淨虧),
   大城仍付出有感代價(上限 40)。
3. **happy80 目標 .80→.78**(建議3):12 種子幸福峰值最高 0.75,.80×45 天在觀測天花板之上
   =無解委託;.78 貼天花板下沿=玩家經營可及。nm 文案同步(守衛釘值-文案同步)。
   **附:happy 判定捨入裂縫修復**——判定原值 cityHappy=0.695 時面板顯 70% 卻不計數
   (T394a 發現 44 個唯一邊界日);判定改用 `Math.round(cityHappy*100)/100`=
   面板顯示什麼就按什麼判,顯示與判定同源。
4. **transit400 目標 400→1200**(建議4):實測有公交的種子運量峰 1128-1935,兩檔(150/400)
   難度實質相同=階梯失效;1200 檔須經營到位才可及。**id 'transit400' 不改**
   (id 入存檔 done[]/act,改 id=舊檔斷鏈;值-名分離以註釋披露)。

獎金數字全部不動(T394a 裁定:問題在可達性與時點,不在價格)。

## 驗收

1) 套件全綠+兩釘恆等(基線零影響的機器證明);2) 守衛:門檻-文案數字一致×3 處/
edu 費公式與界 [6,40]/happy 池值-文案同步+判定捨入字面/transit1200 值-文案同步;
3) 破壞性:門檻改回 6(文案不動)→一致性紅;fee 改回 12→紅;target 改回 .80→紅;
判定改回原值 cityHappy→紅;4) 指南同步(T393 區)。

二期 R3;出卡=本 commit,施工在 bay/kimi。
