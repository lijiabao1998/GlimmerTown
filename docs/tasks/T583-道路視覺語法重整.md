# T583 道路視覺語法重整：層級、標線、橋面與夜間受光

**日期**：2026-09-13　**發卡／施工／合併**：Codex　**車位**：`bay/codex`　**base**：`478d602`／v11.192
**授權**：業主 2026-09-13「T583 更細節按照 Claude 的想法」「按照 Claude 的方式遵守施工紀律，出卡並完成」。
**治理例外**：本輪業主明示同一人出卡、施工、合併；因此 T362「作者不自合」只在本卡由業主逐卡豁免。其餘進場、車位、驗證、交易式合併與部署紀律不豁免。

## 0. 先糾正一條錯誤前提

`index.html` **沒有 2.5 MiB 的全專案上限**。那是舊 ART-LOOP 某一輪用來提早交件的黃色觀察線，
不是瀏覽器格式上限，也不是本專案停止線。業主已於 2026-09-13 明確撤銷這種套用方式。

本卡只服從可實測的硬限制：Canvas 尺寸與像素台帳、T523 每幀落筆預算、瀏覽器記憶體／啟動時間、
存檔可往返、完整測試與六哨兵。檔案變大要據實記錄，但不得只因越過 2.5 MiB 而停止施工。

## 1. 進場探針與問題定義

道路不是空白素材；現況至少已有 T146／T277／T359／T427／T572 多層疊加。本卡先盤點再落筆，
不以「全部重畫」冒充全面更新。

審計面共 **146 鍵**：

- `SPR.roadClass[0..3][0..15]`：64（巷道／支路／次幹道／主幹道）
- `SPR.roadDeco`：46（curb 16、zebra 16、lane 2、街具 12）
- `SPR.hwy[0..15]`＋`SPR.hwyBridge[0..15]`：32
- `SPR.oneway[0..3]`：4

146 是「逐鍵看過」的範圍，不是「146 鍵都必須變」的 KPI。沒有視覺缺口的鍵應留下「合格不動」理由；
已有正確語意的 T582 軌道／輕軌／架空線與 T572 街具互斥不在本卡重畫範圍。

要解的不是單一像素，而是四種結構失調：

1. 四級一般道路共用太多同色／同標線語彙，遠景不易讀出道路層級。
2. 直線有標線、彎道與路口卻突然失去視覺連續性；箭頭／斑馬線／車道線各自存在，但沒有共同秩序。
3. 高速與高速橋有路面，缺少能讀出「高速／橋面」的反光標、伸縮縫與護欄節奏。
4. 夜間已有燈具與光錐，但道路自身沒有與燈位一致的近地受光回應；不得用全路發亮的假夜圖解決。

## 2. 允許觸碰

- `index.html`
  - 道路生成：`genRoadLvl`、`SPR.hwy`／`SPR.hwyBridge`、`SPR.oneway`
  - 在既有道路生成完成後新增一個**零亂數、原地加蓋** helper；不得新增或放大持久 Canvas
  - 地面繪製中既有街燈／橋燈掛點附近，增加與實際燈具同源的道路受光
  - 測試觀測橋與本卡 kill-switch
- `test_fixde.js`：只新增 T583 守衛與必要的測試觀測白名單
- `docs/tasks/T583-道路視覺語法重整.md`、`docs/CHANGELOG.md`、`docs/ARCH.md`
- `docs/tasks/t583-shots/`：真實 Chrome 的代表樣張與 32 組日夜／旋轉／縮放實跑紀錄
- `sw.js`：收尾時只由 `tools/bump.py` 同步版本

## 3. 設計語法（由低到高，不追求滿屏細節）

1. **巷道 rc1**：最安靜、無中心線；只保留暗邊、接縫與極稀疏修補痕，不假裝多車道。
2. **支路 rc2**：單一中心虛線＋清楚路緣；端點要有收頭，轉角不塞滿斑馬線。
3. **次幹道 rc3**：雙向車道節奏、入口短標線與反光點；與支路在縮放 1 倍仍可區分。
4. **主幹道 rc4**：雙黃中心語彙／路口導流短線；中央分隔只在有足夠空間的遮罩出現，不能堵住彎道。
5. **高速 rc5**：冷色路面、雙白線、外側反光標；高速橋另有伸縮縫、護欄影與反光件。
6. **單行道**：箭頭保留四向語意，輪廓加強但不得改 `(direction + viewRot) & 3` 的旋轉公式。
7. **夜間受光**：只在真實 `drawStreetLampPick572`／橋燈成立的格產生，位置與燈具共用；無燈格必須零新增夜光。

所有加蓋均使用整數像素與現有等距軸，顏色取自既有路面／標線色系；不引入寫實貼圖或高飽和霓虹。

## 4. 禁區

- 不動道路建造成本、容量、壅塞、路網連通、`roadMask` 產生、AI 選路、車流與存檔格式。
- 不動 `rotMask`、`viewRotEff`、單行道方向公式。
- 不動 T582 鐵軌／輕軌／架空線／軌道橋資產。
- 不動 `tools/`、T523 預算常數、六哨兵與既有健康地板。
- 新增段不得出現 `R()`／`ri()`／`rand()`／`Math.random()`／`spriteTexRand`。
- 不靠改低 alpha 把缺陷藏起來；不為了過測試放寬既有斷言。
- 不新增持久 Canvas，不改 146 鍵任一畫布的 `width`／`height`／錨點。

## 5. 驗收：先證明「畫對」，再證明「沒拆家」

### 5.1 Node／像素台架

- G1 清冊：146 鍵完整存在，尺寸／錨點不變；實際變動鍵列白名單，其餘逐鍵指紋不變。
- G2 層級：同一個直線遮罩下，rc1..rc5 的識別色／標線計數形成預期差異；不得只驗函式存在。
- G3 連續性：直線、端點、轉角、丁字、十字五類遮罩逐類真跑，標線均在菱形內且不畫到非連接邊。
- G4 方向：四張單行箭頭各有不同重心／尖端，旋轉四次回原方向。
- G5 高速橋：伸縮縫、護欄影、反光件各自有像素；陸上高速不得誤得橋墩／橋縫。
- G6 夜光：同一場景有燈格新增受光、無燈格零新增；受光中心必須由實際 lamp sprite 掛點計算。
- G7 kill-switch：`__noRoadGrammar583=true` 時 146 鍵與 T582 基線逐位元相同，且 draw-time 受光為零。
- G8 零亂數與零模擬：新增段機械掃零亂數；六哨兵逐字相同；`GV.stats()` 繪製前後逐字相同。
- G9 效能：T523 空城／300 天城均在既有紅線內；另記本卡前後同場景落筆差，不改紅線討好結果。

### 5.2 破壞性證明

至少六案，每案都要記 `exit code`、首個 FAIL／stderr；**首紅必須等於目標釘**：

1. 拿掉 rc3 的層級標線 → G2 紅。
2. 把轉角標線畫到非連接邊 → G3 紅。
3. 讓四張單行箭頭共用同一方向 → G4 紅。
4. 拔掉高速橋伸縮縫 → G5 紅。
5. 讓無燈道路也受光 → G6 紅。
6. 新增段注入一個亂數呼叫 → G8 紅。

紅源恢復後 sha256 必須回到交付位元；只看到 `exit=1` 不算，必須核對首紅。

### 5.3 真實瀏覽器

固定流程：`127.0.0.1:8126`、槽 3、清 SW／Cache、帶 cache-bust。

- 同一座道路測試城：巷道→支路→次幹道→主幹道→高速，含端點／彎道／丁字／十字／一般橋／高速橋。
- 白天＋夜間、rot 0/1/2/3、zoom 0.5/1/2/3；桌面與窄視口至少各一輪。
- 驗頁面身份、非空白、無遮罩阻塞、console 零 error；實際切日夜、旋轉與縮放後再截圖。
- 美術判準：1 倍能讀層級，2 倍能看標線秩序，0.5 倍不成噪點；夜間只有燈下有回應。

## 6. 收束、版本與部署

完整閘門全綠後才執行 `python tools/bump.py 11.193`，同步 `docs/ARCH.md` 現況行與 CHANGELOG；
交易式從 canonical master 執行 `python tools/merge_bay.py codex --deploy`。部署後核對 receipt=match、
兩份 runtime SHA256 與 Git blobs 相同、各車位回同步結果。

本卡不以檔案大小停止。若撞到真硬限制（Canvas 建立失敗、記憶體／載入不可接受、T523 紅線、
瀏覽器崩潰、存檔失真或套件無法閉合），停止並保留已驗證的小步提交，不偷改限制。

## 7. 回滾

道路語法 helper、夜間受光與測試分開成可辨識小步；整卡可 revert。kill-switch 可在不改存檔的情況下
回到 T582 視覺，供部署後診斷，但不是永久維持兩套設計的藉口。

## 8. 施工紀錄

### 8.1 進場與落地

- base `478d602`／v11.192；進場權威閘門 `PASS=11421`、0 FAIL、六哨兵
  `476/40021/288/4779/117/77`。`index.html` 由 2,219,628 B 增至 2,224,440 B；
  **只記大小，不把 2.5 MiB 當停止線**。
- 新增 `roadGrammar583()`＋`buildSpritesS14()`：只在既有 100 張 canvas 上以整數 `fillRect`
  原地加蓋，實跑 `keys=100／ink=1634`；兩條開機路徑都在 S13 後掛 S14。
- 64 個一般道路鍵分成四級語彙；16 高速、16 高速橋、4 單行箭頭各自補層級件。
  46 個 `roadDeco` 已逐鍵盤點，既有路緣／斑馬線／街具語意正確，列為「合格不動」。
- 夜間只在 `drawStreetLampPick572()` 已成立且真燈桿 sprite 可畫時多推一張貼地 wash；
  橋燈分支本來已經使用同族 `railWashWarm/Cool`，審過後不重複疊第二層。
  `__noRoadGrammar583` 同時關閉烘焙加蓋與新增 wash。

### 8.2 指紋、瀏覽器與對抗證據

- 正式 `fp_snapshot.py` 連跑兩次，1,441 鍵逐鍵恆等；相對 v11.192：新增 0、移除 0、
  **變更恰 100**＝`roadClass 64 + hwy 16 + hwyBridge 16 + oneway 4`；白名單外 0，
  `roadDeco` 變更 0。基線以 v11.193 重釘。
- 真 Chrome／`127.0.0.1:8126`／槽 3：建立 72×72 測試城，成功操作 309 次、終態道路 297 格；
  日／夜 × rot 0/1/2/3 × zoom .5/1/2/3 共 32 態逐一 `forceDraw()`，每態均取得非空畫面雜湊。
  代表樣張另含 1280×900 桌面與 390×844 窄視口；頁面身份正確、boot 100%、開始遮罩已收、
  console／Runtime exception 均 0。加蓋開／關的同配方白天樣張差異為 3.076%（z1）與 2.056%（z2）：
  能辨識層級但沒有把遠景鋪成噪點。夜景含動畫水面與分時繪製，故不拿整幀差異百分比冒充道路成效，
  只以同圖肉眼確認燈下 wash 有、無燈格沒有新光源。
- 六案破壞性探針均為 control 先綠、突變後 `exit=1`，首紅依序命中 G2／G3／G4／G5／G6／G8；
  突變前後 `index.html` SHA-256 同為
  `7F1B39DBAADE76CB36E153E1D1398F02C80FA21F7CB6434F35AF9E0BB5F8228B`。
  明細見 `docs/tasks/t583-shots/T583-mutation-report.txt`。

### 8.3 自捕與誠實邊界

1. 新增 S14 後，既有 T563 G3b 的精確開機鏈釘正確變紅；處置是把 S14 納入完整鏈，
   `winterize563()` 仍逐字緊接 S9，沒有拆釘或放寬順序。
2. T583 G6c 初版把真註解 `/* ===== T367` 誤寫成 `/* T367` 作段尾錨，前百餘條 T583 斷言綠後
   在此自紅；改讀產品中唯一的真錨，不改被驗功能。
3. 探勘時誤執行 `fp_snapshot.py --help`；此工具沒有 help 分支，仍真的寫了 v11.192 指紋。
   當場精確還原 `docs/SPR_PINS.json` 至 HEAD，之後才按 `bump 11.193 → arch_map → fp_snapshot`
   正式重跑；錯誤寫入沒有進提交。
4. 真 Chrome 的單次開機樣本為 18.984 s；kill-switch 對照另一次為 25.736 s。
   兩次受冷啟動與機器負載影響，**不能宣稱 T583 加速**；只證明兩路都能完成 boot。
5. 第一輪 Chrome 在遊戲報告與四張樣圖都寫完後，清理拋棄式 profile 時因 Chrome 子行程尚未退出而
   `EPERM`／exit 1。先用完整 command line 精確辨認只屬本卡的 profile 行程，未碰玩家 Chrome；
   修成 `Browser.close → 等待 exit → 刪除` 後重跑，最終 owned Chrome=0、測試 profile 已清空。
   這個 exit 1 是測試工具清理失敗，不冒充遊戲紅燈，也沒有把殘留物留給合併器。

### 8.4 終驗與交易

- 完整 Node 套件：`PASS=11547 / FAIL=0 / exit=0`；`python -B tools/verify.py` 九項全綠，
  v11.193 同步、CRLF=0、ARCH 錨點新鮮。
- 六哨兵逐字命中：`seed22=476`、`seed22m=40021`、`seed301=288`、`seed301m=4779`、
  `seed7=117`、`seed7m=77`。
- T523 真跑空城／300 日城落筆 `585/3722`，均低於既有紅線；本卡沒有修改預算常數。
- G9 第一版錯用既有 AI 城量燈下 wash，該城沒有符合真街燈分支的可見道路，得到 `0` 差異；
  這是假造境造成的 false red，不是把產品判綠。修成獨立 seed583、以產品 `streetHash` 真找可出燈座標、
  再用既有測試橋造一格道路後，kill-switch 關／開為 `2464/2465`，**精確新增 1 次落筆**。
- `python -B -m unittest tools.test_toolchain`：63 例全綠；既有斷言未刪改。
- 最終 `index.html` 2,224,440 B。這是增量帳，不是上限；未觸發 Canvas、記憶體、啟動、T523、
  存檔或完整套件硬失敗。
- 本卡由 Codex 出卡、施工並執行合併，是業主對 T583 的逐卡明示例外；沒有獨立第三方覆核，
  此限制在 CHANGELOG 與交付報告同步揭露。合併只准從 canonical master 走
  `python tools/merge_bay.py codex --deploy`；實際 commit、交易棘輪與 receipt 以工具輸出為準。
