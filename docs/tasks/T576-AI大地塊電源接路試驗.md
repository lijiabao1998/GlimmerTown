# T576 AI 大地塊電源接路試驗

日期：2026-09-05。發卡／施工：Codex。車位：bay/codex，驗證埠 8126，槽 3。
授權：業主「注意施工紀律gpt，剩下的自我迭代兩輪」。本卡為第一輪；不是自合或部署授權。
base：077e43c（v11.188）。claim：Codex；認領 commit 為本卡首次提交。
狀態：施工與自驗完成。驗收:未驗證（非作者覆核尚未進行）；不自合、不部署。

## 定位錨與問題

T575 已量到 seed301 的合法 3×3 空地很多，但臨路合法落點為零。
`aiStep()` 一般供電的 `outer1`／`outer1c` 只找既有道路旁；
`lotRoadLink574()` 只被無電源、零人口的緊急紓困使用。一般擴容沒有接路 fallback。
另查到 direct 成功後 clear 仍用建前 powCap，可能同日雙建；本轮不修，避免一次改兩個變量。

## 允許觸碰

1. 隔離試驗：aiStep 一般電源段記錄 direct/clear 是否成功；都失敗才試固定次序、有限搜尋的補路。
2. 接路規劃 helper 放在 lotRoadLink574 附近；只讀世界，不補貼、不拆遷；沿用完整占地、正式 canPlace/placeCost 與 tryP。
3. test_fixde.js 同卡測試橋／新增斷言；必要的獨立只讀健康度跑檯、試驗證據、CHANGELOG、本卡。
4. 只有通過本卡健康閘門的 runtime 才准留下。未通過則封存候選與數據，正式 index.html 保持 base 位元；失敗試驗也是一輪，但不得宣稱 AI 已修好。
5. 候選健康閘門通過後：seed22 人口／金流兩個精確哨兵依重放值更新、HEALTH-PINS 的 seed22 五項地板只上調並追加 history；其他哨兵／地板不動。bump 11.189，ARCH 版本／代碼地圖與本卡實測說明同步；依既有收束程序真跑 fp_snapshot，只同步 SPR_PINS 版本 metadata，像素不得改變。

## 禁區

不動玩家檔案、master、其他車位、存檔／sprite／R()/ri()/rand()/Math.random() 呼叫點、
AI 價格閾值、MAXA／RESERVE、既有清障條件、補貼、電網語義與地塊尺寸。
不降低 HEALTH-PINS 任一地板，不刪既有斷言，不為通過而改 fixture；六哨兵若變動先完整列 delta，不能以重釘掩蓋健康退步。

## 試验配方與驗收

- 開工備份 index/test/sw 到 repo 外，完整 verify 基線實跑。
- Node 跑檯使用正式 test_fixde.js 的 bootstrap；載入前槽 3，全新記憶體 store，顯式 setMapSize(72)。
- 三種子 301/7/22：newWorldSeeded → setDiff(1) → ai(true) → 400 次 step(1)；先在 base 精確重現六哨兵，再測候選。
- 五項原口徑 pop/buildings/roads/zones/poweredBld 全部 ≥ 現有地板；buildings 是占用格（含 ref），不是棟數。另列 root/kinds 作診斷，不偷換口徑。
- 三種子通過才進 12 種子、400/900 次 tick 對照（7/9/15/22/123/301/528/777/3001/3002/3003/5150）。400 日 pop<500 不得比 base 8/12 惡化。
- 若保留 runtime：全路徑預檢、總價／動作邊界、失敗零施工、無臨路真造境、搜尋工作上限、正常路徑不進 fallback；各新守衛附破壞性紅源；實拍只在 8126 槽 3，非白名單 sprite 指紋零變。
- 若否決 runtime：base/candidate SHA、完整三種子數據、觸及的地板與否決理由入卡；驗證正式 index/sw 回到 base 位元。只讀跑檯可交付，必須明確拒絕失效錨點／不完整輸出。
- 每輪提交前 verify ALL GREEN；不合併、不 publish。第二輪據實選下一個安全問題，不靠調弱門檻續工。

## 回滾／停手

候選先在 repo 外生成／量測。只撤回本卡候選，保留試驗原始碼和量測證據，不碰他人變更，不使用 reset/clean。
若任何地板下降，該候選不進正式 runtime；若基線本身紅或外部 writer 改同檔，停止 runtime 施工並據實回報。

## 本輪量測與採用裁決（2026-09-05）

基線 verify 親跑 10385 PASS／exit0／ALL GREEN。三種子獨立跑檯精確重現六哨兵。
候選只在「已有啟動源、direct/clear 皆未成功、每4日」啟動，64候選輪替、最長6格路。
對抗檢查抓到首源 stale startersN 會撞 T348 補助，以及折扣浮點總和預算會留下半路；採用版皆已修。
完整12種子每組24筆，兩組 exit0＋HEALTH576_DONE。400日 pop<500：8/12→7/12；900日：6/12→4/12。

| seed | pop400 base→candidate | pop900 base→candidate |
|---|---:|---:|
| 7 | 117→117 | 576→576 |
| 9 | 1864→1864 | 1916→1916 |
| 15 | 43→43 | 327→327 |
| 22 | 412→476 | 494→476 |
| 123 | 430→1196 | 490→4164 |
| 301 | 288→288 | 406→771 |
| 528 | 264→264 | 182→182 |
| 777 | 2639→2639 | 2657→2657 |
| 3001 | 72→72 | 415→415 |
| 3002 | 1077→1077 | 2857→2857 |
| 3003 | 838→838 | 1315→1315 |
| 5150 | 355→355 | 557→557 |

三官方城400日十五項地板全部不降。seed22 五項為 476/514/84/144/224（前值412/503/80/142/170）；money 35041→40021。
採用理由：首輪定義的健康門檻均通過、整體崩城數下降，且沒有縮占地／改價格／增補助。
誠實邊界：不是所有城市所有時點都改善；seed22 900日人口494→476、金流39786→34210；seed528 的負債未解。
只有最近帶電路，不保證是最缺電分區，不是路網尋路器。64候選有硬上限，但每候選找最近路仍掃既有道路。

證據目錄：`C:\Users\Leon1\AppData\Local\Temp\glimmer-two-rounds-20260905-100348`。
base index SHA256 `a10b8010d77139052aa903a432b24261b673bdd483733273b8061642841daf3e`；
12seed candidate SHA256 `447008475b53edbcdea4404443ee9b50756842892123636396d9ca1d2b31e62e`。
原始檔 `health12-base.log`／`health12-candidate.log`；採用時只移除候選註解中的「試驗」，另以 `panel-final-smoke.log` 在正式車位重放。
新跑檯 `node tools/health_panel.js [--smoke] [--runtime <凍結index>]`：槽3於載入前、零磁碟存檔；輸出完整數字及SHA。
`HEALTH_DONE` 不是完整verify，也不自動判定12城崩城數合格；施工者仍必須比較 HEALTH_SUMMARY。

功能台架真用 tryP／doPlace：兩路＋3×3電廠，动作恰剩3可成／剩2零施工；折扣六路＋地熱1196.45拒絕、1196.46成功剩350.00999999999976。
台架首寫曾錯放參數索引，正例立即紅；修台架索引後正反兩態均實測通過，不把錯誤測試當成功證據。
破壞性驗證：跑檯8案（槽/錨/資料型別/地板/缺列/重列/500邊界等）、施工8案（尾格/动作/資金/距離/搜尋數/輪替/RNG/首源）均要求 exit1＋指定FAIL，原始log留證。

## 第一輪交付自驗

- `python -u -B tools/verify.py`：10447 PASS／0 FAIL／exit0／ALL GREEN，六哨兵吻合，CRLF=0，版本11.189，ARCH錨點全同步（round1-verify-r3.log）。
- `python -B -m unittest tools.test_toolchain`：63 tests／OK／exit0；全部是拋棄式repo，日誌中的合併／部署不是真master或玩家目錄。
- 原生 Chrome 官方 fp_snapshot 兩次獨立開機1389鍵逐鍵恆等；對11.188新增／移除／變更全0。
- 自有8126、載入前槽3、真Chrome：seed22完整400步，base 412/35041、after 476/40021，桌面1280×800及390×844、旋轉0→1→0，pageerror/console error皆0；Service Worker刻意阻擋與Canvas讀回效能提示如實保留。
- 樣張 `base-city576.png`／`after-city576.png`／`after-mobile576.png` 位於上列證據目錄，已逐張檢視，不是空白頁。只作城市／電源落點QA，不當逐像素同城A/B，因兩版城市已分歧。
- 測試腳本首跑只等GV、未等開機完畢，干擾初始化而得到3777；改等__boot426.ready後重現正式基線。另一張首拍殘留閃電全白，改只重繪120幀消退視覺狀態，GV.stats前後完全相同。壞樣張保留，不冒充驗收。
- 手動step不保證HUD文字即刻刷新；量測以同期GV.stats為準。手机頂部既有提示重疊本卡未改，不宣稱全UI已驗清。
- 獨立唯讀覆核查到caller新增段尚缺RNG切片守衛（helper已有）；程式實際零新增呼叫，第二輪T577補caller破壞性驗證，不改本輪AI。
