# T450　「📈 趨勢」獨立分頁：小倍數（small multiples）專業版

- 發卡／施工／合併：**Claude 全鏈**（業主起床 goal 三卡系列之三：**更專業完善的玩法**）
- 車位：`bay/kimi`　起點：`master @ 4b24ac7`（v11.81，PASS 4,953，ALL GREEN）
- 動 `index.html`（純 UI，讀既有資料）⇒ 需 bump、需兩釘位元恆等

---

## 0. 先掃現況救了這張卡（本輪第四次）

原計畫是「加每日歷史 ring buffer＋折線圖」——**兩樣都已經存在**：
`hist`（T112，`:1361`，每日 `{d,money,net,pop,happy}`、cap 365、存檔可選欄位 `hi`、鐵律7 成對歸零）
＋`drawHistChart`（`:19567`，260×80 canvas、`histRange` 30/90/365 切換）。

真正的缺口是**呈現不專業**：四條序列擠在 260×80 的同一張畫布上、
各自獨立縱軸互相疊壓、沒有軸標籤、沒有當前值、沒有零線——玩家讀不出任何數字。
專業城建（CS2 型）的做法是**小倍數**：每個指標一張小圖、各自標尺。

## 1. 落地

1. `statsTabs343` 加第五個分頁鈕「📈 趨勢」（`statsTrend450`），`bindStatsTabs343` 綁 `showTrendPanel450()`。
2. `showTrendPanel450()`：**2×2 小倍數**——💰 資金／📈 淨收支（含零線）／👥 人口／😊 幸福，
   每張 252×96 canvas：漸層填充折線＋min/max 軸標＋**當前值大字**＋起訖天標；
   範圍切換 30/90/365 沿用 `histRange`；`hist` 空時給 dim 後備（面板禁 NaN/undefined 慣例）。
3. 舊的 260×80 迷你圖**保留在城市頁不動**（入口多不是壞事，拆它是另一個決定）。
4. 純讀 `hist`，**零模擬、零存檔格式變更、零 tick 觸碰**；繪製在面板 canvas（不碰 `#game`）。

## 2. 驗收

兩釘位元恆等；真瀏覽器點開「📈 趨勢」的實際渲染（四張小圖都有內容、當前值等於 `GV.stats()` 對應值）；
`hist` 空城時分頁不出 NaN；破壞式紅源逐條驗明；閘門 ALL GREEN、PASS 只增不減。

## 3. 一句話

**數據與圖表都在，只是沒有一個配得上它們的房間。**

---

## 4. 施工紀錄（Claude 全鏈；起點 master `185b0c1` → v11.82，PASS 4,957）

### 4.1 真瀏覽器驗收（跑 40 天之後真的點開）

```
趨勢分頁鈕存在: true
面板顯示: {"money":"$342","net":"+65.7","pop":"356","happy":"52%"}
hist 末筆:  {"d":41,"money":342,"net":65.7,"pop":356,"happy":0.52}
stats().pop=356（面板 pop 相等）
NaN/undefined/Infinity 出現次數: 0　小圖 canvas 數: 4
```

當前值與 `hist` 末筆**逐項相等**；四張小圖（資金／淨收支含零線／人口／幸福）
漸層折線＋min/max 標尺＋起訖天標全部渲染；範圍切換 30/90/365 沿用 `histRange`。
截圖存卡面同名資料夾（560×280 拼圖）。

### 4.2 驗收

兩釘位元恆等（零模擬、零存檔變更、零 tick 觸碰——純 UI 讀既有 `hist`）；
紅源三案全紅（拿掉綁定 → G1；資料改手抄 → G2；渲染函式碰共用亂數 → G3）；
`tools/verify.py` ALL GREEN、PASS 4,953 → **4,957**。
