# T458 聚集經濟（城市科學十卡 #8）

**日期**：2026-08-14　**發卡**：Claude（業主 goal：前沿經濟學/社會學研究進玩法）
**藍圖**：T451 卡面 §0（非重釘點 ⇒ 政策卡，六哨兵不動）。

## 0. 實證依據（已聯網核實，含爭議如實記）

Glaeser & Gottlieb (2009, *JEL* "The Wealth of Cities")——聚集經濟綜述：

- 生產力隨密度上升：**就業密度倍增 ↔ 生產力 +2~3.5%**（彈性估計）；密集都會工資溢價
  最高可達 +28%。
- 機制：近距離面對面互動加速知識外溢（learning/spillovers）。
- **爭議如實記**：幅度估計困難，控制個人人力資本後城市規模效應顯著變小。本卡取保守彈性。

## 1. 遊戲落地（政策「產業聚落區劃」`pol.aggCluster458`，預設 false）

- 誠實簡化（寫進科學頁）：聚集經濟本身無關政策，v1 以「區劃政策」作為啟用開關——
  開啟後商業/工業稅按**都市密度**加成：`bonus = aggK × clamp((urbanDens406−dThr)/(1−dThr),0,1)`，
  上限 `aggK`（3%＝取彈性下緣、保守）。`urbanDens406` 是 T406 既有的都市密度函數（**先掃現況**：
  密度早就有人算好了，不重造）。
- fx：`{aggK:.03,dThr:.5}`；加成帳列 `fin.aggGain`；`GV.sci451` 擴 `aggGain`/`aggOn`。
- 接點在 k2/k3 稅式（mw454 之後 income 之前）⇒ **T386a 字串釘將再次跟版**（出卡先聲明，
  T454 同款）；T454 G1b 的 mw454 塊原文不動（G1c 回歸鏈完好）。
- 預設關三元式短路 ⇒ 六哨兵位元恆等；開啟分岔合法。

## 2. 驗收計畫

G1 表項＋`aggK∈(0,.1)`/`dThr∈(0,1)` 機械複算；G1b 兩接點與 `urbanDens406` 讀表；G1c 前六卡
原文回歸；G3 預設 false＋白名單；G4 同種子對照（開 aggGain>0／關恆 0）。紅源三案（預設 true／
aggK 硬編碼／密度函數繞過）。真瀏覽器：八筆齊、checkbox、aggGain 真入帳。六哨兵全中。
