# T556 挑戰入口接線（玩測 P2-4）【結案：查無缺陷 2026-08-23】

**處置：降級。** 「入口未接線」原判定撤銷——代碼考古證明挑戰系統全鏈路實作完整；但「完成判定不觸發」的症狀在程式化測試環境可重現，根因未隔離，需人工玩測分辨真偽後再決定是否修復。

## 已證實的部分（代碼考古＋乾淨環境實測）

- `#bCh1/bCh2/bCh3` onclick 完整接線：`challenge={id,bonus,label};pendingScenario=null;begin(true);toast('挑戰：'+label,'gold')`（行 22818）。
- 完成判定存在且位於每日結算內（行 14669-72）：pop1k／day200／star3 三案各有 toast＋獎金＋fanfare。
- 乾淨環境點 bCh1：開局 toast 「挑戰：🏁 挑戰：人口1000」正常出現；起始 $3000＝T115 設計（挑戰強制標準）。

## 未解症狀（程式化 harness 下）

AI+`GV.step(1)` 連續推進使 pop 越過 1000 後：
- MutationObserver 全程監聽（200ms 掃描）未見 🏁 完成 toast；
- Debugger 斷點直讀行 14669（0-based 14668）：**該行每 tick 都執行，但閉包 `challenge=null`**；
- 全檔僅 7 個 `challenge=null` 寫入點，靜態分析無一應在此情境觸發。

## 為何需要人工玩測分辨

今日 CDP/headless 環境多次劣化（8125 伺服器死亡、beforeunload 對話框堵主執行緒、SW 快取卡導航、渲染器隨機僵死），不排除「challenge 被清」是 harness 特有路徑（如某次探針誤呼叫 setter 型鉤子 `GV.challenge541()` 無參數＝設 null）。**人類玩家正常遊玩 10 分鐘即可定案**：開新圖→點挑戰→玩到人口 1000→看是否有 🏁 toast+$1500。

## 若確認為真缺陷，調查指引

1. 用 CDP Debugger 斷點行 14669 直讀閉包（方法已驗證可行，見 playtest-0822/v_t556_e2.mjs）。
2. 嫌疑方向：存/讀檔週期是否序列化 challenge；`begin(false)` 路徑；多重 WS 連線下探針誤觸 setter。


## 結案（2026-08-23，Node harness 正例實驗 t556_full.js）

seed301＋AI 160 天長到 pop=1223 的城，`challenge541('pop1k',1500)`（官方 setter，與 T541 測試同源）後單步：
對照步淨額 +483.0、掛挑戰步淨額 +1972.0，**差額 1489≈+$1500 獎金入帳 ⇒ 完成判定正常觸發**。

- 先前「完成不觸發」症狀判定為瀏覽器 harness 特有：本卡 §未解症狀 中 challenge=null 最可能是
  探針自身曾無參數呼叫 setter 型鉤子 `GV.challenge541()`（＝設 null）所致。
- 判決腳本原路徑 `C:\dev\glimmer-town	556_full.js`（完整套件內嵌實驗，exit 0；verdict 檔 OK）——**該檔未入庫、現已佚失**；其前身草稿保全於 `bay-kimi/playtest-0822/t556_node_draft.js`。（本行由 T554 接手時修復：原文的 `	` 被寫入工具吃成 tab＝環境陷阱 §7 又一犯。）
