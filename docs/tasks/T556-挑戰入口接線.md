# T556 挑戰入口接線（玩測 P2-4）

**日期**：2026-08-22　**發卡**：業主直令「出卡並嚴格遵守施工紀律」（玩測報告 bay-kimi/playtest-0822/findings.md）
**施工**：Kimi　**風險**：中低（入口 handler＋既有挑戰/場景系統接線，不新設計機制）

## 0. 實證（玩測實測）

乾淨上下文（editorOn()==false）點開始選單「🏁 挑戰：人口1000」（#bCh1）：
- 開出新世界 day10／**$3000（＝標準起始額）**；
- **無 ✨目標 按鈕**（對照：編輯器試玩有完整目標面板：人口600／期限150天／$3000）；
- 全 HUD 無任何「挑戰／期限／1000」文本；`GV.challenge541()` 無參數回 null；`GV.scDone()` 為空。
- `GV.setChallenge` 鉤子存在（挑戰系統的 setter 早已暴露）。

⇒ 挑戰按鈕開了普通局，挑戰狀態/目標 UI 未接線。

## 1. 修復方向（定位錨）

- 找 #bCh1/bCh2/bCh3 的 handler（grep `bCh1`），查其現行行為。
- 接上既有挑戰機制（`setChallenge`／挑戰定義表），使開局後：
  - 挑戰狀態可查（無參數 `challenge541()` 回當前挑戰或等效狀態）；
  - HUD 出現目標入口（與編輯器試玩同型的 ✨目標/進度顯示）；
  - 達標後計入完成記錄（`scDone()` 或挑戰自己的完成通道）。
- 三張挑戰卡（人口1000／200天$5k／三星城市500人）逐一接線；若挑戰定義表缺項，按按鈕文案補定義。
- 不改挑戰目標數值本身；不改編輯器行為。

## 2. 允許觸碰／禁區

- 允許：index.html 挑戰 handler 與挑戰定義表、目標 UI 顯示條件、test_fixde.js 守衛、CHANGELOG、sw.js（僅 bump）。
- 禁區：編輯器場景系統行為、tick 經濟迴圈、亂數流（鐵律2）、存檔格式（若挑戰狀態需進存檔，採最小欄位並在 CHANGELOG 聲明）。

## 3. 驗收

- G1：點 bCh1 開局後，挑戰狀態非空（無參數 challenge541() 或等效查詢回人口1000 挑戰）。
- G2：開局後 HUD 存在目標入口（✨目標 按鈕或等效），點開可見「人口 1000」目標。
- G3：bCh2/bCh3 同型接線（各自目標可查）。
- 紅源二案：把 bCh1 handler 的挑戰註冊行移除 ⇒ G1 紅；把目標 UI 顯示條件改 false ⇒ G2 紅。
- 全套件 PASS 不低於現行基準、0 FAIL；真瀏覽器 8125 槽3：點挑戰→截圖目標面板。

## 4. 回滾

單 commit 於 bay/kimi，`git revert` 即可。SEEDPIN 應恆等（本卡不碰 tick 模擬；若挑戰狀態進存檔需驗 save→load 往返）。
