# T387b — 醫療容量效果接線(超載降級,非超載恆等)

狀態:**已出卡,待施工(自主 loop R8)。**
發卡:業主 loop 直令(GPT 順位 4 第二階段:「先顯示…再接效果」——顯示層 T387a 已隨 v11.13 上線)。
出卡+施工+自審:Claude(業主 loop 授權)。
前置:master `03eb001`(v11.13,PASS 3437)。路徑:bay-kimi,bump 11.14,merge --deploy。

> 風險披露:本卡動治癒行為。紅線=**非超載城市逐位元恆等**——效果只在 sick>cap 時觸發,
> cap≥sick 的城市(含全部無病城)行為零改變,兩釘+雙重播天然守住。

---

## 一句話

醫療超載時治癒降級:醫院級「次日必癒」與診所「50% 癒」都要消耗床位,
床位用罄的病患**當日不獲治癒**(排隊=GPT 願景);設計保證 **R() 消耗結構完全不變**
(診所骰照擲、結果被容量門檻遮蔽),非超載城恆等可機器證明。

## 設計決策(五條)

1. **實作形**:治癒迴圈(12160-12177)頂宣告 `let medCap387=醫療容量式,medCured387=0;`
   (容量式與 T387a flowStat 同式——但注意 clinics/am/mhN/hospitals 是**後面**才累加的
   計數!治癒迴圈在第一計數迴圈**之後**?偵查確認:clinics @11517 第一迴圈,治癒迴圈
   @12160 在後 ⇒ clinics/am/mhN 可用;hospitals @12205 在**後** ⇒ 不可用!
   **裁決:容量式用 kCnt 不可(顯示側)——改為治癒迴圈前補一個 hospitals 預計數?否。
   用「昨日容量」=flowStat384.med.cap(上一 tick 快照,ok 時)+首日 fallback 無限**
   ——零新計數、語義=「排隊以昨日床位計」(記卡,誠實文案)。
2. **治癒門檻**:醫院級分支 `if(hospital){if(medCured387<medCap387){b.sick=0;medCured387++;}}`
   (原無 R,改後仍無 R);診所分支 `else if(clinic&&R()<.5){if(medCured387<medCap387){b.sick=0;medCured387++;}}`
   —— **R() 照擲**(消耗序恆等),僅結果被遮蔽。超載病患滯留=sickDays 續累=轉死風險
   (既有 12178 機制自然承接,零新增)。
3. **恆等證明結構**:cap≥sick ⇒ 門檻永不觸發 ⇒ 逐位元恆等(首日 fallback 無限=舊行為);
   flowStat.med.sick>cap 的城市才分岔=玩家可從 T387a 顯示層預見。
4. **顯示補強**:流向 tab 醫療節加「今日治癒/滯留」列(medCured387/滯留數鏡像進
   flowStat.med.cured/queued——錨塊內加鍵,守衛繼承)。
5. **12 種子回歸(鐵律19 同款)**:12 seeds×400 天崩城率/人口軌跡對照(改前 vs 改後)——
   預期全同(常規城市不超載;超載需玩家蓄意拆醫院)。若任一種子分岔 ⇒ 停手查明
   (分岔=該種子自然出現超載,屬預期但須逐例確認人口軌跡合理非崩壞)。

## 允許觸碰

`index.html`(治癒迴圈兩分支+medCap387/medCured387 宣告+flowStat.med 加鍵+面板兩列+bump)、
`sw.js`、`test_fixde.js`、`docs/ARCH.md`、`docs/CHANGELOG.md`、本卡、帳本。

## 禁止觸碰

發病骰(12173)/死亡骰(12184)/診所骰的**消耗次序**;瘟疫機制;COV/svcBudget;存檔;tools/。

## 不變量

非超載恆等(兩釘+雙重播);R() 消耗序恆等(診所骰照擲=T364d「上限從 hit 改 attempted」同精神);
PASS 只增(3437 起);verify ALL GREEN 11.14;12 種子對照表入卡。

## 驗收清單

1. 恆等案:cap 充足城 igniteSick→tick→照癒(行為同 v11.13)。
2. 超載案:cap=0 城(無醫療建築但有覆蓋?不可能——覆蓋來自建築;造境:cap 小於病患數
   =1 診所(cap4)+5 病宅+醫院級覆蓋?診所 cap4、病 5 ⇒ 4 癒 1 滯留;次 tick 滯留者續病)。
   注意首日 fallback:須先 tick 一次建立快照再造病。
3. R 序恆等案:同 seed 兩世界(超載 vs 建構相同但 cap 充足)——不可比(建築不同);
   改證:超載城改前/改後版本對照 R 消耗計數(注入計數器於測試副本)或以
   「診所骰在遮蔽下仍擲」的程式碼字串釘+套件兩釘綠替代(卡面授權後者=輕量)。
4. 12 種子×400 天:pop 軌跡逐種子對照改前 master——全同或逐例審。
5. 破壞性:刪門檻(恢復無限治癒)→超載案紅;基線綠。
6. 閘門+真瀏覽器。

## 回滾

單 bay commit revert;超載語義消失回 v11.13 行為。

---

## 施工記錄(施工後填)
