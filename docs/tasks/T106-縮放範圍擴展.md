# T106 縮放範圍擴展（對標 TheoTown 遠近視野）
版本：v3.2 ｜ 難度：★★ ｜ 前置：T96 ｜ 建議模型：Sonnet

## 目標
cam.z 從整數 1..4 擴為乘法檔位階梯 `ZOOMS=[0.25,0.35,0.5,0.7,1,1.4,2,2.8,4]`（√2 步進）。
拉遠一眼看全城、拉近看清單棟建築像素——用戶點名的對標軸。

## 定位錨
- `function zoomStep(` （目前 `clamp(cam.z+d,1,4)`）
- `cam.z` 初始化：`cam.z=Math.max(1,Math.min(3,Math.round(DPR)))`
- 滾輪/捏合/±按鈕三個呼叫點；`clampCam()`；`drawMini()` 視窗框
- T96 groundCache：快取鍵已含 z，換檔自動重建——不許改快取鍵結構

## 允許觸碰
zoomStep 內部（改為索引階梯）、cam.z 初始（就近取階梯值）、clampCam（遠檔時放寬邊界讓全圖置中）、繪製中 `imageSmoothingEnabled`：z≥1 保持 false（像素銳利），z<1 切 true（縮小抗鋸齒閃爍）——只在主畫布層級切，繪製結束復原。

## 禁區
等距數學核心（wx/wy 公式）；sprite 生成；存檔格式；快取鍵結構。

## 硬性不變量
- z≥1 各檔渲染結果與改動前逐像素一致（1/2/4 檔位保留原值，像素回歸必做）。
- 縮放錨點跟隨滑鼠/捏合中心（現有 wx/wy 反推邏輯沿用，只換 nz 來源）。
- 效能：z=0.25 全圖視野下模擬不掉 tick（繪製慢可接受，交給 T107 LOD）。

## 本卡驗收
1. node --check＋test_fixde 380/380 PASS。
2. 瀏覽器（8186，slot='3' 鐵律）：滾輪從最近到最遠走完整階梯無跳變；z=0.25 能看到整張地圖；z=4 建築像素清晰（截 getImageData 對比銳度）；z∈{1,2,4} 像素回歸與基準一致。
3. 小地圖視窗框在各檔位正確反映可視範圍。
4. 零 console 錯誤。

## 回滾
git checkout -- index.html
