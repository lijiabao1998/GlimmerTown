# T127 摩天樓合併（建築更大：2×2 高層）
版本：v3.5 ｜ 難度：★★★ ｜ 前置：T126 ｜ 建議模型：Sonnet

## 目標
RCI 建築長到頭之後還能「更大」：2×2 的同類 lv3 住宅/商業簇在高需求＋高幸福下機率合併為一棟 2×2 摩天樓（住宅塔 k=33、商業塔 k=34），沿用 v3.0 多格建築架構（root＋ref＋sz=2）。

## 定位錨
多格建築先例（機場 4×4/大學 3×3：bl 的 sz 與 ref:[rx,ry]）；tick() 升級迴圈；DRAFTS/PAL/buildSprites 尾端；doze 多格拆除鏈；POPS/稅收 k 分支。

## 允許觸碰
- 合併判定（tick 升級段後）：掃 2×2 全 lv3 同類分區建築、四格皆通電供水、cityHappy>0.55、對應需求>0.3，機率合併（R() 種子流）：四格 bld 清除→root 置 k=33/34（sz=2、ref 鏈接）。
- 新 k=33/34：各 2 變體摩天樓 sprite（高 140-170px，夜窗滿版，樓頂天線/水塔/直升機坪細節），buildSprites **絕對尾端**；DRAFTS/PAL 尾端加行。
- 人口/就業/稅：四格 lv3 總和 ×1.15（獎勵），走既有 k 特判分支先例（economy 鏈按 k 加 else-if，防 T61 幽靈稅教訓）。
- 拆除走既有多格鏈（拆任意格整棟拆、退分區）；火災/廢墟沿多格先例。
- inspect 文案；小地圖色表加 2 色。

## 硬性不變量
- 存檔零新格式（bl 的 sz/ref 既有支援）；舊檔正常。
- 合併走種子流且條件不滿足時零觸發（低幸福城 500 天 0 合併）。
- 新 sprite 尾端生成；SPR 鍵 grep 查撞名；dia hw 偶數。
- 未合併城市的模擬與改動前一致（合併率為 0 時位元回歸）。

## 本卡驗收
1. 三件套照常。
2. 端到端：養一片 lv3 住宅高幸福高需求→observed 合併發生；塔樓人口/稅收=四格總和×1.15（GV 精確驗）；拆一角整棟拆；存讀檔往返塔樓保持（sz/ref 正確）；夜間滿版窗燈可見。
3. 商業塔同輪驗證；低幸福對照城 500 天 0 合併。
4. 零 console 錯誤。

## 回滾
git checkout -- index.html
