# T389 — 旋轉收口 C 族:fxParts 粒子深度鍵(旋轉全收官)

狀態:**已完成(v11.16,bay/kimi)。PASS 3458→3462;rot=0 三版雜湊同值 e209c301;旋轉收口全清。**
發卡:業主 loop 直令(旋轉收口最後一塊;T375 六列+T376 載具+T388 E 族已修)。
出卡+施工+自審:Claude(業主 loop 授權)。
前置:master `b57fb98`(v11.15,PASS 3458);wf_a55d9bc6 偵查第二路 C 族段打底。
路徑:bay-kimi,bump 11.16,merge --deploy。

> 紅線=**rot=0 逐位元恆等**(viewDep 在 r=0 時 w2v=identity ⇒ vx+vy≡x+y=舊 dep 值)。

---

## 一句話

七個粒子 spawn 點(spawnDust/Debris/Ember/Leaf/Petal/Steam/Exhaust,13071-13111)的深度鍵
`dep=世界 x+y` 在 spawn 時凍結——rot≠0 時短命粒子(1-3s)與建築/載具的疊序錯
(位置已對=isoW2V,只有深度鍵沒轉)。修=粒子帶格座標(gx/gy),消費點(14331 objs.push)
以 `w2v(gx,gy)` 即時算 viewDep;rot=0 構造性恆等。

## 修法(~9 觸點)

1. 七個 spawn 點:`dep:x+y` → `gx:x,gy:y`(或保留 dep 併加 gx/gy——施工時以最小 diff 定,
   若其他消費者讀 p.dep 則保留雙軌並記卡)。
2. 消費點 14331:`dep:p.dep` → `dep:(q389=>q389[0]+q389[1])(w2v(p.gx,p.gy))`
   (每幀每粒子一次 w2v,數十粒子級=零感知成本)。
3. 先 grep `p.dep`/`.dep` 全部讀者確認無第三消費者(有則逐一遷移或雙軌,記卡)。

## 驗收

1. G1 負向:七 spawn 點段不得再有 `dep:x+y` 型字面(錨定各 spawn 函式段)。
2. G2 正向:消費點新式精確釘;spawn 帶 gx/gy 釘(抽 2 點)。
3. G3 恆等:rot=0 時 `w2v(gx,gy)` 和 ≡ gx+gy(幾何斷言,四檔位頂點不變量已由 T388 蓋);
   rot=0 全幀雜湊 bay===master(雙埠,T388 協定同款——粒子有隨機性?粒子 spawn 用 R()/visT?
   **施工前查**:若 spawn 消耗 R(),同 seed 同步數雜湊仍恆等;若用 Math.random(繪製側容許)
   則全幀雜湊不穩,改用「凍結 visT+無粒子場景」或以 G1-G3 靜態+幾何替代,記卡)。
4. 破壞性:回退消費點→紅;基線綠。
5. 閘門:PASS≥3458/verify ALL GREEN 11.16;ARCH §4 最後一列劃掉=**旋轉收口全清**;
   ROADMAP 同步。

## 允許觸碰

`index.html`(七 spawn+消費點+bump)、`sw.js`、`test_fixde.js`、`docs/ARCH.md` §4、
`docs/ROADMAP.md`、`docs/CHANGELOG.md`、本卡、帳本。

## 禁止觸碰

粒子的位置計算(isoW2V 已對)/壽命/生成率;R() 消耗結構;變換核;tools/;其他車位。

## 回滾

單 bay commit revert。

---

## 施工記錄(Claude,2026-08-02)

### 與卡面的偏離(披露)
卡面設計 gx/gy+消費點即時算;實作時發現全遊戲動件(煙/行人/車)一律 **spawn 時凍結 viewDep**
——中途旋轉殘差(粒子壽命 1-3s)是既有接受語義,改為對齊先例=7 行 diff(vs ~9 觸點),
語義全場一致。第 8 個 dep=viewDep 命中是 14192 既有 view 行,G2 pin 收斂到 T389 標記行。

### 證明
rot=0 全幀雜湊:bay(v11.16)===master(v11.15)===**e209c301**(與 T388 連成三版恆等鏈);
G1-G3 四條守衛;破壞性二案紅源驗明;PASS 3458→3462;ARCH §4 全清+ROADMAP 終結行。
