# T579 錨點鏽蝕與死守衛復活（三條假綠＋一條元守衛）

**日期**：2026-09-11　**發卡／施工**：Claude　**車位**：`bay/kimi`　**base**：`58c600f`／v11.189
**授權**：業主 2026-09-11「那你根據目前標注，你打算怎麽決定Claude」＋ 09-05「把問題解決吧Claude，除了施工紀律必須要遵守」
——本卡**觸碰既有斷言**，依鐵律須業主授權，原話如上入卡。
**性質**：純測試工程。**不動 index.html／sw.js／tools／六哨兵／健康地板。runtime 零位元差異。**

## 0. 病灶：守衛用文字錨點綁程式碼，錨點會鏽，而且死法是假綠

`docs/BACKLOG.md` P2-4／P2-5。三條守衛目前覆蓋 0 個字元或視窗暴脹，全部恆真：

| 位置 | 守衛 | 現況（位元組偏移實測） | 死因 |
|---|---|---|---|
| `test_fixde.js:5542` | T418 零亂數 段2 | start=**−1** → 段長 **0** | 錨點 `T418 交付②：鋼材→建造加速` 在 index.html **0 次出現** |
| `test_fixde.js:5544` | T418 零亂數 段3 | start=**−1** end=**−1** → 段長 **0** | 兩個錨點皆 0 次出現 |
| `test_fixde.js:6570` | T515 stats515 視窗 | 段長 **156,997** | 終點錨 `function showDetail(` **0 次出現** → `slice(start,-1)` 切到檔尾 |
| `test_fixde.js:13636` | T552 G1 第三子句 | 正則含真 U+0008 | heredoc 吃掉 `\b` 的反斜線 |

對照組（同族仍活）：`:5540` 段長 1,166、`:5546` 段長 154。

### 考古：前兩條死在自己這一族的後續卡手上

`T418a`（2026-08-06、CHANGELOG:193，條目標題**「守衛債四口」**）改寫了 T418 的程式碼與註解：
`T418 交付②` → `T418 交付②＋T418a A1 裁決（真分流）`；
`cargo:Math.min(2,hubs.cargo.length,shipCount)` → `…,1+shipCount`（A2 裁決）。
錨點當場斷裂，守衛從此恆真——**一張專門修守衛債的卡，同時弄死了兩條守衛，36 天無人發現。**

T552 那條的 U+0008 同病：`:5536` 的行內註解親口記著這族守衛**曾經死過一次**
（「原 python 轉義寫成 U+0008 退格字元＝永不匹配假綠」），修好後又以另一種方式死了。

**結論不是「有人粗心」，是機制缺口**：守衛靠文字比對綁定程式碼，程式碼改名／重構／改註解，
錨點就斷；而斷掉的錨點**不會紅，會綠**——空字串通過任何「不得包含 X」的檢查。
**失效方向天生是假綠。** 所以修這三條不夠，必須同時補上「錨點有沒有抓到東西」的元守衛。

## 1. 手法

1. **新增 `seg579(startAnchor, endAnchor, label, minLen, maxLen)`**：
   任一錨點 `indexOf` 回 −1、或 `start >= end`、或段長落在 `[minLen, maxLen]` 之外 → **當場 assert 紅**，
   通過才回傳切片。訊息要指名是哪一個錨點失效、實得段長多少。
2. **三條死守衛改用 `seg579` 並換上實測有效的新錨點**（本卡已先量過）：
   - 段2：`'T418 交付②＋T418a A1 裁決'` → `'cap418--;'`（實測 1,045 字元）
   - 段3：`'cargo:Math.min(2,hubs.cargo.length,1+shipCount)'` → `'// T418/T418a A2 裁決'`（實測 120 字元）
   - stats515 終點錨：`'function showDetail('` → `"$('#bStats').onclick"`（實測 30,753 字元；與既有 `help515` 用 `$('#bHelp').onclick` 同形）
3. **T552**：`/\x08pol\s*\[/` → `/\bpol\s*\[/`（一個字元；修後對現行程式碼仍綠，因 `cmdkRun552` 走 `polDomId514(r.pol)` 再 `.click()`）。
4. **裸站點棘輪**：全檔 `html.slice(html.indexOf(` 形式的裸切片站點數做成**只減不增**的釘
   （現況數字施工時實測後寫死），新站點一律走 `seg579`。

## 2. 明確不動

`index.html`／`sw.js`／`tools/`／`SPR_PINS`／`HEALTH-PINS`／六哨兵／既有斷言的**判準與訊息語意**全部不動。
本卡只換錨點字串、包一層 helper、修一個字元。runtime 零位元差異 ⇒ 不 bump、fp 不變、arch 不漂移。

## 3. 驗收

- **G1 helper 正反例**：`seg579` 對「起錨不存在／終錨不存在／start≥end／段長 0／段長超上限」五種輸入各自必紅；合法輸入回傳正確切片。
- **G2 三條復活後真的會咬**（紅源，本卡核心）：分別對段2、段3 的程式區注入 `R()`，**必須紅在該條自己的訊息上**；
  對 stats515 視窗注入越界內容驗證視窗確實收斂。
- **G3 T552 復活**：在 `cmdkRun552` 函式體注入 `pol[0]=1`，**必須紅**（修前注入不紅＝對照）。
- **G4 裸站點棘輪**：站點數增加必紅。
- `verify.py` ALL GREEN、PASS 只增不減、toolchain 63 例綠、六哨兵恆等。

## 4. 回滾

單卡單提交 revert。runtime 零差異＝玩家端不受任何影響。

## 5. 施工紀錄

待施工閉環後填寫。
