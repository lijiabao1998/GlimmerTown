# T443　工具列顯示價 vs 實扣一致性（ARCH §10.9）＋ SPR 撞名與 `hw` 偶數（§10.10）

- 發卡／施工／合併：**Claude 全鏈**（業主 2026-08-14 goal 的自主迭代授權；**夜班自主選題、業主未逐項核可**）
- 車位：`bay/kimi`　起點：`master @ 5e425b7`（v11.78，PASS 4,912，ALL GREEN）
- 動 `index.html`（只加唯讀鉤子與常數化）⇒ 需 bump、需兩釘位元恆等

---

## 0. 這張卡挑的是哪兩條債

`docs/ARCH.md` §10「有規矩但沒有機器守著的」列了 16 條。本卡收兩條**機械可查、且會直接影響玩家**的：

> **§10.9**　工具列顯示價 vs `placeCost` 實扣沒有一致性斷言（`oneway`／`light` 兩處是字面值硬編碼）。
>
> **§10.10**　鐵律17（`SPR` 撞名）與鐵律6（`hw` 必為偶數）都沒有機器守。
> 實測 `SPR` 有 110 個頂層鍵，4 個被賦值兩次（`police`／`hospital`／`clinic`／`waterTower`，
> 全是 T151/T162 刻意的「後寫後贏」），但沒有任何測試能區分**刻意覆蓋**與**撞名事故**。

§10.9 的玩家後果很直接：**按鈕上寫 $20，實際扣的可能不是 $20**，而且沒有任何東西會發現。
目前絕大多數 `pr` 已經寫成 `'$'+COST.x` ⇒ 天然同源；缺口是 `oneway`／`light` 兩個**兩邊各寫一份字面值**。

## 1. 落地

| # | 內容 |
|---|---|
| 1 | `COST` 補 `oneway`／`light` 兩鍵，`TOOLS` 與 `placeCost` 都改引它（同 T442 的「同一個數字只准有一處可以改」） |
| 2 | `GV` 加唯讀鉤子 `placeCost443(id,x,y)`，讓 Node 能真跑定價公式（不是讀原文） |
| 3 | **窮舉守衛**（T383b 同型）：對每一個 `pr` 能解析成單一 `$N` 的工具，在乾淨平地上真跑 `placeCost`，值必須等於 `N`；不等就**逐項點名** |
| 4 | `SPR` 撞名守衛：靜態掃頂層 `SPR.<key>=` 賦值，重複鍵必須逐鍵列在白名單裡並註明理由；白名單外的重複＝紅 |
| 5 | 鐵律6 守衛：`isoBox`／`boxUnit` 呼叫點的 `hw` 字面引數必須為偶數，例外逐個列白名單 |

## 2. 驗收

兩釘位元恆等；窮舉守衛必須真的跑過 `placeCost`（不是 `html.includes`）；
把任一工具的顯示價改掉 1 元 ⇒ 守衛必須紅並點名該工具；
在 `SPR` 上多寫一個重複鍵 ⇒ 守衛必須紅；把某個 `hw` 改成奇數 ⇒ 守衛必須紅；
閘門 ALL GREEN、PASS 只增不減。

## 3. 一句話

**按鈕上的價格是給玩家看的承諾，`placeCost` 是真的從他錢包裡拿走的數字。**
這兩件事之間，到今天為止沒有任何東西在對帳。
