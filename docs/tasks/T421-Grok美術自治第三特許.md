# T421 Grok 原創素材自治迭代 · 第三特許（無時限）

- 發卡：Claude／業主（2026-08-08；施工者改指 Grok）
- 施工：Grok（車位 `bay/grok`，埠 **8127**）
- 覆核＋合併：Claude（非作者）。Grok **不自合、不 publish**（T361 同款）
- 起點：`master @ 5b8bac3`（v11.46，PASS 4,307→進場後 4,314 含 nightCity 守衛）
- **線別：桌面線交付**（`docs/LINEAGE.md`；移動線由 T420 衍生、倉庫外維護）

**規則**：T420 全文繼承（見 `docs/tasks/T420-DeepSeek美術自治第二特許.md`），受管區標記改 `T421`；本卡只寫增量與帳本。

**窗口**：無時限。每輪最多 2 鍵；每 3 輪（或任一停止條件）交回。  
STOP 僅：`LIMIT` / `CAPACITY` / `AWAIT_REVIEW` / `BLOCKED`（**無 TIMEBOX**）。

---

## Claim（進場）

| 項 | 值 |
|---|---|
| HEAD | `5b8bac3` |
| GAME_VER | 11.46 |
| 車位 | bay/grok clean |
| 起算 | 2026-08-08（接卡即開） |

---

## R00 — 六條硬前置回述（落筆前，覆核方先看）

用自己的話，一條一句：

| 碼 | 回述 |
|---|---|
| **A** | 每個目標鍵、每個變體，先在全檔找出**最後一次**寫入該鍵的生成點（含 HERO 後寫），再量 w/h/ax/ay、色票、≤16px 細節座標與主色逐列 run，**沒表不落筆**。 |
| **B** | 面線走 2:1 等角 `(±2,±1)`；只有真垂直構件才許螢幕豎線，且要對齊既有 pitch。 |
| **C** | 原色像素數 ≤16 的細節色禁止未申報覆蓋；帳本要分清契約違規與畫期是否真被別層遮住。 |
| **D** | 像素結論只認真實瀏覽器 `art_diff`；原文釘／自報橋只能當前哨，不得寫成「像素已驗」。 |
| **E** | pass 在 clipBase 之後，自己保證落在 footprint 菱形內；`sprFootAudit below` 不得惡化（Node mock 無效）。 |
| **F** | 最終點是 `drawHeroPix` 的鍵，手繪牆面像素禁蓋，只加蓋板／地面／既有可加蓋環。 |

### 本批次（首批美術）打算目標鍵與理由（待 nightCity 守衛 commit 後、量測表齊再落筆）

| 候選鍵 | 理由（待 R00 覆核＋量測確認） |
|---|---|
| `6_1_0` 或消防族 v0 | 服務建築、日間辨識可加地面／門廊級細節；**先 grep 最終生成點**（是否 hero 後寫） |
| `15_1_0` 郵局 或 `8_1_*` 垃圾場變體 | 非禁鍵、程序化面較大，易滿足 B/E；若 HERO 則縮到板面 |
| 備選：`60_1_0` 地熱、`10` 水塔 | 垂直構件＋地面，利於 B 落地交代 |

**不做**：k22/23/53、k121–133、k68/71/73/76/112/127；不動 T420 已交付的 26_1_1/2、29_1_1/2 除非修復輪。  
做不到就寫「不動它」合格（T420 R02 先例）。

---

## 增量執行狀態

| # | 增量 | 狀態 |
|---|---|---|
| ① | nightCity 指紋守衛（行為釘） | **進行中 → 本 commit 收** |
| ② | 不對照看得出來測試 | 待首輪美術 |
| ③ | rect 表貼 __t421Replay 原文 | 待首輪美術 |
| ④ | fillRect 基準探針 master 量測 | 待首輪前 |
| ⑤ | 舊「T421 候選 harness」改無編號 backlog | 帳本備註 |

---

## 帳本（輪次）

### R00 / 增量一 — nightCity 指紋

- **改動**：`sprAtlas356` push 帶 `nightCity`；`atlas.html` bPins 加 CRC 列；bake 尾 `__t421NcRefs/Meta/N`；test 行為釘（引用恆等／尺寸 meta／atlas 對帳／N>50）。
- **紅源**：替換 `SPR.bld[k].nightCity` 引用 → 比較器可偵測；刪 bake → N 不 >50。
- **verify**：PASS 4307→**4314**（進場後）。
- **STOP**：無；下一手＝覆核 R00 回述 → 量測候選鍵 → ART-R01。

---

## STOP 日誌

（尚未停止）
