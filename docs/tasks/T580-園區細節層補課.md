# T580 園區細節層補課：屋頂設備／夜間發光／地面標線（美術素材強化）

**日期**：2026-09-11　**發卡／施工**：Claude　**車位**：`bay/kimi`　**base**：`58c600f`／v11.189
**授權**：業主 2026-09-11「直接做Claude，施工紀律必須遵守，但是項目發揮每次最大完成度，開始吧Claude，美術素材强化」
**業主定序（本卡依此排）**：**① 美術風味疊加（更多建築細節）② 能跑 ③ 故事深度。**

## 0. 進場探針：我實拍了 T574 之後的城，看到密度是反的

造境 seed2026、十八座大占地全部真建（機場 7×7／核電・購物中心・煉油・鋼鐵・中央公園・度假酒店 5×5／
大學・體育場・焚化 4×4／電廠・醫院・學校・博物館・港口・火車站・太陽能 3×3／風力 2×2），
劃住商工分區、AI 跑 220 天，得人口 2,469／建築 1,809 的成熟城，四向取景。

**結論：T574 給了城市體量，沒給體量相稱的細節。**

- 7×7 機場的主體是一整片灰板，上面一排燈桿、兩架小飛機；**它旁邊的 1×1 街屋有屋頂顏色、窗格、煙囪、雨棚、招牌、陽台、水塔。**
- 夜景更明顯：小商業樓亮著霓虹招牌，整條街活起來；**5×5 購物中心、4×4 體育場夜裡是一塊黑屋頂。**
- 全城最大的建築，是視覺上最空的建築。**每格資訊量隨占地變大而下降**，方向剛好相反。

**根因**：T570 那一波的加蓋件（女兒牆／瓦紋／灌木／輪廓裁切）**只作用在 `SPR.bld`**；
T574 的園區走 `bakeLot574` 另一條生成路徑，整個細節層從來沒套上去。

**同時排除一項誤判**：風力發電的旋轉葉片是活的——`draw` 的 `bd.k===26` 分支已接
`s.lotMeta574.hooks.rotor`，四幀 `SPR.windRotor` 疊在園區塔頂（index.html:18948-18953）。本卡不碰動畫層。

## 1. 定位錨

- `bakeLot574(k,v,stage,winter)`：園區烘焙總管；DSL 為 `box／tank／stack／tree／garden／road／fence／lamp／court／rows／garage`。
- `box()` 是幾乎每個配方都用的主積木（屋頂由 `roof` 參數分 `flat／gable／glass` 三型）。
- 注入點：`switch` 之後、`parts.sort((a,b)=>a.dep-b.dep)` 之前（新部件仍進 `lotOrder574` 偏序）。
- 夜圖畫布 `nc/ng`；`solid574` 前遮後燈機制；`hooks`（sign／aviation／exhaust／door／lamps／rotor／radar）。

## 2. 手法（三件，全烘焙、零 draw-time、零亂數）

### ① 屋頂設備 `roofKit580`（在 `box()` 內，屋頂面畫完之後）
依 `(k,v,u,vv,du,dv,bh)` 的純整數運算決定位置，**零亂數**：
- **平頂**：空調／機組小箱陣列＋通風管；屋頂長邊加女兒牆壓線（呼應 T570 的 PARAPET 語彙）。
- **大屋頂**（`du*dv` 夠大）：天窗長條（亮色）＋屋頂檢修走道。
- **高樓**（`bh>=40`）：屋頂水塔。
- **醫療族大屋頂**：直升機坪圓環標線。
- **玻璃頂**：沿用既有玻璃格，加脊線通風口。

### ② 夜間發光 `nightKit580`（只寫 `ng` 夜圖）
- **入口門燈**：`box()` 既有門的位置打一圈暖光。
- **招牌**：`hooks.sign` 位置畫發光招牌帶（商業／文化／市政族）。
- **天窗**：冷白微光。
- **車庫**：`garage()` 的捲門透出室內光。
既有窗燈邏輯與 `solid574` 前遮後燈**完全不動**。

### ③ 地面標線 `groundKit580`
- `road()`：補中線虛線（現況只有兩條路緣線）。
- 機場停機坪：`apron580()` 取代 k19/114 的純色 `ground()`，加停機格與滑行導引線。

## 3. 明確不動

`SPR.bld` 全族、S10–S13 烘焙層、`draw` 全部、動畫層（windRotor／radar／fountain／warnBlink）、
模擬／AI／存檔／亂數流、占地目錄、`tools/`、六哨兵、健康地板、T523 預算、LRU 上限與畫布尺寸。

## 4. 驗收（檔尾 T580 塊）

- **G1 死碼釘**：`roofKit580／nightKit580／groundKit580／apron580` 四者定義在場**且真被呼叫**（原文釘，起訖錨走 T579 的 `seg579` 安全切片）。
- **G2 行為釘（真烘焙台架）**：用 `__t574.artBake` 烘代表鍵，數**標記色像素**：
  屋頂設備色、天窗色在日圖必須 ≥ 門檻；門燈色、招牌色在**夜圖**必須 ≥ 門檻。門檻取實測值。
- **G3 不得溢出**：新增像素一律落在既有地塊幾何內——日圖地塊外緣的不透明像素數對改動前**恆等**。
- **G4 帳不變**：畫布尺寸、LRU 條目數與像素上限帳**逐項恆等**（`__t574.artCache`）。
- **G5 指紋**：**出卡時寫錯、施工中更正**——`lotSprite574` 第一行即 `if(k===5)` 直接回傳預烘的 `SPR.lot574['5_1_*']`，k5 **完全不經 `bakeLot574`**，所以那 6 個 lot574 指紋鍵不在本卡射程內。園區美術是執行期 LRU 快取、本來就不在 SPR_PINS 裡。**正確判準：指紋 1,389 鍵零變動、新增 0、移除 0。**
- **G6 零亂數**：剝註解後掃新增段（`R(`／`ri(`／`rand(`／`Math.random`），註解亦不得出現亂數呼叫型字面。
- 紅源四案：①拔 `roofKit580` 呼叫 → G2 屋頂色歸零必紅；②拔 `nightKit580` → G2 夜圖門燈色歸零必紅；
  ③`apron580` 退回純色 → G1／G2 必紅；④新增段注入 `R()` → G6 必紅。
- `verify.py` ALL GREEN、PASS 只增不減、toolchain 63 例綠、六哨兵恆等、T523 落筆不變。
- **樣張**：同造境同鏡頭，改動前後各一組（全景日／夜、機場近景、商業街夜景），交業主定稿。

## 5. 收束

動 `index.html` ⇒ bump **11.190**、arch_map --fix、ARCH 版本行、fp_snapshot（審 6 鍵差異）、
完整閘門、CHANGELOG、合併 `--deploy`。

## 6. 回滾

單卡單提交 revert；三件各自獨立，任一件樣張被業主否決可單獨拆除。

## 7. 施工紀錄

待施工閉環後填寫。

## 8. 施工紀錄（2026-09-11 收）

**v11.190**　**PASS 10,630**　**master 回填於合併後**　**指紋 1,389 鍵零變動（新增 0／移除 0／變更 0）**

### 四次迭代，每一次都由量測或既有守衛推動（不是憑感覺調）

| 版 | 改了什麼 | 為什麼改 |
|---|---|---|
| v1 | 三個 Kit 初版 | — |
| v2 | gable 補屋脊／老虎窗／煙囪；glass 補屋架樑與周邊機組；天窗門檻 1.6→.85、直升機坪 2.2→1.2；AC 步距縮小 | **標記色量測**顯示細節全落在次要平頂小樓：商場 AC=0 SKY=0、學校 AC=0、機場 AC=0、直升機坪全 0。主體恰好都是 gable／glass，第一版對它們幾乎不做事＝**最大的建築仍然最空** |
| v3 | 招牌改暗框＋亮芯、縮小 | 實拍看到 v2 的招牌是「一塊糊在立面上的粉紅色板」 |
| v4 | 夜光全部改走 `LIT580`（先畫日燈具、夜光同一整數矩形） | **T574 G23d 咬住**：夜圖亮點只允許落在日圖的窗／燈色上（`#344d5b`／`#b1c3c5`／`#ddd5ad`），那是它偵測「前樓遮住後窗」的機制。憑空發亮＝違約。**不改既有斷言，改自己的做法** |

### 三次被既有守衛／自己的守衛咬住（全部如實記）

1. **T574 G23d**（k7、k14、k41）：門燈／招牌／天窗光憑空發亮，穿透 414→40→**0**。修法＝`LIT580` 把日燈具與夜光綁成同一整數矩形，前樓的日圖 `fillRect` 會同時抹掉兩者，遮擋天然成立。
2. **T383c 亂數 token 119→124**：我把區域輔助函式命名為 **`R`**——那正是專案亂數函式的名字，五個呼叫點全被算成亂數 token。改名 `LIT580`。**守衛完全正確，是我撞了命名**。
3. **T580 G6a（我自己的元守衛）咬我自己**：零亂數掃描的終點錨 `const box=` 在 index.html **出現 3 次**，`indexOf` 抓到更早的一處，切片變負。這與 T579 要修的三條死守衛**是同一種病**（錨點鏽蝕），差別是這次當場被咬住。已改唯一錨點並加段長合理性判準。

### 落點實測（標記色像素；守衛門檻取七折）

| 建築 | AC | 天窗 | 直升機坪 | 車道中線 | 停機格 | 滑行線 | 門燈(夜) | 屋頂燈籠(夜) | 招牌(夜) |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 商場 5×5 | 336 | 528 | 0 | 87 | 0 | 0 | 33 | 508 | 214 |
| 醫院 3×3 | 357 | 19 | **124** | 50 | 0 | 0 | 65 | 52 | 0 |
| 體育場 4×4 | 502 | 57 | 0 | 0 | 0 | 0 | 90 | 30 | 0 |
| 機場 7×7 | 129 | 182 | 0 | 238 | **465** | **180** | 36 | 164 | 0 |
| 國際機場 9×9 | 105 | 371 | 0 | 324 | **758** | **231** | 60 | 354 | 0 |
| 監獄 4×4 | 685 | 155 | 0 | 0 | 0 | 0 | 149 | 146 | 0 |
| 電影院 2×2 | 63 | 66 | 0 | 30 | 0 | 0 | 42 | 0 | **223** |

### 樣張（同造境同鏡頭，人口／建築／占地逐項相同＝改動只在美術）

seed2026、十八座大占地全建、AI 220 天、人口 2,736／建築 1,715。
醫院屋頂出現直升機坪 H 標線、機場有停機格與滑行導引線、商場夜裡有燈箱招牌與門口暖光。
**美術定稿權在業主**，三件各自獨立，任一件可單獨拆除。

### 本卡沉澱

**「先有燈具，才有光」**——夜圖的每一個亮點都必須在日圖有對應的實體，否則遮擋判定無從成立。
這不只是為了過守衛：它本來就是對的畫法，而 T574 G23d 把這條規則寫成了程式。
