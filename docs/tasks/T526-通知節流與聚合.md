# T526 通知節流與聚合（通知中心不該是火災刷屏的環形緩衝區）

**日期**：2026-08-18　**發卡**：Claude（業主授權：自己玩→出卡→施工→合併）

## 0. 玩測量到的數字

在實驗複本的 `toast()` 裡插計數器（`window.toast` 攔不到——它是模組作用域函式，
上一版探針因此量到假的 0），跑 600 天 AI 城：

| 難度 | 通知總次數 | 平均間隔 | 前五名 |
|---|---|---|---|
| 標準 seed3002（pop 1,736） | **418** | 每 1.4 天一次 | 火災 86／犯罪 66／廢棄 64／工業洩漏 57／洪水 34 |
| 困難 seed3003（pop 4,895） | **942** | **每 0.6 天一次** | 犯罪 205／廢棄 180／火災 165／工業洩漏 106／疾病 61 |

T311 已經處理過「同屏最多 2 條」的視覺刷屏，但**發生率**沒動過。真正的傷害在通知中心：
`log` **只存 100 筆**（超過丟最舊），942 次重複通知會把真正重要的訊息
（瘟疫爆發、城市升級、T520 的電力未併網警告）在幾天內擠出緩衝區。

## 1. 落地：在唯一漏斗裡節流，不在呼叫點

`toast()` 是全檔唯一寫 `log` 的地方（`log.push` 僅一處）——節流只寫在這裡。

- **同類判定**：訊息前 N 字為 kind key（同一句重複＝同類）。
- **首次永不抑制**：第一次出現一定即時顯示 ⇒ 罕見事件（瘟疫/隕石/升級）天然不受影響，
  **不需要任何白名單**（白名單＝人為判斷，會過期）。
- **窗內重複折疊**：`NOTIF_WIN526` 天內的同類重複不再彈窗，累計次數；窗口結束後的下一次同類事件
  帶上「（近 N 天共 M 起）」一次呈現——**被抑制的次數必須被看見，不能靜靜吞掉**。
- 狀態 `notif526`（lastDay/pending/計數）於 `newWorld`／`load` 成對歸零（鐵律7），不進存檔
  （`log` 本來就不進存檔，已查證）。

## 2. 驗收計畫

- G1 節流必須在 `toast()` 單一漏斗內（呼叫點零改動）。
- **G2 行為（靈魂）**：同一場 600 天困難城，通知總數必須顯著下降；
  **且訊息種類數不得減少**——不准靠「整類丟掉」來降低數字。
- G3 被抑制的次數必須出現在後續訊息文字裡。
- G4 首次出現永不被抑制。
- G5 六哨兵位元恆等＋鐵律7 歸零。
- 紅源三案：抑制不記數（靜靜吞）／首次也抑制／節流寫在呼叫點。
