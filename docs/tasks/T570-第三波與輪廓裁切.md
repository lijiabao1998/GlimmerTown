# T570 第三波＋輪廓出界裁切：女兒牆環線／前庭灌木／屋頂瓦紋／clip（S13）

**日期**：2026-08-29　**發卡**：Claude　**施工**：Claude（業主 08-29 欽定「出卡的時候順便把
建築物美術超過輪廓給修掉，下一張卡，你出卡，你施工，你迭代」；美術定稿權在業主）
**路標出處**：T568 §4 第三波（平頂女兒牆＋k1 前庭灌木叢＋屋頂瓦紋，樣張先證不髒再上）
＋業主點名的輪廓出界病（T353 農場 patch 出界／T355 k112 浮空的全面版）。

## 0. 進場探針：全鍵輪廓審計（判準三代演進，數據入卡）

對 1,382 鍵逐像素審計「不透明像素是否落在 footprint 菱形＋合法垂直區內」：
- **v1（sz 表法）**：k121-133 誤報 75%——它們是 sc:.5 二倍畫布且 sz 在 SZB 補充表，
  MSZ 主表沒收；**表驅動判準會被表漂移騙**。
- **v2（plate 自我描述）**：改用 plate 色系直接量每鍵菱形（最寬 plate 行＝2hw、最末行＝底頂點）
  ——免疫 sc 與 sz 表，v1 假榜全消。
- **v3（尖端容差）**：plate 菱形底尖端行寬 <2 被量化截斷＝below 高估（k25 假 below 44），
  容差放寬（帶內 allow+3、底 ay+3）後定榜。

**終榜：310 鍵非零違規、42 鍵真病（maxOver≥8 或 below≥8）**。標紅視覺定位判讀：
- **k18 港口全變體＝設計性跨水**（吊臂/碼頭伸進水域）——**豁免**；k90/k97 水岸同語意預防性豁免。
- 其餘＝貼地裙邊/底緣雜物越過菱形前緣與側翼（樹叢、底座、招牌裙邊）＝玩家看到的
  「美術壓鄰格」，裁切安全；k17 側面門架橫伸 30px 是最大單體結構，裁後若殘入豁免（樣張裁決）。
- **k22/k53 農牧族豁免**（榜上無真病；且 farmSea/farmGrow 季節畫布生成於 clip 之前，
  裁 base 不裁季節版＝換季輪廓不一致——寧可不裁）。

## 1. 手法：`buildSpritesS13()`（S12 同構骨架，四件）

1. **輪廓裁切（CLIP）**：全 SPR.bld 鍵（豁免族外）按 v3 判準把容差外 opaque 像素 alpha 清 0
   （帶內超 allow+3、below 超 ay+3；**垂直區不裁**＝塔尖天線豁免）。
   **win563 冬側版同步裁**（同幾何同判準）——否則冬季輪廓回歸。night 畫布同步裁
   （夜燈不得亮在被裁區）。容差內的 1-3px 描邊/尖端自然保留。
2. **女兒牆環線（PARAPET）**：`[123]_[23]_*` 鍵（lv2/3）平頂偵測（頂緣同 y 連續 ≥8 欄）→
   頂緣列壓暗 −20 成環線、其下一行提亮 +8 作頂面反光。
3. **前庭灌木叢（SHRUB）**：k1 鍵 plate 帶前緣左右 1/4 處各一團 3×2 深淺綠叢；
   位置由 key 字串雜湊決定（**零 R()**，同鍵恆定）。
4. **屋頂瓦紋（TILE）**：坡頂族（非平頂）屋頂段每隔 3 行整行壓暗 −8——幅度保守起步，
   **樣張先證不髒再上**（T568 原文），髒即砍件。

- 掛載：`buildSpritesS12();buildSpritesS13();` 雙開機路徑；T563 G3b 鏈釘再收緊；
  `__t570BakeCount/__t570Keys/__t570ClipPx` 計數釘；`__t570Proc` 台架橋。
- fp 白名單：**CLIP 觸鍵清單**（審計 310 鍵集合內）＋PARAPET/TILE 的 `[123]` 族
  ＋SHRUB 的 k1 族＋對應 win563 鍵——落筆鍵由 `__t570Keys` 記錄、與 fp diff 逐鍵對帳，
  帳不平＝紅。

## 2. 為什麼安全

全烘焙零 draw-time（T523 不動）；零亂數（key 雜湊非 R()）；哨兵位元恆等。
CLIP 只清 alpha 不動色版；動 index.html ⇒ bump 11.184、arch、fp --fix、合併 --deploy。

## 3. 驗收計畫（檔尾 T570 塊）

- **G1 掛載鏈**恰 2＋計數釘＋舊鏈釘收緊。
- **G2 台架**（T569 幾何擴充）：合成鍵加「出界裙邊像素」「平頂段」「坡頂段」——
  精算 CLIP 清除數／PARAPET 環線與頂面落筆／TILE 隔行落筆／SHRUB 兩團座標；
  豁免驗證（垂直區塔尖不裁、容差內描邊不裁）。
- **G3 fp 對帳**：變更鍵集合 == `__t570Keys` 記錄集合，族外 0、新增 0、移除 0。
- **G4 豁免釘**：k18/90/97/22/53 全變體位元不動（fp diff 不得出現）。
- **G5 零亂數**。**G6 六哨兵＋census 不變**。
- 紅源兩案＋（位元法）：①拔 bootstrap 掛載 → 鏈釘咬；②CLIP 判準失能 no-op → G2 清除數 0 咬。
- **樣張 A/B**：對照 v11.183 雙 origin；特寫大戶（k16/17/7/15）裁前裁後＋第三波三件；
  逐件裁決（TILE 髒即砍、k17 門架殘即入豁免重跑）。

**明確不動**：S10-S12 本體；豁免五族像素；各生成器本體；draw-time 各層；模擬層。

## 4. 回滾

單卡單提交 revert 回 v11.183；S13 兩掛載註解即回。
