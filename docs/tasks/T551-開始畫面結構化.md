# T551 開始畫面結構化（模式散落三處、偏好與難度混裝、規則沒跟上元件）

**日期**：2026-08-21　**發卡**：Claude
**業主原話**：「我覺得UI排列還是不夠結構化Claude，這個項目你具有完整決定權限，大方向在我，實際執行你自主」

## 0. 實測基線（六視窗，改前）

| 視窗 | 內容高 vs 視窗 | 快速設定排列 | 水平溢出 |
|---|---|---|---|
| 390×844 | **需捲動 180px** | 2+2+2 | 無 |
| 430×932 | 需捲動 137px | 2+2+2 | 無 |
| 768×1024 | 需捲動 56px | 2+2+2 | 無 |
| 1280×720 | 一屏容納 | **5+1 留洞** | 無 |
| 1440×900 | 一屏容納 | **5+1 留洞** | 無 |
| 1280×620 | 溢 5px | 5+1 | 無 |

另兩項幾何事實：桌面 `.startPrimary` 寬 **280px**、`.startConfig515` 寬 **557px**
（主行動比次級面板窄一半＝視覺重量顛倒）；手機 config 320px、extras 280px（邊界參差 24px）。

## 1. 結構性缺陷（五條，皆有程式碼佐證）

1. **模式選擇散落三處**：開拓新地圖在頂部主鍵、戰役/編輯器在右欄 extras、挑戰在最底獨立 section。
   四者是同一類問題「我要開哪一種局」，卻分屬三個容器、三個層級。
2. **一個容器裝兩件事**：`.startConfig515` ＝ 難度（這一局的參數）＋ 快速設定（這台機器的口味），
   只用一行 10px 小標題隔開。
3. **規則沒跟上元件**：桌面 line 366 寫死 `repeat(5,minmax(0,1fr))`，而 `grid.appendChild` 實際掛了
   **6 顆**晶片（`ds/ri/q/ms/bg/nc`——夜景是後加的）。**6 的因數只有 6/3/2/1，寫 5 必留洞**，
   這條規則是「元件變了、規則沒跟」的化石。
4. **`.help` 三行混裝三種資訊**：操作鍵位／上手步驟／機制說明，同字級同顏色擠在一起。
5. **繼續 vs 新地圖無主次**：兩顆同尺寸、同綠、同 `startGlow` 動畫。

## 2. 決策過程（三提案 × 對抗評審）

三位設計者各自獨立提案（動線軸／分類軸／克制軸），加上我的基線「四問四區」，交由評審對打評分：

| 提案 | 判決 | 關鍵 |
|---|---|---|
| 開局路徑單軸版 | solid | 資訊軸最強，但兩處**未申報的靜默壞版** |
| **五類歸位・參數隨行** | **strong（勝）** | 唯一看懂 line 325 特異度陷阱並開出正確處方 |
| 先開局，其餘收抽屜 | **淘汰** | 直接刪掉 help 第 2 行＝違反「全部保留」硬約束 |
| 四問四區（我的基線） | weak | 軸線對，但關鍵處留選擇題、沒碰特異度與 12 條原文釘 |

**最終結構 ＝ 勝案的容器骨架 ＋ 落選案的資訊軸與六項具體做法**（見§3）。

### 評審抓到、我原本會踩的地雷

`#start .startPrimary button`（line 318）與 `#start .startMore button`（line 325）**特異度同為 (1,1,1)**，
而 325 在原文更後面故獲勝。把 `#bNewGame` 搬進 `.startMore` 轄區後若無覆寫，它會**無聲變成 13px 小鈕**、
與難度鍵同大、主次反轉——而 headless 與 fake DOM 都量不到，只有真瀏覽器看得見。
處方：`#start .startConfig515>#bNewGame{…}` ＝ (2,1,0) 穩壓 (1,1,1)。

## 3. 落地（六區）

| 區 | 位置 | 內容 |
|---|---|---|
| **Z1 品牌與前提** | `#start` 直屬 | logo／h1／en／sub ＋ 新增 `.subMech`（help 第 3 行上移，與標語一虛一實成段） |
| **Z2 續玩** | `#start` 直屬，**不進 `#startMore`** | `.startPrimary` 只留 `#bContinue`；因 `#bNewGame` 移出，line 318 的 `startGlow` 自動只命中一顆＝全畫面唯一會呼吸的元素 |
| **Z3 開新的一局** | `#startMore` 第 1 section | `#bNewGame` ＋ 新增 `.pathHint`（help 第 2 行降級為 caption）＋ 難度 label ＋ `.diffrow` ＋ `#diffDesc547` |
| **Z4 現成的局・工具** | `#startMore` 第 2 section | 標題改「現成的局・工具」；`.startAltRow515`（編輯器＋戰役）＋ 戰役選單 ＋ **挑戰 section 巢狀進來**（清框、改 1px 頂線） |
| **Z5 快速設定** | `#startMore` 第 3 section（新增 `.startPrefs551`） | 快速設定 label ＋ 唯一的 `.settingsGrid`；**桌面 6 欄、手機 3 欄**（只取 6 的因數） |
| **Z6 註腳** | `#start` 直屬，移到 `#startMore` **之後** | `.help` 只剩第 1 行（操作鍵位）；**class 與 CSS 一字不改**＝T428 G11 原文釘保持綠 |

**難度為什麼貼著新地圖**：`begin()` 只有 `#bNewGame` 這條吃 `DIFF_MONEY[diff]`；戰役與挑戰強制標準、
編輯器強制沙盒。把「難度對其他模式無效」變成看得見的結構事實。

**視覺層級五階**（不新增任何色相，只重新分派既有兩個角色）：
`#bContinue`（琥珀＋唯一動畫）→ `#bNewGame`（綠、大、無動畫，靠形狀差而非亮度）→ 難度四鍵（不動）→
次級開局鍵 13px → 晶片與標籤 10-11px。說明文字一律 `--text-dim`／`#96a4c9`，
**永不與可點物同亮度＝「這行不能點」的唯一視覺契約**。

## 4. 跟版聲明

### 必改（三條，理由入卡）
- **T515 G2**：`config515.appendChild(grid)` → `prefs551.appendChild(grid)`，並補釘 `setLabel`。
  判準由「難度與偏好同屬設定」改為「**難度是這一局的參數、偏好是這台機器的口味**」。
- **T515 G3**：刪兩條子句（`>.startChallenge515{grid-column:1/-1}` 巢狀後永久不匹配＝死規則；
  `.startChallenges515` 桌面三欄在 255px 窄欄內會把挑戰標籤擠成三行）；
  新增 `>.startPrefs551{grid-column:1/-1}`。CSS 端同步刪除對應規則——**不留死規則**。
- **T515 G1（加嚴，不是放寬）**：除原本三 role 順序外，另釘
  (a)`#bNewGame` 在 config section 內、(b)`#bContinue` 在 `.startPrimary` 內且該容器**不在** `#startMore` 內、
  (c)`#bEditorMode` 與 `#bCampaign` 同在 `.startAltRow515`、(d)challenge 巢狀在 extras 內。
  沒有 (a)(b)，下一張卡可以無聲把難度搬回繼續鈕旁邊，本案的分類理由就白寫了。

### 新釘（三條，防靜默壞版）
- `#start .startConfig515>#bNewGame{…}` 必須在場（特異度倒掛，見§2）。
- `.startPrefs551 .settingsGrid` 的 3 欄／6 欄同時在場，且全檔不得再出現 `#start .startConfig515 .settingsGrid`。
  訊息寫明：**欄數只能取晶片數的因數**。
- `#start #diffDesc547{font-size:11px;letter-spacing:0;text-transform:none}`——它在 :22615 以
  `className='startMoreLabel'` 生出，等於把 10px/letter-spacing:3px/uppercase 的**標籤樣式套在一整句中文說明上**。

### 明確不動（逐條列出，不默默略過）
T428 G9（`.settingsGrid` 原文串）／G10（altRow compact flex）／G11（`.help` 原文串含 line-height:1.9）／
G13・G13b（逃生閥）／G14（`#logo` 三檔整數比例）／G15（桌面 `.scmenu` 保留 220px 內層滾動）／
G16（`.startMore .diffbtn` padding）／G17（矮桌面斷點）／T423 R03（`grid.appendChild(ds)` 子字串）／
T405 G3（`bg.onclick` 含 `location.reload`）。

**逃生閥誠實性**：`html.noT428` 的 16 條規則在本案下仍然誠實——它承諾的是退回 T428 的**尺寸**而非結構，
六個目標的巢狀關係本案都沒動。**卡面不得暗示它能回退本次結構改動。**

### 施工規則
所有新增桌面規則一律寫進既有 `@media (min-width:900px){\n  #start{` 區塊之內
（T428 G4 咬的是「最後一個 min-width:900px 區塊必須是逃生閥退回區」，另開新區塊即紅）。

## 5. 驗收計畫

- **G1（靈魂）結構釘**：四條巢狀關係（見§4 T515 G1 加嚴）逐條斷言。
- **G2 欄數與晶片數綁定**：桌面 6 欄、手機 3 欄同時在場；`repeat(5` 在 settingsGrid 語境絕跡。
- **G3 特異度覆寫在場**（防 13px 靜默壞版）。
- **G4 死規則絕跡**：`.startMore>.startChallenge515` 選擇器不得殘留。
- **G5 事件歸屬**：所有既有 id 與 onclick 由原物件承接（沿用 T515 G2 手法）。
- **G6 零亂數**：開始畫面注入不得消耗亂數流（T515 G6 沿用）。
- **G7 六哨兵位元恆等**（開始畫面不在 tick 路徑）。
- **行為證明**：重跑六視窗量測——晶片零空洞、手機捲動量下降、無水平溢出、主鍵不得小於難度鍵。
- 紅源：①拿掉特異度覆寫 → G3 應紅；②桌面欄數改回 5 → G2 應紅；③把挑戰搬回 startMore 直屬 → G1 應紅。

---

## 6. 施工紀錄（2026-08-21 收）

**版本** v11.177　**PASS** 6,137 → 6,143　**master** f37cebae　**六哨兵位元恆等・像素指紋 0 變更**

### 行為證明（六視窗，改前 → 改後）

| 指標 | 改前 | 改後 |
|---|---|---|
| 快速設定排列（桌面） | **5+1 留洞** | **6**（零空洞） |
| 快速設定排列（手機） | 2+2+2 | **3+3**（零空洞） |
| 主鍵 vs 難度鍵 | 主鍵 280 寬、面板 557 寬（重量顛倒） | 主鍵 **539×57** vs 難度鍵 67×43（主次成立） |
| 1280×620 矮桌面 | 溢出 5px | **一屏容納** |
| 手機捲動量 | 180px | 173px |
| 手機區塊寬 | config 320／extras 280（參差） | 三區塊**全部 320**（切齊） |
| 水平溢出 | 無 | 無（維持） |

### 落地與卡面一致（六區），另補三點
1. `.startPrimary` 在無存檔時高度為 0（`#bContinue` 是 `display:none`）——因該容器無框無底色，
   視覺上只是多一點留白、不是空盒子，**接受並記錄**。不採用任何咬 inline style 的狀態選擇器：
   :22609 是靠**清空** inline 來顯示的，CSS 給預設 `display:none` 會讓它永遠不出現。
2. 桌面 `.startPrefs551` 跨滿最後一列（824px 內寬、6 欄每顆約 132px），1440 仍吃滿 840px＝
   保住 T428/T515 的三欄節律原文釘。
3. 挑戰巢狀進 extras 後在 255px 內欄直排（基礎段 `grid-template-columns:1fr`），
   桌面三欄覆寫連同 CSS 一併刪除。

### 守衛（+6：三條跟版改判、三條新釘）
見卡面§4。新釘的三條都是「靜默壞版」型——特異度倒掛、欄數與晶片數脫鉤、樣式與內容錯配，
三者共通點是**不報錯、headless 量不到，只有真瀏覽器看得見**。

### 紅源三案（全咬中）
①拔特異度覆寫 → G3a／②桌面欄數改回 5 → G3b／③挑戰搬回 startMore 直屬 → G1c。

### 過程自捕
**G1b 第一版誤用 `static515` 切片**：它的起點錨是 `<div class="startMore"`，而 `.startPrimary`
在它之前——等於在一個不含目標的字串裡找目標，永遠紅。守衛當場咬到，改切整個 `#start` 塊
並補一條反向斷言（`#startMore` 之後不得再出現 `.startPrimary`）。
教訓同 T528/T544：**切片式守衛要先確認切片真的涵蓋目標**（觀測面空跑家族）。

### 設計決策的來源
本卡的結構不是我的第一直覺。三位設計者獨立提案 + 對抗評審後，我的基線被判 **weak**——
理由是「軸線正確但它是四份提案的最小公倍數，不是獨立貢獻；且在關鍵處留了選擇題」。
勝案的真正價值是**風險控管**：它是唯一預先解掉 line 325 特異度倒掛、也是唯一把 DOM 搬移
限制在 `#startMore` 內部因而保住 T428 四組原文釘的方案。如實記錄：若照我的基線施工，
會在 line 325 與 G11/G2/G3 上各撞一次。
