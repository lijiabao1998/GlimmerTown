# T381 — EDU 教育場髒快取：鐵律19 裁決版（顯示即時、模擬恆等）

狀態：**已完成（v11.4）。業主直令「你修好吧，最高權限授權」⇒ Claude 出卡＋施工＋自審。**

> **自審風險披露**：違反「作者不自合」常規，係業主明示授權。補償＝12 種子 400 天
> 崩城率對照（鐵律19 正規程序）＋四案破壞性證明＋首版修法被自己的數據否決的完整記錄。

## 病灶（T343 偵查 X1 實測）

EDU 教育場只在 `rebuildCov()` 被寫入；`doPlace`→`stampCov` 增量更新 COV.* 從不動 EDU。
⇒ 建校後 EDU 恆 0 直到下次 load＝**間歇性髒快取**。七個讀取點受害。

## 首版修法（獨立會話）：把 EDU 加進 stampCov 增量鏈 —— **被鐵律19 否決**

修法本身技術正確（增量與全量位元恆等、實測驗證）。但 12 種子 × 400 天 AI 崩城率對照：

| seed | 修復前 pop/money | 修復後 pop/money | 判 |
|---|---|---|---|
| 5 | 2737/3k | 2053/0k | ↓ |
| 9 | 3164/11k | 2191/1k | ↓ |
| 15 | 182/-1k | 224/-2k | 崩→崩 |
| 22 | 4550/25k | **822/0k** | ↓↓ |
| 30 | 3860/22k | 2931/29k | ↓ |
| 77 | 2925/1k | 2633/1k | ≈ |
| 123 | 4070/5k | **493/0k** | 健→崩 |
| 200 | 1416/1k | 897/-2k | ↓ |
| 256 | 625/0k | 648/0k | ≈ |
| 301 | 4153/300k | **411/0k** | 健→崩 |
| 999 | 4281/129k | 3613/66k | ↓ |
| 1234 | 3732/66k | 4175/248k | ↑ |

**崩城率 1/12 → 3/12，9/12 掉人口。**（T346d 的合格線＝「三勝一負且崩城率不變」）
機制：模擬經 T326/T346d 歷次重釘已校準在「成長期 EDU≈0」上；EDU 一即時，
`eduTerm` 推高財富判定、AI 的 `aiEduSum` 看到真覆蓋就停建學校 ⇒ 全城軌跡向下。
**照原樣上線會讓玩家的城市變差。**

## 裁決版修法（本卡落地）

- **模擬側四讀點**（`eduTerm` 9865／`aiEduSum` 10394／`eduSumT342→techGold` 10773／`eduIndMul` 11432）
  **維持吃快取** ⇒ 模擬路徑與 master 逐位元恆等（3 種子完整軌跡含錢逐位相同，實測）。
- **顯示側兩點**改即時 `eduStaticAt`：市民需求卡（15167）、晶片抽樣（16807）——
  玩家建校當下需求卡就顯示真值。這兩點原碼自註「純顯示、零存檔、零 R()」。
- `GV.eduLiveAt` 新鉤子；WIP 的重複鍵 `GV.eduAt`（被 17663 既有鍵覆蓋的死碼）移除。
- 統計面板的 `eduAvg342` 不動：它餵 `techGold`（模擬），顯示模擬實際用值是誠實的。
- **「模擬全面即時化」＝未來的再平衡卡**：需重調 eduTerm/aiEduSum 係數並以多種子
  崩城率驗收（鐵律19 ③），上表就是它的基線數據。

## 破壞性證明（四案）

| 注入 | 結果 |
|---|---|
| ★1 重接「模擬即時化」（被否決版） | 紅：seed301 pin 4153→實得 411（**釘子本身就是擋這個的網**） |
| ★2 需求卡改回吃快取 | 紅：T381 顯示側守衛 |
| ★3 模擬側 eduTerm 偷改即時 | 紅：seed301 pin |
| ★4 GV.eduLiveAt 移除 | 紅：TypeError |

## 驗收

- 3 worst 種子（301/22/123）完整軌跡（100/200/300/400 天 pop+money）與 master 逐位相同。
- 套件全綠、兩釘原值、verify ALL GREEN、v11.4 三處同步。
- T381 測試塊：顯示即時非 0 ＋ 模擬快取同刻為 0（分離斷言）＋ rebuildCov 後兩者一致（公式同一）
  ＋ 靜態守衛釘死四模擬讀點吃 EDU[、兩顯示點用 eduStaticAt、stampCov 無 EDU 增量。

## 過程記錄（含我自己的錯）

- 獨立會話未 commit 即結束 ⇒ WIP 保護性提交 `4eae5ba`（防 OneDrive 回退）。
- 合併 master 時我手工縫合測試檔失敗兩次（縫掉一個 `&&`；後把 T381 塊插進 `.catch` 變死碼），
  第三次改用行號插入才對。教訓：**拼接後必驗「塊在成功路徑上且真的執行」**，死碼塊全綠＝假綠。
- 首版修法的否決不是它寫錯，是**單種子好壞說明不了什麼**（鐵律19「單種子變好＝運氣」的反面同真）。
