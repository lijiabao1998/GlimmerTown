# T524 科學頁篩選（5.7 屏的牆，給玩家一把梳子）

**日期**：2026-08-18　**發卡**：Claude（業主授權：自己玩→出卡→施工→合併）

## 0. 玩測量到的問題

📚 城市科學頁：**4,574 px（≈5.7 屏）、7,318 字、53 條研究、無搜尋、無篩選、無分類**。
玩家想知道「我開了哪些政策」或「跟交通有關的有哪些」，只能從頭捲到尾。
（對照：CS2 只有 12 個政策，社群仍普遍反映找不到。）

## 1. 先掃現況

- 分組真相源**早就有**：`SCI_GRP514` 六組（住房與土地／勞動與機會／交通與可達／
  環境與健康／產業與財政／社會與文化），是 T514 指揮台在用的。
- 但它只涵蓋 **51/53**：`oppAtlas455`（機會地圖）與 `mobility456`（世代流動）**不在任何組**
  ——它們沒有 `polKey`（一個是唯讀圖層、一個是常駐機制）。
  **不能為了湊分組把它們塞進 `SCI_GRP514`**：那張表同時驅動指揮台的政策卡片，
  塞進去會讓非政策條目長出開關。

## 2. 落地

科學頁頂部加一列篩選鈕，**全部由 `SCI_GRP514` 導出**＋一個機械化回退桶：

- `全部（53）`／六個分組鈕（各帶條目數）／`🧭 唯讀與常駐`（回退桶＝不屬於任何組者）
- `✅ 只看已啟用`（玩家最常問的那個問題）

狀態 `sciFilter524` 預設 `'all'`、不進存檔、零模擬（顧問/tick 完全不碰）。

## 3. 驗收計畫

- **G1 完備性（本卡靈魂）**：53 條研究每一條都必須恰好被一個篩選涵蓋
  （分組 51 條＋回退桶 2 條＝53，無孤兒無重複）——機械複算，未來往 SCI451 加條目
  而忘了分組時，它會落進回退桶而不是消失。
- G2 篩選鈕與組名由 `SCI_GRP514` 導出（不手寫）。
- G3 行為：選「交通與可達」後頁面只剩該組條目數；「只看已啟用」在零政策時顯示 0 條、
  開一個政策後顯示 1 條。
- G4 六哨兵位元恆等。
- 紅源三案：回退桶拿掉（兩條唯讀研究從頁面消失）／組名手寫（與表脫鉤）／篩選漏算計數。

---

## 4. 施工紀錄（Claude 全鏈；起點 master `bd02745` → v11.153，PASS 5,838）

### 4.1 落地（四個接點）

`sciFilter524` 狀態（預設 all、不進存檔）＋`sciGrpOf524`／`sciPass524` **判定只寫一次**
（篩選鈕計數與列渲染共用）＋科學頁頂部九顆篩選鈕（由 `SCI_GRP514` 導出＋回退桶＋只看已啟用，
各帶即時計數）＋`guideTab===7` 綁定＋`GV.sciFilter524()` 唯讀/設定鉤。

**自捕**：第一版把分組映射寫了兩份（`grpOf524`/`grpOf524b`）——正是本季一路在消滅的東西，
當場收斂成單一 helper。

### 4.2 行為驗證（真瀏覽器）

| 篩選 | 頁面高 | 內容 |
|---|---|---|
| 全部（53） | 4,636 px | — |
| 🚌 交通與可達（9） | **934 px（縮短 80%）** | 含壅堵費、不含空品管制 |
| ✅ 只看已啟用（零政策） | 198 px | 空 |
| ✅ 只看已啟用（開壅堵費） | 294 px | 含壅堵費、不含租金管制 |
| 🧭 唯讀與常駐（2） | 386 px | 含機會地圖、世代流動 |

九顆鈕的即時計數：53／12／9／9／9／6／6／2／0。NaN 殘留 0。六哨兵位元恆等。

### 4.3 守衛

**G1 完備性（靈魂）**：53 條每條恰好被一個篩選涵蓋（無孤兒無重複）＋回退桶必須真的接住
`oppAtlas455`/`mobility456`；G2 鈕與綁定由 `SCI_GRP514` 導出；**G2c 計數與渲染必須呼叫同一個
判定**；G3 行為四連（分組正確、只看已啟用隨政策變化）。

### 4.4 紅源（三案）

| 紅源 | 結果 |
|---|---|
| ① 回退桶失效 | **G1 紅**（孤兒 `["oppAtlas455","mobility456"]`＝那兩條會從頁面消失） |
| ② 組名手寫脫離 SCI_GRP514 | **G2 紅** |
| ③ 判定寫兩份 | 第一版用同名重宣告 ⇒ **語法錯誤、Node 直接崩**＝沒驗到（那種寫法根本進不了 repo）。改成真實走鐘場景（計數處就地寫 inline 判定，語法合法）後 **G2c 紅** |

### 4.5 驗收

`verify.py` ALL GREEN、PASS 5,775 → **5,838**；六哨兵位元恆等。
