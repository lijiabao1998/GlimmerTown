# T592 園區配置混合（業主定稿）：A1 置中與 A2 靠後在同城混用

**發卡／施工**：業主 2026-09-26 看過 `docs/tasks/t591-shots/T591-decide-A1-vs-A2.png` 後定稿：「591靠後靠前都沒問題，但是我覺得混合甚至更好，下一步吧」。Claude 出卡＋施工。
**claim**：Claude；canonical master 直寫（T373 路徑），base `3152002`／v11.200。開工前 `git fetch origin` 驗兩向差 0。驗證埠 8129+（真 Chrome、載入前設槽 3），不碰 8123。
**合併權**：直寫 master，落地後 `merge_bay.py --publish` 與 `git push`。Claude 出卡＝施工同一方，未經第三方覆核，如實聲明。

## 做法

1. T591 分支的配置改成依「類＋款」決定：`(k*7+v*3)` 為奇數用 A2 靠後，偶數用 A1 置中。同一款永遠同一種擺法，同城裡兩式都會出現。
2. 不按格子另外雜湊：配置跟著款走，園區快取鍵不變、條目數不增加（T591 覆核已記錄近景預算對 vark 類最多三倍，不再加碼）。
3. `window.__t591Mode='center'|'back'` 保留給出樣工具強制單一式，快取鍵另帶標記（`_c591`／`_b591`）。

## 允許觸碰

- `index.html`：T591 分支的 `mode` 一行、`lotKey591`。
- `test_fixde.js`：T591 G1c 原文釘、G2 的期望位置（依混合規則）、G2f 補地下限依配置分級、新增混合守衛（兩式都真的出現）。
- `docs/ARCH.md`、`sw.js`（只經 bump）、`docs/CHANGELOG.md`、本卡、`docs/tasks/t592-shots/`。

## 驗收

- verify.py ALL GREEN、PASS 不降、六哨兵恆等；工具鏈 OK；指紋零差異。
- 真 Chrome：36 類各款日冬烘焙零例外、夜光穿透 0；真放置實拍一張混合城。

## 回滾

`git revert <T592 commit>`；或 `window.__t591Mode='center'` 回到全部置中。

## 施工紀錄

2026-09-26，Claude（Opus 5.5）施工，v11.200→v11.201。

**落地內容**
- T591 分支的 `mode` 改成 `(k*7+v*3)&1?'back':'center'`（`window.__t591Mode` 可強制單一式）；`lotKey591` 在強制置中時另帶 `_c591`。
- 混用後 G2f 補地下限抓到：靠後的小園區（新圖四周只剩 1 格）前帶太淺，廣場、橫路、停車都放不下，只剩一條路和一兩棵樹。改成新圖正前方鋪廣場、右前低矮花圃 `bed`（不種樹，免得擋立面）、右帶樹林；前帶夠深（≥1 格）的大園區維持原本的廣場＋橫路＋停車。
- 窄帶樹林（<.62 格）原本常常一棵樹都排不進去（2×2 警察局園區只剩一盞燈和本體，T574 G12d 部件數下限紅），改成窄帶排一列中線樹，只有兩格以下時不放花叢；淺廣場（<.7 格）不放花台。這兩項也會讓置中配置的部分樹位小幅移動。
- 檔案 3,789,049→3,790,864 bytes，行數 42,023→42,042。

**守衛**：T591 G1c 原文釘跟著 `lotKey591` 更新；T591 G2 的期望位置依混合規則（規格在守衛裡另抄一份）；新增 T592 G1：大於 2×2 的款日景兩式各至少 40 次（實得 65／65）；G2f 下限依配置分級（靠後小園區樹 ≥2、置中 ≥4；剩 2 格起兩式都要有停車）。

**真 Chrome**：36 類 v0–v2 日冬 208 次烘焙零例外、夜光穿透 0（輪廓外光暈 36 像素同 T591）；真放置混合城日、夜、冬、旋轉實拍 errLog 0，圖在 `docs/tasks/t592-shots/`。像素指紋 1,548 鍵零差異。

**紅源**：整倉複本單一突變「拿掉混合、全部置中」exit 1，首紅 T591 G2（位置依混合規則）。

**收束**：六哨兵 seed301 780／seed301m 2297／seed7 117／seed7m 77／seed22 639／seed22m 14523 逐位恆等；`verify.py` ALL GREEN（PASS=12301、CRLF=0、11.201 雙版本同步、ARCH 錨點新鮮）；`python -B -m unittest tools.test_toolchain` 63 tests OK；`git diff --check` 乾淨。
