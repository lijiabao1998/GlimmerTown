# T560 教學 h3 啟動源判準（同族病第九例：兩處數電廠，判準不同步）

**日期**：2026-08-25　**發卡**：Claude（馬拉松；peer 玩測 P3-2 唯一定讞為真缺陷的一條）

## 0. 病灶（同族病譜系第九例）

「什麼算電廠」這條規則寫在兩處、判準不同步：

| 位置 | 計數 | 用途 |
|---|---|---|
| `computePower()`（:12383-12389） | k5＋k58 核電＋k59 水力＋k60 地熱＋k62 焚化 | 電網啟動源（真相） |
| 經濟日結迴圈（:14524） | **只有 k5** | `plants*4` 維護費＋achStats＋**傳給 checkHints** |

後果：首發電力選核電／水力／地熱／焚化的玩家，電網已啟動、居民已入住，
教學提示卻**永遠停在 h3「蓋一座電廠」**——peer 玩測實測命中（太陽能情境）。

太陽能/風力（k25/26）**刻意不算**：它們是併網容量、不是啟動源（資訊面板原文
「需電廠併網啟動」）——h3 對純太陽能玩家持續顯示是**正確的引導**，不是 bug。

## 1. 落地（最小侵入）

經濟迴圈**新增獨立計數** `pwrStart560`（k5/58/59/60/62，與 computePower 啟動源判準逐位對齊），
`checkHints(pwrStart560, roads)`。**`plants` 一字不動**——它還餵 `plants*4` 維護費與 achStats，
動它的語意會誤傷財報（這正是同族病的另一種犯法：修 A 處撞壞 B 處）。

**同族病防治**：套件加同步釘——經濟迴圈的 `pwrStart560` k 集合必須與 `computePower`
啟動源 k 集合逐位一致（原文級比對兩處的 k 清單），未來加新啟動源（如 k128 變電所？
——不，k128 只是 BFS 中繼、非啟動源，卡面明記）改一處不改另一處＝紅。

## 2. 為什麼這張卡是安全的

`pwrStart560` 是新變數，既有變數零觸碰；checkHints 只讀它做 `===0` 判斷；
提示系統不在模擬路徑。⇒ **六哨兵位元恆等、像素指紋 0 變更**。
動 index.html ⇒ bump v11.180（與 T559 同波收束）、arch --fix、fp_snapshot、合併 --deploy。

## 3. 驗收計畫（檔尾 T560 塊）

- **G1 行為 A/B**：造境（路≥3＋zone≥4）後——(a)無啟動源 → h3 顯示（基線，防恆真）；
  (b)建核電 k58 完工＋日結 → h3 **不再**顯示（hintTxt 不含「蓋一座」或已進 h4+）。
- **G2 太陽能不誤退**：只有太陽能的同造境 → h3 仍顯示（設計語意釘）。
- **G3 同步釘**：兩處 k 清單原文比對。
- **G4 plants 語意不變**：`plants*4` 維護費原文釘＋achStats.plants 仍為 k5 計數。
- **G5 零亂數／G6 六哨兵位元恆等**。
- 紅源：①`pwrStart560` 改回只數 k5 → G1(b) 紅；②k 集合漏掉 k62 → G3 同步釘紅；
  ③把 h3 判準改成 `cap>0`（太陽能也退場）→ G2 紅（防「順手放寬」的反向失敗型）。

**明確不動**：`plants` 變數全部三個用途；h3 文案；k25/26 的併網語意；k128 中繼定位。

---

## 4. 施工紀錄（2026-08-25 收）

**v11.179（波次版）**　**master 回填於合併後**

紅源③首打（checkHints 呼叫改 pwrStart560+solars）撞上 G3 呼叫形狀原文釘——原文釘比行為釘先咬。
補打改為「太陽能另行計入」（新增一行、兩條原文釘保持綠）直取 G2 行為釘。T558 教訓二度應用；
順帶實證：G3 的兩條原文釘與 G2 行為釘構成三層縱深，改判準的三條路徑各有守望。
過程自捕：hintShown/hintsOff 不在套件共享作用域——newWorld 本就成對重置（鐵律 7，:11285），
手動清理既多餘又出界，改重播種即乾淨。
