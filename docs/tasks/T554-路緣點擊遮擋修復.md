# T554 路緣點擊遮擋修復（玩測 P2-1）【撤卡：查無缺陷 2026-08-25】

**處置：撤卡（查無缺陷）。** 環境恢復後接手（Claude），判決實驗證明「路緣點擊被遮擋」是
**探針座標系錯誤的假象**——玩測 P2 三連（T554/T555/T556）至此全數證實為 harness 誤用，
遊戲本體零缺陷。

## 撤卡依據（判決實驗 t554_verdict.py，2026-08-25）

| 原證據鏈 | 真相 |
|---|---|
| 「權威座標」`GV.w2v((x-y)*32,(x+y)*16+16)` 後點擊 | **`GV.w2v` 是格索引旋轉變換（T367），rot=0 時是恆等函數**——探針把世界像素餵進去、拿回原值、直接當螢幕 CSS 座標點下去，等於少乘一整個相機投影。點到的根本不是目標格 |
| 「同格同工具，唯一變因是像素位置 ⇒ 命中判定被路體遮擋」 | 假座標下「中心」與「下半 12px」是**兩個不同的落點格**：落在可建格＝「成功」（zones+1，但畫到別格）、落在路/水＝被 `commitRect` 框選語意靜默跳過＝「失敗」。同格異果的觀察本身是錯的 |
| 「UI✗但地可建＝命中 bug」（兩輪重現） | 正確座標重播：**可建 4 格 UI 點擊 4/4 全中、落點全部精確**（閉環驗證：UI✓ 後同格 oracle `place('zr')` 因已佔而失敗）；「UI✗但地可建」病態**零例**。對照組用 peer 假座標則完整重現「命中 bug」假象（假點落在 (160,320) 等處、oracle 目標格仍可放） |
| toTile 純數學「與觀察矛盾 ⇒ 必有未發現的 intermediates」 | 數學沒有矛盾——**觀察錯了**。toTile 就是唯一真相源，放置路徑無像素拾取、無高度補償 |

另注：round 探針在 `GV.newWorldSeeded()` 後未關開始畫面——`#start` 覆蓋層仍在時，
所有螢幕座標點擊都落在覆蓋層上（接手判決實驗第一輪親踩同坑，`elementFromPoint` 全報被遮）。

## 殘留的唯一真問題（移交 P3 批次）

**無效格單擊零回饋**：對路格點住宅區，實測 zones／money／toast 計數（`GV.notif526().total`）
三軸全不動——`commitRect` 對無效格 `continue` 的框選語意在單格點擊時同樣生效＝靜默。
peer 建議「失敗時給 toast/閃紅」的後半句是對的，與 P3-1（大建築壓分區無警告）同屬
「操作回饋」家族，合併出卡。

## 探針工程教訓（入譜系）

1. **座標系鉤子先驗語意再用**：`w2v/v2w` 是格索引旋轉、`lookAt` 才是相機定位、`center` 是唯讀查詢
   ——三者在本輪玩測中被誤用了兩個。同 T555/T556 教訓（setter 型/clamp 型誤呼叫）合為一條：
   **呼叫 GV 鉤子前先 `String(fn)` 查簽名與語意**。
2. **螢幕座標點擊前必過 `elementFromPoint` 遮擋審計**——否則點在覆蓋層上與點在畫布上無法區分。
3. **驗屍法要閉環**：UI 成功後對同格跑反向 oracle（應「已佔」）才能證明落點精確；
   只看計數器增加會把「畫到別格」誤判為成功。

---

## 原卡面（2026-08-22 施工暫停版，保留作歷史）

### T554（原標題） 路緣點擊遮擋修復（玩測 P2-1）【施工暫停：環境障礙 2026-08-22】

**處置：卡面有效、缺陷已雙重重現，但根因考古被測試環境劣化阻斷。** 依鐵律「驗收數據只寫真跑結果」，不在量不到數據的環境盲改提交。施工暫停，待環境恢復（建議：重開機後由任一方接手，或業主人工配合一次）。

## 缺陷實證（兩輪獨立重現，驗屍法歸因乾淨）

方法：`GV.place('road')` 鋪基準路排 → UI 選住宅區 → 對南側鄰格做 CDP 座標中心點擊 → 失敗者用程式化 `place('zr')` 同格驗屍。

- 第一輪（8125 健康時段）：可建地形上 UI 中心點擊成功率 **1/4**（(24,3) 中心✗/下半✓ 為原發現；(9,7) 中心✓）。
- 第二輪（8129 乾淨 origin）：8 格中 6 格地形不可建、**(8,7)(9,7) 兩格「地可建但 UI 點不進」＝命中 bug 重現**。

## 已排除／已知

- `toTile()` 為純數學逆變換＋v2w 旋轉（rot=0 恆等）——理論上同格異像素必同結果，與觀察矛盾 ⇒ 必有未發現的 intermediates（像素拾取？事件路徑分流？paintLast 去重？）。
- `doPlace` 失敗時會 toast（silent=false 路路徑）⇒ 靜默失敗代表**呼叫鏈根本沒到 doPlace**，或 toast 出現但未被觀察到。
- `GV.center(x,y)` 是唯讀查詢（回傳螢幕中心的格子），不移動相機——探針曾誤以為可用來置中。
- `GV.canPlaceTool(t,x,y)` 恆回 null（對水域也一樣），不可作為地形真值源；地形真值請用 `place('zr')` 驗屍法。

## 環境障礙記錄（2026-08-22 下午起無法穩定量測）

1. 8125 http.server 死亡（背景程序壽命）→ 所有 evaluate 回 undefined。
2. 遊戲 beforeunload 保護框：location.reload/navigate 彈模態框堵死渲染主執行緒；harness 已加自動應答（cdp.mjs connect 內 Page.javascriptDialogOpening → handleJavaScriptDialog）。
3. Service Worker 快取卡導航：每個 origin 第一次載入正常，之後經 SW 的導航隨機卡死 → 改用全新端口可解（8131 也復發後此假說部分動搖，見下）。
4. **最終狀態：連處女端口 8131 的 pristine 頁面也無法完成啟動**（Chrome 啟動窗口期隨機失效），機器級資源/同步問題，非代碼問題。

## 接手指引

1. 環境恢復確認：about:blank evaluate `1+1` 回 2、遊戲頁 45 秒內 GV 就緒。
2. 重現流程照 `playtest-0822/v_t554_r5.mjs`（驗屍法）；插樁可用 python 於 canPlace/paintTo 函式首行插入 `window.__log554` push（勿用 console.log——headless 收 console 需 Runtime.enable＋事件收集）。
3. 修復原則不變：放置類工具以數學逆變換為準；靜默失敗兜底 toast。
