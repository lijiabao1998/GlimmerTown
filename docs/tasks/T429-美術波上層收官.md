# T429　美術波上層收官（源自移動線 T482 T425/T426/T429/T430/T431/T441/T368a）

狀態：**已合併部署 v11.65**（業主明示「出卡施工並合并，即便如此dsk，也要嚴格遵守施工紀律」）（T433 2026-08-14 校正：本行原本停在合併前狀態，與 docs/CHANGELOG.md 矛盾）
發卡／施工／合併：DeepSeek（業主 2026-08-11 明示「出卡施工並合并，即便如此dsk，也要嚴格遵守施工紀律」＝本卡授權出卡＋施工＋合併；仍須完整閘門與交易式 `--deploy`）
claim base：`7dd37f0`（v11.64，PASS 4563）  
施工車位：`bay/deepseek`，驗證 origin `127.0.0.1:8128`，只用 slot 3

> 內容源自移動線 T482（`index_T482_GPT.html`，v12.34）的 GPT 微雕美術波上層——以 482 為移動側唯一真相。
> 本卡是移動線 GPT 美術波（T420–T432）的收官：搬完後桌面與移動線美術全量對齊，美術線封卷。

## 1. 玩家目標

1. 屋頂說出用途：住宅水箱/衛星碟/曬衣/菜箱，商業 HVAC/天窗/服務梯，工業管橋/排氣筒/維修平台（以 T417 真屋面雪線為錨，不漂在透明天空）。
2. 立面說出時間：補漆色差/排水管濕痕/拆牌孔位/外露電纜/低位移漬——「建築用久了」。
3. 人物說出動作：聊天/遛狗/等車/坐著/搬箱/推嬰兒車/拍照（6~14 秒緩慢擺動，無 citizen state）。
4. 材質說出天氣：玻璃濕反光/磚牆吸水/金屬水線/泥濺/設備積雪/晴天玻璃高光。
5. 夜間說出光源：樓梯燈/販賣機/雨棚燈珠/警示 beacon/月台標記——比 T423 街區人格更小更克制。
6. 全城共享一股風：樹搖/螢火/蝴蝶等既有擺動統一朝風向偏，`__noWind441` 時逐像素恆等於改動前。
7. 公園有人：遊樂場孩童/公園晨跑者/遛狗公園狗與主人（draw-time 決定性）。

## 2. 改前基準（真瀏覽器／真 canvas）

- 桌面 v11.64 已有：`streetHash/kcatOf/viewRotEff/rotMask/STREET_POPCOUNT/SPR.ped(adult/child)/T417 屋面線/_roofLine417/objs+ped+dog 機制/wetLvl/snowLvl/weather/visT/detailPermit432/detailAlpha432/streetPermit432`（T427 已搬 T432）。
- 桌面 POL 索引 `idx(x,y)=>y*N+x`＝移動線 `o.x+o.y*N` 同構（T425 的污染讀取相容）。
- 桌面粒子消費端：樹搖 sway L16004／螢火 L16008-16011／蝴蝶 L16021／秋葉 L16031／花瓣 L16039（groundCache 後的逐格自然粒子區）；T368a 父循環同構（objs 收集區）。
- 兩釘 `4153/4550`、`sprAtlas356` 台帳 1,756 張、固定城 save 逐位元組恆等。

## 3. 允許觸碰

- `index.html`
  - 新增 `/* ===== T429 美術波上層移植層（源自移動線） ===== */` 區塊：退化橋＋七件（T425/T426/T429/T430/T431/T441/T368a）。
  - draw() 幀首插 `updWind441();`；建築層接線 5 行（rooftop/facadeMemory/materialResponse/nightMicro/microLife，依移動線 L21336-21353 順序插入既有 T427 四層段）；粒子消費端各一行 `+windX441*…`（螢火/蝴蝶必接，樹搖/秋葉/花瓣/雨斜率/煙漂移按桌面實際代碼逐一接，接不到的不接並記帳）；T368a 公園有人段插 objs 收集區。
  - 可新增 T429 專用決定性 helper；CSS 零改動。
- `test_fixde.js`：新增 T429 守衛；T427 守衛不需動（層是加法）。
- 本卡、`docs/CHANGELOG.md`；release tail 才准動 `docs/ARCH.md`、`sw.js`。
- `attic/t429-review/` 放本卡前後日夜實拍證據。

## 4. 絕對禁區

- 不改任何 sprite（buildSprites/S1..S9 零觸碰）；atlas/metadata/canvas 台帳不得變。
- 零 `R()`／`ri()`／`rand()`／`Math.random()` 新增消耗；層內一律 streetHash/visT 決定性。
- 不碰 tick／經濟／AI／放置／存檔／服務工作者；不動 T426 啟動管線與 T427 八層。
- `__noWind441=true` 時畫面必須與改動前逐像素恆等（T441 不變式①——零風即零位移）。
- 不新增外部素材、字型、依賴；CSS filter/blur 禁用。
- 玩家 origin `localhost:8123` 禁止造境、清槽與測試。

## 5. 施工三段

### S1　退化橋＋七件移植（原碼保留）

- 退化橋：`visualWealth446D=(bd)=>((bd&&bd.we)===2?2:(bd&&bd.we)===0?0:1)`（真實映射既有 we，零亂數）；`roofLifeIntensity446=()=>1`；`window.__noRoofLife446=true;`（T446D 生活密度未移植——T425 貧富加蓋/T426 財富差異分支隨之失效，走基線路徑）；`rhythmMicro448=()=>1`（T448 作息母場未移植）。
- 七件函式原碼移植（移動線 L19154-19211／19213-19235／19294-19335／19337-19367／19369-19394／19741-19758／20431-20464）：
  `roofAnchor425/drawRooftop425/drawFacadeMemory426/drawMicroLife429/drawMaterialResponse430/drawNightMicro431/updWind441`＋`let windX441=0`＋T368a 公園有人段（objs.push 版）。
- kill-switch：`__noRooftop425/__noFacadeMemory426/__noMicroLife429/__noMaterial430/__noNightMicro431/__noWind441/__noParkLife`。

### S2　接線

- draw() 幀首（`function draw(dt){` 後第一行）：`updWind441();`。
- 建築層（T427 四層段內，依移動線順序）：`drawRooftop425(ctx,o,bd,s,bx,by,z,drawA);`、`drawFacadeMemory426(...)`、`drawMaterialResponse430(...,wetLvl,snowLvl,nightDepth);`、`drawNightMicro431(...,nightDepth);`、`drawMicroLife429(...,nightDepth);`。
- 粒子消費端：螢火 `+windX441*1.2*z`、蝴蝶 `+windX441*4*z`（移動線同款係數）；樹搖/秋葉/花瓣/雨/煙逐處評估後接或記帳不接。
- T368a：objs 收集區（桌面自然粒子迴圈）插入遊樂場孩童/晨跑者/遛狗公園段（k126/k4/k92 判斷，`animOn&&!window.__noParkLife`）。

### S3　守衛

- 七件函式存在＋接線（幀首 updWind441、建築層 5 行、粒子消費端、T368a 段）；kill-switch 七個；退化橋四個；層區塊零 `R()/ri()/rand(/Math.random/spriteTexRand`；觀測橋 `__t425RoofCount/__t426FacadeCount/__t429LifeCount/__t430MaterialCount/__t431MicroLightCount/__t441Wind`；`__noWind441` 零風不變式（windX441≡0 邏輯靜態守）。

## 6. 驗收

1. 語法檢查（`new Function`）通過。
2. `node test_fixde.js` → **PASS ≥4563／0 FAIL**（只升不降）、exit 0、verify ALL GREEN、工具鏈 54/54。
3. 兩釘 `4153/4550` 恆等；固定城 `rawSave()` 位元組恆等；`sprAtlas356` 台帳/FNV 恆等；CRLF=0。
4. 真瀏覽器（8128、slot 3）：日/夜/雨（weather=2 手動）實拍存 attic/t429-review/；七層觀測橋計數全實證；`__noWind441` 開關 A/B 對照（關=恆等於改動前）；console error=0。
5. 破壞性紅源至少三案（整倉複製）全 exit 1 且 FAIL 具名 T429：M1 移除建築層接線；M2 層內注入 `R()`；M3 移除 kill-switch。
6. 啟動耗時 p50/max 不得比 5102/5187 惡化 >15%。

## 7. 硬停與回滾

立即停卡，不合併：需要改玩法/存檔/AI/亂數流；兩釘/台帳/save 位元組漂移修不回；真瀏覽器黑屏/console error/畫面過密不可讀；verify 掉數修不回。回滾只回退本卡小提交；不動玩家存檔。

## 8. 合併／部署

業主已授權本卡自合：

`python tools/merge_bay.py deepseek --deploy`

仍必須從 canonical master 執行，由工具重跑 base/source/integration 棘輪、原子部署並重寫收據；不得手工複製 runtime。

## 9. 施工閉環（DeepSeek，2026-08-11）
## 9. 施工閉環（DeepSeek，2026-08-11）

### 實作與量化

- **六件移植**（源自移動線 T482，原碼結構零改動）：T425 屋頂考古（`roofAnchor425/drawRooftop425`）、T426 立面記憶（`drawFacadeMemory426`）、T429 微型人物（`drawMicroLife429`）、T430 材質反應（`drawMaterialResponse430`）、T431 夜間微光（`drawNightMicro431`）、T441 全局風場（`let windX441/updWind441`）。T368a 公園有人桌面 T368 已含——不重複移植（記帳）。
- **退化橋**：`visualWealth446D`（真實映射 bd.we）、`roofLifeIntensity446=()=>1`、`window.__noRoofLife446=true`、`rhythmMicro448=()=>1`（T446D/T448 未移植，恆等退化；搬市政網絡波時移除）。
- **接線**：draw() 幀首 `updWind441()`；建築層 T427 四層段內插入 5 行（facadeMemory/rooftop/materialResponse/nightMicro/microLife，依移動線 L21336-21353 順序）；粒子消費端 3 處（樹搖 `+windX441*1.5*z`／螢火 `+1.2*z`／蝴蝶 `+4*z`，螢火/蝴蝶係數移動線同款、樹搖係數本卡自定記帳；秋葉/花瓣/雨斜率/煙漂移桌面無移動線同構消費端，不接並記帳）。
- **改名**：T429/T430/T431 層內局部 `R`→`Rp`×3（同 T427 先例，桌面守衛嚴於移動線）。
- index 21,449 行；兩釘/台帳/save 位元組恆等；CRLF=0。

### 真瀏覽器與效能（127.0.0.1:8129、slot3、AI 404 天城 1,805 建築）

- 日間 roof=135／facade=528／wind=0.208；雨天（weather=2）mat=280／wind=0.861；夜間 micro=2,950。三態截圖 `attic/t429-review/`。
- **life=0 根因查明**：AI 城相機位置恰停農場群（視口全 k=63，KCB='F'，role 鏈設計上 else return 0——移動線同款設計，非缺陷）；harness 逐建築調用實證 calls=165/pass=40/lifeCount=40，通過率符合 detailPermit432 slot 機制預期。
- **施工紀律記錄**：sw.js cache-first（APP_VER 不 bump 即命中舊緩存）在 8128 三度餵舊版干擾驗證；教訓＝改 index.html 後驗證一律換端口（8129）或 bump；本卡最終以 8129＋harness 雙軌完證。

### 守衛與破壞性證明

- 官方施工閘門 PASS=`4582`→**`4618`**（+36：T429 G1-G6）／FAIL=0／exit 0／verify ALL GREEN；工具鏈 54/54；兩釘 `4153/4550` 恆等；CRLF=0。
- 紅源三案全紅具名（完整 test_fixde.js 實彈、exit 1）：M1 刪幀首 updWind441→**T429 G2**；M2 退化橋注入 `R()`→**T427 G5**；M3 刪零風判段→**T429 G6**。
- 業主本 task 已明示「出卡施工並合并」；卡面如實寫作者自驗，不冒稱非作者覆核。
- release tail：`GAME_VER/APP_VER=11.65`（`tools/bump.py`）、ARCH 版本/行數同步、CHANGELOG 手寫一條；合併前仍須在 frozen source／integration 各重跑 canonical verifier。
