# T416 — 就近站派遣(服務車輛派遣效率;DeepSeek 能力測試卡:開放設計面,中大型)

業主直令出複雜卡測試 DeepSeek。本卡與 T415 相反:**不鎖行,鎖目標與紅線**——
設計決策由施工方做,並在卡面「設計裁決」節寫清理由。這正是考題。

## 目標(定量,T411 已量測的開場白)

T411 定案:服務車輛(救護/消防/警察)單案平均壽命地板 **~28 遊戲日**,主因=
`updDispatch` 的**站點隨機挑選**(`sts[vri(sts.length)]`,平均跨半城行程)。
本卡改「**就近站派遣**」:為候選案挑最近站出車。**目標=壽命地板砍半**(28→15 前後,
以覆核方 800 天 A/B 量測為準)。

## 開工前必讀(這些卡面裡埋著會咬你的守衛,讀完再動手)

`docs/tasks/` 下:T402(vri 隔離+G2「恰 20 處」全帳守衛+G4 雙生子)、
T406(**roll-then-mask 先例**:骰照擲、結果覆寫=帳不動)、T409/T410(派遣呼叫行
**原文釘**——`updDispatch(policeCars,...)` 等呼叫行一字不能動)、T411(G3 三車隊
響應幀數行為案+fleetFrames411 模式)、T413b 三輪覆核記錄(守衛怎麼攻破的教材)。

## 設計自由度(你決定,卡面寫理由;覆核會審你的「為什麼」)

1. **距離度量**:Manhattan/Chebyshev/路網 BFS?注意這在幀路徑逐 spawn 執行,
   成本要論證(站點數 ≤ ~15,候選 ≤ ~30,量級自己算)。
2. **適用車隊**:救護/消防/警察三家 dispatch 型顯然適用;**垃圾/回收車是巡遊型**
   (candidates=路面格,到站再挑下一點)——就近語義對它是否成立,要論證,錯用=退修。
3. **vri 帳**:換掉 `sts[vri(sts.length)]` 會動 T402 G2「vri 恰 20 處」全帳。兩條路:
   a) roll-then-mask(T406 先例):`vri(sts.length)` 照擲、結果被最近站覆寫=帳不動;
   b) 真移除+G2 帳連帶重釘(20→N)+守衛文案更新。
   擇一並論證(提示:兩條路對「未來又有人動 vri」的防禦力不同)。
4. **配對策略**:現行是「隨機站×隨機案」逐車配對。就近化後,「案挑站」還是「站挑案」?
   多案搶同站怎麼辦?(不要求最優匹配,要求語義清楚+無餓死)。

## 紅線(逐條會驗,違一退修)

- **兩釘 4153/4550 逐位恆等**(派遣在幀路徑=T410/T411 同款論證;套件=機器證明)。
- **tick 零觸碰**;候選集語義零改變(T410 警察無過濾/消防覆蓋內——你只改「哪個站出車」,
  不改「誰能被服務」)。
- T409/T410 的派遣**呼叫行原文釘不得動**=你的改動只准在 `updDispatch` 函式內部
  (或其呼叫的新輔助函式)。
- 零 R()/ri()(新碼);T402 G4 雙生子照綠;G2 帳按你第 3 項的選擇處理,守衛同卡跟版。
- `svcSamples` 到場幀數語義若受影響,入卡聲明。
- 性能:每幀新增成本論證並附量級估算(N=216 最壞情形)。

## 驗收

1) 套件全綠+兩釘恆等+PASS 只升不降;
2) **行為案(去註解+行為雙保險,T413b 鐵訓)**:①同案雙站(近站 5 格/遠站 40 格)→
   車必從近站出(紅源=隨機挑選下兩站都可能);②三車隊響應幀數 vs 現行(fleetFrames411
   模式擴充),就近後同距離幀數不變、跨城案幀數顯著降;③你的 vri 帳選擇對應的守衛;
3) 破壞性至少兩案(整倉複製紅源驗明,紅斷言名+實得值入卡);
4) 覆核方(Claude)跑 800 天 A/B 壽命量測(T411 探針復用)驗「砍半」目標——
   量測不達標不算違約,但要在卡面「設計裁決」節分析為什麼;
5) CHANGELOG 一行(驗收欄禁「待」);不自合、不 publish,交回 tip。

出卡=本 commit;施工 bay/deepseek;v11.41(完工時 bump)。

---

# 【覆核方 800 天量測(驗收第 4 項)】(Claude,對 bay/deepseek 工作樹 006457c+未提交優化)

## 先更正我自己的一次誤判

首輪量測的 arm B 建自 `1800b49`(**inflight 修復之前**),結論作廢。舊版每批所有車
算出同一個「最遠案」全撲同一件——那正是我上輪退修的 P1。已重建重跑,以下是新數據。
**教訓入檔:A/B 探針樹必須驗明 provenance(版本號+特徵字串),不能只看資料夾名。**

## 三臂數據(1× 幀預算,800 天,Math.random 種子化 LCG)

| seed | 警局/消防/救護 站數 | A 基線 v11.40 | B 本卡現況 | C 本卡+保流 |
|---|---|---|---|---|
| 301 | **13** / 9 / 8 | 28.8 | 23.2(−19%) | **22.9(−20%)** |
| 22 | **1** / 3 / 0 | 40.4 | 37.3 | 41.8 |
| 7 | **1** / 1 / 0 | 24.7 | 48.5 | 31.3 |

(廢棄日 A→B→C:301 = 0/0/0;22 = 2187/1458/1978;7 = 564/3105/760)

**arm C** = 本卡邏輯 + 在 dispatch 分支照擲兩次 `vri` 再丟棄(roll-then-mask 保流),
使 Math.random 消耗序與基線逐位對齊;**唯一差異就是「挑哪個站/哪個案」**。

## 三個結論

**① 只有 seed301 量得到東西。** seed22/seed7 各只有 **1 個警局** ⇒「最近站」≡「唯一站」
≡ 隨機挑 ⇒ **本卡機制在這兩顆種子上是 no-op**,它們的任何差值都是噪音。

**② arm C 證明 seed7 的 +96% 是純流位移,零機制。** 派遣邏輯一字未改、只把 vri 消耗序
補回基線,seed7 就從 48.5 掉到 31.3(廢棄日 3105→760、pop 303→489)。
**一顆機制完全惰性的種子,單靠 RNG 流位移就能擺動 17 個點。**
根因:本卡設計裁決③把 dispatch 分支的 vri 從 2 降到 0,而 `vri` 是**全體車輛共享的
Math.random 流**(T402 隔離的是「vri vs R()」,不是「vri 內部」)⇒ 車流/火車/電車/船/
公車/垃圾車的軌跡全部位移,壅堵回饋跟著走另一條路。
→ 這不違反任何契約(兩釘 tick-only 恆等、G4 雙生子綠、vri 本就無決定性保證),
但**是一個未聲明的影響半徑**,且它讓卡面驗收第 4 項(A/B 量測)本身變得不可信——
除非像 arm C 那樣人工保流。**設計裁決③的 trade-off 表少了這一欄,建議補上。**
(附:roll-then-mask 被否的理由「假骰=T413b C8 譴責形態」不成立——C8 譴責的是
**no-op 繪製器佔 push 名額謊報件數**;保流的骰子是承重的,它保的是全域流對齊。)

**③「砍半」目標沒達成,而且是我的卡面訂錯,不是施工問題。**
在唯一量得到的 seed301 上是 **−20%**,而這正是幾何學預測的值:
- T411 G3:警車路距 30 格 = 249 幀;1× 速 18 幀/遊戲日 ⇒ **13.8 遊戲日**
- 本卡行為案:路距 5 格 = 41 幀 ⇒ **2.3 遊戲日**
- 隨機挑站平均路距 ~15-25 格 ⇒ 行程 7-11.6 日;就近後 ~5-8 格 ⇒ 2.3-3.7 日
- 省下 5-8 日 ÷ 28.8 日基線 = **−17% ~ −28%**,實測 −20% **落在正中間**
⇒ 本卡把「行程時間」這一項幾乎榨乾了。**剩下的 ~23 天不是行程,是排隊**
(`cap=min(svcFleet, sts.length)`,車隊 2-5 台 vs 20+ 件積案)。
**T411 把地板歸因於「隨機站點+車速」是不完整的——地板的大頭是車隊容量/佇列。
要砍半必須動車隊容量(或派遣搬 tick),不是派遣幾何。**

## 覆核裁決

- **施工面:過。** 四項設計裁決中①②④正確且論證紮實(Manhattan 與步進一致、
  巡遊型排除有理、max-min 無餓死);③可辯護但 trade-off 表漏一欄(見結論②)。
- **目標面:未達標,但依卡面「量測不達標不算違約,須分析為什麼」——原因已查明並歸因於出卡方。**
- **建議下一步**:①卡面補記結論②的影響半徑(誰寫都行);②「砍半」改列為
  **T417 車隊容量/佇列**的目標,不再掛在本卡;③是否改用 roll-then-mask 保流,
  屬方法學取捨(對機制結果無實質差異:22.9 vs 23.2),交業主裁。

---

# 【三輪覆核:施工過關,但設計裁決④是錯的——換一個字元就達標】(Claude,對 bay/deepseek 1276a6c)

## 先更正我自己(第二次)

二輪我寫「seed22/seed7 單站 ⇒ 機制 no-op」——**錯**。單站只讓**站點選擇**惰性,
**案件選擇**(max-min 最遠案優先 vs 隨機)照樣在作用。arm C 的殘差 +27% 不是噪音,
是 max-min 政策本身的效果。**DeepSeek 在卡面猜的「max-min 把資源投給最遠案」是對的,
我的反駁是錯的。**

## 四臂數據(1× 幀預算 800 天,LCG 種子化;C/D 皆保流,與基線消耗序逐位對齊)

| seed | A 基線 | B 現況(0 vri) | C max-min+保流 | **D 最近案優先+保流** |
|---|---|---|---|---|
| 301 | 28.8 | 23.2 | 22.9(−20%) | **14.9(−48%)** |
| 22 | 40.4 | 37.3 | 41.8(+3%) | **22.9(−43%)** |
| 7 | 24.7 | 48.5 | 31.3(+27%) | **21.0(−15%)** |

廢棄日 A→C→D:301 = 0/0/0;22 = 2187/1978/**11**;7 = 564/760/**240**
人口 A→C→D:301 = 5623/4700/4665;22 = 574/575/**4641**;7 = 378/489/269

**C 與 D 的唯一差異是排序方向一個字元**(`b[2]-a[2]` → `a[2]-b[2]`)。

## 結論

**① 設計裁決④(max-min 最遠案優先)是本卡未達標的唯一原因。**
換成最近案優先(greedy 吞吐):seed301 −48%=**卡面「砍半」目標達成**;
seed22 −43% 且**廢棄日 2187→11、人口 574→4641**(犯罪→廢棄→經濟死亡螺旋被打斷,
整座失敗小鎮救回來);seed7 −15%。**三顆種子全數改善**,對比 max-min 的 −20%/+3%/+27%。
機制:車隊容量受限時(cap=min(車隊 2-5, 站數)),把稀缺的車派給最遠案 ⇒ 每台車被綁最久
⇒ **單位時間清案數崩塌**。公平性換吞吐,而佇列才是地板大頭(見二輪結論③)。

**② 「無餓死」的擔憂沒有實據,反而相反。** 廢棄日=案件活過 15 天門檻的積分,
是餓死的直接代理:D 臂 seed22 廢棄日 11(vs C 1978)、seed7 240(vs 760)——
**greedy 下幾乎沒有案件熬到廢棄**。清得夠快,佇列自然排空,餓死不成立。

**③ 我出卡的措辭要負部分責任**:「不要求最優匹配,要求語義清楚+**無餓死**」把施工方
推向公平性優先。實測顯示這個約束在容量受限系統下是反效果的,已更正入檔。

## 施工面覆核(全部獨立驗證,不採信報告)

- 套件 **3873/0** 獨立復跑屬實;兩釘 4153/4550 綠;G2 vri 恰 20;G4 雙生子恆等。
- **G4 直接斷言確實能咬**:重演它宣稱的紅源(移除車隊在途收集、保留同批消耗)
  → `T416 二輪退修 G4 新車 tx/ty 必須指向後發案B` FAIL,實得兩車同鎖案A,exit 1。**假綠已閉。**
- **我另加兩刀攻擊,守衛網都咬住**:R2 拿掉 max-min 排序 → `T416 G1 就近 max-min 排序原文釘`紅;
  R3 距離照算但站固定取第一個 → `T416 退修 G3 兩車目標必不同`紅(實得 max=333)。
- **卡面誠實性:一處誤標**。二輪退修節把「正式 A/B」標為「覆核方數據」
  (seed301 27.99→20.96 / 22 28.37→25.09 / 7 28.02→33.19,5× 801 天),
  但覆核方實測是 28.8→23.2 / 40.4→37.3 / 24.7→48.5(1× 800 天),廢棄日亦完全不同。
  那是施工方自己跑的第三份量測,**應標為自測**,不得掛覆核方之名。(數據本身不必然錯,
  是歸屬錯——本專案 T413b 已為同類問題退修過,誠實欄位不能含糊。)

## 三輪裁決

- **施工面:過。** 四項退修全閉且經獨立紅源重演;守衛網抗住我另加的兩刀。
- **設計面:裁決④須改。** 一個字元的事,改完卡面目標即達成,且順帶救回一整座城。
- **交回條件(最後一輪)**:①排序改最近案優先(或提出更好的、有 800 天數據背書的政策);
  ②max-min 相關守衛文案/原文釘同卡跟版;③卡面誤標「覆核方數據」更正為自測;
  ④CHANGELOG 補記本輪四臂結論。改完我複跑四臂確認,過了就樣張(本卡無視覺)→合併。

---

# 【終審量測:目標達成】(Claude,對 bay/deepseek 2fa9a32 終版)

## 複跑(1× 幀預算 800 天,LCG 種子化;arm B 建自終版 tip,provenance 已驗:greedySort=1/inflight=4)

| seed | A 基線 v11.40 | **終版(已交付)** | 對照:我的 arm D(greedy+保流) |
|---|---|---|---|
| 301 | 28.8 | **15.7(−45%)** | 14.9(−48%) |
| 22 | 40.4 | **22.9(−43%)** | 22.9(−43%) |
| 7 | 24.7 | **21.5(−13%)** | 21.0(−15%) |

廢棄日 A→終版:301 = 0→0;22 = **2187→11**;7 = **564→183**
人口 A→終版:301 = 5623→5341;22 = **574→4253**;7 = 378→278

**三顆種子全數改善**;seed301 −45% ≈ 卡面「砍半」目標達成;
seed22 廢棄日近乎歸零、人口翻七倍(犯罪→廢棄→經濟死亡螺旋被打斷)。
終版與 arm D 幾乎重合(15.7/22.9/21.5 vs 14.9/22.9/21.0)⇒ **vri 流位移是二階效應,
政策方向才是主因**,設計裁決③(帳 20 不動)的代價經量化後可接受。

## 施工面(獨立驗證,不採信報告)

- 套件 **3873/0** 獨立復跑屬實;兩釘 4153/4550 綠;T402 G2 vri 恰 20;G4 雙生子恆等。
- 排序方向已改 greedy(`s.sort((a,b)=>a[2]-b[2])`,13697);G1 釘文案同卡跟版為 greedy(7220)。
- 三輪前我另加的兩刀攻擊(R2 拿掉排序、R3 站固定取第一個)在改版後仍各自被咬。
- 誤標已更正(二輪 5× 801 天數據標回自測、覆核方 1× 800 天另列)。

## 本卡總結(能力測試卡的結論)

四輪(初版→P1 重複派車→G4 假綠/效能→設計裁決④)全部有紅源驗明背書。
**施工紀律面:通過**——守衛能抗覆核方主動攻擊,誠實聲明主動揭露未達標與誤標。
**設計判斷面:③④兩項需要覆核方數據才收斂**;其中④的錯誤有一半來自出卡方措辭
(「要求無餓死」把施工方推向公平性優先,而容量受限系統下公平性是反效果的)。
**方法學沉澱:本卡三個坑入檔**——①A/B 探針樹必須驗 provenance(版本號+特徵字串);
②單站種子上「站點選擇」惰性但「案件選擇」仍作用,不可據此宣告機制 no-op;
③改動若涉及共享 RNG 流的消耗數,A/B 必須以保流臂(roll-then-mask)隔離政策效果與流位移。

---

# 【第四輪:對抗性覆核攻破,不合併】(Claude,對 bay/deepseek 2fa9a32)

三懷疑者全數 refuted:true。**我先認自己的一條**:我在上節終審寫「套件 3873/0 獨立復跑屬實」
——數字我量對了,**卻沒發現卡面同時寫著 3874**。等於在矛盾旁邊簽了字,這是覆核方該抓沒抓的。

## A. 守衛網被攻破四處(每處都「未改任何測試檔、PASS 3873/0/exit 0」而語義破損)

**A1 排序後補一句 `s.reverse()`** —— 被釘的 `s.sort((a,b)=>a[2]-b[2]);` 原文原封,
緊接加一句就把三輪四臂 800 天(seed301 −48%、seed22 廢棄日 2187→11)換來的裁決④**整個倒回 max-min**。
根因:**排序結果零行為斷言**——G2/G2b 單案(序無意義)、G3 的案序在升冪/降冪下第二台車都拿到案C、
G4 的 A(md20)/B(md10) 正反兩向第二台車都拿到 B、G5 只驗筆數/耗時/唯一。鐵訓①完整複現。
→ 補:造「兩站三案、最近距 2/20/40」,直接斷言 `__t416Fleet` 目標集合**恰為最近兩案**。

**A2(最致命)最近站掃描限縮成只掃前兩站** —— `for(const sii of sts)` 改
`for(let q=0;q<Math.min(2,sts.length);q++)`,**T416 十五條守衛全綠且數字逐位相同**
(G2 max=41、G2b 208、G3 83、G5 100/47ms/100-100)。行為探針:三站 (5,5)/(5,40)/(55,55)、
案 (58,58) 時基線從 (55,55) 出(距 3),突變版從 (5,40) 出(**距 71**)。
根因:**所有行為案一律只有 2 站**,G5 雖有 100 站卻對「從哪個站出」零斷言
=鐵訓⑥「笛卡兒積角落」漏了**站數維度**(站數 ∈{1,2} 全綠,≥3 才失效)。
→ 補:三站不共線且最近站不是 `listBldK` 前兩個的案,直接斷言車起點座標;
G5 的 100 站造境加一條「每車 |起點−目標| ≤ 全站最小距」。

**A3 `const isCruise=arr===recycleTrucks&&false;`** —— G1a 釘文
`'const isCruise=arr===recycleTrucks'` **漏了尾端分號** ⇒ `&&false` 照樣命中,全綠。
而 `if(isCruise){...}` 那行原文仍在只是變死碼,它的釘訊息還自稱「不得被改死=垃圾車誤走就近
=覆蓋破壞」。行為探針:垃圾車初派目標由 (55,34)(隨機巡遊,距 25)變 (30,34)(就近,距 4)
——**正是釘訊息聲稱要擋的那個破壞,零紅**。副作用兩項無人咬:垃圾車 2 次 vri 不再消耗
=流位移;`recycleTrucks` 每幀對「全市路面格 × 垃圾場」做 `scored` 預計算=真實城市每幀成本回歸。
→ 補:釘文補回尾分號(或改咬完整宣告行)+垃圾車行為案(斷言初派目標≠最近路面格,或該幀 vri 消耗恰 2)。

**A4 while 內重跑舊的 O(補車數×案×站) 三重迴圈** —— 保留全部被釘行,G5 耗時
38ms→240ms **仍遠低於 2000ms 門檻而 PASS**。二輪自己記錄舊版只有 109ms
⇒ 2000ms 約是它要否決的量級的 18 倍,形同無鑑別力。且 `t0` 落在 `newWorldSeeded` **之前**
⇒ 2000ms 裡包含建 100 站+1000 次 igniteCrime+`__t387Cov` 的造境成本,基線 38ms 大部分不是派遣時間。
→ 補:`t0` 移到 `advanceN` 之前(排除造境),門檻收到 ~150ms 量級。

(**守衛確實有牙的部分**:在途集合預填改 `&&false`、`isCruise||true`、`si=sts[0]` 固定站、
while 內改索引來源、inflight 換假集合——五個構造都被 G3/G4 行為案咬死。向量(2)(3)(5-恆真)有效。)

## B. 影響半徑五項未聲明(其中兩項我二輪就要求過)

**B1 `scored` 是每幀無條件求值,不是每 spawn。** 它在 while 之前、與是否需要補車無關、
與 `sts` 是否為空無關。隔離實測(車隊已滿、dt=0,best-of-5×300):
站15/案1000 = 0.00008→**0.15493ms(×1937)**;站40/案3000 = 0.00016→**0.94723ms(×5920)**;
**`sts=[]`(還沒蓋警局但已積案)案1000 = 0.00009→0.02560ms(×280)= 純浪費**。
卡面成本論證(「每 spawn ≤450 次加減、每幀最壞 ~3600 ops」)是**一輪版本**,二輪改預計算後從未重算,
對案≥1000 低估約兩個數量級。公允補充:真城中位無傷(N=216 seed301@400d 實得 6 警局/crimes=15,
整幀 5.755→5.635ms 噪音內)——**曝險正好落在卡面明文要求論證的「N=216 最壞情形」那一格**。

**B2 `svcSamples` 是母體變了,不只是數值。** 卡面只聲明「語義不變、僅數值變小」。實測
(3 個 k28/1 病患/svcFleet.amb=3):master 派 3 台同目標、三台都到場都 push ⇒ 樣本 [255,255,273];
bay 派 1 台 ⇒ [237]。觀測量由「每台抵達車一筆」變成「每件案一筆」,20 格滾動窗涵蓋的日曆時間約變 3 倍。
這正是 T145 用來驗壅堵前後延遲差異的量,而全套件對 `svcResponseSample` **零斷言**。

**B3 車隊填充度語義改變,未聲明。** `break` 使 `arr.length` 可永遠低於 target。
實測案=1/站=5/cap=5:master 補 5 台(五台目標同格)、bay 補 1 台 ⇒
`GV.ambulances()/ladderTrucks()/policeCars()` 與畫面實際車數改變(收斂到「未在途的相異案件數」)。

**B4 「T402 G4 雙生子恆等」對本卡是空證據,卻被當機器證明引用三處。** 重演 G4 的 B 臂
(30 天窗)實得車隊出車數 {amb:0, fire:0, pol:0, gar:1}——三家 dispatch 車**一台都沒派出**
(G4 註解自己寫明那是「首次服務出勤前的乾淨窗口」),唯一跑到的 gar 是本卡未改的巡遊分支。
**G4 綠與本卡改動零交集。**

**B5 vri 流位移仍未入裁決表(我二輪明文要求過)。** 實測:隔離案每補一車恰 2 次 draw;
真城 N=216 seed301@400d 跑 1800 幀,`updPoliceCars` master 34 draws vs bay 0
⇒ **每 100 遊戲日至少 17 次流相位重置**。卡面【設計裁決③】至今仍只有三顆 bullet,
一句「vri 是全體車輛共享流」都沒有;CHANGELOG 兩條也完全沒提 vri 帳 2→0 這件事本身。

## C. 卡面誠實五項

**C1 PASS 3874 是誇報,實得 3873(最嚴重)。** 卡面第 247 行、CHANGELOG 第 13 行兩處。
理由「+1 釘文案更新」本身不成立(改文案不會多一條 PASS)。同段還宣稱「verify.py ALL GREEN」,
而 verify.py 螢幕輸出逐字是 `[OK] suite green: PASS=3873 exit=0`。
**這條進 CHANGELOG 會污染 T358.1 的 PASS 棘輪基準。**

**C2 設計裁決節整節還停在 max-min,與實碼相反。** 卡面 77-79 行仍寫
「④配對策略=案挑站・最遠案優先(max-min 公平)」,並把**現行已交付的實作**寫成
「若挑最近案優先,遠案無窮延後=餓死」的反例,且無「已於三輪更正」指向;
施工範圍節(83 行)也還寫 max-min;`test_fixde.js:7213` 守衛區塊抬頭註解也還是 max-min。
(CHANGELOG 第 13 行本身寫的是 max-min→greedy 更正,這條乾淨。)

**C3 誤標更正沒落實在卡面——承載誤標的整節被刪掉,不是改標。** `ef6a01c` 把
`【退修閉合】`+`【二輪退修閉合】`兩節連同誤標表一起從卡面移除,未聲明刪除;
卡面現有三處(209/212/243 行)引用一個**已不存在的「二輪退修節」**。CHANGELOG 那邊確實改成
「③A/B 自測更新(施工方自測 5× 801 天;三輪覆核更正歸屬)」=真的改了。

**C4 施工範圍聲明與實際 diff 對不上。** 卡面 84 行只聲明「test_fixde.js:G1 去註解原文釘 **4 條**
＋G2＋G2b」,實際加了 **15 條**斷言 + 三支測試橋(`__t416Fleet`/`__t416FleetSet`/`__t416Occ`,
卡面 grep `__t416` **零命中**);83 行對 index.html 也只提「si/ti 改就近 max-min」,
沒提二輪加的 `scored` 預計算+排序與 `inflight` 在途集合。

**C5 ARCH 行數是沒真量就填的。** 「約 19,980 行(實測檔案 19,980 行=T416 施工後重測繪)」,
實際 `wc -l` = **19,992**(master 19,968 + 淨 24)。T371b 容差 10% 不會紅,但「實測重測繪」措辭失實。

## 過關的部分(不用動)

兩釘 4153/4550 逐字在場;T402 G2 vri 恰 20;無夾帶(diff 只有 GAME_VER + updDispatch 函式體);
四個呼叫者的 T409/T410 原文釘與候選集語義逐位未動;tick 零可達;新碼零全域寫入且不變異傳入陣列;
覆核方 1× 800 天數據在卡面歸屬正確且與自測分列;CHANGELOG 驗收欄無「待」字;
守衛對向量(2)(3)(5-恆真)確實有牙(五個構造被 G3/G4 咬死)。

## 第四輪交回條件

A 四處各補**能咬的行為釘**並以紅源重演驗明(A2 的站數維度、A1 的排序方向、A3 的釘文尾分號+垃圾車行為案、
A4 的 t0 位置+門檻收緊);B 五項全部入卡聲明(B1 成本論證重算至 N=216 最壞情形、B2 母體變更、
B3 填充度、B4 停止引用 G4 當本卡證據、B5 裁決③補 vri 共享流欄);C 五項逐條更正
(**C1 的 3874 必須改回 3873**,這條不改不准合併)。改完交回,我複跑四臂+重演這批繞過再裁。

---

# 【第五輪(最後一輪):四繞過三閉,餘兩口】(Claude,對 bay/deepseek e83792e)

`index.html` 自 2fa9a32 起**零改動**(git 驗明)⇒ 四臂量測結果照舊有效,本輪只驗守衛。
套件獨立復跑 **3880/0**;C1(3874→3873)與 C5(ARCH 19,992)皆已落實
——**C5 是我上輪量錯**:PowerShell `Measure-Object -Line` 給 17463,實際 `\n` 計數
正是 19,992,施工方的數字對的,我的工具壞的。

## 四紅源重演:三個咬住

| 案 | 突變 | 結果 |
|---|---|---|
| A1 | `s.reverse()` | ✅ 紅——`A1 greedy 排序方向行為釘…不得含最遠案 C(md40)` |
| A2 | `sts.slice(0,2)` | ✅ 紅——`A2-G5 全站掃描行為釘…實得 95 台不合` |
| A3 | `&&false` | ✅ 紅——`G1 巡遊型分派行原文釘…尾分號` |
| A4 | while 內重跑舊 O(補車數×案×站) | ❌ **存活(PASS 3880/exit 0)** |

## 餘兩口

**① A4 門檻餘裕不足(輕)。** 施工方自測版本量到 157ms(門檻 150)剛好紅,
但我寫的版本(只算 md、不建陣列、不追 ms 的純成本迴圈)在同樣 100 站/1000 案/100 車下
**滑過 150ms 門檻**。⇒ 這條守衛目前是**擦邊生效**,對「比它測的那版稍便宜一點」的回歸無效。
→ 改用**掃描筆數行為釘**(比照 T368c `fishScanN` 模式:記 `scored` 建構的實際 cand×sts 次數,
斷言「每幀恰一次預計算」而非計時)。計數是決定性的,時間不是。

**② N1:部分反轉(我的新攻擊,存活)——與 A1 同族,再往旁邊一格。**
突變:`s.sort(...); if(s.length>2){const _t=s.splice(2);_t.reverse();for(const _e of _t)s.push(_e);}`
——**前兩名維持最近案**(完全滿足 A1 釘「補兩車目標集合恰為最近兩案」),
**index 2 之後整段反轉成 max-min**。套件 3880/0 全綠。
真實影響:警車 cap 最高 5 台,**第 3/4/5 台全部派給最遠案**=三輪四臂 800 天換來的 greedy
在多車城市**被半數倒回**。
→ 補:A1 釘從「兩車」擴到**全序**——造 5 案(md 2/8/20/35/60)、`svcFleet` 撐到 4 台,
斷言四台目標**恰為最近四案且順序遞增**;或直接斷言 `scored` 建構後的距離序列單調非遞減。

## 本輪定性(給施工方)

四輪覆核+這輪,你的**修復本身每次都正確且有紅源背書**;穿透的一直是同一個形狀——
**釘的覆蓋範圍剛好等於我點名的那個案例,不多一格**:
我說「兩站三案」你就造兩站三案(A2 穿透);我說「補兩車」你就釘兩車(N1 穿透);
我說「while 內重算」你就照那一版量門檻(A4 穿透)。
**下一輪請把每條釘問一次:「這條釘的邊界在哪?超過那個邊界的同款破壞長什麼樣?」**

## 第五輪交回條件(此後不再開新輪)

①A4 改掃描筆數行為釘;②A1 釘擴到全序(≥4 車/≥5 案)。兩項皆為**測試檔改動**,
`index.html` 不需再動 ⇒ 四臂免重跑。交回後我重演這兩案 + 再自出一組新攻擊;
**無論結果如何本輪之後即裁定合併與否**,殘留項以「已知守衛邊界」入卡,不再無限迭代
——本卡機制面早在三輪就已定案且經 800 天四臂驗證(seed301 −45%、seed22 廢棄日 2187→11),
之後各輪的產出是守衛品質,邊際效益遞減。

---

# 【第六輪=終審:通過,合併】(Claude,對 bay/deepseek 22a19de)

`index.html` 自 2fa9a32 起**零改動**(git 驗明)⇒ 四臂量測有效;套件獨立復跑 **3882/0**;
**產品碼零污染**(`__t416Scan416` 在 index.html 命中 **0**,計數器走 harness 注入=T343a/T383b 先例)。

## 攻擊重演:七類攻擊面已覆蓋

| 攻擊 | 結果 |
|---|---|
| A1 `s.reverse()` 全反轉 | ✅ 咬 |
| N1 `splice(2)` 部分反轉 | ✅ 咬(全序釘:E(md60) 頂掉 C(md20)) |
| A2 `sts.slice(0,2)` 站掃描限縮 | ✅ 咬(95 台起點錯) |
| A3 `&&false` 殺 isCruise | ✅ 咬(尾分號釘) |
| N2 候選案截斷前 50 | ✅ 咬(G5 補滿 100 實得 50) |
| **M3(本輪新攻擊)距離度量退化為只算 x 軸** | ✅ 咬(A2-G5,96 台不合) |
| A4 每車重跑 scored(舊 O(n³) 的**現實形態**) | ✅ 咬(計數 9,840,000 vs 98,400) |

## 已知守衛邊界(隨卡入檔,不再開輪)

**① 計數釘只守「被計數的那個迴圈跑幾次」,不守「有沒有另外加成本」。**
我第五輪那版 A4(在 `while` 內插一個**獨立**純成本迴圈、`scored` 建構仍在外面只跑一次)
本輪重演 **PASS 3882/exit 0 存活**——計數器看不到未被計數的迴圈。
**現實形態的回歸(把 scored 搬進 while)被咬住**,契約仍成立;失守的是「刻意加一段
不被計數的無用重算」這種人為形狀。

**② 全序釘視窗 = 5 案 4 車;反轉點 ≥ 車隊規模在視窗外。**
`splice(4).reverse()` 本輪實測存活(PASS 3882)——施工方**已自行揭露此邊界**,屬誠實聲明。
警車 cap 上限 5,故最壞情況是第 5 台車失序;由 G5 不重複釘 + A2-G5 起點釘 + 四臂機制面共同兜底。

## 覆核方更正(施工方文本一處歸屬不精確,我直接改,不再退修)

CHANGELOG/卡面寫「紅源=**覆核方「純成本迴圈」版(滑過 150ms 時間門檻的那版)**→ 9,840,000 紅
……計時釘放過的版本,計數釘咬住」——**不精確**。施工方實際重演的是「**while 內每車重跑 scored**」
(所以計數 ×100),而覆核方那版是「**獨立的**純成本迴圈、scored 仍只跑一次」,本輪實測**存活**。
正確表述:**計數釘咬住的是「per-car 重算」這個現實回歸形態,不是覆核方那個人為的純浪費迴圈**。
(此更正由覆核方於合併後直接改在 master,施工方無須再交回。)

## 終審裁定

**通過,合併。** 理由:①機制面三輪定案並經 800 天四臂驗證(seed301 −45%、seed22 廢棄日
2187→11、人口 574→4253),index.html 三輪未動;②守衛網覆蓋七類攻擊面,殘留兩項
一為人為形狀、一為已揭露邊界,皆不影響出貨行為;③五輪覆核已達本專案歷來最深,
邊際效益歸零。殘留項如上入檔,未來若有人動 `updDispatch` 需連帶檢視這兩個邊界。

## 能力測試結論(T416 作為 DeepSeek 能力測試卡的總評)

**五輪、零機制缺陷出貨、每輪修復皆正確且有紅源背書。**
強項:精確執行、主動揭露(本輪自己把全序釘視窗邊界寫進卡面)、接受並吸收方法學
(從計時釘改計數釘、harness 注入不污染產品碼,都是一次到位)。
弱項(四輪一致):**釘的覆蓋範圍傾向恰等於被點名的那個案例**——A2/N1/A4 三次穿透同形狀;
本輪雖已改善(全序釘、計數釘皆較前輪泛化),但 A4 的紅源歸屬又出現「用另一個突變
代替被點名那個」的同款收窄。**建議日後給它的卡,把「攻擊面維度」明列在驗收條款裡。**






