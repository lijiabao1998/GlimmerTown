# T593 都會巨廈新圖（105／106，實驗線 T516 生成器移植）

**發卡／施工**：業主 2026-09-26「下一步吧，你很棒完全沒問題的」（T592 定稿之後）。下一步的盤點見本卡「定位錨」：住商工（k1–3）走實驗線的超街區立面系統，要分期並先請業主看決策單；105／106 在實驗線是靜態 `SPR.bld` 鍵，可以照 T590 的做法先上。Claude 出卡＋施工。
**claim**：Claude；canonical master 直寫（T373 路徑），base `119f29e`／v11.201。來源釘在實驗線 `origin/main` **15ac877**（2026-09-26；比 T590 讀的 25ec3b8 新 38 個提交，含 T670 工業配色）。開工前 `git fetch origin` 驗兩向差 0。驗證埠 8129+（真 Chrome、載入前設槽 3），不碰 8123／8199。
**合併權**：直寫 master，落地後 `merge_bay.py --publish` 與 `git push`。Claude 出卡＝施工同一方，未經第三方覆核，如實聲明。

## 定位錨

- 住宅巨廈 k105／商業綜合體 k106 由 3×3 同類 Lv2+ 簇進化而成（`index.html` T341 段），放置時寫死 `v:0`；主線現圖是 T341 的 208×220 程序圖。
- 實驗線：`applyT516MetropolitanArt` 以 `makeTowerSprite516(105,2,true)`／`(106,0,true)` 生 304×470 巨廈（ax152／ay466，只用 `metroRand516` 局部亂數），接著 T541 夜暈；`variants574/b05.js` 再補兩類的 v1／v2（各自帶 T541 夜暈）。T573 邊緣光因面積超過 62000 跳過。
- 實驗線的 `isoBox`、`windows` 與主線版本不同（T541 受光方向等），要照實驗線版本另存一份，不能用主線的。
- 同一份盤點的其餘部分（住商工立面、超街區、財富、高密度、冬季、天際線梯度、33／34）另有決策單，本卡不碰。

## 做法

1. 內嵌實驗線 15ac877 的 `metroHash516／metroRand516／metroPalette516／metroPlate516／metroFacade516／metroGable516／metroCategoryMark516／makeTowerSprite516`，以及它們用到的 `isoBox`、`windows`、`outlineSprite`（實驗線平色版），全部加 `593` 尾碼改名；`b05` 批次原樣內嵌為 `port593_b05(A)`。
2. 新增 S20 `buildSpritesS20()`，接在 S19 之後，兩條開機鏈各一次：先生 105／106 v0，套 T541 夜暈（只套這兩張），再以只含這兩個鍵的代理精靈表跑 `b05`，只收 105／106 的 v1／v2（b05 其他類的段落在代理表裡找不到底圖、原地返回，不碰 T590 已定的鍵）。新物件帶 `__t590`（沿用 T590 對新圖的繪製端處理）與 `__t593`。
3. 繪製端：兩類加入 T590 的格雜湊選款（放置寫死 v:0）；新圖 470 高，剔除邊距只對這兩類放寬；T413b 寫死的商業霓虹對新 106 跳過（實驗線靠 T625 錨點修正畫在前緣，主線沒有 T625，照舊錨點會浮在塔後）。
4. 零世界亂數：全部只用 `metroRand593`（mulberry32 局部）與 b05 內的局部 `R`；六哨兵逐位恆等。
5. 逃生閥：`?noT593=1`／`window.__noPort593`。

## 允許觸碰

- `index.html`：T593 區塊、兩條開機鏈各加一次 S20、繪製端選款條件、剔除邊距、霓虹條件。
- `test_fixde.js`：新增 T593 守衛；既有守衛因換圖轉紅者（預期：T417 M3 量到 105 屋頂道具、T421 夜之城、T590 G5c 選款原文釘、零亂數掃描）逐條記錄再決定。
- `docs/SPR_PINS.json`（白名單：`bld/105_1_*`、`bld/106_1_*`）、`docs/ARCH.md`、`sw.js`（只經 bump）、`docs/CHANGELOG.md`、本卡、`docs/tasks/t593-shots/`。

## 驗收

- verify.py ALL GREEN、PASS 不降、六哨兵恆等；工具鏈 OK；CRLF 0。
- 真 Chrome：主線新圖與實驗線 15ac877 的 105／106 v0–v2 日夜逐位相同。
- 真城實拍：巨廈日夜、旋轉；螢幕邊緣剔除不會提早切掉塔身。

## 回滾

`git revert <T593 commit>`；或 `?noT593=1`／`window.__noPort593=true`。

## 施工紀錄

2026-09-26，Claude（Opus 5.5）施工，v11.201→v11.202。

**落地內容**
- `index.html` 在 T590 區塊之前插入 T593 區塊（約 1,140 行）：實驗線 15ac877 的 `metroHash／metroRand／metroPalette／metroPlate／metroFacade／metroCategoryMark／metroGable／makeTowerSprite`、`isoBox`、`windows`、`winShape570`（全部加 593 尾碼；主線同名函式版本不同，不共用）、T541 夜暈的單張版 `halo593`、`b05` 批次原樣內嵌為 `port593_b05`、`buildSpritesS20`。描邊沿用 T590 的 `outlineFlat590`（與實驗線 `outlineSprite` 逐字相同，已比對）。區塊放在 T590 區塊之前，不進 T590 的零亂數掃描窗，另有自己的掃描窗。
- 兩條開機鏈各一次 S20（同步鏈 `…S19();buildSpritesS20();`，分段鏈在 S19 下一行）。
- S20：105 v0＝`makeTowerSprite593(105,2,true)`、106 v0＝`(106,0,true)`，各套一次夜暈；以只含這兩鍵的代理精靈表跑 b05，只收 105／106 的 v1／v2。新物件帶 `__t590`（沿用 T590 繪製端對新圖的處理）與 `__t593`。
- 繪製端：105／106 加入 T590 的格雜湊選款（放置寫死 v:0）；剔除下緣只對新巨廈另放寬 440*z（塔頂在錨點上方約 466px，底座離開畫面下緣時塔身仍要畫）；T413b 寫死的商業霓虹對新 106 跳過。
- 抽取時的一個坑：`metroFacade516` 的參數預設值 `opt={}` 被第一版抽取器當成函式本體，已改成先配對參數括號再找本體。

**真 Chrome**
- 像素忠實度：主線新圖與實驗線 15ac877 的 105／106 v0–v2 共 6 張，日圖、夜圖、尺寸、錨點**逐位相同**（CRC32 對照）。S20 報告 ok、零例外、6 鍵、夜暈 7,024 像素。
- 真城實拍：一排 105／106 夾舊住商與舊 33／34，日、夜、近景、旋轉 90°、「底座在畫面下緣外」各一張，errLog 0；最後一張確認塔身照畫。圖在 `docs/tasks/t593-shots/`。

**像素指紋**：1,548→1,552 鍵，新增 4（`105_1_1／_1_2`、`106_1_1／_1_2`）、變更 2（`105_1_0`、`106_1_0`）、移除 0，全在白名單內；`--check` 零差異。

**既有守衛調整**
1. T590 G1a／G1b 開機鏈原文釘：S19 之後接 S20（位置一起釘）。
2. T590 G421 夜之城取代數 56→58（T593 的 105_1_0、106_1_0 也是新圖物件）。
3. T590 G5c 選款原文釘：條件多了 `||bd.k===105||bd.k===106`。

**新增 T593 守衛**：G1（S20 呼叫恰 2、定義恰 1）、G2a–c（零例外、恰換 6 鍵；304×470、錨 152／466、1:1、日夜齊；b05 只收 105／106，T590 已定的 44／48／65／87／102 v0–v2 一個都不被換掉）、G3a–c（零世界亂數：區塊內不得 ri／Math.random／spriteTexRand；`rand(` 只准是 windows593 的參數流；`R(` 只准是 b05 內 20 處局部 R）、G4a–c（剔除放寬、106 不疊霓虹、夜暈與代理表原文）、G5a–b（逃生閥真撥：關掉 S20 重建後六鍵退回 T341 舊圖 208×220，再打開全部回來）。

**紅源（整倉複本、不含 .git，兩份複本並行，逐案單一突變）**
| 突變 | 結果 | 首紅 |
|---|---|---|
| n1 同步鏈拿掉 S20 | exit 1 | T590 G421（巨廈沒換到，夜之城取代數退回 56） |
| n2 b05 直接寫真精靈表 | exit 1 | T590 G7a（b05 蓋掉 48／65 的 v1／v2，新物件沒有冬側版） |
| n3 不套夜暈 | exit 1 | T593 G4c |
| n4 剔除不放寬 | exit 1 | T593 G4a |
| n5 逃生閥失效 | exit 1 | T593 G5a |
| n6 生成器塞 Math.random | exit 1 | T593 G3a |
| n7 巨廈不選款 | exit 1 | T590 G5c（選款原文釘） |
| 控制組（無突變） | exit 0 | — |

n1、n2 的首紅是排在前面的既有守衛，攔到的是同一個錯誤的後果（巨廈沒換到／其他類被蓋掉）；n2 的目標釘 T593 G2c 已加強為逐一檢查 44／48／65／87／102 的 v0–v2 仍是 T590 物件。夜暈像素 Node 看不到，n3 由原文釘攔下，實際像素由真 Chrome 逐位比對承接。

**待決（決策單第 5 題）**：新巨廈旁邊的舊 33／34 摩天樓顯得矮小（見對照圖），要不要一起換成實驗線 T516 款，交給業主看決策單再定；本卡照卡面預設三款依格雜湊。

**收束**：六哨兵 seed301 780／seed301m 2297／seed7 117／seed7m 77／seed22 639／seed22m 14523 逐位恆等；`verify.py` ALL GREEN（PASS=12317、CRLF=0、11.202 雙版本同步、ARCH 錨點新鮮）；`python -B -m unittest tools.test_toolchain` 63 tests OK；`git diff --check` 乾淨。
