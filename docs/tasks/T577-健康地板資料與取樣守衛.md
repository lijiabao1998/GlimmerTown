# T577 健康地板資料與取樣守衛

日期：2026-09-05。出卡／施工：Codex。車位 bay/codex，base 7e754e7（T576，v11.189）。
授權：業主「注意施工紀律gpt，剩下的自我迭代兩輪」的第二輪；兩輪到此結束，不另續卡。
claim：Codex；認領 commit 為本卡首次提交。驗收:未驗證（Codex施工中）。不自合、不publish。

## 定位錨與真問題

`healthFloor575()` 先 Math.round 再比較地板，Infinity 與小數可放行；
T575 G1c/G1d 僅看整數與末筆字串，負值／不安全整數／空白理由／錯日期／歷史方向不一致均缺網。
隔離bootstrap對現版做12案注入，全部 exit0（schema-before-*.log），不是凭推測增加抽象框架。
T576規劃helper已有零乱数釘，新增caller區尚未單獨釘住；此卡補網、不改AI。

## 允許觸碰

1. test_fixde.js：T575資料驗證旁增加小型完整history validator；實際取樣在取整之前驗原始值。
2. 新增validator正反例與呼叫端驗證；保留T575原有G1c/G1d及所有既有斷言，不更動期望值。
3. T576 caller兩個唯一內容錨之間的零乱数靜態守衛，沿用stripCommentsAndStrings438，不依行號或首個右括號。
4. docs/HEALTH-PINS.json僅更正__meta過度保證文字；本卡、CHANGELOG及必要的ARCH狀態說明。

## 禁區

index.html/sw.js/runtime、tools/、AI、亂數、存檔、sprite全不動；不bump、不改六哨兵／15地板／measured／target／history。
不得以假造授權記錄讓下降變合法；本卡只驗內部資料契約，不假稱驗證了業主真授權或git歷史永不被改。
不改玩家／master／其他車位，不清檔、不結束既有進程。

## 驗收契約

- floor/measured/target/history.value：非負安全整數。actual：同樣驗原始型別與數值，不讓取整／強制轉型掩蓋錯值。
- history逐筆驗：非空物件、真實YYYY-MM-DD日期、非空白why/authorized；首筆init且等measured；後續date非遞減，up/down與數值嚴格方向一致；尾值=floor。
- 正例含目前15項、同日上調、合法下調資料、合法閏日；後兩項只是資料格式正例，不構成降低正式地板的授權。
- 每組拒收規則均附可重放突變：12既有假綠案重新跑必須exit1且首紅指名T577；actual另對五欄逐一注入字串／小數／Infinity，必走真healthFloor575。
- caller前後段各植入一次直接R呼叫，新增守衛必紅；註解及字串內容綠，避免文字假紅。
- 對base的15項數值／歷史逐位元等值，T576之前history前綴對077e43c獨立核對；不加入永久git依賴或新ledger schema。
- 完整verify ALL GREEN，PASS≥10447，六哨兵恆等；runtime／sprite基線SHA與7e754e7相同，故不重烘、不部署。

## 回滾／停止

僅撤銷本卡新增段，保留紅源與證據；不reset、不刪他人資料。
若舊資料不能符合契約，先釐清原語義，不擅改历史追綠；出現並行writer或需碰禁區即停。
完成兩輪後交回Claude／非作者，不自行發第三輪。

## 證據位置

`C:\Users\Leon1\AppData\Local\Temp\glimmer-two-rounds-20260905-100348`。
T576第一輪自驗10447 PASS、工具鏈63例綠、Chrome1389鍵零變均已完成，與本卡分開提交。
