# T525 上手階梯延長（教學在機場就停了，而遊戲最深的三個系統還沒登場）

**日期**：2026-08-18　**發卡**：Claude（業主授權：自己玩→出卡→施工→合併）

## 0. 玩測發現

既有上手提示 `checkHints()` 是一條乾淨的階梯：

| 提示 | 觸發 |
|---|---|
| h1 | 道路 <3 → 先鋪路 |
| h2 | 分區 <4 → 劃住宅區 |
| h3 | 無電廠 → 蓋電廠 |
| h4 | 首位居民入住 → 劃商業/工業 |
| h5 | pop ≥150 且無車站 → 鐵路 |
| h6 | pop ≥400 且無機場 → 機場 |

**然後就沒有了。** 而這時遊戲真正的深度才剛開始：

- 🔬 **科技樹 36 個節點**（實測 250 天城市 $33,314 閒置、研究進度 0/36、第一個節點只要 $400／
  20 天就能完成）——600 天的 AI 城至今 0 研究。
- 📋 **委託與 🏛️ 專精**（`CMS_MIN_RANK385`／`SPEC_MIN_RANK386` 解鎖，四選一永久加成）。
- 📚 **城市科學 53 條研究、51 個政策開關**——這是本作最貴的內容層，**教學一個字都沒提過**。

一個玩到 pop 400+ 的玩家，可能永遠不知道這些存在。

## 1. 落地：延長既有階梯，不另造教學系統

在 `checkHints()` 尾端續四階（沿用既有 `showHint(key,msg)`／`hintShown` 一次性機制）：

- **h7 科技樹**：`pop≥250` 且錢夠第一個節點且沒有研究進行中 → 指路 📊→🔬。
- **h8 委託**：達 `CMS_MIN_RANK385` 且人口達 `CMS_MIN_POP385` 且從未接單 → 指路 📊→📋。
- **h9 專精**：達 `SPEC_MIN_RANK386` 且未選 → 說明四選一與永久性。
- **h10 城市科學**：`pop≥600` → 指出 📚 頁有 `SCI451.length` 條研究、政策在 🏛️ 區。

**門檻與數量一律引既有常數**（`CMS_MIN_RANK385`／`CMS_MIN_POP385`／`SPEC_MIN_RANK386`／
`SCI451.length`／科技節點費用），**不手寫任何數字**——T442「$12 過期三十幾張卡」的教訓。

## 2. 驗收計畫

- G1 四條新提示的門檻/數量必須引常數（機械掃：新提示區段不得出現手抄門檻數字）。
- G2 行為：造境四種狀態，對應提示各自觸發一次且只一次（`hintShown` 一次性）。
- G3 既有 h1-h6 原文回歸（只能追加）。
- G4 六哨兵位元恆等（提示是顯示層）。
- 紅源三案：門檻手寫（G1 紅）／新提示不觸發（G2 紅）／重複觸發（一次性壞掉）。

---

## 3. 施工紀錄（Claude 全鏈；起點 master `2c30250` → v11.154，PASS 5,846）

### 3.1 落地

`checkHints()` 尾端續四階（沿用既有 `showHint`／`hintShown` 一次性機制、既有 h1-h6 一字未改）：
h7 科技樹（`pop≥250`＋沒研究＋錢夠第一個節點；**節點數與費用由 `TECH343` 導出**）、
h8 委託（`CMS_MIN_RANK385`／`CMS_MIN_POP385`）、h9 專精（`SPEC_MIN_RANK386`）、
h10 城市科學（`pop≥600`；**研究條數由 `SCI451.length` 導出**）。

### 3.2 行為驗證

seed525 跑 400 天（pop 2,909）：`hintShown` 走完
**h1／h2／h3／h4／h5／h6／h7／h8／h9／h10 十階**——續段四條全部真的觸發，
且集合無重複鍵（一次性成立）、只增不減。六哨兵位元恆等。

### 3.3 紅源（三案）

| 紅源 | 結果 |
|---|---|
| ① 研究條數手抄成 `53` | **G1 紅** |
| ② 委託門檻手抄成 `rankIdx+1>=3&&pop>50` | **G1b 紅** |
| ③ h7 永不觸發 | **G3 紅**（已顯示清單少了 h7） |

### 3.4 驗收

`verify.py` ALL GREEN、PASS 5,838 → **5,846**；六哨兵位元恆等。
