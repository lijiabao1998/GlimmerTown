# T573 冷卻塔地腳與等距構圖修復

日期：2026-08-30
發卡／施工：Codex（業主明示「冷卻塔其實是存在偏差的，遵守施工紀律，出卡修一下」）。
施工 claim：`bay/codex`，認領 base `0378c7332048953449e2fae8ffa39550667d3284`，v11.186；認領提交 `276b8a7`。
狀態：`STOP: AWAIT_REVIEW`；Codex 自測完成，不自合、不 publish，交非作者覆核。

## 0. 病灶與範圍裁定

目標是 k5 的 `SPR.plantVar[1]`（灰色雙塔＋青綠低廳），不是 k58 核電廠。
T572 真像素結果顯示 v1 位元未變；「T570 下半裁切計數為零」不能證明地腳在地塊內。
現行 plate 頂 y86、中心 y102、底 y118，塔腳卻都在 y90..91：
左塔中心 x22、底寬26，明顯跨出該行菱形；高度投影與實際接地混在一起。
此卡修單張素材的空間構圖，不改全局 T570 的 3px 容差政策，也不拿整個菱形剪罩削塔身。

## 1. 定位錨

- `buildSprites` 內 T225 的 `// v1 冷卻塔廠` 區塊。
- `plate()`／`isoBox()` 的 2:1 像素地塊公式；`SPR.plantVar[1]`。
- `test_fixde.js` T572 後新增 T573 台架；既有測試注入橋與像素 canvas 可複用。
- `docs/SPR_PINS.json` 的 `plantVar/1`；其餘 1382 鍵必須恆等。

## 2. 允許觸碰

1. `index.html` 的 k5 v1 生成區：塔的中心、接地高度、半徑、原有廳與塔的前後畫序；
   可加等距橢圓塔口／塔腳以明確體積；同區 `smoke`／`lamp` **視覺掛點**隨塔頂同源更新。
2. `test_fixde.js`：只增 T573 幾何、像素、掛點、禁區守衛；測試橋不進正式 runtime。
3. 本卡、CHANGELOG、ARCH、SPR_PINS、版本雙處（預定 11.187），照既有收束工具。
4. 證據／探針放工作樹外暫存目錄；8126 槽3；修前 index 備份留存。

## 3. 禁區

- `w=88/h=120/ax=44/ay=118`、k5/MSZ/占地不變；不改道路、draw-time 排序、陰影管線。
- v0/v2、k58、其他素材、全局裁切／outline helper 不動。
- 不改 tick/canPlace/doPlace/AI/存檔/經濟/煙霧更新；只移發煙與警示燈的視覺起點。
- 保留 plate 一次與原位置的亂數消耗；新增段零 R/ri/rand/Math.random/spriteTexRand。
- 不修改、稀釋既有斷言，不重釘六個模擬哨兵，不提高 T523 上限。

## 4. 驗收

- G1 真跑正式 v1 生成區，用像素台架讀兩座塔腳；沿實際地塊離散公式驗所有接地像素在內，
  不是用「裁了幾像素」代替。兩塔分離、等距錯位，低廳保留。
- G2 smoke/lamp 與實際塔口／紅燈像素對位；夜圖仍有兩塔紅燈與廳窗。
- G3 metadata 恆等；生成段零新亂數，plate 恰一次；其他家族像素 0 變動鍵。
- 紅源至少：恢復左塔外移、放大地腳、煙霧掛點回舊值，各自必紅；不能只驗斷言存在。
- 真瀏覽器修前後同城同鏡頭（日／夜、zoom2、四向路側），確認地腳不壓路；
  高塔正常向上遮擋與物理地腳越界分開判讀，console 零錯。
- `verify.py` ALL GREEN／PASS 不低於實跑基線；toolchain 全綠；六哨兵恆等。
- 指紋新增0、移除0、只改 `plantVar/1`（含其夜圖），兩次 snapshot 確定性一致。

## 5. 收束與回滾

- bump → arch_map → ARCH 實數 → fp_snapshot 審差異 → 完整閘門 → 提交 bay/codex。
- 狀態 `STOP: AWAIT_REVIEW`；本卡沒有 T572 的一次性自合特例，交 Claude／Kimi 覆核。
- 若需要擴占地、動遊戲規則、改全局裁切、影響其他精靈或越過預算，立即停止回報。
- 未通過則只撤本卡差異；合併後回滾由非作者 `git revert` 本卡提交走既有交易式部署。

## 6. 實跑紀錄

### 實作與幾何

- 塔腳由 `(48,95)`／`(30,104)` 定位，差 `(18,-9)` 為 2:1；塔高 36／42。
- 底座半寬 `[4,7,8,7,4]`，地腳連正式 `outlineSprite` 的描邊共 262 個受測像素，越界 0。
- 塔身之間 11 行皆有至少 2px 真透明間隙，非「兩根塔擠成灰牆」。
- 原青綠廳尺寸與窗不動，改在雙塔後繪以還原前後遮擋；塔口凹面與側面暗線沿用原色系。
- smoke 改 `[4,-61]`／`[-14,-58]`，lamp `[-14,-59]`，皆由同一組塔座標算出。
- 只改 v1 生成段（另有版本字），不改全局裁切。圖塊 metadata、粒子更新／vri 次數不變。

### 閘門（Codex 親跑）

- 進場 `verify.py`：6420 PASS／exit 0／ALL GREEN／11.186。
- 終態 `verify.py`：**6435 PASS／exit 0／ALL GREEN／11.187／CRLF=0**。
  +15 = 12 條 T573 新守衛 + 新 CHANGELOG 條目的 3 條簽核驗型；沒有刪減斷言。
- 六哨兵實得：seed22=3423、seed22m=1461、seed301=6801、seed301m=640249、seed7=260、seed7m=50；原值不動。
- `python -B -m unittest tools.test_toolchain`：**63 tests／277.986s／OK／exit 0**（全部 tempfile）。
  既有 bump 工具會印 ResourceWarning，本卡沒有改工具或掩掉警告。
- T523 同實作測得空城585、300天城7432（上限7500），終態完整套件亦通過。
- 指紋兩次真 Chrome 快照：1383 entries／117 families，兩次逐鍵恆等。
  新增0、移除0、變更恰1：`plantVar/1`；其餘1382鍵逐位元不變。
  日圖 `d39c195b → 93659040`，夜圖 `31191d7b → a42b76bf`。

### 破壞性證明（隔離行程，不改工作樹）

`red.js` 讀正式 suite 的 bootstrap 與 T573 區塊，在獨立 Node 行程攔截 index 讀取，
餵入一份完整突變 index 位元組；不是改正式檔再猜有沒有還原，也不宣稱每案跑全套模擬。
每案保留 `red-*.html` 紅源與 `red-*.log`，先 green exit0 → 各紅 exit1 → green exit0。

| 紅源 | 第一條紅釘／實得 |
|---|---|
| 左塔回舊地脚 x22/y92/h44 | G1d，220像素、越界從 `[13,89]` 起 |
| 底座半寬放大8px | G1d，420像素、越界從 `[58,92]` 起 |
| 僅加寬上部塔身 | G1e，地腳仍合格、11行間隙實得0（未被其他釘代咬） |
| smoke 改回旧起點 | G2a |
| lamp 改回 x=-22 | G2b |
| 夜圖紅燈 y+6 | G2c |
| 塔生成段插 R() | G3b |
| 移除雙塔生成呼叫 | G1b（注入匹配先斷言，不允許 no-op） |
| 雙塔同一水平地脚 | G1f |
| 廳窗夜圖 y-10 | G2d |

### 真瀏覽器與交付界線

- 同頁對照 `plantVar/1` 與 `bld/58_1_0`，再取 before/after；確認目標身份，不憑縮圖猜 k。
- 8126、槽3在載入index前設定；四廠四邊路 fixture，rot0/1/2/3、zoom2，無地腳伸上道路。
- 日相位55、夜相位100（CYCLE=110）；夜圖雙燈與廳窗對位；正常高塔向上遮擋不當成地腳越界。
- 原生＋縮放鈕可用；390×844窄視口正常繪圖；console error=0。
- 蒸汽走既有 `GV.advanceN(.05,80)`（speed0），實生12顆、下一次13顆；未更改粒子管線。
  幾何／對位由 G2 的像素與掛點斷言承擔，不把粒子數字充當肉眼清晰度指標。
- 樣張與完整探針保留於工作樹外：
  `C:\Users\Leon1\AppData\Local\Temp\glimmer-t573-review-20260830`。
  `before-sprite.png`／`after-sprite.png`、`before-road-day.png`、`before-road-night-r0.png`、
  `after-road-day-r0..r3.png`、`after-road-night-r0.png`、`after-road-detail.png`、`after-mobile.png`。
- 主線仍 `0378c73`；玩家 index/sw 的 SHA256 與进場值一致，没有合併或部署。

### 自捕與未宣稱

- 初寫廳窗測點誤選 dx11（該窗本來就不發光）；紅後查原公式，改測 dx7 的既有亮窗 `(57,99)`，
  沒改夜燈條件迎合測試；移動該夜窗的紅源 G2d 已獨立咬中。
- 第一版 CHANGELOG 驗收欄漏寫簽核人，T557 在 PASS204 報紅；補「Codex 自測、未經第三方覆核」後完整重跑。
- 探針最初夜相位170其實等效60（白天），依 CYCLE 修成100後重拍；未沿用錯誤的日景作夜景證據。
- 自測通過不是非作者覆核或業主美術點頭；T573 未合併、未部署，不延用 T572 一次性自合授權。
- 範圍外發現：全檔控制字元掃描命中 `test_fixde.js` 的 T552 G1 正則（`pol` 前的 U+0008，
  master 同處也存在；本卡新增段沒有控制字元）。該正則不是預期的 word-boundary，可能漏攔 policy 直寫；
  本卡不改既有 T552 斷言，交後續獨立測試修復卡處理。
