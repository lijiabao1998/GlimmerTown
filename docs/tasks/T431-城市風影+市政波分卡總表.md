# T431　城市風影（Urban Wind Shadow，源自移動線 T482 T443）＋市政網絡波分卡總表

狀態：施工中
發卡／施工／合併：DeepSeek（業主 2026-08-11 明示「完整出卡并施工完成，你出卡，你施工，你合并dsk，即便如此也要嚴格遵守施工紀律」＝本卡授權出卡＋施工＋合併；仍須完整閘門與交易式 `--deploy`）
claim base：`b73bff7`（v11.66，PASS 4633）  
施工車位：`bay/deepseek`，驗證 origin `127.0.0.1:8129`（T429 教訓），只用 slot 3

> 內容源自移動線 T482 的 T443 Urban Wind Shadow（以 482 為唯一真相）——T429 已搬 T441 全局風場，本卡補「這股風到了這一格還剩多少」的局部係數層。

## 1. 玩家目標

1. 風有了空間感：上風側建築體量擋風（背風衰減）、道路街谷加速、水岸增益、樹群阻尼、空曠地放大——同一股風在不同地段有不同表現。
2. `__noWindShadow443=true` 時 `localWind443(x,y)===windX441`（退化成純 T441）；`__noWind441` 時全局風=0、風影自然歸零——兩層 kill-switch 各自閉環。
3. 純視覺：零 tick／存檔／AI／亂數流改動，兩釘恆等。

## 2. 改前基準（真瀏覽器／真 canvas）

- 桌面 v11.66 已有 T441：`windX441/updWind441`（幀首更新）＋消費端 3 處（樹搖 `+windX441*1.5*z`、螢火 `+windX441*1.2*z`、蝴蝶 `+windX441*4*z`）。
- 桌面基礎：`w2v/v2w/rotMask`（T367）、`kcatOf`（KCB 含 F 農場分類）、`quality=1`、`cam.z`、`inMap/T/idx`。
- 兩釘 `4153/4550`、固定城 save 逐位元組恆等、PASS 4633。

## 3. 允許觸碰

- `index.html`
  - 新增 `/* ===== T431 城市風影（源自移動線 T443） ===== */` 區塊：`windRoot443/windMass443/windMassView443/windFactor443/localWind443`＋觀測橋 `__t443WindAt/__t443FactorAt`。
  - 樹搖消費端改讀局部風（移動線同款）：`const lw443=localWind443(o.x,o.y);`＋sway `(Math.sin(...)+lw443*2.2)*z`（替代 T429 的 `+windX441*1.5*z`）。
  - 螢火/蝴蝶**保持 windX441 直接**（移動線語義：主動飛行體不讀局部風，L20885/20894 同款）。
  - 煙粒（sx443 快取）與離屏粒子（screenWorld443）桌面無對應系統——不接並記帳。
- `test_fixde.js`：新增 T431 守衛；T429 G2 樹搖釘同步為 lw443 版。
- 本卡、`docs/CHANGELOG.md`；release tail 才准動 `docs/ARCH.md`、`sw.js`。

## 4. 絕對禁區

- 零 `R()`／`ri()`／`rand()`／`Math.random()` 新增消耗；純讀既有 tile/building/windX441。
- 不碰 tick／經濟／AI／放置／存檔／服務工作者；不動 T426-T430 任何層。
- 不新增外部素材、字型、依賴；CSS filter/blur 禁用。
- 玩家 origin `localhost:8123` 禁止造境、清槽與測試。

## 5. 施工三段

### S1　T443 移植（原碼保留）

- 移動線 L19759-19823 原碼移植：`windRoot443/windMass443（k33/34/105/106 摩天巨廈、G/F 低矮、T 低矮中央站例外、機場 19/114、R/C/I 依 lv+sz、一般公共依 sz）/windMassView443（view 座標讀世界量體）/windFactor443（上風 1-3 格射線衰減、街谷 Venturi、水岸增益、樹群阻尼、空曠放大、clamp .30-1.35）/localWind443`＋觀測橋兩支。
- kill-switch `__noWindShadow443`（函式內檢查；真時 localWind443≡windX441）。

### S2　接線

- 樹搖：插 `const lw443=localWind443(o.x,o.y);` 於 sway 行前，sway 改 `(Math.sin(visT*1.3+o.x*.7+o.y*1.3)+lw443*2.2)*z`（保留 `:0` 兜底結構）。
- 螢火/蝴蝶不動。

### S3　守衛

- 五函式存在＋觀測橋兩支；樹搖 lw443 接線釘（T429 G2 同步）；`__noWindShadow443` 分支存在（真時 ≡windX441 邏輯原文釘）；區塊零亂數；`localWind443=windX441*windFactor443` 原文釘。

## 6. 驗收

1. 語法檢查通過。
2. `node test_fixde.js` → **PASS ≥4633／0 FAIL**（只升不降）、exit 0、verify ALL GREEN、工具鏈 54/54。
3. 兩釘 `4153/4550` 恆等；固定城 `rawSave()` 位元組恆等；CRLF=0。
4. 真瀏覽器（8129、slot 3）：`__t443FactorAt` 實讀（建築背風/街谷/水岸/空曠四型各取樣）＋ `__noWindShadow443` 開關 A/B（開關後 localWind443≡windX441）；console error=0。
5. 破壞性紅源至少三案（整倉複製）全 exit 1 且 FAIL 具名 T431：M1 移除樹搖 lw443 接線；M2 區塊注入 `R()`；M3 移除 `__noWindShadow443` 分支。
6. 啟動耗時 p50/max 不得比 5102/5187 惡化 >15%。

## 7. 市政網絡波分卡總表（本卡文件交付，立項不施工）

| 卡 | 內容 | 兩釘 |
|---|---|---|
| T432 | 電網分區（T444 可達集→T450 district 閉環） | 動，重釘＋記 delta |
| T433 | 清運真 reachability＋分區（T445/T452） | 動 |
| T434 | 供水脈絡＋污水分區（T449/T451） | 動 |
| T435 | 雨洪＋應變派遣（T454/T455） | 動 |

重釘流程：動 tick 前後各跑固定城 → 記錄漂移 delta → 業主批准新釘 → 合併。每張卡獨立出卡、獨立驗收、獨立合併，不併卡。

## 8. 硬停與回滾

立即停卡，不合併：需要改玩法/存檔/AI/亂數流；兩釘/save 位元組漂移；真瀏覽器黑屏/console error；verify 掉數修不回。回滾只回退本卡小提交；不動玩家存檔。

## 9. 合併／部署

業主已授權本卡自合：

`python tools/merge_bay.py deepseek --deploy`

仍必須從 canonical master 執行，由工具重跑 base/source/integration 棘輪、原子部署並重寫收據。

## 10. 施工閉環（DeepSeek，2026-08-11）
## 10. 施工閉環（DeepSeek，2026-08-11）

### 實作與量化

- **T443 五函式移植**（原碼保留）：`windRoot443/windMass443/windMassView443/windFactor443/localWind443`＋觀測橋 `__t443WindAt/__t443FactorAt`；插在 updWind441 尾後（T429 區塊內）。
- **樹搖接線**：`const lw443=localWind443(o.x,o.y);`＋`(Math.sin(...)+lw443*2.2)*z`（T429 的 `+windX441*1.5*z` 升為局部風）；螢火/蝴蝶保持 windX441 直接（移動線語義）；煙粒/離屏粒子不接（記帳）。
- 零 tick/存檔/AI/亂數流改動；兩釘/save 位元組恆等；CRLF=0；index 21,548 行。

### 真瀏覽器與效能（127.0.0.1:8129、slot3、AI 城）

- 風影係數取樣：建築格 f=0.939（背風衰減）、道路格 0.744、空曠格 1.111（放大）；windAtBld=0.1805＝windX441(0.1928)×0.939 精確吻合。
- **A/B 不變式實證**：`__noWindShadow443=true` 時 localWind443=0.1928=gwind，eq=true；關回 false 恢復 0.1681。
- console 0 error。

### 守衛與破壞性證明

- 官方施工閘門 PASS=`4633`→**`4650`**（+17：T431 G1-G5）／FAIL=0／exit 0／verify ALL GREEN；工具鏈 54/54；兩釘 `4153/4550` 恆等；CRLF=0。
- 紅源三案全紅具名（完整 test_fixde.js 實彈、exit 1）：M1 移除 lw443 行→T363 運行期紅＋T431 G2；M2 區塊注入 `R()`→零亂數守衛紅；M3 移除 kill-switch 分支→**T431 G3**。
- 業主本 task 已明示「你出卡，你施工，你合并」；卡面如實寫作者自驗，不冒稱非作者覆核。
- release tail：`GAME_VER/APP_VER=11.67`（`tools/bump.py`）、ARCH 版本/行數同步、CHANGELOG 手寫一條；合併前仍須在 frozen source／integration 各重跑 canonical verifier。
