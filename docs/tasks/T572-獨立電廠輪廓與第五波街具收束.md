# T572 獨立電廠輪廓與第五波街具收束

**日期**：2026-08-29  
**發卡／施工**：Codex（業主本輪明示「出卡、施工、合併、迭代」）  
**施工 claim**：`bay/codex`，base `6a4db85`（v11.185；基線 `PASS=6403 / FAIL=0`）  
**性質**：T570 漏網缺陷修復＋T568 第五波現況收束；玩法、經濟、存檔全部不動。

## 0. 病灶與進場探針

業主樣張中央下方灰色雙曲面塔，精確對應 `k5 發電廠` 的
`SPR.plantVar[1]`「冷卻塔廠」（88×120、1×1、`ax=44/ay=118`）。
它不是道路排序錯覺：T570 `buildSpritesS13()` 的輪廓裁切只遍歷 `SPR.bld`，
而發電廠走獨立容器 `SPR.plant`／`SPR.plantVar`，故下緣越出地塊的像素未被裁。

T568 第五波列出的白天燈桿／長椅／消防栓，在現行主線其實已由
T146/T289 落地：`SPR.streetLamp` 與 `SPR.roadDeco.bench/hydrant` 均存在。
因此本卡不得重造素材；真正剩餘問題是燈桿選址與 T146 小件選址互不知情，
同一道路格可能重疊，且舊 T568 所寫 7420/7500 是歷史快照，必須重跑 T523。

## 1. 定位錨

- 生成鏈：`buildSpritesS13()`／`proc570()`／`clipOn()`。
- 獨立電廠：`SPR.plant`、`SPR.plantVar[1]`、`SPR.plantVar[2]`。
- 道路地面層：`T165+/T289` 街燈分支、`T146` `pc===2` 小件分支。
- 預算：`GV.drawCensus523()` 與 `test_fixde.js` T523 300 天城上限 7500。

## 2. 允許觸碰

1. `index.html`
   - 抽出／擴充 T570 既有裁切入口，使獨立發電廠三張日圖及對應夜圖共享同一幾何裁切；
   - 只裁地塊底部越界像素，塔身垂直區、煙霧掛點、燈點、尺寸與錨點不動；
   - 讓街燈與 T146 小件使用同一個決定性「此格已有小件」判定，街燈優先、同格不重疊；
   - 不新增 sprite 鍵、不新增 draw call 家族。
2. `test_fixde.js`
   - 新增 T572 行為守衛與紅源可咬點；同步 PASS 棘輪。
3. `docs/tasks/T572-*`、`docs/CHANGELOG.md`、`docs/ARCH.md`、版本字串與既有指紋基線（如工具要求）。

## 3. 禁區

- 不碰 `canPlace()`、`doPlace()`、`tick()`、`aiStep()`、存檔欄位與經濟公式。
- 不改 `R()/ri()/rand()/Math.random()` 的數量、次序或作用域；本卡新增段零亂數。
- 不改 k5 的 `w/h/ax/ay`、煙霧座標、建築尺寸／MSZ、道路 mask 與快取鍵。
- 不擴大 T570 對水岸／農牧豁免族的既有裁決；不以重畫冷卻塔掩蓋輪廓問題。
- 不把 T523 7500 上限往上調；預算超線時先降街具密度或撤街具改動。

## 4. 驗收

### G1 冷卻塔越界

- 合成 1×1 plate＋塔身台架：底部越界像素裁掉、垂直塔身保留。
- `SPR.plantVar[1]` 日圖與夜圖同裁；`SPR.plant`／v2 同入口掃描但無合法像素誤刪。
- 真瀏覽器在 8126／slot 3、zoom 2，路旁 k5 v1 A/B：不得再壓上道路；煙霧與塔頂燈仍對位。
- 破壞性紅源：移除獨立容器掛載，G1 必紅；把垂直區也裁掉，塔身保留釘必紅。

### G2 第五波街具收束

- 白天燈桿、長椅、消防栓三件真存在；不新增同功能 sprite。
- 同一道路格「街燈」與 T146 小件互斥；關閉本卡互斥逃生閥可回到 v11.185 選址。
- 純 `streetHash` 決定性，零新亂數、零狀態、零存檔。
- T523：空城與 300 天城均綠，300 天城不得高於 7500；本卡實得數寫回本節。
- 破壞性紅源：拔掉互斥條件後，同格重疊守衛必紅。

### G3 全局不變量

- `python tools/verify.py` ALL GREEN，PASS 只增不減；六哨兵原值。
- `python -B -m unittest tools.test_toolchain` 全綠。
- 指紋：新增 0、移除 0；差異只准獨立電廠白名單，其他既有鍵逐位元不變。
- 真瀏覽器：主流程可進、console 零錯、8126 只用 slot 3；樣張放工作樹外。

## 5. 收束與合併特例

- 完工後 bump `11.186`，同步 GAME/APP/ARCH/CHANGELOG，狀態先寫 `STOP: AWAIT_REVIEW`。
- 按 T362 常規作者不自合；但業主本輪已明示 Codex 同時「施工、合併、迭代」，
  故只對 T572 記錄一次性業主授權：完整閘門與瀏覽器證據通過後，Codex 可從 canonical master
  執行 `python tools/merge_bay.py codex --deploy`。此例外不改 `docs/COLLAB.md`。

## 6. 停手／回滾

- 發現需改 k5 尺寸、MSZ、道路 mask、快取鍵、亂數流，立即停手回報。
- T523 超 7500 且無法靠撤去街具互斥以外的本卡內容恢復，街具段撤回；冷卻塔修復獨立交付。
- 冷卻塔修復造成塔身／煙霧／夜燈任一錯位，整段回滾，不用手改錨點補償。
- 回滾：`git revert` T572 實作提交；部署由交易式收據復原。

## 7. 施工紀錄

見 7b（Claude 接手收束後回填）。
---

## 7b. 施工紀錄（2026-08-29 收；Codex 主體施工、Claude 接手驗證與收束）

**v11.186**　**PASS 6,420**　**master `c1c7831`**
**fp 變更恰 2 鍵（plant/＋plantVar/2）新增 0 移除 0——plantVar/1 位元不變，見下**

### 接力點（Codex 斷於限額時的現場）
守衛 14 條與兩段實作已落 bay 工作樹（未提交）；未跑紅源、未取真瀏覽器證據、收束鏈未動。
Codex 留下的 t572-k58-geom.png 是一張卡在 boot 畫面的擱淺截圖——死因＝隱藏分頁 rAF 停擺、
T426 啟動管線段間讓出永不返回（已入 GV-HOOKS §二-11）。已移出工作樹（樣張紀律：樣張放工作樹外）。

### 本卡最重要的一筆帳：出界像素在 v0/v2，不在 v1（卡面 §0 診斷修正）
活頁同判準審計（master vs bay 雙側）：
- `plant/`（v0 燃煤）出界 **53** → 裁後 0（op 3229→3176，差恰 53）
- `plantVar/2`（v2 燃氣）出界 **78** → 裁後 0（op 2769→2691，差恰 78）
- `plantVar/1`（v1 冷卻塔）出界 **0**——塔腳本在 T570 v3 判準容差帶（±3px 描邊政策性保留）內，
  **裁前裁後 CRC 位元恆等（36b2e9a3 雙側同值）**；53+78=131＝`__t572PlantClip` 全鏈對帳恰等。
卡面 §0 把業主樣張的壓路感記在 v1 出界像素上是診斷偏差：機制病（獨立容器漏掃）為真且已修，
v1 的壓路感=容差 3px＋draw-time 建築陰影。**業主若要收緊 v1，屬 T570 容差政策線，另卡裁決。**
（附帶教訓：收束者第一輪也把 A/B 樣張裡的陰影差讀成了裁切差——CRC 才是真相源，眼睛不是。）

### 驗證結果（Claude 接手後補齊）
- 套件全綠；紅源四案位元法全咬、首紅皆目標釘（G1a/G1e/G2c/G2f）、還原 sha256 恆等。
- T523 census 實得（G2 本節回填）：空城 585、300 天城 **7432**（上限 7500，紅線由既有閘承接；
  街具互斥只減不加落筆）。
- 真瀏覽器：`__t572PlantScan=3`、`__t572PlantClip=131`（8126 原生頁＋shim 複本雙實測同值）；
  乾淨載入 console 零遊戲錯誤（唯 python http.server 下 SW 註冊失敗，無害）。
- 樣張：電廠 A/B 雙 origin 同 seed 同框（t572_ab_plant.png）；街具同頁逃生閥 A/B
  （t572_ab_street.png：v11.185 選址＝燈桿插在長椅上，T572 互斥＝同格讓位，其餘逐件同位）。

### 本卡沉澱
1. **獨立容器是表驅動遍歷的盲區**（T570「表驅動判準會被表漂移騙」同族）：共用刀抽出，不複製判準。
2. **隱藏分頁 rAF 停擺會把 T426 boot 卡死**（GPT 擱淺的直接死因；入 GV-HOOKS §二-11）；
   探針解法＝MessageChannel 版 rAF shim（不受隱藏節流；只動排程零像素效應）。
3. **fp 對帳的「不變」與「變」同等重要**：plantVar/1 不變不是漏，是無病誤診的證明；
   眼睛會把陰影讀成裁切差，CRC 不會。

