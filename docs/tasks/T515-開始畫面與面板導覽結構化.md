# T515　開始畫面與面板導覽結構化

## 0. 出卡理由

T428 解決了開始畫面在矮視窗「置中後上端不可達」與桌面只用窄欄的問題，
但把編輯器、難度、挑戰、偏好設定與戰役仍當成 `#startMore` 的平級子項；
桌面三欄 grid 因此按插入順序把它們拉成 **280 → 240 → 310 → 1040 → 520 → 240px**
的無規律節奏。挑戰甚至比主 CTA 更寬，玩家看不出哪些是開局、哪些是附加玩法。

全局掃描也找到同型問題：`#info` 在窄直式視窗仍只取 `max-width:70vw`，
統計五分頁、指南八分頁與歷史／趨勢操作列把多個互動元件硬塞進單列。
本卡只收這兩個「缺少語義群組與窄寬排版規則」的 UI 根因；不把 HUD、工具列、
T514 政策指揮台或資料表重構混進來。

## 1. 基線、權限與定位錨

| 項 | 值 |
|---|---|
| 基底 | `9137389`（master / bay-codex 同步） |
| 起始版號 | v11.144 |
| 基線閘門 | `python tools/verify.py`：PASS=5417 / FAIL=0 / ALL GREEN |
| 施工車位 | `安卓探索\bay-codex`（埠 8126、僅槽 3） |
| 一次性權限 | 業主明示：**Codex 出卡、施工、自審後以交易式流程自合併與部署**。這只豁免 T362 的「作者不得自合」，其餘閘門、clean、收據與原子部署規則完全不豁免。 |

定位一律以實際內容 grep，不依漂移行號：

- `#start .startMore`、`@media (min-width:900px)`、`html.noT428`（開始畫面 CSS）
- `const moreEl=$('#startMore')||startEl;`（開始畫面動態注入）
- `#info{`、`.techTabs343`、`function statsTabs343`、`function showHelp`、`function showTrendPanel450`、`id="histR30"`（共用面板）
- `test_fixde.js` 的 T428 G1–G17 與 `docs/tools/ui_probe.html`（既有版面契約）

## 2. 合約：要做什麼

### A. 開始畫面：外層仍三欄，內層才有結構

`#startMore` 在桌面仍保留 T428 要求的三欄 grid；它只負責外框，不能再直接排列異質按鈕。
新增無 id 的語義 `section.startSection515`：

1. `startConfig515`：既有「難度」標籤／四鈕 + 新可見「快速設定」標籤／既有五張設定卡。
2. `startExtras515`：既有 `#bEditorMode` 與既有 `#bCampaign` 收進 `.startAltRow515`；
   既有 `#scMenu` 留在同 section 內、仍由 campaign 控制。
3. `startChallenge515`：既有「挑戰」標籤 + 新 `.startChallenges515`，其中**恰好**放既有 `#bCh1–3`。

桌面：設定區跨兩欄、其他玩法跨一欄、挑戰跨全欄；挑戰內部三等欄，單卡不得再被拉成滿版條。
手機：sections 單欄、挑戰單欄；既有 `#scMenu{max-height:220px;overflow-y:auto}` 不變。
每個既有按鈕的 id、文案、順序語義、onclick/event handler、localStorage 鍵及開局行為必須逐字保留。

### B. 共用資訊面板：具名控制群組，不碰通用 `.row`

1. 直式窄寬的 `#info` 亦採既有安全居中寬度模型（`width:min(560px,92vw)`／`max-width:92vw`），
   不動 landscape T372 的無 transform 置中契約。
2. 新增具名 `.panelTabs515`：統計五分頁保留 `techTabs343`，成為 `techTabs343 panelTabs515`；
   指南八分頁也用此類。窄寬採可預期 grid，不橫向推出面板。
3. 新增具名 `.panelAction515`／`.panelMeta515`：趨勢的 30/90/365、城市歷史的 30/90/365
   分別成為可換行的操作群與說明群。既有 `trR*`、`histR*` id 與事件不可變。
4. `showInfoPanel()` 的唯一 display 寫入、`statsTabs343()` 的既有 id/class、指南陣列順序、
   `guideTab` 分支與所有模擬資料來源不動。
5. 施工中的 390px 真 Chrome 驗證發現同一個窄面板根因：`statTab()` 的第三欄 `.u`
   仍固定 `white-space:nowrap`，把指南內容撐至 405px。只在
   `@media (max-width:420px)` 的 `#info .stab` 將表格改為「key／數值」兩欄，`.u`
   落整行並允許換行；數值欄仍右對齊。不得改 `table.dtab`、桌面 `.stab` 或通用 `.row`。

## 3. 允許觸碰

- `index.html`：上述 CSS、開始畫面 HTML/動態注入、面板輸出 class；只限呈現結構。
- `test_fixde.js`：T515 守衛，以及因 DOM 合約已正當改變而**升級** T428 G10 的舊直子釘。
- `docs/tools/ui_probe.html`：加入真瀏覽器量到的 T515 hierarchy／同寬／可達性不變量。
- `docs/tasks/T515-開始畫面與面板導覽結構化.md`、`docs/CHANGELOG.md`、`docs/ARCH.md`。
- `tools/test_toolchain.py`：僅限必跑 57 例工具鏈回歸揭露的既有 `re` 標準庫漏匯入與同一自檢的未關閉檔案 warning；只補 `import re`／context manager，不改任何測試斷言、交易或部署行為。

## 4. 禁區

- 不動 tick、經濟、政策公式、AI、地圖、存檔 schema、RNG／R()/ri()/vri()／Math.random() 流。
- 不動 HUD、工具列、T514 科學政策／套餐／稅率／預算／車隊的內容結構。
- 不改任何既有 DOM id、按鈕文字、事件語義、`SAVEKEY`、`bNightCity` handler 或 `showInfoPanel()` 呼叫時機。
- 不把全域 `#info .row` 或一般 `.row` 改成 grid；只准具名 T515 class。
- 不新增玩家端資產、不碰 deployment 目錄、不手動複製 runtime。

## 5. 驗收閉環

### 機械

1. `node test_fixde.js`：PASS 只增不減，0 FAIL；六條 seed pins 原值不變。
2. `python tools/verify.py`：ALL GREEN、CRLF=0、版本雙處同步。
3. `python -B -m unittest tools.test_toolchain`：全綠。
4. T515 source guards 至少要咬住：
   - 三個 `startSection515`、challenge wrapper 與 `#bCh1–3` 的唯一歸屬；
   - T428 G10 由「直子」升級為 `.startAltRow515` 真結構釘，不能留下死 selector；
   - `techTabs343 panelTabs515`、指南八 tab 的群組、趨勢／歷史 action/meta class；
   - 窄幅 `#info .stab` 的說明欄必須落次列，且 media guard 不得擴及桌面；
   - 禁止 `.row` 全域重排、禁止模擬／RNG token 進入本卡 CSS/HTML 修改區。
5. 至少四個破壞性驗證，逐一「注入→紅→還原→綠」：挑戰退回裸 sibling、
   editor/campaign 脫離 compact row、指南 tab 退回裸 row、窄面板 action grid 被移除。

### 真瀏覽器（8126、槽 3；不碰 8123）

- 尺寸：1440×900、1280×620、390×844、390×390；展開戰役另測。
- 開始畫面：主 CTA 首屏可見；所有既有控制均可點；無水平溢出；挑戰三鈕在桌面同寬（誤差 ≤1px）且單鈕 ≤280px；
  五設定卡同寬；config/extras/challenge 的群組與垂直順序可讀；戰役清單仍內捲可達。
- 面板：指南八 tab、統計五 tab、趨勢／歷史範圍鈕與 `statTab` 資料列在窄寬均可逐一觸及，
  `scrollWidth<=clientWidth`；
  開關前後 `GV.stats()` 位元相同、console 零產品錯誤。
- 以 `docs/tools/ui_probe.html`、實圖 before/after 與人眼檢查共同驗收；Node mock 不假裝能證明瀏覽器 DOM hierarchy。

## 6. 回滾

本卡僅 CSS／DOM 結構；若真瀏覽器有任一尺寸出現不可達或控制行為漂移，停止、不 bump、不合併。
未合併時以車位 commit 回退；已合併才依交易收據與正式回退卡處理，禁止手改玩家目錄。

## 7. 施工紀錄

- 2026-08-17：Codex 認領。施工前 canonical master 曾被誤以 `bump.py --help` 寫入兩個版本字串；
  業主明示授權後，僅精確還原 `GAME_VER`／`APP_VER` 為 v11.144，已確認 canonical clean，未形成提交或部署。
- 2026-08-17：必跑 `tools.test_toolchain` 在基底 `9137389` 即因 `test_no_duplicate_test_names()` 使用
  `re.findall()` 卻漏 `import re` 而報 `NameError`；T515 的 diff 未碰此檔。為使卡面的工具鏈閘門可真跑，
  只補標準庫匯入與等價的 context manager（清除既有 `ResourceWarning`），完整 57 例重跑結果須記於本節，禁止改既有斷言掩蓋錯誤。
- 2026-08-17：開始畫面實測。隔離 Chrome（Playwright、service workers blocked、`127.0.0.1:8126`、slot 3）下：
  1440×790／1280×620 的三挑戰均為一列、各 243px；五設定卡各 103px；390×844／390×390 皆只有垂直捲動，
  主 CTA 在首屏，三挑戰各 302px 單欄。展開戰役後清單仍是 `clientHeight=220 / scrollHeight=331 / overflow-y:auto`。
- 2026-08-17：面板實測。390×844 指南八鈕為 2 欄×4 列、每鈕 151×30px；統計五 tab、趨勢／歷史範圍鈕均可逐一點擊，
  `document=390/390`、`#info=355/355`、`#infoBody=327/327`，零水平溢位。開啟指南、八個指南分頁、五個統計分頁與六個範圍鈕前後，
  `JSON.stringify(GV.stats())` 皆為 183 bytes 並逐位相同；page errors=[]。
- 2026-08-17：Browser-first 在 app 內沒有可用的隔離 slot 3 context，依前端驗證規範 fallback 至上述系統 Chrome；
  不曾連線 8123 或讀寫玩家存檔。開始畫面／面板的實圖人眼檢查通過。
- 2026-08-17：對抗性守衛重放。群組裸 sibling、editor/campaign 脫離 compact row、指南回退半套 tablist、
  窄幅規則搬出 `max-width:420px`、桌面規則改成 `min-width:1px`、動態注入尾段插入 `R()` 均被 T515 G1–G6 擋紅；
  全檔控制字元=0、`git diff --check`=0。
- 2026-08-17：最終機械閘門：`python tools/verify.py` **PASS=5424 / FAIL=0 / ALL GREEN**，
  六支 seed pins 原值相符、CRLF=0、GAME_VER/APP_VER=v11.145；`python -B -m unittest tools.test_toolchain`
  **57/57 OK**（已無 `NameError`／`ResourceWarning`）；`python tools/arch_map.py --check` 99 錨點、0 漂移、最大間距 712≤800。
- 施工狀態：**完成**。業主一次性授權 Codex 自審、自合與部署；本卡未經獨立覆核，合併時仍必走 `merge_bay.py` 的完整交易、收據與位元驗證。
