-# T408 — 摩天樓向市中心聚集(TheoTown 觀感對標 收官刀;**需重新釘值**)

業主授權:「嚴格遵守施工紀律,即便是你施工、你出卡、你迭代」。
本卡是 T405-T407 之後唯一剩下的觀感差異,且是**三刀中唯一動模擬的**。

## 病灶(偵查 wf_abea8b7c D 組 + T407 成熟期實拍)

800 天成熟城(pop 5549、14 座塔)實拍:密度與天際線層次已與 TheoTown 同檔,
但**摩天樓散落全城,讀不出市中心**。根因:

- 塔由 2×2 合併生成,合格條件=四格同 k、lv≥2、通電、**通水**、非火災、未被 mgDone 佔用;
- 合格後 `R()<.02` 每簇每日擲骰,**沒有任何空間偏好**;
- 原有的地價閘門已被 **T341c 明確移除**(實測供水簇全在低地價邊緣、高地價中心無水=閘門成空集);
- AI 水管走 `%6===3` 均勻格網 ⇒ 全圖均勻供水 ⇒ **唯一的空間選擇力量是「哪裡有水管」**。

## 設計:機率調制,不是硬閘

`if(R()<.02)` → `if(R()<.02*twMul408(x,y))`,其中
`twMul408 = 0.3 + 1.9×dens`(dens=T407 的都市織理密度,radius 3,`URBAN406` 口徑)。

- **R() 仍每簇呼叫一次、順序不變**(只是比較的閾值變動)⇒ 亂數流位置零位移;
- 市中心 dens≈1 ⇒ ×2.2(合併加速);城郊 dens≈0 ⇒ ×0.3(大幅延後,非禁止);
- 用調制不用硬閘的理由:硬閘會讓邊緣**永遠**長不出塔=玩家在郊區蓋滿也沒回饋,
  且總塔數會塌;調制只改「先後」與「密度分布」,總量大致守恆。
- 巨廈(k105/106,3×3)本卡**不動**:它另有 subst≥5 與吸收公園等複雜條件,
  一卡一紅線;塔驗證通過後再評估。

## 這張卡必然破兩釘——重新釘值程序(紀律要求,逐項執行)

1. **改動前**先量 12 種子×400 天基線(pop/money/happy),存檔備查;
2. 施工;
3. 量新值,**逐種子比對**:任一原本健康(pop>1000)的種子若崩到 <500,視為回歸失敗、退修;
4. 以新值重釘 `seed301` 與 `seed22` 兩條斷言,**CHANGELOG 明列舊→新**;
5. 12 種子新舊對照表入卡面(可追溯)。

## 紅線

- R() 呼叫次數與順序零位移(鐵律2);`twMul408` 本身零亂數。
- 不動合格條件(lv/pw/wa/fire/mgDone)、不動 TOWER_POP/JOBS、不動存檔。
- 不動巨廈路徑。
- **任一健康種子崩城即退修**——寧可不做,不可打壞既有平衡(T346d/T348 血淚)。

## 驗收

1) 守衛:`R()<.02*twMul408(x,y)` 字面+`twMul408` 零亂數+係數界 [0.3,2.2];
2) **聚集實證**:800 天 AI 城,塔的平均都市織理密度須顯著高於「全體 lv2+ RCI」的平均密度
   (改動前塔與 RCI 應無顯著差);
3) 兩釘以新值恆等;12 種子無崩城;
4) 破壞性:調制拆掉→聚集斷言紅;係數反向(郊區加速)→紅;
5) 樣張:同種子同視角,改動前後對照。

出卡=本 commit;施工 bay/kimi;v11.34。


---

## 施工結果:**設計失敗,已退修**(2026-08-03)

依卡面施工完畢後,按第 1/3 步做的 A/B 量測**否決了本卡自己的假設**。

### 400 天(12 種子):看起來像成功

無崩城;301 4153→4679、22 4550→4898、3 4257→6924,其餘完全不變。
塔-RCI 密度差 +3.9 → +5.2。**若只看這組數據,我會誤判為成功而落地。**

### 800 天(整倉複製 A/B,決定性):實為失敗

| seed | 人口 前→後 | 塔數 前→後 | 塔-RCI 密度差 前→後 |
|---|---|---|---|
| 301 | 6638 → **6235** | 13 → 15 | +6.1 → +6.9 |
| 22 | 5998 → **5508** | 29 → 31 | +12.0 → **+10.7** |
| 123 | 4129 → 4129 | 0 → 0 | —(無塔) |
| 1 | 6255 → 6255 | 5 → 5 | +3.0 → +3.0(完全未變) |
| 3 | 9216 → **7074** | 2 → 3 | +3.4 → **−0.1** |
| 5 | 3182 → 3182 | 0 → 0 | —(無塔) |

**平均聚集度 +6.1 → +5.1(變差),且三個種子人口全跌(−403/−490/−2142)。**

### 失敗機制(本卡最有價值的產出)

塔一旦形成就變成 k33/34,**不再是 lv2+ RCI,反而擋住鄰居再合併**(合併要求四格同 k 且 lv≥2)。
加速市中心合併=**提早把市中心的候選簇消耗殆盡**,之後市中心再無簇可合,而外圍以較低速率
持續累積,800 天下來反而追平——**聚集不成、反倒攤平**。人口下降則因為提早合併
縮短了 lv3 的成長期,而塔的 TOWER_POP 是固定值。

### 另一個方法論教訓

**短視野(400 天)的結論與長視野(800 天)相反。**本卡若照一般調參卡的 400 天慣例驗收,
會得到「人口上升、聚集改善」的假成功。凡是改變「發生時機」的機制,必須量到系統飽和為止。

### 退修範圍與保留

- **index.html 完全還原**(`git checkout --`),與 master 位元相同;`twMul408`、`urbanDens406` 皆為 0 處。
  **兩釘回到 4153/4550、PASS 3621 全綠。**
- 中途曾把密度計算從 pickV406 內聯抽成 `urbanDens406()`(供 twMul408 共用),
  並以「兩釘恆等」證明其為純重構;但**退修後只剩一個呼叫者,抽出已無價值,故一併還原**。
  (本節初稿誤記為「保留」,收束核對時發現與程式碼不符,已更正——卡面與程式碼必須逐字對得上。)

### 若日後仍要做,正確方向(非本卡)

問題不在「合併機率」而在**塔會凍結該地塊的再合併能力**。可行方向:
①讓塔本身可再合併成巨廈(現行巨廈是 3×3 直接成形,與塔無關);
②改動供水的空間分布(讓水管向市中心集中),從源頭造成稀缺;
③接受現況——實測塔本來就偏向密集區(+6.1),「散落感」其實是「多個密集口袋」而非隨機。
**三者都動模擬、都需重新釘值,建議先有明確的觀感目標再開卡。**
