# T558 執行期開關真 A/B（修無鑑別力斷言：恆真式一律判紅）

**日期**：2026-08-25　**發卡**：Claude（馬拉松第三站；ROADMAP 真欠第 2 條「空轉守衛清理」）
**出處**：T371 審計記「test_fixde.js:413 等開關無鑑別力」；T552 稽核確認該行（現 :1223）
自 T361 落地後零修改、前方無非零基線＝恆真式。

## 0. 稽核（9 個執行期被撥動的 __no* 開關逐一驗屍）

| 開關 | 判定 | 依據 |
|---|---|---|
| `__noRefineryFx` | 健康 | T364b ellipse 追蹤 muted/active A/B，決定性重跑恆等＋團數下限 |
| `__noFlow384` | 健康 | 開啟 meta.nodes≥4 基線 → 逃生閥 → null |
| `__noVeh` | 健康 | 雙生子 30 天 pop/money/happy/buildings 恆等 |
| `__noRoofProps417` | 健康 | 關態落筆 n≥1 基線 → 開態 0 → 關態復原 |
| `__noFarNight422` | 健康 | 多情境 on/off screen 逐值恆等＋drawn 計數 |
| `__noOpp455` | 健康 | meta.tiles>0 基線 → 逃生閥 → null |
| `__noPowerDistrict432` | 健康 | 雙城 150 天 pop＋rawSave 位元恆等 |
| **`__noLife`** | **恆真式** | 新圖上開著開關斷言船隊=0——船隊本來就 0，開關根本沒被行使 |
| **`__noWteFx`** | **無鑑別力** | 只驗 forceDraw「不拋錯」——排氣效果死了也測不到 |

其餘 100+ 個未被執行期撥動的 `__no*` 是靜態逃生閥（常駐關閉釘/原文釘看守），
**不在本卡範圍**：ROADMAP 掛帳的實名病灶就是 :413 那一條，逐一 runtime 化是另一個工程，
收益未證——明確拒絕範圍蔓延。

## 1. 落地

**判準（入方法論）**：執行期開關斷言必須先確立「關閉態的非零基線」、再驗「開啟態的差異」；
恆真式（兩態同值也綠）一律判紅。

兩個修法都放**檔尾新 T558 塊**，原位只留指路註解。原因（原型實測撞出來的，不是推測）：
T361/T363 塊位於套件**共享狀態區**——原型曾在原位重播種，後方 SVK 存檔測試當場倒
（依賴前面積木蓋的城），且中段重播種會動**亂數流尾端**（鐵律 2 雷區）。檔尾＝下游零依賴。

- **__noLife 真 A/B**：seed301 建港口（k18 cargo hub）→ `__t412Set` 完工 → 幀推進生船 →
  **基線 lifeShips>0（關閉態）** → 開啟＋一幀 → 清空=0。
- **__noWteFx ellipse 追蹤 A/B**（手法沿 T364b）：seed301 AI 60 天（垃圾>0）→ addMoney（canPlace
  含資金檢查，T265 同法）→ 建 k62 完工 → zoom2/lookAt/visT55 → muted/active/repeat 三跑 →
  **決定性恆等＋排氣團差 ≥2**。

## 2. 為什麼這張卡是安全的

只動 `test_fixde.js`。不碰 index.html ⇒ 不 bump、不 arch、不 fp_snapshot、六哨兵無涉
（哨兵塊自行重播種，檔尾塊在其後）。原型已在 lab 全量副本驗證：**rc=0、PASS 6,191→6,196**。

## 3. 驗收計畫

紅源三案全部「保字面、殺語意」（後綴改名/前置 false），避免誤咬靜態原文釘：
- ①index 的 updLifeShips 開關失效（`__noLife`→`__noLifeQ`，僅 :15623 一點）→
  **清空斷言紅**——這正是原恆真式測不到的破壞，鑑別力提升的直接證明；
- ②k62 排氣開關失效（`__noWteFx`→`__noWteFx0`）→ muted 也畫 → A/B 差=0 → 紅；
- ③k62 活動排氣死亡（`garbage>0&&animOn`→`false&&…`）→ active 也不畫 → 差=0 → 紅。
  ②③是**相反的兩個失敗型**（開關失效 vs 效果死亡），A/B 對稱性都要咬住。

**明確不動**：7 個健康開關；100+ 靜態逃生閥；T361/T363 的全部靜態接線釘。

---

## 4. 施工紀錄（2026-08-25 收）

**不 bump（測試層卡）**　**主套件 6,191 → 6,199**（守衛 +5；CHANGELOG 條目落地被 T557 判準看守再 +3——自我指涉的量測在寫完帳之後定格，同 T557 教訓）　**master 回填於合併後**

### 過程自捕（一件，直接改寫了落地位置）
卡面原構想是在 T361/T363 塊**原位**升級。原型在 lab 全量副本跑第一輪就撞出事實：
原位重播種會炸掉套件前段共享狀態區的城（後方 SVK 存檔測試當場倒），且動亂數流尾端。
落地改為**檔尾獨立塊＋原位指路註解**。教訓：**套件前段的積木共享一座城，任何造境
（newWorldSeeded）只能放在檔尾或已知重播種區**——這條寫進指路註解，防下一個人再踩。

### 施工法（新）
lab 全量副本先行驗證（rc=0、PASS 6,196）→ 補丁以單一真相源套回 bay → **cmp 位元一致**
＝lab 的綠直接適用於 bay。比「改完再跑」多一道位元橋接，少跑一輪套件。

### 紅源三案（全咬中，保字面殺語意）
①`__noLife`→`__noLifeQ`（僅 updLifeShips 一點）→ 清空斷言紅——原恆真式對這個破壞**全綠視而不見**，
鑑別力提升的直接證明／②`(window.__noWteFx,true)` 逗號運算子 → muted 也畫、差=0 紅／
③`garbage>0&&animOn`→`false&&…` → active 也不畫、差=0 紅。②③為相反失敗型，A/B 對稱咬合。

### 過程自捕（之二）
案二首打後綴改名（`__noWteFx0`）——紅是紅了，**紅在 T438 G3 白名單守衛上**（改名＝白名單裡的
開關失去讀取端，它管的正是這型脫鉤）。紅源紀律：**崩掉/撞別的釘＝沒驗到**。換逗號運算子
保讀取端殺語意重打，咬中本卡 A/B 釘；順帶實證 T438 G3 是活的。
