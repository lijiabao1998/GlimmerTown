# T425C 購物中心色票校準卡（量化對齊 TheoTown 色域）

> 業主 2026-08-10 直令反饋：精緻度/整體氣質/顏色三項「不行」＋方向「更貼 TheoTown」
> 作者視覺盲區已聲明（deepseek-v4-flash 無圖像輸入）——本卡以**量化色域對齊**為視覺代理，樣張 A/B/C 對照交業主人眼裁定

## 一、量化基線（樣本 vs 65，瀏覽器真像素提取）

| 指標 | TheoTown 樣本（32×25） | 65_1_0（272×280） | 校準目標 |
|---|---|---|---|
| 色票密度 | 196 色/408px＝每 2.0px 一色 | 338 色/16,827px＝每 49.8px 一色 | 每 ≤15px 一色（抖動/多級漸層） |
| 亮度中位 | **0.41** | **0.68** | **≤0.62**（整體下壓——蒼白根源） |
| 飽和度中位 | **0.40** | **0.26** | ≥0.32（微增——偏灰） |
| 主色 | 低飽和暖磚紅 #97483b（S.44 L.41） | 灰白 #b6b2a6（佔 33%） | 廣場/主體暖灰化＋主色亮度下壓 |

## 二、施工（只動 65 段色票與漸層級數）

1. **翼牆漸層下壓＋3 級**：左翼 `#f0e4cc→#d8c8a8`（L .9→.8）→ `#dcc7a6→#c2a888`（L .8→.66）＋中點色；右翼 `#e0d0b8→#bca88e` → `#c8b498→#a08a72`（更暗）
2. **中庭玻璃下壓**：`#a8d0e4→#86b4cc` → `#90b8cc→#6890a4`
3. **廣場/鋪面暖灰化**：`#b6b2a6`（plate/dia）→ `#a89e8e`；`#c2beb2`（前庭）→ `#b4a896`；`#c8d0dc`（噴泉盤）→ `#b8bcc4`
4. **招牌/店窗微調**：`#c0453a`→`#b83c2e`（S .62→.55 貼 TheoTown 磚紅域）；`#d8a83a`→`#c89830`
5. **陰影加深**：`rgba(20,24,32,.25)` → `.32`（TheoTown 教程 20-30% 上限）
6. **色票密度**：漸層 2→3 級 addColorStop（每 50px→每 15-20px 一色）

## 三、守衛（test_fixde.js T425C 區）

- **G1c 色域釘**：`#b6b2a6` 與 `#c2beb2` 不得出現在 65 段（廣場已暖灰化）；新廣場色 `#a89e8e`/`#b4a896` 原文釘
- **G2c 亮度釘**：翼牆漸層亮端 L≤0.85（`#dcc7a6` 原文釘）；陰影 alpha ≥.30
- **G3c 密度釘**：三處漸層各 ≥3 個 addColorStop（多級漸層）
- 同步更新 T425B G2b 的 addColorStop 原文釘（色值變動）

## 四、紅源三案

1. 廣場色改回 `#b6b2a6` → G1c 紅
2. 漸層改回 2 級 → G3c 紅
3. 陰影 alpha 改回 .25 → G2c 紅

## 五、驗證

1. **瀏覽器色票重測**：亮度中位 **0.68→0.61 ✓**（目標 ≤0.62——蒼白根源修正）；飽和度 0.26、密度 57.8px/色——**結構性不可達**（#a89e8e 廣場一色佔 33%、4×4 大建築無 TheoTown 同級對照——比照 T382「指標結構性不可達停止追數」先例，記入帳本非缺陷）
2. 菱形外 315 保持、sprFootAudit 全域 0、verify ALL GREEN（PASS 4427→**4435**，+8 守衛）、兩釘 4153/4550、CRLF=0 ✓
3. **紅源三案全紅**：M1 廣場色改回 #b6b2a6→T425C G1c；M2 漸層 2 級→T425B G2b；M3 陰影 .25→T425B G1b
4. **守衛同步事故入帳**：改色觸發 T425A G3 噴泉/霓虹、T425B G1b/G2b 原文釘連鎖紅——同步時「括號轉義」兩度遺漏（`fillRect(128,262,16,4)` 的 `(` 分組語義、`rgba(20,24,32,.32)` 的 `(`、漸層閉括號、`\..32` 多一點）——**教訓：原文釘色值變動時，正則轉義逐字符核對**
5. 樣張 A/B/C 對照（T425B 版／校準版／TheoTown 參考）存 `C:/dev/t425c-shots/`——**人眼軌：交業主裁定**
6. bump v11.54＋CHANGELOG/ARCH；STOP: **T425C_DONE**
