# T394a — 平衡數據採集器與報告(純工具+文檔卡;二期 R2)

目標:委託獎金/合約價/專精乘數至今全是拍的保守值,零數據。本卡建採集器跑實測,
產出數據報告,T394b 調參卡只在數據顯示偏差時才開。**零 index.html 變更,不 bump。**

## 方法(偵查工作流 wf_c1ceab31 三向定案)

1. **驅動器**:整倉複製到 scratchpad(不污染 master/bay),node 驅動器置於副本根目錄
   (切片代碼用 __dirname 讀 index.html),切片配方=讀 test_fixde.js 至唯一標記
   `// ---- 測試輔助 ----` 為止 eval——mock DOM/GV/測試橋(__t385*/__t386*)全就位。
2. **基線跑批**:12 種子(301,22,123,1,2,3,5,7,11,15,42,99)×400 天,
   `newWorldSeeded(s);setDiff(1);ai(true)` + step(1)×400(step clamp 60,必須迴圈),
   逐日記錄:money/pop/happy/rank/cityPoints/transit ridership/steelUsed/steel/fuel
   存量/tradeGold/taxI+taxC(__t386Fin 橋)/fires/紓困觸發。JSONL 落 scratchpad。
3. **委託完成率=離線重算**(核心省工:接單零模擬副作用,L12443 註釘死):
   對 11 條池目 × 每個合法接單日 D 網格算:acc=Σ日流量[D,D+days)≥target;
   hold=窗內存在 holdN 連續達標(逐日未達即清零);stock=第 D+days 天存量≥target;
   tech=hasTech343('C6') 時刻。**happy 捨入陷阱**:stats().happy 是 toFixed(2),
   L12450 原始比較用 cityHappy 原值——施工時先探 GV.happyBreakdown() 是否露原值,
   否則用 2dp 並把 |v−target|<0.005 的邊界日標記為「不可判」,如實入報告。
4. **專精四臂**:4 專精 ×12 種子 ×400 天,種子首達 Lv6 之日 __t386Set(id)(橋對 edu
   自帶 rebuildCov),對照基線臂比終點 money/pop/happy+逐日 taxI/taxC/tourists/
   ridership/fires。同種子確定性對照=效果的複利真值。
5. **報告**:docs/tasks/T394a-平衡數據報告.md——破產語義自定義(無 game over,
   money 可負,$250 紓困+T348 補助都記次)、rank 軌跡(各種子到 Lv3/4/5/6 天數=
   委託解鎖時刻)、11 池目完成率+餘裕分布、專精臂差、調參建議(有偏才開 T394b)。

## 已預埋的發現(偵查已證,報告須含)

- AI 市長 wants 表不含煉油廠/鋼鐵廠/造船廠(k121-123)→ steel40/80、ct_steel60、
  ct_fuel80 在 AI 基線完成率恆 0,屬玩家專屬內容,不是價格偏差,解讀時分開。
- techC6 需玩家操作科技樹,AI 基線大概率同樣恆 0(跑批驗證)。
- diff===3 沙盒關閉委託/專精;採集全程 diff=1(對齊 12 種子先例與兩釘)。

## 紀律紅線

- 驅動器只在 scratchpad 副本跑;master/bay 零寫入(報告文檔除外)。
- step 迴圈中途只呼叫純讀 GV 方法(消耗 R() 的方法會令種子軌跡分岔,偵查風險清單
  第4條);__t386Set 只在專精臂用且該臂不與釘值比對。
- 原始 JSONL 留 scratchpad,報告只入摘要表(不塞肥數據進倉)。
- 報告=交付物;純文檔+工具卡不 bump 直寫 master(T371b/T392 先例)。

## 驗收

1. 基線 12×400 跑通,seed301 pop@400=4153/seed22=4550 與兩釘恆等(驅動器正確性鐵證)
2. 11 池目完成率表+餘裕分布(happy 邊界不可判日如實列)
3. 專精四臂 ×12 種子對照表
4. 報告落 docs/tasks/,含方法/數據/發現/調參建議四段,帳本更新

自主 loop 二期 R2;出卡=本 commit。
