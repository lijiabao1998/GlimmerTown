# T386b — 外貿合約 v1(CMS385 池擴展・stock 期末驗收型)

狀態:**已完成(v11.11,bay/kimi)。PASS 3407→3415;施工中測試抓到面板呈現序 bug 並修。**
發卡:業主 loop 直令(GPT 順位 3 的合約半邊;偵查裁決 b 先 a 後——合約不依賴專精即可上線,
專精動稅式證明重,拆開走)。出卡+施工+自審:Claude(業主 loop 授權)。
前置:master `8b87d37`(v11.10,PASS 3407)。路徑:bay-kimi,bump 11.11,merge --deploy。

---

## 一句話

委託池加兩條「外貿合約」:**到期日驗收庫存**(`stock` 新型:第 days 天時 `steel/fuel≥target`
即成、否則過期)——玩家必須「囤」對抗每日消耗(造船吃鋼/工業吃燃料)=真機會成本;
零模擬寫入(不扣庫存,文案=「驗收庫存」非「運走」),零接單恆等紅線與存檔 schema 原樣復用。

## 設計決策(六條)

1. **stock 型語義=期末驗收**(Against the Storm 到期結算):el<days 期間只顯示進度
   (bar=stock/target 即時);`el>=days` 那天 `stock>=target`→完成發獎,否則過期。
   tick 錨塊加一個型別分支,**依然只寫 cms385+money**(扣庫存方案被否決:會生 steel 第二寫者、
   簽約城位元分歧擴散全下游、當日 fin/flowStat 快照變假數據——偵查風險 1 記卡)。
2. **池加兩條**(存檔零新鍵=池成長不動 schema):
   `ct_steel60 {nm:'外貿合約:鋼材庫存驗收 60',ic:'🚢',type:'stock',src:'steel',target:60,days:120,bonus:3200,minRank:6}`
   `ct_fuel80 {nm:'外貿合約:燃料庫存驗收 80',ic:'⛽',type:'stock',src:'fuel',target:80,days:120,bonus:3000,minRank:5}`
   target 皆 <120(兩存量 CAP);bonus 高於委託檔但攤提 <$30/天(獎勵通脹風險 4);
   佔同一 act 槽(v1 接受「簽約期不接委託」,面板文案言明);沙盒不出(T385 慣例沿用)。
3. **面板 stock 型呈現**:進度 bar=當前庫存/目標(可跌——文案「期末驗收」說清楚);
   剩餘天數即倒數;`t:'warn'` 當庫存<target 且剩<20 天。
4. **T385 收尾補課(同卡捎帶,二項)**:
   a. 補實作卡面宣稱的 `pop>50` 門檻(cmsAccept385 與面板 dim 各一;T385 卡面寫了沒做);
   b. T385 卡施工記錄補披露該偏離(偵查抓到=誠實記錄)。
5. **驗收沿用 T385 全套**:錨塊靜態禁亂數、鐵律7 釘、驗型(新 id 自動入白名單=CMS_BY_ID385
   驅動,零驗型改動)、零接單恆等、存讀往返。新增:stock 型功能鏈(囤鋼到期完成/庫存不足
   到期過期兩案)、elig 邊界(低 rank 池過濾後仍給滿 3 條)。
6. **T386a(專精)另卡後行**:spec386 單字串欄+sq() 效果 helper+委託頁「城市方向」區+
   dozeArm 兩擊確認(偵查四路已打底,本卡不做)。

## 允許觸碰

`index.html`(CMS385 池+tick 錨塊 stock 分支+面板 stock 三元+cmsAccept385 pop 門檻+bump)、
`sw.js`、`test_fixde.js`(新斷言;白名單無新 id——按鈕沿用 bCommAcc385_*)、`docs/ARCH.md`(版本行+
§5 cms385 句補 stock 型)、`docs/CHANGELOG.md`、`docs/tasks/T385-*.md`(補披露)、本卡、帳本。

## 禁止觸碰

T385 全部禁區照舊;**嚴禁 steel/fuel/goods 任何寫入**(stock 型純讀);tools/ 不動。

## 不變量

零接單恆等(兩釘+雙重播);零委託存檔零新鍵;PASS 只增(3407 起);verify ALL GREEN 11.11。

## 驗收清單

1. stock 完成案:造境 rank6+鋼庫存 100+簽 ct_steel60→step 到期→money+bonus、done 收錄。
2. stock 過期案:庫存 20 到期→過期零副作用、不入 done、n+1。
3. stock 純讀案:簽約前後與到期日,`GV.chain346().steel` 軌跡與未簽約對照城完全相同
   (同 seed 兩世界對照=庫存零寫入的機器證明)。
4. pop 門檻案:pop≤50 時 accept 拒絕+面板 dim。
5. 破壞性:錨塊塞字串 'R()'→紅;基線→綠(T385 套路)。
6. 閘門+真瀏覽器(8125 槽3)四 tab 健在+合約卡渲染。

## 回滾

單 bay commit revert;池條目自動從 offers 消失,已簽玩家 act 指向未知 id 時
tick 錨塊白名單雙保險會清空(T385 既有防禦)。

---

## 施工記錄(Claude,2026-08-02)

### 測試抓到的呈現序 bug(修掉)
pop dim 分支插在 act(進行中)之前 ⇒ 簽約後人口波動跌破 50,面板顯示「人口 50 解鎖」
而看不到自己的合約。修=分支序改為 diff→act→rank→pop→三選一(進行中優先於門檻 dim)。

### 補課二項(T385)
pop>50 門檻補實作(accept+面板 dim);T385 卡施工記錄補披露該偏離(偵查抓到)。

### 閘門與證明
PASS 3407→3415/0 FAIL;verify ALL GREEN 11.11;純讀機器證明(無廠城完成 tick 後 steel 恆 100);
期中達標不提前完成專項;破壞性三案紅源驗明(含池條目刪除案);真瀏覽器 8125 槽3 v11.11 健康。
