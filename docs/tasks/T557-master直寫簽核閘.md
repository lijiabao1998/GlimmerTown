# T557 master 直寫簽核閘（守衛站在沒人走的門口）

**日期**：2026-08-25　**發卡**：Claude（業主「你把全部問題解決吧」馬拉松；ROADMAP 真欠第 1 條）
**出處**：T370a（2026-07-29）自陳「當班施工者直接寫 master 完全繞過合併。這個缺口本身另立卡」
——**163 張卡過去，卡沒立**。T552 ROADMAP 稽核重新挖出。

## 0. 病灶

merge_bay 的 `assert_signoff_landed`（禁「待」＋具名，掛 `start_transaction` preflight）
技術上健全、有 test_toolchain 四例、有三次真實咬人記錄（T343a／T413b／T381）——
但它**只在合併交易觸發**。而 T541 起的實際工作流大量直寫 master
（工具鏈卡、撤卡、文件卡），這條路全程無閘。

## 1. 落地

把同一判準搬進**主套件** `test_fixde.js`（T371 簽核落點守衛之後新增一塊）：
任何 verify——bay 驗證、integration 驗證、**merge 時的 frozen base（master）驗證**——都會執行，
直寫 master 的產物最遲在下一次合併的 base 驗證被咬住。

**分界錨點＝T553 條目**（本卡立卡時的最新簽收，錨點字串而非行號）。其後的新條目：
- (a) 必須有驗收欄（沿 T371）；
- (b) 驗收欄必須**具名** `SIGNOFF_PARTIES557` 之一（Claude/Kimi/Codex/Grok/DeepSeek/業主/GPT）；
- (c) 驗收欄不得含**待覆核語意**（`驗收:待`／`待非作者覆核`／`待覆核`）。

**刻意不溯及**：歷史 24 條「待」全在分界之前、豁免照舊——其中 14 條「實機冒煙待人」、
5 條「樣張待業主人眼裁定」是**合法且可能永久**的狀態（T552 稽核判定「該禁的是待覆核語意，
不是待字」，本卡照此收斂範圍）；四條 2026-07-29「待非作者覆核」是歷史欠條，處置權在業主
（決策項），本卡不代裁。

**同族病防治（譜系防治第八例）**：`SIGNOFF_PARTIES557`（JS）與 merge_bay 的
`SIGNOFF_PARTIES`（Python）是同一條規則的兩份實作、跨語言無法共用——
由 test_toolchain 新增**同步釘**互相看守：解析兩份名單、逐位比對，任一邊改了另一邊沒跟＝紅。

## 2. 為什麼這張卡是安全的

只動 `test_fixde.js`（加守衛）與 `tools/test_toolchain.py`（加同步釘）。
不碰 `index.html`/`sw.js` ⇒ **不 bump、不 arch、不 fp_snapshot、六哨兵無涉**。
主套件 PASS +2（錨點在場＋名單規模），工具鏈 60 → 61 例。

## 3. 驗收計畫

- **G1（靈魂）**：分界後條目逐條驗 (a)(b)(c)，失敗訊息指名條目卡號與缺什麼。
- **G2 同步釘**（test_toolchain）：JS 名單 == Python 名單，逐位。
- 紅源：①CHANGELOG 頂部注入 `驗收:待非作者覆核` 條目 → G1(c) 紅；
  ②注入 `驗收:通過`（無具名）條目 → G1(b) 紅；
  ③JS 名單刪掉「業主」 → G2 同步釘紅。
- 綠收尾：還原後主套件全綠＋工具鏈全綠。

**明確不動**：merge_bay 的 preflight 閘（雙閘並存＝縱深，不是冗餘——merge 閘在交易入口早咬，
主套件閘管直寫路徑）；T371 的四條結構守衛；歷史條目。

---

## 4. 施工紀錄（2026-08-25 收）

**不 bump（測試層卡）**　**主套件 6,186 → 6,191**（塊本身 +2；本卡 CHANGELOG 條目落地後成為分界後第一條被管條目，再 +3——自我指涉的量測要在寫完帳之後才定格）　**工具鏈 60 → 61 例**　**master `fe69986`**

### 落地與卡面一致
- test_fixde.js：T371 塊之後新增 T557 塊——錨點釘（T553 條目在場）＋名單非空釘＋
  分界後逐條 (a)(b)(c)。上線當下分界後為空集＝前瞻守衛；本卡自己的 CHANGELOG 條目
  是它管的第一條，落地即自我看守。
- test_toolchain.py：`SignoffPartiesSyncTests` 同步釘——解析 JS 名單與
  `merge_bay.SIGNOFF_PARTIES` 逐位比對。

### 紅源三案（全咬中）
①注入「驗收:待非作者覆核」條目 → 待覆核語意釘／②注入「驗收:通過」無具名條目 → 具名釘／
③JS 名單刪「業主」 → 同步釘。

### 過程自捕（一件）
**現任閘門咬了本卡的 CHANGELOG 條目**：merge_bay 的簽核閘以整行**第一個**「驗收:」字面量切欄——
我在正文裡原樣引用紅源字串「驗收:待非作者覆核」，搶走了切點、被判 pending。
規則入帳（CHANGELOG 陷阱家族第三條，同「prose 裡的 || 爆欄位」「禁待字」）：
**條目正文不得出現「驗收:」字面量**，引用時改述不引原文。
這也是雙閘縱深的一次實地演示：老閘先咬、新閘（只驗最後一欄）判準更準——兩者判定面不同、互補。

### 誠實邊界（沿 merge_bay docstring）
本閘與 merge 閘一樣**驗的是落點不是事實**：具名把「漏記」變成「說謊」，是可查核性提升
而非覆核證明；四方共用同一 Git 身分，作者自簽在物件層不可辨。縱深＝merge 閘（交易入口早咬）
＋本閘（直寫路徑補防）＋自簽聲明慣例（「未經第三方覆核，如實聲明」）。
