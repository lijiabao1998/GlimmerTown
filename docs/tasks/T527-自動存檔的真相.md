# T527 自動存檔的真相（RULES 第 18 條建立在一個從未成立的前提上）

**日期**：2026-08-19　**發卡**：Claude（業主授權：自己玩→出卡→施工→合併）

## 0. 事實

T526 收尾時 `arch_map --fix` 印出這個錨點：

```
index.html:22126  setInterval(()=>{if(tiles)save();},25000);
                  // 世界尚未建立（啟動畫面）時跳過自動存檔
```

`git log -S "if(tiles)save()"` 追出它自 **2026-07-18 `6e4d31a`**（FIX-G/H/I 側線任務）就存在。
T447 是 **2026-08-14**。所以 T447 寫進 `docs/RULES.md` 與全局記憶的這句：

> 「`index.html` **沒有自動存檔**，全檔 `save()` 只在存檔按鈕的處理器裡呼叫一次，
>  所以『把遊戲載起來放著』不會覆蓋任何槽」

**在寫下的那一刻就是錯的**——不是過期，是從未成立。

而 `docs/ARCH.md` §11 那張表**一直有**「自動存檔 | `setInterval(()=>{if(tiles)save();},25000);`」
這一列。三份文件互相矛盾了三十幾張卡沒人發現——**與 T447 自己收的那條債完全同型**
（T447 收的是「三份文件互相引用一條不存在的鐵律」，它自己又製造了一條反向的）。

推測 T447 為什麼會量錯：全檔 `save()` 有數百處是 canvas `ctx.save()`，
自動存檔那行是**裸呼叫** `save()`，在雜訊裡被淹掉了。

## 1. 為什麼要緊（風險的實際形狀）

RULES 18 的防護點是「呼叫 `GV.save()` 之前先釘槽 3」——它假設**只有顯式呼叫會寫存檔**。
真相是：**把 index.html 載進瀏覽器、放著 25 秒，就會寫入當前選定的槽**，
不需要任何 `GV.save()`、不需要按任何按鈕。

⇒ 「在玩家目錄（8123）不得跑測試」這條，過去被理解成「不要在那裡跑腳本」；
   真實強度是「**不要在那個 origin 用玩家的瀏覽器 profile 把頁面打開超過 25 秒**」。

**本季實際曝險為零**，但那是靠兩層與這條結論無關的獨立保險：
① 所有探針跑實驗複本（127.0.0.1:8234）而非玩家目錄；
② 每次拋棄式 `--user-data-dir`，localStorage 依 origin+profile 分割、跑完即刪。

## 2. 順路發現：RULES 有兩條都編號 18

`docs/RULES.md` 第 30 行（存檔槽紀律，T447 新增）與第 37 行（buildSprites 亂數流對齊，既有）
**都是 18**，其後全部連號偏移。T447 插入時撞號了。

## 3. 落地

1. **改寫 RULES 18** 的「另註」段：陳述自動存檔存在、週期 25 秒、觸發條件是
   `tiles` 已建立（啟動畫面不寫），並把防護點從「呼叫前釘槽」改成
   「**載入前釘槽**」——`localStorage.setItem` 必須在 iframe/page 開始載入 index.html **之前**。
2. **修 RULES 編號撞號**（18 之後全部順延），並修正引用處。
3. **機器守 G1**：`test_fixde.js` 釘住自動存檔這件事本身——
   `setInterval` + `save()` + 25000 三者同在一行必須存在，且 RULES 必須提到它。
   任何一邊被改掉、或哪天真的拿掉自動存檔而沒同步文件，套件就紅。
   （這是「文件與程式碼互相矛盾三十張卡沒人發現」的直接對策。）
4. **機器守 G2**：RULES 條目編號必須嚴格遞增無重複（機械複算）。
5. **修全局記憶**：`glimmer-town-project.md` 裡 T447 那段的錯誤結論，就地更正並註明來源。

## 4. 驗收計畫

- G1 自動存檔三要素原文釘 + RULES 必須描述它（雙向）。
- G2 RULES 編號嚴格遞增無重複（機械複算，不手抄清單）。
- 六哨兵位元恆等（本卡不動 `index.html` 的執行語意）。
- 紅源：①拿掉自動存檔那行不改文件（G1 應紅）②RULES 刪掉自動存檔描述（G1 應紅）
  ③把編號改回撞號（G2 應紅）。

---

## 5. 施工紀錄（2026-08-19 收）

**版本** v11.155（不 bump）　**PASS** 5,857 → 5,864　**`index.html` 一個位元都沒動**（故不重新部署）

### 行為證明（本卡最硬的一條）

原文釘只證明那行程式碼在，不證明它會寫。探針**不呼叫任何 API、不按任何按鈕**，
只 `newWorldSeeded(527)` 然後純靜置（實驗複本 + 拋棄式 profile，槽先釘 3、先清空 `s3`）：

```
已清空 glimmerville.v1.s3，現在 = null
已建立世界（seed 527）。從這一刻起不再呼叫任何 API、不按任何按鈕，純靜置。
★ 第 24 秒：槽 3 被寫入了，長度 5636 bytes
  前 80 字：{"v":1,"n":72,"gameVer":"11.155","seed":527,"money":3000,"day":1,...
```

T447 的斷言被實測推翻。

### 這件事到底牴觸了幾份記錄

追下去發現不只 ARCH：

| 來源 | 寫的是 | 日期 |
|---|---|---|
| `index.html` `6e4d31a` | 自動存檔存在 | 2026-07-18 |
| `ARCH §11` 表 | 「自動存檔 \| `setInterval(...,25000);`」一直有這一列 | — |
| 全局記憶 `player-save-overwrite-authorization` | 「**25 秒自動存檔**差點把第 67 天的城市蓋成第 1 天」＝**親身事故** | 2026-07-29 |
| T447 → `RULES 18` + 記憶 | 「沒有自動存檔」 | 2026-08-14 |

**最值錢的一條教訓**：那不是一條沒人知道的事實——它寫在我自己的記憶裡，
而且是一次真實的近失事故換來的。**一份新調查如果不先查既有記錄，會很有自信地覆蓋掉親身經驗。**
（T447 的方法是「剝掉註解與字串後 grep `save()`」；為什麼那樣還會漏掉裸呼叫的那一行，
本卡沒有重建當時的腳本，不編造原因，如實留白。）

### 順路發現：`鐵律N` 是三重撞號

`鐵律18` 在 repo 裡同時指三件事：RULES 的存檔槽紀律（T447 新增）、RULES 的
buildSprites 亂數流對齊（既有）、COLLAB 那套獨立編號的行中註釋 `/* */`。
T447 編號時本檔已經有一條 18，撞號後 19/20 全部錯位，而且沒有任何東西會發現。

### 落地

1. `RULES 18` 改寫：防護點從「呼叫 `GV.save()` 之前」改成「**開始載入 `index.html` 之前**」，
   並寫明真實強度是「不得用玩家的 profile 在該 origin 把頁面打開超過 25 秒」。
2. `RULES` 內部撞號修掉（18 存檔槽／19 buildSprites／20 tick 分類鏈／21 工具列價），
   新增「編號空間須知」明寫本檔 1-21 與 COLLAB 那套「鐵律N」是兩套獨立編號。
   **歷史卡面（如 T361 引「鐵律18」指 buildSprites）保留原文不改寫**——帳本的更正是再記一筆。
   兩套編號要不要合併＝業主層級決定，本卡不動。
3. `ARCH §10.5` 那段錯誤結論就地劃掉並附推翻證據；`ARCH §11` 本來就是對的，不動。
4. 全局記憶兩份更正：`player-save-overwrite-authorization` 把自動存檔升為標題級事實；
   `glimmer-town-iron-rules` 第 3 條**原文意思整個相反**（寫成「槽 3 是玩家存檔位，測試不得寫入」，
   而 RULES 18 與 `ARCH:429` 的原意是槽 3 正是**測試專用槽**），一併更正。

### 守衛（全在 `test_fixde.js`＝閘門上，T447 M3 教訓）

- **G1** `index.html` 恰有一處 `setInterval(()=>{if(tiles)save();},N)`（在 `htmlBare438` 上掃，
  註解裡談論自動存檔餵不飽它）。
- **G1b** 週期恰為 25000ms（改週期就必須同步 RULES，否則「放著 25 秒」那句會失真）。
- **G1c** **反向**：RULES 必須原樣寫出那行並說明它存在——單向釘擋不住「程式碼在、文件說不在」，
  那正是 T447 留下的狀態。
- **G1d** RULES 不得再出現「沒有自動存檔」的斷言。
- **G2** RULES 條目編號機械複算必須是 1..N 嚴格遞增無重複；**G2b** 保留編號空間須知。

### 紅源（五案，全紅到指定守衛）

| 案 | 破法 | 該紅 | 實得 |
|---|---|---|---|
| ① | 拿掉自動存檔那行、不改文件 | G1 | **先撞 T371b** |
| ①a | 複製成兩處自動存檔（ARCH 錨點仍在） | G1 | G1 YES（實得 2 處） |
| ①b | 週期改 60 秒並同步 ARCH 錨點 | G1b | G1b YES |
| ② | RULES 刪掉自動存檔描述 | G1c | G1c YES |
| ③ | 編號改回撞號（兩條 18） | G2 | G2 YES |

**①的結果本身是個發現**：自動存檔那行**同時是 `ARCH §11` 的 grep 錨點**，
所以「直接刪掉它」早就有絆線（代碼地圖閘門）——真正沒人守的方向是
「程式碼在、文件說相反」，那正是 G1c 補的。
第一版紅源沒紅到 G1 ⇒ 照紀律補了 ①a／①b 兩個不動錨點的破法（**紅源沒紅到你想驗的那一條，等於沒驗**）。

### 自捕
G1d 第一版當場咬到我自己——RULES 裡引述 T447 那句錯誤斷言時原樣抄了「沒有自動存檔」六個字。
守衛沒錯（它就是要擋這串字），改寫引述為「斷言它**不存在**」。
