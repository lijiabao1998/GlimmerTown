# T529 委託不該發你做不到的單（83% 過期率的發單側修法）

**日期**：2026-08-19　**發卡**：Claude（業主授權：自己玩→出卡→施工→合併）

## 0. 實測（帳本調查筆記五的完整數據）

模擬「見單就接」的玩家（AI 蓋城、我接單）500 天 × 三座城：
~~12 件結案、只完成 2 件＝83% 過期率；成熟城獎金佔財富累積 0.16%。唯一完成過的都是 `transit400`。~~

**⚠ 上面這組數字是錯的（施工中自己抓到並更正，見第 6 節「兩件我自己的量測錯誤」）**：
判定完成用的是「`cms385.done` 有沒有變長」，而 T385 原文寫著「done 只收不重複 id——
**重複完成照發獎但不重記**」⇒ **二次完成被誤判成過期**。
改用原始碼插樁精確計數後，正確基線是 **四城 16 件結案、完成 2、過期 14＝過期率 87.5%、獎金實付 $4,000**。
本節以下的「逐條可完成性」表不受影響（它量的是來源指標，不經過 done 判定）。

逐條可完成性（500 天 AI 城，窗口取整局最有利者＝上限估計）：

| 委託 | 目標 | 三城實得 | 判定 |
|---|---|---|---|
| transit150 / transit400 | 運量 hold | 401/201/51、301/76/9 天 | ✅ 可完成 |
| trade1200 / trade3000 | 出口金 acc | 1541/0/0、2052/0/0 | ⚠ 需糧食出口城 |
| happy70 / happy80 | 幸福 hold | 7/2/0 天、**0/0/0 天** | ❌（幸福峰值 .70/.74/.62） |
| steel40 / steel80 / ct_steel60 / ct_fuel80 | 鋼材/燃料 | 全 0 | ❌ 城裡沒有加工廠 |
| techC6 | 研究 C6 | AI 科技 `done:[]` | ❌ 在 AI 局 |

## 1. 兩個發單側的根因（都不在模擬層）

**根因一：發單不看城市有沒有能力做。**
鋼材/燃料四條委託（**$10,200 獎金池**）需要 `k122 鋼鐵廠`／`k121 煉油廠`；
而這兩座**不在 AI 的 78 條 `wants` 清單裡**，玩家也很少主動蓋
⇒ 城裡鋼材/燃料恆 0，這四張單發出去只有一個結局：過期。
（給錢手動蓋齊後這條鏈**是通的**——鋼 2/日、燃料 3/日、工業稅乘數 ×1.12 確實啟動、
`ct_fuel80` 其實可達；礦脈約 240 天耗盡＝有時限的加成。所以**機制沒壞，是發單沒看條件**。）

**根因二：合格委託 ≤3 時根本不抽選。**
```js
const elig=CMS385.filter(...);
if(elig.length<=3)return elig.slice();   // ← 直接全回，沒有抽選
```
低階城市（`minRank` 過濾後只剩 ≤3 條）每次都拿到**完全相同的三張**
⇒ 過期後又抽到同一張。實測 seed528 `trade1200` **連續過期三次**、
seed301 `techC6` 過期兩次。玩家視角＝遊戲一直塞同一張你做不到的單。

## 2. 為什麼這張卡是安全的

`cmsOffers385()` 全檔只有三個呼叫點：`cmsAccept385`（玩家動作）、委託面板 UI、`GV` 鉤子。
**tick 裡沒有任何呼叫** ⇒ 改發單邏輯對模擬**零影響** ⇒ **六哨兵應位元恆等**。

## 3. 落地（只動發單，不動任何目標值、不動 AI、不動 tick）

1. **可行性閘門**：每條委託標一個 `feas` 判定（純讀現況、零亂數）：
   - `steel*` / `ct_steel60` → 城裡有 `k122`
   - `ct_fuel80` → 城裡有 `k121`
   - `trade*` → 城裡有貿易站（`k?` 由 COST/工具反查，不手抄）
   - `transit*` / `happy*` / `tech*` → 無結構前提，恆可行
   不可行者**不進發單池**。城市蓋了加工廠，那幾張單自然出現＝**引導而非懲罰**。
2. **不連發剛過期的那張**：記 `lastExp`（不入存檔、鐵律7 三處歸零），
   發單池排除它；若排除後池空則放行（**永不讓池變空**＝不准靜靜關掉整個系統）。
3. **抽選在池 ≤3 時也要動**：`elig.length<=3` 那條早退拿掉，讓 `lastExp` 排除生效。

**刻意不做**：不動任何目標值。`happy80` 的 .78 高於我實測的幸福峰值（.74），
但我的數據**全是 AI 城**——手玩城市可能達得到，**憑 AI-only 數據去調目標值是不誠實的**，
如實記在卡面與帳本，留給業主或後續有玩家數據時再定。

## 4. 驗收計畫

- **G1（靈魂）發單池永不為空**：任何城市狀態下 `cmsOffers385()` 至少回 1 條
  ——這條擋的是「可行性閘門寫太嚴，把整個系統靜靜關掉」。
- **G2 可行性**：沒有加工廠的城市，發單池不得含 steel/fuel 四條；
  蓋了 `k122` 之後 steel 系必須出現（雙向）。
- **G3 不連發**：剛過期的 id 不得出現在下一次發單池（除非池會因此為空）。
- **G4 零亂數**：發單側新增碼不得消耗 `R()/ri()`（機械掃，非條件跳過）。
- **G5 六哨兵位元恆等**（發單不在 tick 路徑）。
- **行為證明**：重跑「見單就接」500 天 × 三城，**過期率必須顯著下降**，
  且完成件數不得下降（不准靠「不發單」把分母做小＝G1 的鏡像）。
- 紅源：①閘門寫成把所有單都濾掉（G1 應紅）②沒有加工廠也發 steel 單（G2 應紅）
  ③連發剛過期的單（G3 應紅）。

---

## 6. 施工紀錄（2026-08-19 收）

**版本** v11.157　**PASS** 5,884 → 5,913　**master** 待填　**六哨兵位元恆等**

### 落地
- `CMS_FEAS529` 依 **(type,src)** 而非只看 src 的可行性表（5 條規則，計數釘 G4b 守）：
  `acc/steel` 要鋼鐵廠(k122)＋造船廠(k123)、`stock/steel` 只要 k122、`stock/fuel` 要 k121、
  `acc/trade` 要貿易站(k91)、`hold/happy` 要**曾經達到過那個水準**。
- `cmsOffers385` 逐級回退（可行且非剛過期 → 可行 → 非剛過期 → 全部），**永不回空池**。
  排除放在既有 `elig.length<=3` 早退**之前**，所以那條早退不必拿掉也能生效
  （卡面第 3 節原本寫「要拿掉」，落地時發現不必——如實更正）。
- `lastExp529` 在過期路徑記下 id；`happyPeak529` 純觀測峰值。兩者皆不入存檔、三處歸零（鐵律7）。
- 觀測鉤 `GV.cms529()`：peak／lastExp／offers／eligAll／逐條 feas／四座廠計數。

### 行為證明：插樁 A/B，四城 × 500 天（AI 蓋城 + 見單就接）

A＝修前等價版（`cmsFeas529` 恆真、不寫 `lastExp529` ⇒ 兩個 filter 退化為恆等，與 T529 前完全相同）

| | A 修前等價 | B 現況 |
|---|---|---|
| 結案 | 16 | 21 |
| 完成 | **2**（首次 2／重複 0） | **10**（首次 3／重複 7） |
| 過期 | 14 | 11 |
| **過期率** | **87.5%** | **52.4%** |
| **獎金實付** | **$4,000** | **$14,000（3.5×）** |

修前 14 件過期裡有 11 件是**結構不可能**的單：`techC6`×2（AI 城不研究）、`trade1200`×3（無貿易站）、
`steel80`、`ct_steel60`×2、`ct_fuel80`、`steel40`、`trade3000`。修後這些全部不再發出，
名額落到做得到的委託上——seed22 從「happy80／ct_steel60／trade1200 全過期」變成
**完成 transit150 五次（$5,000）**；seed301 從 $2,000 變 $9,000。

殘餘 11 件過期幾乎全是 hold 型（`transit150`／`transit400`），性質是**「試了沒達成」而不是「不可能」**
——玩家做不到一個做得到的目標是遊戲，做不到一個不可能的目標是 bug。seed3002 四件全過期，
那座城的運量在被發單的窗口裡撐不住目標，屬真實殘留、如實記。

### 兩件我自己的量測錯誤（都已更正，數字全部重算）

1. **「83% 過期率」那個數字是錯的。** 前兩輪用「`cms385.done` 有沒有變長」判定完成，
   但 T385 原文寫著「**done 只收不重複 id——重複完成照發獎但不重記**」
   ⇒ **二次完成被我誤判成過期**。改在完成/過期兩條分支各插計數器後才分得出
   「首次完成／重複完成／過期」三種結局。**用衍生訊號當判準，就會量到別的東西。**
2. **G1 的斷言比事實強。** 第一版寫「**任何**城市狀態下發單池都不得為空」，
   而探針的發單池大小分佈顯示 seed528 有 **42 天池是空的**——那是 `minRank` 造成的
   （低階城市本來還沒解鎖委託，屬既有設計），不是我的閘門。
   改成精確不變量：**閘門不得把一個本來非空的池弄空**，並補 G1b 用 rank 0 造境守住那條分界。

另有一條順序錯誤（第三次犯同型）：紅源第一輪三案**全撞在 T371b**
——我 bump 了 `GAME_VER` 卻還沒更新 ARCH 版本行，閘門先紅、fail-fast，等於什麼都沒驗。
**T452 踩過一次，這次又踩**。正確順序：bump → `arch_map --fix` → **ARCH 版本行** → 套件 → 紅源。

### 刻意不做
不動任何目標值。`happy80` 的 .78 已被 T394b 由 .80 下修過一次（理由寫著「12 種子幸福峰值最高 0.75」），
**但 .78 仍在天花板之上＝修得不夠**；我的數據全是 AI 城，**憑 AI-only 數據調目標值不誠實**，
改以「曾經達到過那個水準」的閘門處理，目標值本身留給有玩家數據時再定。

### 紅源三案（全紅到指定守衛）
①閘門恆假且拿掉回退鏈 → G1／②閘門恆真 → G2／③不記剛過期 id → G3（行為釘）。
