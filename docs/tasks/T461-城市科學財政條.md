# T461　城市科學財政條（showStats 可讀日帳）

- 發卡／施工／合併／二次自審：**Grok 全鏈**（業主 goal：自主迭代三張卡，自己出卡自己施工自己合并，合并前二次檢查）
- 車位：`bay/grok`　起點：master 同位 `81f5c39`（v11.91）
- 動 `index.html` ⇒ bump **v11.92**；政策關閉時位元恆等（只讀 fin／pol，不改 tick 公式）

## 0. 為何做

城市科學十卡（T451–T460）把七個政策的**自屬觀測值**寫進 `fin.*`，T460 矩陣也用它們當最可信證據——但玩家在 📊 統計面板**看不到**這些日帳，只能靠 `GV.sci451` 或讀報告。本卡補上「看得見的帳」。

## 1. 交付

1. **`SCI_FIN461` 表**：六列（congRev／rentCut／lvtRev／mwCost／aggGain／cleanCost）——fin key、標籤、單位、sign 只寫這裡。
2. **`sciFinRows461()`**：由表長 `statTab` 列；值走 `sciFinVal461`（讀 `fin`），狀態讀 `pol`。
3. **showStats**：在工業供應鏈之後插入「📚 城市科學·今日帳」；模板 `${t461Html}`。
4. **`GV.sciFin461`**：測試／對帳橋。

## 2. 紅線（可證）

- 表恰 6 列 finKey 恰等帳本六欄。
- 列值不得手抄美元字面；必須 `sciFinVal461`。
- 政策全關：`on=false` 且 `v=0`。
- 開壅堵費 + tick：`congRev.on===true` 且與 `sci451().congRev` 恰等。
- 關閉可逆。

## 3. 不做

- 不改任何 tick 公式／SEEDPIN。
- 不在本卡做指南頁摘要（T462）與守衛加強紅源表（T463 可續）。

## 4. 驗收

`python tools/verify.py` ALL GREEN；本卡 G1–G4 斷言；二次自審見 scratch `review-T461.md`。
