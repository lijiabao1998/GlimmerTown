# T447　槽 3 紀律的機器守（ARCH §10.5）＋ 補一條「大家都在引用但不存在」的鐵律

- 發卡／施工／合併：**Claude 全鏈**（業主 2026-08-14 goal 的自主迭代授權；**夜班自主選題、業主未逐項核可**）
- 車位：`bay/kimi`　起點：`master @ f3a42b9`（v11.79，PASS 4,940，ALL GREEN）
- **不動 `index.html`** ⇒ 不 bump；`merge_bay … --no-deploy`

---

## 0. 先讀原文（這一夜第五次，而這次讀出來的東西最離譜）

`docs/ARCH.md` §10.5：

> **槽 3 紀律完全沒有機器守。** **RULES 鐵律3**、COLLAB、VERIFY 都寫了，
> 但沒有任何東西檢查一份 probe 腳本裡有沒有 `setItem('glimmerville.v1.slot','3')`。

去讀 `docs/RULES.md`——**它總共 17 條，沒有一條在講槽 3。**
鐵律3 的原文是：

> 3. 禁止刪除任何既有功能、註解、以及 `window.GV` 除錯 API。

而 `docs/VERIFY.md` 兩處都寫著「（鐵律3）」：
`:13`「等於覆蓋玩家存檔（**鐵律3**、COLLAB 第二節明令禁止）」、
`:35`「`localStorage.setItem('glimmerville.v1.slot','3')` // 只用槽 3（**鐵律3**）」。

**三份文件互相引用一條不存在的鐵律。** 這比行號過期嚴重一級：
過期的行號只是指錯地方，**不存在的規則是整條引用鏈都懸空**——
每一份都以為「規矩寫在別人那裡」。

## 1. 順路量到的第二件事：這個風險比文件寫的小

`ARCH §10.5` 與 `VERIFY.md:13` 的語氣是「開了 probe ＝ 覆蓋玩家存檔」。實際掃過 `index.html`：

- **沒有自動存檔。** 剝掉註解與字串之後，全檔呼叫 `save()` 的地方**只有一處**——
  `:19377` 的存檔按鈕點擊處理器。光把遊戲載起來放著，不會寫任何一個存檔槽。
- 不經使用者動作會寫進 `localStorage` 的只有三個**偏好／進度**鍵：
  `.scDone`（劇本完成）、`.viewRot`（視角）、`.snd`（音效模式）。

⇒ 真正會覆蓋玩家存檔的，是**明確呼叫 `GV.save()`（或去點那顆存檔鈕）的 probe**。
實測倉庫裡 7 個會 iframe 載入遊戲的瀏覽器工具（`atlas.html`、`docs/tools/` 六支）
**沒有任何一支呼叫 `GV.save()`** ⇒ **今天的實際曝險是零**。

**但這不代表不用守**：T370 那次事故的 10 個 probe 就是 `newWorldSeeded`＋`addMoney` 型，
下一個人寫 probe 時多打一行 `GV.save()` 就會踩到，而**現在沒有任何東西會攔他**。

## 2. 落地

| # | 內容 |
|---|---|
| 1 | `docs/RULES.md` **補第 18 條**：任何會載入遊戲的 probe／工具，動任何「會寫入存檔槽」的 API 之前必須先 `localStorage.setItem('glimmerville.v1.slot','3')`；玩家目錄（8123）一律不得跑測試 |
| 2 | 修 `docs/VERIFY.md` 兩處「（鐵律3）」的錯誤引用，改指新的第 18 條 |
| 3 | 更正 `ARCH §10.5`：如實寫「RULES 原本**沒有**這條、已於 T447 補上」，並記下「無自動存檔」這個把風險縮小的實測 |
| 4 | **機器守**（`tools/test_toolchain.py`）：倉庫追蹤的檔案裡，凡出現 `GV.save(` 或 `.save()` 型存檔呼叫且會載入遊戲的，必須在同檔內先設槽 3；`backups/`／`_ds/`／`attic/`（整份遊戲複本，不是 probe）與 Node 測試（`test_*.js`，跑在 mock localStorage 上）逐條列白名單並註明理由 |

## 3. 驗收

新增一份會 `GV.save()` 而沒設槽 3 的 probe ⇒ 守衛必須紅並點名檔名；
白名單內的檔案不得誤報；`docs/RULES.md` 第 18 條與 `VERIFY.md` 的引用必須對得上（守衛驗）；
**不動 `index.html`**；閘門 ALL GREEN、PASS 只增不減。

## 4. 一句話

**三份文件互相引用一條不存在的鐵律**——每一份都以為規矩寫在別人那裡。
