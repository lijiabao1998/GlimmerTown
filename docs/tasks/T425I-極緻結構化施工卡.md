# T425I 極緻結構化施工卡（boxUnit 六技法落地，按 T425H 方案）

> 業主 2026-08-10 直令：「出卡施工吧 dsk，嚴格遵守施工紀律」（批准 T425H 方案）
> 全鏈授權（出卡／施工／合併），比照 T423-T425G 先例。**範圍：boxUnit 函數＋65_1_0 遷移**。

## 一、施工內容（T425H 方案落地）

### 1. `boxUnit(g,ng,cx,by,hw,h,cL,cR,cTop,opts)` 函數（isoBox 極緻升級，六技法內建）
- **①三面色階**：左面（受光）5 級 `shade(cL,+2/+1/0/-1/-2)`、右面（背光）5 級 `shade(cR,-2/-3/-4/-5/-6)`（按 dx/hw 分段）
- **②稜線體系**：左稜最上 1px `shade(cL,+3)` 亮線（外摺角）、右稜最下 1px `shade(cR,-4)` 暗線（內摺角）
- **③窗洞內凹**（`opts.win={gx,ht,lit}`）：深底（面 -6）＋玻璃 `#2a3550`＋上亮框（+2）＋下暗框（-4）＋決定性夜燈（`dx%7<3`，零 rand）
- **④dithering**：窗洞玻璃與底交界 1px 棋盤格（奇偶決定性）
- **⑤斜邊節奏點**：左斜面每 4 列 1px `shade(cL,+4)` 亮點（抗鋸齒節奏）
- **⑥平鋪紋理**（`opts.tex='brick'`）：二色磚紋
- 保留：底部 AO（.13×3）、前角線（.25）、頂面 dia＋diaEdge 簷口陰影/頂緣受光
- **零 rand**：全部 shade()/決定性座標

### 2. 65_1_0 十四箱體遷移（isoBox → boxUnit，幾何參數不變）
- 主箱/左翼/右翼：帶 `win`（窗洞陣列 gx=10）
- 次箱/翼頂箱/天窗/招牌塔/雨棚/台階：不帶窗
- 移除 65 段 `windows()` 呼叫（boxUnit win 取代——T378 K5 同步）
- isoBox 函數保留（v1-v4 及其他鍵不動）

## 二、守衛（test_fixde.js T425I 區）

- **G1i 函數釘**：`boxUnit` 函數存在（三面色階/稜線/窗洞/節奏點原文釘）
- **G2i 遷移釘**：65 段 boxUnit ≥14（isoBox 不再用於 65）
- **G3i 窗洞釘**：主箱 boxUnit 帶 win 參數＋夜燈（dx%7）
- **G4i 零 rand**：boxUnit 內無 rand/R/ri（原文釘）
- 同步：T425E G1e（isoBox→boxUnit）、G2e/G5e、T378 K5（windows→boxUnit win）

## 三、紅源三案

1. boxUnit 內刪稜線 → G1i 紅
2. 65 一個箱體改回 isoBox → G2i 紅
3. 主箱 win 移除 → G3i 紅

## 四、驗證

1. verify ALL GREEN（PASS 4451→**4460**，+9 守衛）、兩釘 4153/4550、CRLF=0 ✓
2. **紅源三案全紅具名**：M1 刪稜線→T425I G1i；M2 中庭箱改回 isoBox→T425C G3c；M3 主箱 win 移除→T425 G1a
3. 菱形外 ≤509（箱體側面垂直結構基線）、sprFootAudit 全域 0
4. **樣張 158,435 bytes**（較 T425G 33,530 增 4.7 倍——5 級色階/窗洞/稜線/節奏點內容量化實證）`C:/dev/t425i-shots/t425i_mall_boxunit.png`——人眼軌
5. **施工事故入帳**：①11 個箱體遷移時漏 ng 參數（boxUnit 簽名 (g,ng,...) 與 isoBox 不同→參數錯位 shade(undefined) 崩潰）——re 補齊；②守衛同步正則轉義四度踩坑（JS 字面 `\(` vs Python 正則）——最終以字面 replace 解決
6. bump v11.59＋CHANGELOG/ARCH；STOP: **T425I_DONE**
