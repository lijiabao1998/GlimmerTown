# T586 舊城單格地腳與地標精修

**發卡／施工**：Codex；業主 2026-09-23 指定「發佈 T585，續修舊建築地基穿模，素材細節由施工者決定」。
**施工 claim**：bay/codex；base `8ea876f`（master 同步）；埠 8126、槽 3。T585 已另用 canonical `--publish` 發佈、receipt=match，本卡與該發佈分開。
**進場基線**：v11.195；T585 發佈閘門親跑 11,652 PASS／0 FAIL；車位與 master 0/0 clean。新卡施工前重新核對。
**合併角色**：依 `docs/COLLAB.md` 第八節，施工者不自合；本卡交非作者覆核與 canonical 交易式合併。T585 的一次性自合授權不沿用。

## 定位錨與病因

- `index.html` 的 T345 類別徽記 pass 對 `SPR.bld` 做下緣菱形裁切，但整段被 `if(!window.__noBadge)` 包住；T405 預設關徽記，故正常玩家路徑跳過這段裁切。T291／T425 照顧多格底座，不涵蓋這批舊單格素材。T570 S13 容差刀有意保留局部輪廓，也不是精確占地刀。
- 真 Chrome、徽記預設關、8126／槽 3 的原圖像素量測：`67_1_0` 鐘樓 189、`69_1_0` 燈塔 238、`29_1_0` 舊回收站 248、`30_1_0` 舊消防升級設施 199 個不透明像素落在單格下緣界外。圍路實拍見施工證據；這四鍵是本卡第一批，不把它說成全城已清零。
- T574 新建的 k29/k30 真園區另走 `lotSprite574()`；老存檔的一格遺產不會擴大占地。本卡只治舊 sprite 的錯誤地腳，不竄改其存檔或升級規則。

## 允許觸碰

- `index.html`：在 S16 後新增一個零亂數、只處理既有畫布的 S17 狹窄 pass，白名單限 `29_1_0`、`30_1_0`、`67_1_0`、`69_1_0`；日／夜／既有冬側同一地腳輪廓，兩條開機路徑對稱各呼叫一次。只在單格菱形下緣精確裁切與白名單內加必要的貼地細節。提供 `?noT586=1` 回退。
- `test_fixde.js` 僅追加 T586 守衛，不鬆動舊斷言。`docs/tasks/T586-舊城單格地腳與地標精修.md`、本卡截圖／探針、`docs/CHANGELOG.md`、`docs/ARCH.md`、`docs/SPR_PINS.json`；`sw.js` 僅由版本工具同步。

## 禁區

- 不動模擬、建造／占地、電水路、存讀檔、AI、經濟、既有隨機流、玩家目錄、8123 或 s1/s2。不得因畫得順眼而把舊 k29/k30 偷改成多格。
- 不改 `SPR` 鍵、`w/h/ax/ay/sc`，不重排 `buildSprites`，不碰 T345 徽記開關語義或全域大遮罩；所有新段零 `R()`／`ri()`／`rand()`／`Math.random()`／`spriteTexRand()`。
- 禁止只拿「任何 exit=1」宣稱守衛有效；破壞性回歸要首紅精準指向 T586 目標。

## 驗收閉環

1. 先在拋棄式 Chrome 以相同舊城造境拍 before；圖中四鍵旁緊貼道路和鄰建，含日／夜、rot0..3、zoom 1/2、窄視口。確認哪些外伸是地腳、哪些是合法高處輪廓。
2. 正常模式：四鍵日間下緣界外不透明像素歸零；夜圖／冬圖若存在，界外亮點不得殘留。回退模式同場景須恢復原像素。徽記開／關兩態都要真跑，不得再由徽記開關決定占地正確性。
3. 精修以可讀性為準：鐘樓、燈塔保留塔身與夜光；舊回收站、舊消防設施保留識別結構，底部不得變成一刀切的怪異懸浮牆。若某鍵視覺不過關，寧可從白名單撤下並如實記錄，不能用全域裁切硬推。
4. 1,441 鍵的指紋審計僅允許已定稿白名單變更，其他鍵逐位不動；種子與六亂數哨兵恆等，`GV.stats()` 開面板前後不變。`python tools/verify.py` 0 FAIL 且 PASS 不下降；`python -B -m unittest tools.test_toolchain` 全綠。每條新增守衛至少一次合法破壞性突變，記錄首紅和任何假綠。

## 回滾／交付

卡面先提交作 claim，再分小步施工。成品透過 `?noT586=1` 回到本卡前圖像；出問題則回退本卡施工 commit、重新驗基線。版本只在視覺與機械閉環後 bump；CHANGELOG 驗收欄在非作者審前保持「未驗證」。提交乾淨 bay/codex，交非作者覆核合併；本卡不自合、不 publish。
