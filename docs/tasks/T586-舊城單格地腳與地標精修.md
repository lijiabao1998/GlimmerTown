# T586 舊城單格地腳與地標精修

**發卡／施工**：Codex；業主 2026-09-23 指定「發佈 T585，續修舊建築地基穿模，素材細節由施工者決定」。
**施工 claim**：bay/codex；base `8ea876f`（master 同步），認領 commit `1211080`；埠 8126、槽 3。T585 已另用 canonical `--publish` 發佈、receipt=match，本卡與該發佈分開。
**進場基線**：v11.195；T585 發佈閘門親跑 11,652 PASS／0 FAIL；車位與 master 0/0 clean。新卡施工前重新核對。
**合併角色**：原定依 `docs/COLLAB.md` 第八節交非作者覆核；T585 的一次性自合授權沒有沿用。業主於 2026-09-23 另對 **T586 本卡**明示「我授權你合并 gpt」，故本卡由 Codex 以具名例外自合。這不構成非作者覆核，也不授權後續卡自合。

## 定位錨與病因

- `index.html` 的 T345 類別徽記 pass 對 `SPR.bld` 做下緣菱形裁切，但整段被 `if(!window.__noBadge)` 包住；T405 預設關徽記，故正常玩家路徑跳過這段裁切。T291／T425 照顧多格底座，不涵蓋這批舊單格素材。T570 S13 容差刀有意保留局部輪廓，也不是精確占地刀。
- 真 Chrome、徽記預設關、8126／槽 3 的初探指出 `67_1_0` 鐘樓、`69_1_0` 燈塔、`29_1_0` 舊回收站、`30_1_0` 舊消防升級設施均有下緣界外像素。最終可重跑的精確量測與 A/B 圍路實拍見施工實錄；這四鍵是本卡第一批，不把它說成全城已清零。
- T574 新建的 k29/k30 真園區另走 `lotSprite574()`；老存檔的一格遺產不會擴大占地。本卡只治舊 sprite 的錯誤地腳，不竄改其存檔或升級規則。

## 允許觸碰

- `index.html`：在 S16 後新增一個零亂數、只處理既有畫布的 S17 狹窄 pass，白名單限 `29_1_0`、`30_1_0`、`67_1_0`、`69_1_0`；日／夜／既有冬側同一地腳輪廓，兩條開機路徑對稱各呼叫一次。只在單格菱形下緣精確裁切與白名單內加必要的貼地細節。提供 `?noT586=1` 回退。
- `test_fixde.js` 追加 T586 守衛；T563 G3b 的完整生成鏈字面釘只准同步收緊為「S16 後恰接 S17」，不得放寬它的 S9→winterize 前置。`docs/tasks/T586-舊城單格地腳與地標精修.md`、本卡截圖／探針、`docs/CHANGELOG.md`、`docs/ARCH.md`、`docs/SPR_PINS.json`；`sw.js` 僅由版本工具同步。

## 禁區

- 不動模擬、建造／占地、電水路、存讀檔、AI、經濟、既有隨機流、玩家目錄、8123 或 s1/s2。不得因畫得順眼而把舊 k29/k30 偷改成多格。
- 不改 `SPR` 鍵、`w/h/ax/ay/sc`，不重排 `buildSprites`，不碰 T345 徽記開關語義或全域大遮罩；所有新段零 `R()`／`ri()`／`rand()`／`Math.random()`／`spriteTexRand()`。
- 禁止只拿「任何 exit=1」宣稱守衛有效；破壞性回歸要首紅精準指向 T586 目標。

## 驗收閉環

1. 先在拋棄式 Chrome 以相同舊城造境拍 before；圖中四鍵旁緊貼道路和鄰建，含日／夜、rot0..3、zoom 1/2、窄視口。確認哪些外伸是地腳、哪些是合法高處輪廓。
2. 正常模式：四鍵日間下緣界外不透明像素歸零；夜圖／冬圖若存在，界外亮點不得殘留。回退模式同場景須恢復原像素。徽記開／關兩態都要真跑，不得再由徽記開關決定占地正確性。
3. 精修以可讀性為準：鐘樓、燈塔保留塔身與夜光；舊回收站、舊消防設施保留識別結構，底部不得變成一刀切的怪異懸浮牆。若某鍵視覺不過關，寧可從白名單撤下並如實記錄，不能用全域裁切硬推。
4. 1,441 鍵的指紋審計僅允許已定稿白名單變更，其他鍵逐位不動；種子與六亂數哨兵恆等，`GV.stats()` 開面板前後不變。`python tools/verify.py` 0 FAIL 且 PASS 不下降；`python -B -m unittest tools.test_toolchain` 全綠。每條新增守衛至少一次合法破壞性突變，記錄首紅和任何假綠。

## 回滾／交付

卡面先提交作 claim，再分小步施工。成品透過 `?noT586=1` 回到本卡前圖像；出問題則回退本卡施工 commit、重新驗基線。版本只在視覺與機械閉環後 bump。依業主對本卡的新授權，施工者對抗性複查後具名自合；CHANGELOG 必須寫明沒有非作者覆核。交易式合併後才可 `--deploy`，並核對收據、玩家位元與車位狀態；本例外不外溢到下卡。

## 施工實錄（施工者自驗，非獨立覆核）

- T585 發佈先於本卡：canonical master `8ea876f` 執行 `python tools/merge_bay.py --publish`，發佈前 canonical verifier PASS 11,652／0 FAIL，玩家 `index.html`／`sw.js` 逐位與 git blob 一致、receipt=match；未在玩家 8123 上測試，更未開 s1/s2。此後只在 bay/codex 施工，玩家仍是 v11.195。
- 真 Chrome 於 8126／槽3 建立一個**舊存檔語義**的四建築圍路城：`bd.lot574` 缺省，四鍵仍走 `SPR.bld`；新建的 T574 園區路徑未被偷換成測試對象。`T586-browser.js` 以頁面內暫時橋接直寫拋棄式測試城，不寫產品檔或玩家存檔。徽記預設關、版本 11.196：回退模式四鍵地腳下緣界外數逐鍵為 295／199／313／362，合計 **1,169**；正常模式四鍵全為 0，徽記開啟時也全 0。日／夜、rot0..3、zoom1/2、390px 窄視口見 `t586-shots/`，console/pageerror 均 0；上部塔身與燈光仍在。
- 自捕一：第一次 A/B 截圖期間沒暫停城市時鐘，`GV.stats()` 前後變化造成假紅；測試城先 `G.setSpeed(0)` 後重拍，三組（回退／正常／徽記開）各自 stats 完全相同。這是造境錯誤，非產品的繪圖副作用。
- 自捕二：初次全套件第一個紅點是既有 T563 G3b，它逐字釘到 S16→checkpoint；加入 S17 後必須把它**收緊**到 S16→S17→checkpoint，S9→winterize 的前置一字不放寬。已在卡面授權區同步記錄，不拆守衛求綠。
- 自捕三：第一次 CHANGELOG 驗收欄寫了「未驗證」但沒署施工者，T557 的紀律釘紅；補「施工者 Codex」後重新跑完整閘門。這是文檔契約失配，沒有修改 T557 斷言。
- 指紋先以 `--check` 在舊基線下得到預期 exit1：兩次快照 1,441 鍵恆等，新增／移除 0、變更恰四鍵 `bld/29_1_0`、`30_1_0`、`67_1_0`、`69_1_0`；審明後用既有工具更新 `docs/SPR_PINS.json` 至 v11.196。白名單外 0。
- 破壞性探針 `T586-unit.js` 不改磁碟產品檔：control 8 條 T586 斷言綠；`mask-dead` 首紅 G3、`whitelist-wrong` 首紅 G1a、`kill-switch-dead` 首紅 G1b、`night-detached` 首紅 G1a。它是隔離快檢；最終整套件仍須正式親跑。
- 未一刀裁全城：學校、消防局等其他舊鍵也存在較小的像素級下緣外伸，部分是牆面或植栽而非石質底座，不能直接套本卡四鍵裁刀冒稱已驗。應另以實拍與實際 footprint 逐鍵立卡；本卡只聲明四個真圖的修復。
- 最終本車位 `python tools/verify.py` 實跑 **PASS 11,663／0 FAIL／ALL GREEN**，CRLF=0、GAME_VER/APP_VER=11.196；六哨兵 `476／40021／288／4779／117／77` 與 v11.195 完全一致。`python -B -m unittest tools.test_toolchain` 獨立跑 **63 tests／OK／exit 0**；`fp_snapshot.py --check` 在更新基線後 exit 0、1,441 鍵變更 0；T586-browser.js 於最終原始碼重跑 exit 0。這些都是 Codex 自驗，不能當成非作者覆核。
- 業主追加本卡具名合併授權後，Codex 在獨立 Chrome 8126／槽3 重新跑原卡的 A/B 圍路驗收：四鍵回退界外 295／199／313／362，正常／徽記開皆 0；日夜、rot0..3、窄視口再拍，三態 stats 完全不變、console 零錯誤。開機首頁可進新地圖、`GV.stats()` 有效。`T586-unit.js` 四個破壞案再跑首紅均命中本卡守衛。此為同一施工者的對抗性複查，**不是第三方獨立覆核**。
