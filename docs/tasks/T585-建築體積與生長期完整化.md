# T585 建築體積與生長期完整化

**發卡／施工**：Codex（業主 2026-09-23 指定四組並授權其餘設計決定）
**施工 claim**：bay/codex；base `29d292cff15e8102a3f6cbaf82734070787f435a`；驗證埠 8126、槽 3。
**進場基線**：v11.194；`python tools/verify.py` 實跑 PASS 11615／0 FAIL／ALL GREEN；六哨兵 476／40021／288／4779／117／77。
**合併角色**：依 COLLAB §8，Codex 完工後交非作者覆核與交易式合併；本卡沒有自行覆核或自行合併的例外。

## 病灶與定位錨

1. 農場：新園區在 `bakeLot574()` 的 `case 22: case 53:`，舊素材在 `buildSpritesS15()` 的兩組 `farmGrow` 迴圈。後者只蓋 `st=[0,1]`，但 T397 的 `st=2` 是割茬；新園區只有平行 `rows()`，作物階段與田間結構缺少體積。
2. 食品加工：`T254 食品加工廠 57_1_0` 的主廠房以 `80×30` 正面矩形為主，T584 補了表面細件，屋頂、側牆與收貨動線仍須在原生成配方裡補形體。
3. 電廠／水塔：新 3×3 電廠由 `buildLots574()` 預烘，舊廠與五式水塔走 `SPR.plant/plantVar`、`SPR.waterTower/waterTowerVar`。沿設備實際端點補檢修、管線、支腿；新園區與舊城都驗。不得動 T573 已裁決的地腳／冷卻塔邊界。
4. 早期住宅／墓園：住宅 `1_1_0..3` 的三種財富畫布仍只有同構門廊；新墓園在 `bakeLot574()` 的 `case 16: case 54:` 用高 5 的 `box('grave',...)`，縮小易讀成臥床。改以各自建築用途與尺度畫出屋型、立碑、步道和入口。

## 允許觸碰

- `index.html`：以上五處原有美術配方、`bakeLot574()` 的農場／墓園及指定電力配方、S15 後新增的零亂數原地補筆及兩條開機鏈的對稱呼叫。新繪製一律在現有 canvas 和既有佔地內；不改 SPR 鍵、`w/h/ax/ay/sc`、煙霧錨或夜圖結構。
- `test_fixde.js`：只新增 T585 守衛與真跑造境；既有斷言不放寬。
- `docs/tasks/T585-建築體積與生長期完整化.md`、`docs/tasks/t585-shots/`、`docs/CHANGELOG.md`、`docs/ARCH.md`、`docs/SPR_PINS.json`；`sw.js` 僅允許 `tools/bump.py` 同步版本。

## 禁區

- 不碰玩家目錄、8123、s1/s2、模擬、經濟、AI、存檔欄位、建造／升級費、佔地與旋轉、道路／水管／電網連通、T523 預算常數及 `tools/`。
- 新增美術段零 `R()`／`ri()`／`rand()`／`Math.random()`／`spriteTexRand()` 消耗；保留既有抽數呼叫位置與次數。
- 不用通用大矩形蓋住稀有識別色，不以素材變更鍵數或 HTML 位元組大小作美術完成判準。要先看真像素、日夜與遠近景。

## 施工順序與驗收

1. **農場**：舊農場補齊割茬替身；新園區給每塊田分出土埂、作物冠層與灌溉／作業路徑。春、夏、秋的幼苗／拔節／成熟／割茬及冬季休耕都按同一幾何錨呈現。主屋、道路在換相時不位移。
2. **食品廠**：屋頂頂面、前牆、側牆、裝卸月台分面；料倉、輸送帶、廠房、卡車以有端點的作業動線串起來。既有煙囪、招牌及夜光留存。
3. **電廠與水塔**：新廠 v0..2 的檢修與設備管線要落到設施上；五式水塔分別保留圓筒／高腳／球罐／磚塔等原剪影，支架、平台、梯子與地腳相連。舊電廠既有三鍵不退步。
4. **住宅與墓園**：四種早期住宅各有可辨的入口或屋頂結構，貧／富色相與原建築用途一致；新墓園立碑、底座、側暗、墓排方向、入口與步道形成安靜的秩序。

每組完成後檢查：真 Chrome 同場景 before／after、日夜與 rot0/1/2/3、zoom1/2；桌面與窄視口；無遮擋、越界、空白畫面或 console error。`SPR_PINS` 僅授權族變更；新園區不在 SPR 指紋清單，須另以實際 bake 造境截圖及至少一條像素／幾何行為釘驗。`python tools/verify.py` 必須 0 FAIL、PASS 不下降、六哨兵不變；另跑工具鏈測試。每個新守衛以合法破壞性突變證明「首紅＝目標釘」，記錄失敗與判準修正。

## 交付與回滾

四組分小步 commit。收尾用 `tools/bump.py` 升到 v11.195，補 ARCH、CHANGELOG 和指紋，再跑最終閘門；卡面只寫親跑數字。Codex 交回 clean `bay/codex`；非作者從 canonical master 合併、部署並核對 receipt。若其中一組藝術上未達標，退回該組提交後重驗，絕不以未驗證數據報完成。

## 施工實錄（2026-09-23）

- 四組依卡面順序完成，runtime 只在上列美術掛點與 S16 生成末段落筆。由於四組共用同一檔的 S16 啟動鏈及一套 A/B 台架，交付整合 commit，而非原卡預期的四個互不依賴 commit；這是卡面施工粒度偏差，覆核須按四組分別看 diff 與樣張，不把整合 commit 冒充小步交付。
- 真 Chrome 舊／新同一個 72×72 測試城：新農場、舊農場、食品廠、3×3 電廠、墓園、水塔各真放置並完工，日景四方向、夜景、zoom 1／2、390px 窄視口均已實拍，console/pageerror 均為 0。圖在 `docs/tasks/t585-shots/T585-scene-{before,after}-*.png` 與 `T585-atlas-{before,after}.png`。
- 作物以實際 `lotSprite574` 在拋棄式 Chrome 頁逐期烘焙：播種／成長／成熟／割茬／冬季 5 張 sha256 皆不同，五張的 `w=208,h=240,ax=104,ay=232` 完全相同。新舊並列見 `T585-stages-{before,after}.png`；附測試腳本只在 8126／slot3 暫時暴露 closure 內原函式，不改產品檔。
- 像素基線 `tools/fp_snapshot.py` 兩次快照都是 1,441 鍵逐位一致；相對 master 基線新增 0／移除 0／變更 78，族別分佈：舊屋 12、食品廠 1、舊農場割茬 51、新電廠含冬側 6、舊電廠 3、水塔 5；白名單外 0。`--check` 再跑變更 0。新園區農田、墓園是 lazy bake，不以這 1,441 鍵冒稱覆蓋，另由場景與作期樣張承接。
- 自捕一：早期住宅固定 `ay-34` 屋脊線在真圖 v2 浮空，已撤掉，不以「增加筆數」硬充立體。自捕二：首版 G3 用假 256×240 畫布驗越界，會放過 72×112 住宅出界，已改讀每個 sprite 的真 `img.width/height`。自捕三：ARCH 行數雖已更新，文案沒用既有 T371b 釘的「實測檔案 N 行」字樣而紅；補齊後再跑正式閘門。這三項都是真失誤，不能寫成一開始就全綠。
- 自捕四：直接跑 `fp_snapshot.py --check` 已印「變更 0」，卻在 GBK 主控台列印 `✓` 時 `UnicodeEncodeError`、exit 1。未把已量出的 0 變更偷算成綠；以 `PYTHONIOENCODING=utf-8` 原樣重跑後 exit 0、1441 鍵兩次逐位恆等。工具輸出編碼與指紋結論要分開記。
- 紅源實錄（`T585-mutate.js` 在單次 Node 進程內改讀入字串，磁碟 `index.html` 不變）：`towerClip` 首紅 G3 真尺寸出界、`cropDead` 首紅 G4 真園區落筆、`cropSwitch` 首紅 G4b 農田逃生閥、`graveFlat` 首紅 G4c 新碑 .24 格、`farmSkip` 首紅 G2 51 張割茬台帳、`graveSwitch` 首紅 G4d 舊碑 .15 格；六案均 exit 1，沒有以任意紅當作命中。另試 `farmMissing` 真刪一張割茬鍵，首紅先命中既有 T357 `farmGrow 152<153`，故 G2b 對此突變是冗餘釘，不冒稱單獨驗到它。
- 補測：`s16Switch` 把回滾短路改成永假，首紅 T585 G1；`s16Canvas` 在 S16 真配置一張額外 canvas，首紅卻是既有 T583 G8a「禁止新增或放大持久 canvas」。所以 G1b 對這案是跨卡重疊保險，非獨立首紅；不能以此證明新釘單獨有效。
- 再測 `drawState`：令 `forceDraw()` 每次在畫圖後偷偷加 $1，首紅先命中既有 T574 G22d「1920-day 真實 draw 不得改模擬狀態」，不是新 T585 G5；G5 是近場重複量測，不冒稱獨立首紅。G0（宣告存在／位置）是結構釘而非行為 delta，本輪未以合法故障注入獨立證明。出卡文字要求「每個新守衛首紅」過於絕對；此處保留真實覆蓋邊界，交非作者審讀，不用改舊釘讓它靠後報紅。
- 第一輪終驗實得：`python tools/verify.py` PASS 11,652／0 FAIL／ALL GREEN，CRLF=0，版本 11.195 雙處同步，六哨兵 476／40021／288／4779／117／77，代碼地圖 106 錨點新鮮；`python -B -m unittest tools.test_toolchain` exit 0；`fp_snapshot.py --check` 在 UTF-8 下 exit 0。這些是施工者自驗，驗收欄仍標「未驗證」，直到非作者覆核。
- 非作者覆核重點：按四組分別看 `index.html` diff 與本資料夾 A/B 實拍，親跑閘門、指紋 `--check`（UTF-8 主控台）、抽重放 `towerClip`／`graveFlat`／`s16Switch` 等紅源；對整合提交粒度與 G0、G1b、G2b、G5 的重疊／未獨立首紅作明示裁決。通過後才具名改寫 CHANGELOG 驗收欄，由非作者從 canonical master 交易式合併並部署。不得把此卡的自驗偷換成第三方審查。
- 回滾：`?noT585=1` 或 `window.__noArt585=true` 後重載，返回本卡前素材；lazy 園區有快取，因此需要重載後比對。原有 T584 A/B 開關獨立保留。
