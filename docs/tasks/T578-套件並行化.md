# T578 套件並行化：三座長視野見證城移入 worker（治慢性逾時，斷言原文原位、值不變）

**日期**：2026-09-06　**發卡／施工**：Claude（業主 09-05「把問題解決吧Claude，除了施工紀律必須要遵守」）
**base**：`9f1b968`／v11.189　**車位**：`bay/kimi`
**性質**：純測試工程。**不動 index.html、不動 sw.js、不動 tools/、不動任何斷言文字／期望值、不縮短任何模擬視野。**

## 0. 病灶：套件已經跨在閘門的時限上

`tools/verify.py:299` 對 `node test_fixde.js` 設 **900 秒**硬限。實測：
- 空機：**327 秒**（PASS 10,552）。
- 業主另一專案（`前沿競技場/sparse_squares`，四個 python 滿載）同時跑時：**1,116 秒**→`exit code 124`→閘門判 RED。
- T574 施工期間 Codex 踩到兩次；T576/T577 合併前我親跑 verify 也逾時一次——這已是慢性病，且套件每張卡都在長大。

帶時間戳實測（空機 327s），三段**獨立世界**的長模擬吃掉 **56%**：

| 區塊 | 配方 | 秒 |
|---|---|---:|
| T341 巨型合併鏈 | seed22／最多 1200 天（巨廈實際第 **1159** 天才出現＝無早退空間） | 98.5 |
| T574 G18／T542 見證城 | seed3002／900 天 | 54.6 |
| T526 G3 通知節流 | seed3003／困難／600 天 | 29.8 |

三者都以 `newWorldSeeded` 起頭、自足、其後的測試各自重新造世界——**沒有狀態依賴**，是天然的並行單元。

## 1. 手法：worker_threads 並行，主執行緒原位重放

1. 檔頭：非主執行緒時靜音 `console.log/error`（**否則 worker 的 PASS 行會流進 stdout、灌大 PASS 棘輪＝作弊**）。
2. helper 段尾：主執行緒立刻 `t578Spawn` 三個 worker；每個 worker 重跑同一份 bootstrap（lines 1..eval(js)＋helper），
   從 `__filename` 依 `/* @@T578:BEGIN name */…/* @@T578:END name */` 標記**抽出區塊原文**用 `eval` 執行——
   同一份程式碼，不複製；worker 內的 `assert` 是**錄音機**（記 `[ok,msg]`，首紅即停），完成後 `postMessage`＋`Atomics.notify`。
3. 原斷言位置：主執行緒 `t578Replay(name)`——`Atomics.wait` 阻塞到 worker 完成、`receiveMessageOnPort` 取回錄音、
   **逐筆呼叫真的 `assert(ok,msg)`**＝PASS 行文字、順序、數量、首紅即停全部與原本逐字相同。
4. 逃生閥 `T578_INLINE=1`：三個區塊在原位 inline 跑（同一段原文），供等值證明與除錯。

**原型已證等值**：T341 區塊 inline 與 worker 各跑一次，`{towerDay:119, megaDay:1159, mroot:[20,50], mk341:105,
pop:3208, money:215830, day:1161}` **逐項相等**（IDENTICAL=true），各 ~90s。

**原型踩到並記入設計的坑**：遊戲 bootstrap 內有 `setInterval`（250ms rAF 後備、25s 自動存檔），
worker 事件迴圈永不自然結束——主執行緒**不可等 `exit`**，收到 `message` 後主動 `terminate()`。

預估：主執行緒 327−183≈144s；最長 worker（T341）≈106s 與主執行緒重疊；**約 160s**，滿載 3.4× 也在 900s 內。

## 2. 明確不動

index.html／sw.js／SPR_PINS／HEALTH-PINS／六哨兵／15 地板／tools/ 全部零位元差異；三個區塊內**任何一個字元**不改
（只在外面包標記與 `if`）；不縮短視野、不改種子、不改難度；既有斷言零刪改。

## 3. 驗收（檔內 T578 守衛）

- **G1 結構釘**：三對標記各恰一次、成對、且每個區塊首句是 `newWorldSeeded`（自足性＝不准把有狀態依賴的測試搬進去）。
- **G2 靜音釘**：檔頭的 worker 靜音行在場（防 PASS 灌水）。
- **G3 行為釘**：worker 模式下三個 job 都交回錄音且錄音非空；印 `T578 <name> worker Xms waited Yms` 遙測行。
- **G4 等值證明（紅源演習，不入套件）**：`T578_INLINE=1` 全套與 worker 模式全套的 **PASS 行逐行 diff 為空**、PASS 數相同。
- 紅源：①拔掉靜音行→G2 紅；②T341 標記區內 seed 22→23→**在 T341 原位**首紅（證明失敗會回到原位）；
  ③區塊內注入 `throw`→原位「worker 例外」紅；④刪一對標記→「無結果」紅；⑤ Atomics 旗標不 notify→逾時紅（不得無限等待）。
- `verify.py` ALL GREEN、PASS 只增不減（＝原 10,552＋T578 新釘）、toolchain 63 例綠、六哨兵恆等、fp 零變（runtime 位元未動）。

## 4. 回滾

單卡單提交 revert；或 `T578_INLINE=1` 即回到序列執行（同一份原文）。runtime 零差異＝玩家端不受影響。
