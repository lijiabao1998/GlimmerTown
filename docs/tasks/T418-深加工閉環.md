# T418 — 深加工閉環:乘數鏈→物流鏈(DeepSeek 大卡)

業主裁決:鋼鐵廠與經濟層面未閉環,經濟先行(T419 美術特化後續,美術表達依賴本卡語義)。
**本卡把 T364b 的「錢的乘數」改成「物的流動」**——玩家要能看見:油在被燒、鋼在蓋樓、船在下水。

## 現況(出卡方已驗屍,你的定位錨都在這裡)

`/* ===== T364b A 深加工鏈 BEGIN` 區塊(tick 內,T284 goods 段之前):

- **fuel 零消耗=純開關**:`fuel+=fuelMade` 只進不出;`fuelTaxMul=fuel>0?…:1`——
  蓋過一天煉油廠拿到乘數後,煉油廠從此沒有存在感(庫存頂到 `FUEL_STOCK_CAP` 就 `fuelMade=0`)。
- **造船廠的船不存在**:`steelUsed` 只變成 `shipTradeTaxMul` 與 `shipPortGold`,
  沒有船隻實體、沒有下水、沒有可見載體。
- steel 唯一出口=造船廠;`freightTaxMul=fuel>0`(貨運中心 k110/frt342 跟油**毫無互動**,純開關)。
- 現成模板三件:**T284 goods「供貨率→乘數」**(`gGain/gUse284/goodsMul284` 段,決定性 tick-only);
  **T336 貿易站 k91 糧食盈餘出口換金**(里程碑 `src:'trade'` 掛著);
  **T361a lifeShips**(k18 貨輪純視覺、不入存檔、`SPR.lifeShip`)。
- **工期系統(T259)**:`bd.age` 每日 +1,0-8 施工、`age>=9` 完工;
  繪製端**處處整數比較**(`bd.age===1`、`(bd.age|0)>=9`、`bd.age<4`)。

## 兩釘保恆等的機制證明(出卡方已實測,這是本卡的設計地基)

兩條釘軌跡(`newWorldSeeded(301)+setDiff(1)+ai(true)+step×400` 與 seed22 同配方)普查:

| | seed301(pop 4153 ✓) | seed22(pop 4550 ✓) |
|---|---|---|
| 煉油廠/鋼鐵廠/造船廠 121/122/123 | **0/0/0** | **0/0/0** |
| fuel / steel | **0 / 0** | **0 / 0** |
| 貨運中心 k110 | **0** | **0** |
| 油井 49 / 礦場 50 / 港口 18 | 3 / 3 / 2 | 0 / 0 / 0 |

⇒ **新機制全部鎖在「121/122/123 存在或 fuel/steel>0」的惰性閘後,兩釘位元恆等可保,不必重釘。**
連帶的硬性設計約束:①**本卡不動 AI wants**(AI 蓋不蓋煉油廠是另一張卡——動了 AI 就動兩釘);
②新機制在惰性時**零亂數消耗、零流程重排**(純算術恆等,如 ×1/+0 可以,新 R()/ri() 呼叫不行)。

## 三件交付(全做完才交)

1. **fuel 流化**:貨運中心(k110)每日耗油(方向 `FREIGHT_FUEL_USE≈2/座/日`,數值施工方以量測定案入卡),
   供油率→`freightTaxMul`(**照抄 T284 供貨率模式**,滿供 ×上限、無油 ×1);
   盈餘經貿易站(k91)出口換金(**照抄 T336 模式**,有貿易站才出口)。
   無貨運無貿易站時=只積庫存(cap 擋住),行為與現狀一致。
2. **鋼材→建造加速**:施工中建築每日耗鋼(方向 1 鋼/座/日,**每日總量上限與 steelMillN 掛鉤**防瞬吃光)
   → 該建築當日 `age` **額外 +1**(9 天→約 5 天)。玩家肉眼可見的閉環:蓋鋼鐵廠,城裡的樓蓋得快。
   **age 必須保持整數**(繪製端處處整數比較,見現況);鋼不夠時誰先吃=**決定性順序(tickBld 序)**;
   範圍(全部建築 or 僅 3×3+)施工方以觀感與平衡定案入卡。
3. **船變實體**:`steelUsed` 累積 → `shipProgress`;滿 `SHIP_STEEL`(方向 ≈30)→ `shipCount+1`
   (**上限=港口數×2**,無港口不造);船→每日貿易收入(可吸收或並存既有 `shipTradeTaxMul`,施工方提案);
   **視覺=lifeShips 貨輪生成上限跟 `shipCount` 綁定**(tick→draw 單向讀,面板純讀鐵律同源,
   **不新增任何 vri 呼叫點**——T402 G2「恰 20 處」不准動)。
   `shipCount`/`shipProgress` 入存檔:標量欄位、僅非零落盤(T364b fuel/steel 慣例)、
   newWorld/load 成對歸零(鐵律7)、舊檔剝欄容錯(T412 慣例)。

## 卡面替你先關掉的坑

**① 兩釘是紅線不是祈禱**——上表就是你的機制證明模板。施工後第一件事:兩軌跡重跑,
pop 必須逐字 4153/4550。若紅了,先找你哪裡在惰性路徑消耗了亂數或重排了流程,不是來談重釘。
**② age 加速別用小數**:`age+=1.5` 會炸繪製端整數比較(`age===1` 施工動畫分期)。額外 +1 一次,乾淨。
**③ 每日鋼消耗要有總量上限**:不設上限,一座鋼鐵廠日產 `STEEL_MILL_RATE` 存量會被 50 座施工樓一天吃穿,
玩家看不到「有鋼=快」的穩定感受。上限與 steelMillN 掛鉤=多蓋鋼鐵廠有意義。
**④ 船的視覺別碰 vri 帳**:lifeShips 的隨機巡遊是 frame path 既有行為;你只動「生成上限」這一個數
(讀 tick 的 shipCount),別動它的隨機邏輯。T402 G2/G4 全部守衛必須原樣照綠。
**⑤ 量測方法論(T410/T416 血訓)**:終點 pop/money 是混沌噪音,不作結論依據。用直接指標:
fuel 庫存曲線(流化前恆漲頂 cap vs 流化後動態平衡)、工期個案跟蹤(同一棟樓 9 天 vs 5 天)、
船數階梯、出口金流量。A/B 探針樹先驗 provenance(版本+特徵字串)再量。

## 守衛的攻擊面維度(驗收條款,不是建議;T416/T417 兩卡鐵訓)

**參數類守衛不准只用 fixture**——本卡的優勢是 tick 模擬在 headless 是**真跑**,
行為案直接造境真跑真斷言(`__t384Bld` 橋樣式造建築、`GV.step` 推日、`chain346` 讀口驗流量)。
每條行為釘跨下列維度取樣:

| 維度 | 必測取值 |
|---|---|
| fuel/steel 庫存 | 0 / 1 / 中 / **cap**(cap 時 made=0 的既有語義不得破) |
| 貨運/貿易站/港口數 | 0 / 1 / 多(0 時該機制必須惰性) |
| 鋼加速 | 無鋼(9 天)/ 有鋼(加速天數)/ 鋼不足多樓搶(決定性順序)/ **總量上限真的是上限** |
| 船 | 0 港(不造)/ 進度跨日累積 / 上限真的是上限 / 存讀往返恆等(T412 樣式:save→load→shipCount 恆) |
| 惰性 | **兩釘軌跡逐字 4153/4550**(這就是 0 維度的總案) |

破壞性**至少五案**(整倉複製紅源、合法突變、首行 FAIL 逐字入卡),其中至少一案「邊界外一格」型
(例:總量上限釘若寫 ≤N,再測 N+1 的繞過)。

## 紅線

- **兩釘 4153/4550 位元恆等**(不重釘;機制證明見上表);零亂數(新機制禁 R()/ri()/Math.random/vri)。
- 鐵律7(新欄位成對歸零)+存檔僅非零落盤+舊檔容錯;RLE_F 不動(標量欄位,非逐格)。
- 不動 AI wants、不動 vri 帳(T402 G2 恰 20)、不動 T284/T336/T140 既有守衛面。
- 面板/統計零壞值(NaN/undefined/Infinity);`chain346` 讀口擴欄(fuelUse/fuelExport/shipCount/shipProgress)。
- CHANGELOG 驗收欄禁「待」字;**不自合、不 publish**,交回 tip。

## 驗收

1) 套件全綠+兩釘恆等+PASS 只升不降;2) 守衛跟版清單逐條核,含上表維度逐格;
3) 量測入卡(fuel 曲線/工期個案/船數階梯/出口金,A/B 樹 provenance 先驗);
4) 破壞性五案紅源;5) Claude 對抗性覆核工作流(三懷疑者);6) 樣張(船隻視覺)業主定奪後才 merge。

出卡=本 commit;施工 bay/deepseek(8128);v11.43(完工時 bump)。


---

# 【T418 施工閉環】(bay/deepseek 交回 tip)

## 三件交付（全部完成，量測定案入卡）

### ① fuel 流化（貨運耗油＋供油率乘數＋盈餘出口）
- `fuelUse418=frt342>0?min(fuel,frt342*FREIGHT_FUEL_USE):0`（FREIGHT_FUEL_USE=2/座/日）——
  貨運中心（k110）每日從庫存耗油；**frt342=0 整段惰性**（fuelUse=0、fuel-=0 恆等）。
- `freightTaxMul=1+(1.08-1)×(供油率)`——照抄 T284 供貨率模板（滿供 ×1.08、無油/無貨運 ×1）；
  原「fuel>0 純開關」退役（T364b 舊守衛同步新語義：無貨運中心 ×1）。
- 盈餘出口：`fuelExport418=tp336>0?min(fuel−貨運自需, tp336×4):0`＋`fuelExportGold=×$2`——
  照抄 T336 模板（有貿易站才出口）；無貿易站/無盈餘精確 ×0。
- **量測（A/B 直接指標）**：無貨運 fuel 曲線 [45,90,120,120]（恆漲頂 cap=120，流化前語義不破）；
  有貨運 [118,118,98]（動態平衡——油在被燒）。

### ② 鋼材→建造加速（9 天→5 天）
- 施工中建築（age<9）每 tick 耗鋼 1/座 → 當日 age 額外 +1；**純算術零亂數零流程重排**。
- **設計定案（量測定案）**：消耗走**當日鋼產出額度**（cap418=steelMade，不碰 steel 庫存）——
  ①天然防瞬吃光（庫存只被造船廠吃，steelTaxMul「存量>0」語義不退化）；
  ②上限與 steelMillN 掛鉤（多蓋鋼鐵廠=多額度）；
  ③鋼不足時 tickBld 序先到先吃（決定性）。
- **量測**：同一公園個案——無鋼 9 天完工（age 0→9）、有鋼 **5 天**完工（卡面「9 天→約 5 天」精確達成）。
- **坑② 守衛**：age 恆整數（`Number.isInteger` 逐棟斷言）——ageFrac 破壞（+0.5）紅。

### ③ 船變實體（steelUsed→shipProgress→shipCount）
- `if(steelUsed>0&&po>0){shipProgress+=steelUsed;if(shipProgress>=30){shipProgress-=30;shipCount++;}}`；
  上限 `shipCount≤po×2`（無港不造、上限真的是上限）；每日貿易收入 shipDailyGold=shipCount×$6（與 shipTradeTaxMul 並存）。
- 視覺：lifeShips cargo 生成上限 `min(2,港口數,shipCount)`——tick→draw 單向讀、**零新增 vri 呼叫點**（T402 G2 恰 20 不動）。
- 存檔：shipCount/shipProgress 僅非零落盤（T364b fuel364 慣例）、newWorld/load 成對歸零（鐵律7）、舊檔剝欄容錯（T412 慣例）。
- **量測**：1 港船階梯 30 天=1 艘、60 天=2 艘、之後封頂 2（階梯＋上限雙驗）。

## 兩釘恆等（機制證明兌現）

seed301 pop **4153**、seed22 pop **4550** 逐字不變；全部新機制鎖在惰性閘後
（frt342>0 / steelMillN>0&&steelMade>0 / steelUsed>0&&po>0），兩釘軌跡
（121/122/123=0、fuel/steel=0、k110=0）整段不執行——零亂數消耗、零流程重排。

## 守衛攻擊面維度表（tick 真跑造境，逐格驗證）

| 維度 | 取值 | 斷言 |
|---|---|---|
| fuel 庫存 | 0/1/中/**cap** | cap=120 後 fuelMade=0 語義不破（60 天頂 cap 案）；無貨運惰性 |
| 貨運/貿易站數 | 0/1/多 | 0 時 fuelUse=0、乘數 ×1、零出口（惰性）；1 座耗油 ≤2、乘數 (1,1.08]、出口 ≤4 |
| 鋼加速 | 無鋼 9 天/有鋼加速/多樓搶序/**上限真的是上限** | 無鋼 age+1/日；tickBld 序先到先吃；3 天總加速 ≤ Σ當日 steelMade（含鋼廠自身） |
| 船 | 0 港不造/進度跨日/階梯/上限/**存讀往返** | shipProgress 累積、30 鋼=1 船、1 港封頂 2、save→load shipCount 逐字一致 |
| 惰性 | **兩釘軌跡逐字** | 4153/4550 恆等（0 維度總案） |

## 破壞性六案（整倉複製紅源，首行 FAIL 逐字）

| 案 | 合法突變 | 紅點 |
|---|---|---|
| ① fuel 耗油刪除 | `fuelUse418=frt342>0?...:0`→恆 0 | 惰性閘釘紅 |
| ② 乘數回純開關 | `freightTaxMul=fuel>0?1.08:1` | T364b 供油率語義釘紅 |
| ③ **邊界外一格** | `cap418=steelMade+1`（上限多給 1） | 上限案：3 天總加速 9 > 額度 6 紅 |
| ④ 船上限刪除 | `if(shipCount>po*2)` 刪 | 1 港實得 4 船紅 |
| ⑤ 0 港也造 | `steelUsed>0&&po>0`→`steelUsed>0` | 無港不造釘紅 |
| ⑥ age 小數加速 | 加速 `b.age++`→`+=0.5` | 恆整數斷言紅（坑②） |

## 交付

- 套件 **4207 / 0 FAIL / exit 0**（4157→4207 只升不降）；兩釘 4153/4550 恆等；CRLF=0；bump **v11.43**。
- chain346 擴欄：fuelUse/fuelExport/shipCount/shipProgress/shipDaily；flowStat 擴欄 count/progress/use/export 零壞值。
- 不動 AI wants、不動 vri 帳（T402 G2 恰 20）、不動 T284/T336/T140 既有守衛面。
- 樣張前置：船隻視覺（k18 貨輪隨 shipCount 出現）交回 Claude 代拍＋對抗性覆核；業主定奪後才 merge。
- 不自合、不 publish，交回 tip。


---

# 【T418 覆核退修閉環】(bay/deepseek 對 8fd15f1 覆核意見，逐條修復)

覆核獨立驗證確認：verify 4207 PASS、工具鏈 54/54、船隻視覺正常（1 艘 $6／1 港 2 艘 $12）。
五個阻塞點全數修復＋守衛補強，PASS 4157→**4223**。

| 覆核阻塞點 | 修復 | 守衛（破壞必紅） |
|---|---|---|
| ① load() 未歸零 T418 每日快照（有船城市載入空城未 tick 時 shipDaily=12 殘留） | load 行補 `fuelUse418=0;fuelExport418=0;`（shipDailyGold418 已有） | 行為案：有船存檔→newWorld 空城→load→未 tick 斷言 shipDaily/fuelUse/fuelExport 全 0；破壞（刪歸零）紅 |
| ② 多船廠不完整兌換（同日 70 鋼應 2 艘＋進度 10，實得 1 艘＋40） | `shipCount+=floor(shipProgress/SHIP_STEEL);shipProgress%=SHIP_STEEL;`（progress 恆 <30） | 行為案：注入 shipProgress=58＋steel364=100＋2 船廠→step→2 艘＋進度 0；破壞（還原單次兌換）紅（+1 艘＋30） |
| ③ fuel cap 測試假綠（漏蓋油井 fuel 恆 0 只驗 ≤120） | cap 案真蓋油井，斷言改 **fuel===120** 精確值 | 原案即紅點（漏蓋油井→fuel=0≠120） |
| ④ 刪掉鋼加速 b.age++ 全套仍 exit 0 | 正向 delta 斷言：**有鋼 3 天增量 > 主循環 3** | 破壞（刪 b.age++）紅（增量 3=主循環，3>3 假） |
| ⑤ 零亂數守衛 \b 是 U+0008 退格＋切片涵蓋既有亂數 | \b 改 JS 字面量（U+0008 清零）；切片縮到**四段掛點**（深加工鏈/fuel 流化/船兌換、鋼加速、lifeShips want 行、fuel 出口） | 破壞（鋼路徑注入 R()）紅（惰性閘釘＋零亂數段2 雙紅；R() 在惰性閘後、兩釘仍綠） |
| ⑥ 存讀只斷言 shipCount | 存讀雙斷言 shipCount＋shipProgress；**補舊檔剝欄案**（手動剝兩欄→load→0，T412 慣例） | 已含在案 |

兩釘 seed301=4153／seed22=4550 全輪恆等（含破壞性案各自重跑，pins 2/2——除 loadReset 語法破壞案 exit 1 屬紅源成立）。
套件 **4223 / 0 FAIL / exit 0**；verify ALL GREEN；CRLF=0；v11.43 不變。
樣張（船隻視覺）覆核已通過；不自合、不 publish，交回 tip。
