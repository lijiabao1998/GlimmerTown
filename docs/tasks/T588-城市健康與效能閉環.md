# T588 城市健康與效能閉環

**發卡／施工**：業主 2026-09-23 指示「①②③全做完；數據可以被打破、修改，但是紀律不行」，第三項由 Codex 負責。卡號接已入主的 T587。
**claim**：bay/codex，base `4bc012c`／v11.197；先從 canonical master 驗證 0/0 同步並作唯讀現版量測，再動產品碼。驗證僅本車位與拋棄式 Node／Chrome；不以 8123、玩家 s1/s2 測試。
**合併權**：T586／T587 的例外沒有自動延續；業主於 2026-09-23 對本卡另行明示「明示授權 Codex 自合 T588」。因此本卡可由 Codex 在完整自審、凍結 OID 閘門全綠後從 canonical master 交易式合併並發佈；**沒有非作者獨立覆核，不能冒稱第三方簽核**。

## 定位錨與問題

- `docs/BACKLOG.md` P1-1/P1-2/P1-3 仍記 T574 大地塊後的 AI 成長崩塌、成熟城 CPU 增幅與園區快取壓線，但那些是舊時點、不同城形或不同負載的紀錄，不能直接當 v11.197 現況。
- `tools/health_panel.js` 提供官方 72 圖／標準難度／12 種子／400+900 天唯讀配方與五個健康欄。先完整實跑，逐城保留人口、資金、道路、分區、通電、root 與設施型別；絕不只看 PASS。
- `aiStep()` 的供電接路、道路與分區擴張，以及 `lotSprite574()`／`lotFarFrame574()`／`lotObjectOrder574()` 是待量測候選，**不是預設罪魁**。先用同一凍結城市拆分模擬 tick、AI 規劃與繪製成本，避免把「城形不同」寫成「程式變慢」。

## 允許觸碰與施工順序

1. **T588a 診斷**：官方 12 城全跑、三個卡住種子逐日取樣與設施／合法落點剖面；同城 save/load 或固定重新造境多次量效能。每個 probe 確認入口、輸出完整性及固定種子；探針、原始數據與否決實驗納入本卡證據。
2. **T588b 最小修復**：若根因可局部定位，只改 `index.html` 的實際 AI／園區熱點與對應 `test_fixde.js`；產品快路徑必須保留功能與同城畫面／占地契約。禁止單純把補助／收入／人口旋鈕加大當作根因修復。必要的測試橋須限定此卡並留回退開關。
3. 完成後更新六哨兵、健康地板及其 history **僅限於實測且有明確 delta 理由的欄位**；業主允許數據更新，不等於准許靜默降低健康度。bump 雙版本、同步 ARCH／SPR_PINS／CHANGELOG、附首紅故障注入與真瀏覽器驗收，再交合併。

允許檔案：`index.html`、`test_fixde.js`、`docs/HEALTH-PINS.json`、`docs/SPR_PINS.json`、`sw.js`（僅 bump）、`docs/ARCH.md`、`docs/CHANGELOG.md`、`docs/BACKLOG.md`（只同步 P1 事實狀態）、本卡與本卡證據；現有工具若需改函式簽章或全域閘門，停手另報。先確定真掛點後縮小白名單，不把整個 AI 與 draw 一口氣重寫。

## 禁區與停手線

- 不動玩家存檔、其他車位、`data.v`／存檔 schema、建築真實占地、水電路真實連通契約、外部環境；不加新 `R()`／`ri()`／`rand()`／`Math.random()` 呼叫或挪動既有亂數流。不得刪既有精確斷言、降低閘門、修改測試造境求綠。
- AI 任何改動都可能改種子數據；先列 **before→after 全 12 城**、六哨兵與財政，再同步釘，不能以單城改善掩蓋其他城崩塌。若 400 天低於 500 人的城數或 900 天惡化、健康地板下降，先退回候選或報業主裁決。
- 若效能問題只在不同城市之間比較成立，而同城沒有可證的熱點，不做性能劇場；如改善須拆存檔格式、改整套渲染架構或犧牲視覺，停在可覆核證據，不假報「已解」。
- 不把 `index.html` 2.5 MiB 當停止線；業主已明示沒有此上限。真正停手條件是瀏覽器／Canvas／存檔／記憶體／效能／閘門的實測硬失敗或超出本卡授權。

## 驗收

- 官方現版 12×900 panel 完整 exit0，before/after 同配方，400 天低人口城數目標由現版值降至 **≤4/12**（T574 之前的結構基線，不冒稱恢復當時每城絕對人口），900 天低人口城數不得升；其餘逐城大幅退步必須揭露並裁決。若經實驗證明目標無法在守住紀律下達成，不能將本卡假標完成。
- 固定城市同一快照至少三次各 100 步、記中位數與環境負載；單論效能修補需同城改善且無功能／畫面回歸。園區 LRU 用真 Chrome 視角、日夜、旋轉、縮放記峰值及冷／暖幀烘焙次數，不把 Node 空像素當 GPU 證據。
- `node test_fixde.js` 全綠、PASS 只增不減；`python tools/verify.py` ALL GREEN；`python -B -m unittest tools.test_toolchain` 全綠；素材快照只有允許鍵變、非素材修補應零鍵；六哨兵任何重釘都附原值→新值與對應原因。
- 每條新守衛都做合法破壞性注入，**首紅必須是目標釘**；同城重放、真瀏覽器 8126／槽3、console 零錯、`git diff --check` 0。發佈後再驗 receipt=match 及玩家檔案逐位一致。

## 回滾

所有實驗先放車位／記憶體，正式 runtime 僅留通過健康與效能閘門的最小修法；否決版本記原因，不以 `git reset --hard` 擦掉可恢復證據。未完成、外部 writer 或守衛紅而不能修時停止合併部署並報實際狀態。合併前由 canonical 交易式閘門再驗 B/S/M，嚴禁施工者假借前兩張卡的授權自合。

## 施工取證與否決案（2026-09-23）

現版 v11.197 由 canonical master 真跑 `tools/health_panel.js` 12 種子各 900 天，400 天低人口 7/12、900 天 4/12。`T588-diagnose.js` 用相同 72 格／標準難度／AI 配方量卡住的城；它只在記憶體搭測試橋，不寫玩家存檔。seed22 是「有錢、低人口、六格道路網格所有臨路候選已被占用」；seed301／5150 是「真 3×3 電源仍有合法地塊，但最近 X→Y 短接路被地形或建築切斷、補助只夠廠不夠整份真施工單」。問題是合法拓撲選路與施工費，不是人口生成式或收入倍率不足。

| 記錄的試法 | 實測／否決理由 |
|---|---|
| 任何低人口城每 10 天廣域 BFS 接橋 | seed22 400 天 476→1085，但 seed301 資金 4779→−607、seed5150 1246→−916；把健康城的資金也燒在路上，撤回。 |
| 12 格電源替代接路＋一律多給 $180 | 400 天低人口降到 4/12，但 $180 是拍腦袋調補助，不是根因證據；撤回。 |
| 無條件長接路＋按真施工單補費 | 整體曾達 400 天 4/12、900 天 3/12，但 seed123 900 天 4164→1741，健康城受混沌路徑影響大退；撤回，改為持續兩輪供電鎖且規劃半徑已過半圖才擴路。 |

最後採用兩個窄口：`powerAltLink588` 僅在上述深鎖狀態找另一條通電路與兩軸折線，最多 12 格、保留建廠 action，所有格過真 `canPlace`，最後仍由 `tryP` 逐格扣費落地；`powerAidCost588` 只在沒有真臨路地塊時按**被選中**的地塊＋接路逐格價目＋既有保留金算夠本次施工的上限（小城原 $50 時序不變）。`roadGridBridge588` 僅用在有核電資金、人口<500、無供電鎖、規劃已全圖且原六格網格完全無合法臨路點的城；最長六格、十天一次、先算完整價，再從既有路往外真鋪。不改 AI 早期一般決策、價格表、存檔格式、隨機流或玩家城市。

## 官方 12 城量測

來源：base=`4bc012c`／v11.197 與本卡候選 v11.198 的 `tools/health_panel.js`，兩臂完整 exit 0，72 格、難度 1、slot 3、各 900 次 tick，同時在 400 日取點。下表是人口，不是單一 PASS 數；未列資金的城其 400 日人口在兩臂精確相同。

| seed | 400 日 base→T588 | 900 日 base→T588 |
|---|---:|---:|
| 7 | 117→117 | 576→576 |
| 9 | 1864→1864 | 1916→1916 |
| 15 | 43→43 | 327→327 |
| 22 | 476→639 | 476→3632 |
| 123 | 1196→1196 | 4164→4164 |
| 301 | 288→780 | 771→901 |
| 528 | 264→264 | 182→182 |
| 777 | 2639→2639 | 2657→2657 |
| 3001 | 72→72 | 415→415 |
| 3002 | 1077→1077 | 2857→2857 |
| 3003 | 838→838 | 1315→1315 |
| 5150 | 355→740 | 557→1295 |

400 日 `pop<500` **7/12→4/12**，900 日 **4/12→3/12**，十五健康地板無下降。seed22 的 400 日資金 40021→14523、seed301 4779→2297、seed5150 1246→711：為真道路／電源施工與其後城市軌跡分岔，**不是財政改善**；三城仍為正值，沒有把路費藏掉。seed301 的併網容量 90→180、占用建築格 550→613、道路 175→199、分區 209→221、通電占用格 192→296；seed22 對應人口／建築／道路／分區／通電五地板全部只升。精確六哨兵只重釘 seed301 的 288/4779→780/2297 和 seed22 的 476/40021→639/14523；seed7 的 117/77 一字未動。T432 G7 世界形狀精確釘併網容量 90→180，孤島0／單 district 與真正的孤島不併網行為釘未放寬。

**沒有宣稱全面治癒**：seed7、15、528、3001 在 400 日仍不到 500；seed7 到 900 日才回到 576，而其餘三城 900 日仍低於 500。T574 以前 seed301 400 日曾有 6801 人，本卡只恢復到 780；P1-1 的「回到 4/12」階段目標達成，不等於每城絕對量級回復。

## 效能與畫面邊界

`T588-perf.js` 從 canonical master 造 seed22 第 300 日存檔，SHA-256 `e95f2d406e7f64e06bed94045a08849e3913ffc61380e8aa65a08ec1eb8f845a`，原始 26,849 B；兩臂各從**相同存檔位元**起跑三次，各跑 100 天。探針先自捕一個假比較：`load()` 不重置全部衍生人口欄位，初版在單一遊戲實例反覆 load 使第一輪起點人口 0、後兩輪沿用上一輪 476；**初版 1,897→1,558 ms 數字作廢**。正式版每輪重新建立拋棄式遊戲實例、再載入同一份存檔，三輪起點均為 `[day301,pop0,money28642]`，各臂端點逐輪恆等。base 的 total 中位 **1,681.72 ms**、AI **1,418.70 ms**、非 AI **263.01 ms**；T588 為 total **1,595.20 ms**、AI **1,260.96 ms**、非 AI **330.19 ms**。total 差 −5.1%、AI −11.1%，但非 AI +25.5%；兩臂之後施工路徑合法分岔，端點資金 39,981 vs 38,992、人口同為 476，單機三次有時序噪音，**不能宣稱因果性加速或全面無退化**。這只排除本次樣本的總步長顯著變慢，沒有證明 T574 舊成熟城 +81% 已消失。沒有找到同城可歸因的新渲染熱點，所以本卡不碰 `lotSprite574`／畫質；P1-2 留作獨立、同一城市畫面負載的後續實驗。

真 Chrome 在 127.0.0.1:8126、獨立槽 3 直接 step 正式碼與真 canvas：seed22/301 第 400 天分別 639/780 人，另 seed22 第 900 天 3,632 人；畫面重繪前後 `GV.stats()` 逐位相同、console error 0。日／夜、rot=1、zoom=1、窄視口實拍在 `t588-shots/`；T423 HUD tween 需等到顯示追上真值再截圖，首版截到前一城的負數 HUD 已否決重拍。400 日園區 LRU 冷畫 9/6 次、暖畫追加 0 次，日夜追加 0 次；旋轉加到 13/8、縮放加到 16/12，峰值 1,566,720／1,288,192 像素。900 日 seed22 冷畫13、暖／夜追加0、旋轉後17、縮放後33，峰值 3,796,480 像素；三城都低於 12,000,000 像素／128 項硬帽。T523 真 canvas 落筆 7,500／6,331／8,577，攔截後方法均恢復。900 日成熟城冷／暖幀單次 102.4／15.3 ms，**單次時序不是穩健效能基準**，只作冷烘焙有成本的示意。這些城**不能證明**舊 T574 某個 11,999,744 像素場景已消失；P1-3 保留重場景觀測，不擅自拉高快取帽。

八個拋棄式紅源 `T588-red.js`：替代接路、真施工費、援助實際使用、網格橋規劃、橋實際使用、深鎖門、富城門、注入 R()，全為 `exit=1` 且**首紅依次精準命中 G1/G2/G7(5150)/G5/G7(22)/G6b/G6c/G6a**；不是只看到 exit1。素材兩次真 Chrome 快照 1,441 鍵逐鍵恆等，對 v11.197 新增0／移除0／變更0。完整套件與發佈數據記本卡收束段及 CHANGELOG。

## 收束與交接

- 最終 v11.198 工作樹把官方 `node tools/health_panel.js` **12 城各 900 日重跑至 `HEALTH_DONE`／exit0**：`HEALTH_SUMMARY` 為 400 日 `4/12`、900 日 `3/12`、`belowFloor:[]`，表內 seed22／301／5150 的人口、金流與健康欄再現。這是最終版本的獨立重跑，不是沿用早期候選輸出。該次 seed3002 900 日行的牆鐘 `ms=420232` 明顯異於其餘樣本，雖然其人口與城市欄位均與前次相同，**不能用此單行牆鐘值宣稱效能好壞**；效能判斷以上文同存檔三輪探針及其明示限制為準。
- 版本 `11.198`：`GAME_VER`／`APP_VER` 同步；ARCH 地圖 108 錨點、零漂移；`fp_snapshot.py --check` 在 UTF-8 主控台通過，兩次 Chrome 快照 1,441 鍵零漂移；`git diff --check` 通過。
- `python -X utf8 tools/verify.py` 親跑 `PASS=11695`／`FAIL=0`／`ALL GREEN`（CHANGELOG 入帳使 T557 三條文件守衛增加）；`python -X utf8 -B -m unittest tools.test_toolchain` 親跑 63 例 `OK`。破壞性八案均首紅目標釘；focused 行為案與三座城市真 Chrome 檢查通過。正式合併前須對**提交後的凍結 OID**再跑閘門，不能把工作樹的綠當合併結果。
- 業主特許本卡作者自合；本卡證據只有施工者自驗，沒有非作者覆核。合併須由 canonical master 的 `merge_bay.py codex --deploy` 執行，B/S/M 與玩家 receipt 依工具實測回報，不預填為通過。若合併或部署紅，停在可恢復狀態，不改守衛求綠。
- P1-1 僅閉合「400 日低人口城數 ≤4/12」階段目標，四個低人口城仍在；P1-2 同城因果性效能修補未證實、P1-3 舊 11,999,744px 壓線重場景未重演，兩項留在 BACKLOG，不寫已解。
