# T457 經濟連結度（城市科學十卡 #7）

**日期**：2026-08-14　**發卡**：Claude（業主 goal：前沿經濟學/社會學研究進玩法）
**藍圖**：T451 卡面 §0。政策卡（預設關）——T456 重釘後的哨兵基線**不動**。

## 0. 實證依據（已聯網核實）

Chetty, Jackson, Kuchler, Stroebel 等（2022, *Nature* Social Capital I & II）——
210 億筆臉書好友關係、逐 ZIP/高中/大學的社會資本地圖：

- **經濟連結度**（低 SES 者朋友中高 SES 占比）是**已知最強**的向上流動預測子；
  EC +10% ↔ 向上流動 +2.4%。
- 拆解成兩個管道：**暴露**（能不能遇到）與**交友偏誤**（遇到了交不交），兩者都獨立預測
  郡層級的因果流動效應。
- 混合場所（不同階層共用的空間）是暴露的基礎設施。

## 1. 遊戲落地（政策「混合社區計畫」`pol.ecMix457`，預設 false）

- `ecIdx457(i)`＝住宅格經濟連結度 0-1：半徑 `mixR` 內**異財富級**住宅占比（暴露）×(1−venueW)
  ＋混合場所覆蓋（公園/學校/圖書館，各算一，除 3）×`venueW`。常數只在 `SCI451.fx`。
- 開啟後：T456 世代流動的暴露修正多一條**第二管道**——環境/機會評分沒把目標抬上去的住宅，
  若 `ecIdx457 ≥ ecThr` 且目標未達富級，目標 +1 級（`ecUp457` 計數）。
  ＝把「跨階層互動本身推動流動」寫進機制，正是這篇的貢獻（連結度獨立於鄰里品質預測流動）。
- 關閉時三元式短路 ⇒ `ecIdx457` 連算都不算、**六根哨兵位元恆等**；開啟分岔合法（indSubsidy 先例）。
- fx：`{mixR:3,venueW:.3,ecThr:.5}`。對帳：`GV.sci451` 擴 `ecUp`/`ecOn`。

## 2. 已知代價（出卡先聲明）

接線點就在 T456 的修正塊裡 ⇒ **T456 G1c/G1d 的 needle 必須跟版**（合法演進跟版＋理由，
T454 撞 T386a 同款）；T456 G4 三組介入計數跑在預設關世界 ⇒ 不動。

## 3. 驗收計畫

G1 表項＋`venueW∈(0,1)`/`ecThr∈(0,1)` 機械複算；G1b `ecIdx457` 與第二管道讀表；G1c 前五卡
原文回歸（T456 needle 跟版後的新原文）；G3 預設 false＋白名單；G4 行為：政策開的種子世界
`ecUp457>0`（決定性⇒釘精確值）、關閉世界恆 0。紅源三案＋六哨兵全中。真瀏覽器：科學頁七筆齊、
checkbox 開關、ecUp 真計數。

---

## 4. 施工紀錄（Claude 全鏈；起點 master `466a4e1` → v11.89，PASS 5,031）

### 4.1 落地（十個接點，指南零改動）

`SCI451` 新增 `ecMix457`、`ecIdx457(i)`（異級鄰居占比×(1−venueW)＋公園/學校/圖書館覆蓋×venueW，
常數全讀表）、T456 修正塊後追加第二管道 `tgt1`（**政策短路在最前**：關閉時 `ecIdx457` 連算都不算）、
`ecUp457` 計數（鐵律7 成對歸零）、pol 初始/面板 checkbox `polEcMix457`/`polToggle`/`GV.pol` 白名單、
`GV.sci451` 擴 `ecUp`/`ecOn`。第七筆自動長出。

### 4.2 T456 兩針跟版（出卡時已聲明的代價）

第二管道把計數與落筆行從 `tgt` 改讀 `tgt1` ⇒ T456 G1c needle 之二與 G1d 範圍尾標更新
（合法演進跟版＋理由；`tgt` 原式原文不動——T453 發明的「原文只能追加不能改寫」在此以
「追加後跟版」的形式成立）。

### 4.3 行為驗證

G4 同種子對照（seed 4571、150 天 AI）：**開 ecUp=69、jobs=1,342 vs 關 ecUp=0、jobs=1,027**
——混合社區計畫讓同一座城多長 30% 就業，第二管道的效果不是裝飾。六哨兵全中＝預設關位元恆等。
真瀏覽器（UI 路徑）：七筆齊、checkbox 勾選→科學頁 ✅ 已啟用、120 天 ecUp=37、殘留 0。

### 4.4 紅源（三案，整樹複本）

| 紅源 | 結果 |
|---|---|
| ① `pol` 預設改 true | **G3 紅** |
| ② ecThr 硬編碼 .5 | **G1c 紅** |
| ③ `f457` 權重表繞過（整組硬編碼） | **G1c 紅** |

### 4.5 驗收

六哨兵位元恆等；`verify.py` ALL GREEN、PASS 5,023 → **5,031**；引用聯網核實。
