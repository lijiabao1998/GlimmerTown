# T555 沙盒難度半殘修復（玩測 P2-3）

**日期**：2026-08-22　**發卡**：業主直令「出卡並嚴格遵守施工紀律」（玩測報告 bay-kimi/playtest-0822/findings.md）
**施工**：Kimi　**風險**：低（難度表＋開局參數映射）

## 0. 實證（玩測實測，三方證據互相印證）

1. 開始選單選中 🧪沙盒 後，「起始資金…」說明行**整行消失**（簡單/標準/困難分別正常顯示 $5000×0.5／$3000×1／$1500×1.5）。
2. 沙盒開局實發 **$3000**＝標準額（`GV.stats().money`）。
3. 沙盒狀態下 `GV.diffDesc547()` 回「起始資金 $5000・災害機率 ×0.5」＝簡單難度的描述。

⇒ 沙盒分支缺表項/未接線：UI 說明行無內容、開局參數回落標準、描述查詢錯位。

## 1. 修復方向（定位錨）

- 找到難度定義表與 `diffDesc547` 的資料源（grep `diffDesc547`／起始資金）。
- 補齊沙盒表項，使三者一致：**說明行有字、開局實發＝說明金額、diffDesc547 回沙盒自己的描述**。
- 數值取現有描述鏈已承諾的「$5000・災害 ×0.5」（不改平衡設計，只接線）。
- 若調查發現沙盒另有設計語意（如免破產），以代碼既有事實為准並在 CHANGELOG 如實記。

## 2. 允許觸碰／禁區

- 允許：index.html 難度表與其渲染行、`#bDiff*` 相關 handler、sw.js（僅 bump）、test_fixde.js 新增守衛、CHANGELOG、ARCH.md 版本行（若閘門要求）。
- 禁區：亂數流（鐵律2）、tick 經濟迴圈、存檔格式、其他難度數值、backups/attic。

## 3. 驗收

- G1：四難度各選一次，說明行皆非空且含「起始資金」。
- G2：沙盒開局 `GV.stats().money` === 5000（與說明行一致）。
- G3：`diffDesc547()` 於沙盒回沙盒自身描述（含 $5000 或與 G1 說明行一致的字串）。
- 紅源二案：拆掉沙盒表項 ⇒ G1 紅；把開局金額改回 3000 ⇒ G2 紅。
- 全套件 PASS 不低於現行基準、0 FAIL；真瀏覽器 8125 槽3 截圖四難度說明行。

## 4. 回滾

單 commit 於 bay/kimi，`git revert` 即可；不動種子釘（本卡零 tick 觸碰，SEEDPIN 應全數恆等）。
