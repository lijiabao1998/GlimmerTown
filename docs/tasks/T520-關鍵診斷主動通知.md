# T520 關鍵診斷主動通知（打破沉默的最後一哩）

**日期**：2026-08-18　**發卡**：Claude（業主授權：自己玩→出卡→施工→合併）

## 0. 問題：診斷存在，但玩家看不到

T519 讓顧問會說「⚡ 電源未併入路網」了。但玩測量到兩件事讓這句話到不了玩家：

1. **`cityAdvisor()` 只在 📊 面板開啟時執行**——不開面板的玩家永遠不會被診斷。
2. 面板本身 **3,798 px（≈4.7 屏）**，顧問區排在 `happyHtml` 之後、約 3,300 px 處
   （面板組裝順序：城市名／財政／運輸／地鐵／科學今日帳／每日支出／建築統計／城市指標／
   季節市場／幸福構成 → **顧問** → 等級／評分）。

一個新手蓋了孤島電廠、城市 90 天長不出一棟房子，他要先想到開 📊、再捲三千多像素，
才會看到那句救命的話。

## 1. 先掃現況：通知系統早就有

T114 通知中心：HUD 🔔 鈴鐺＋未讀紅點＋事件日誌（`log[]`），**且每條可點擊跳鏡頭**
（`camLookWorld` 換算已寫好）。既有 tick 側警告先例：垃圾超載 toast，用 `lastGarbT` 做 30 天節流。

⇒ 本卡＝**把關鍵診斷接進既有通知管道**，不新建 UI（T456/T517/T519 同型第四次）。

## 2. 落地

tick 內每 10 天一次（`day%10===0`）走**既有 `tickBld` 索引**（不新增全圖掃描）找電源建築，
呼叫**既有 `powerDiag432`**；若為 `island` 且距上次警告 ≥60 天 ⇒ `toast(...,'bad')`
（toast 既有實作會寫入 `log[]` ⇒ 鈴鐺紅點自動亮、可點擊跳鏡頭）。

- 節流變數 `islandWarnT520`，`newWorld`/`load` 成對歸零（鐵律7）。
- **零亂數**：不呼叫 `R()`／`ri()`；純觀測＋toast。
- 六哨兵必須位元恆等——toast/log 不進模擬狀態；**若哨兵破了就代表我碰到了不該碰的東西**。

## 3. 驗收計畫

- G1 檢查塊必須走 `tickBld` 與既有 `powerDiag432`（不自建掃描/判定）＋零亂數機械掃。
- G2 節流變數鐵律7 成對歸零。
- **G3 行為（本卡靈魂）**：孤島造境跑 30 天 ⇒ `GV.logSnapshot()` 內必須出現電源警告且帶座標；
  電廠挨路造境跑 30 天 ⇒ **零電源警告**（不誤報）。
- G4 六哨兵位元恆等。
- 紅源三案：拆掉節流（洗版）／改自建判定／挨路城誤報。

---

## 4. 施工紀錄（Claude 全鏈；起點 master `34cbb65` → v11.149，PASS 5,747）

### 4.1 落地（四個接點，零新 UI）

tick 內 `day%10===0` 且距上次 ≥60 天時，走**既有 `tickBld`**（零額外全圖掃描）找電源建築
（型別走既有 `POWER_DIAG_SOURCE_432A`）、呼叫**既有 `powerDiag432`**；孤島 ⇒ `toast(...,'bad',x,y)`。
toast 既有實作會寫入 `log[]` ⇒ 🔔 鈴鐺紅點自動亮、通知中心可點擊跳鏡頭。
節流變數 `islandWarnT520` 在宣告／`newWorld`／`load` 三處歸零（鐵律7）。

### 4.2 行為驗證（真瀏覽器：玩家什麼面板都不開）

孤島電廠城只是放著跑：**第 10 天畫面自己跳出**
「⚡ 發電廠周邊 1 格沒有帶電道路：電力送不出去，全城不會生長 → 用道路連到電源旁 1 格」；
🔔 鈴鐺紅點亮；通知中心該條帶座標 `@42,18`（可點擊跳鏡頭）。挨路城零通知。
**六哨兵位元恆等**——tick 內加了讀取與 toast，模擬狀態一動未動。

### 4.3 守衛

G1 原文前哨（10 天節奏／既有 tickBld／既有 powerDiag432／60 天節流）；
G1b 零亂數機械掃（**錨不含字串字面**——T517 G1e 的教訓當場套用）；G2 鐵律7 三處歸零；
**G3 行為釘寫成「恰好一條」**——同時守住「沉默」與「洗版」兩端。

### 4.4 紅源（三案）

| 紅源 | 結果 |
|---|---|
| ① 拆節流 | 第一版把 `if(day%10===0&&day-islandWarnT520>=60)` 整條改掉 ⇒ **先咬 G1 原文釘**＝沒驗到想驗的那條。改成「只破行為不動原文」（留著條件、拿掉 `islandWarnT520=day`）後 **G3 紅（實得 3 條洗版）** |
| ② 自建判定取代 powerDiag432 | **G1 紅** |
| ③ 挨路也報 | **G3c 紅** |

### 4.5 驗收

`verify.py` ALL GREEN、PASS 5,738 → **5,747**；六哨兵位元恆等。
