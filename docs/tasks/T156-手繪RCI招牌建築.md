# T156 手繪 RCI 招牌建築（最常見建築升級）
版本：v4.3 ｜ 難度：★★★ ｜ 前置：T148（HERO_PIX）｜ 建議模型：Sonnet

## 目標
RCI 住/商/工是螢幕上最多的建築，目前全程序化。挑各級最具代表性的變體共 9 座（住 lv1/lv2/lv3 各 1、商 lv1/lv2/lv3 各 1、工 lv1/lv2/lv3 各 1，即 `1_1_0`/`1_2_0`/`1_3_0`/`2_1_0`/`2_2_0`/`2_3_0`/`3_1_0`/`3_2_0`/`3_3_0`）升級為逐格手繪像素，其餘變體維持程序化——手繪與程序化混排正是「素材混合」的實踐。

## 紅線
像素逐格原創，畫風與既有一致（64×32、2:1）。**嚴禁參考/描摹 TheoTown 或任何既有遊戲**，summary 聲明。

## 定位錨
- T148 `HERO_PIX`/`drawHeroPix`（沿用格式）；`buildSprites` 絕對尾端（接 T155 之後）；`__noHeroRci` 開關
- k=1/2/3 的 `_w0/_w2` 財富色變（T125）、T128/T147 detailPass 疊加

## 允許觸碰
- `HERO_PIX` 追加 9 座 RCI 招牌建築（住宅溫馨屋頂/陽台、商業櫥窗招牌、工業廠房煙囪管線）；footprint/錨點與程序化版逐位元組一致（皆 72×112 ax36 ay110）。
- **財富色變相容**：手繪版須讓 T125 的 `_w0/_w2` 派生與 detailPass 疊加仍能作用（若 HERO_PIX 覆蓋了 base 鍵，確認 `_w0/_w2` 生成鏈與 detail 疊加對這 9 鍵仍正確——不正確則在 summary 說明取捨）。

## 硬性不變量
- 只替換這 9 個 base 鍵；其餘 SPR 鍵 checksum 位元一致（`__noHeroRci` 證明）。
- footprint/錨點一致；生長/升級選鍵、財富色變、detail 疊加不錯位（實機長跑生長驗）。
- night 窗燈＋冬季雪化同步；入冬零錯誤。

## 本卡驗收
1. 三件套（node --check / test_fixde 380+ / 瀏覽器零錯誤）。
2. `__noHeroRci` 開時位元一致；關時 9 鍵改變。
3. 長跑生長：住商工自然長出手繪版、財富貧富版仍生效、detail 疊加不越界；小地圖色正確。
4. 夜燈＋入冬雪化正常；色彩多樣度上升。

## 回滾
git checkout -- index.html
