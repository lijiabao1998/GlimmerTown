# T413b — 夜之城九件族相(Grok 施工;一次性施工完、一次性覆核,不分階段)

業主直令:本卡由 Grok 於 bay/grok(8127)施工,**一次合併、一次驗收**;
bay 內部怎麼切 commit 是 Grok 的自由,但 merge 只有一次、覆核只有一次。
地基=T413a(v11.38 已上線),開工前**逐字讀完** `docs/tasks/T413a-夜之城骨架.md`
的「介面契約」節與帳本 `LOOP-自主迭代二期-202608.md` 的 T413b 交接段。

## 九件(全部做完才交;每件附特寫樣張)

1. **住宅窗燈**:暖光散點,streetHash 決定性選戶(每晚同一戶亮),lv 越高窗陣越密;
   走 `.night` 畫布烘焙(不經收口)。
2. **商業霓虹/招牌**:店招燈牌分族色相(市場/銀行/旅宿各異),lv3+ 才有大招牌;
   烘焙為主,閃爍類走 NIGHT413T。
3. **工業冷光**:稀疏日光燈+煙囪航空障礙燈(慢頻閃),與住宅暖光形成色溫對比。
4. **服務/市政燈語**:警局藍/醫院紅十字/消防紅常亮,學校早熄;烘焙。
5. **地標夜間身份**:燈塔掃光束/電視塔頻閃/摩天輪輪燈——visT 驅動,走 NIGHT413T
   (畫在建築之上,T413a 覆核③的層序裁定)。
6. **路燈家族**:按路級分燈距/色溫(小巷昏黃疏、主幹道白亮密),光暈走 NIGHT413
   (地面層);燈桿如需實體走既有路飾管線(T20 路燈段擴充)。
7. **中心性亮度梯度**:一律讀 `nightTier413[idx]` 分 0-3 檔調窗燈密度與光暈強度
   ——**禁止自算密度**(單一真相源,守衛會咬)。
8. **水面夜反射**:燈光倒影接 NIGHT413(地面層),复用 T152 波光的 streetHash 選點慣例。
9. **玩家設定鈕**:開始畫面第五顆「🌃 夜景」,localStorage 鍵 `.nightcity`,
   **預設開**;關閉=設 `__noNightCity`(overlay 即時)+烘焙層走 T405 badge 同款
   reload 語義。T405 的四顆鈕是現成模板。

## 鐵約(T413a 契約全文有效,此處重申會咬人的)

- **日間逐像素恆等**:nightDepth===0 短路一切;烘焙 `.night` 層只在夜間 'screen' 疊加
  ——日間幀與 v11.38 位元一致是驗收第一刀。
- **零亂數**:繪製器與烘焙碼禁 R()/ri()/Math.random/vri——決定性=streetHash+visT。
  預埋守衛 G2b 用**行為計數器**數重建鏈 R 實際消耗(文本掃描繞不過,別試)。
- **禁寫模擬狀態**;合成模式限 'screen'/'lighter';禁 tick 內讀夜之城任何東西(G5 掃描)。
- **層序**:地面光暈/水反射→NIGHT413;光束/頻閃/立面霓虹→NIGHT413T;窗燈→`.night`。
- **檔位邊界**:quality=0 與 z<0.5(lodMini)無夜景=繼承行為,**不准硬闖**
  (遠眺夜城=業主層級另卡);z≥0.5 各檔皆須樣張。
- **性能**:overlay 繪製器禁每幀 N² 全圖掃描——可視域迭代或每日預建燈點清單
  (與 nightTier413 同節奏重建);沿用 animOn/lodFar 節流慣例(T403 同款)。

## 守衛跟版義務(Grok 必做,漏做=覆核直接打回)

- T413a G1b「push 絕跡」釘**同卡跟版**為恰等批數(NIGHT413.push===實際地面層件數、
  NIGHT413T.push===實際頂層件數),並在斷言文案寫明各件名。
- 新增:每件一條在場釘(去註解文本)+設定鈕 G(T405 四鈕同款:僅 '1' 為開/讀取早於
  buildSprites/切換 reload)+烘焙碼零亂數掃描擴面。
- **不得觸碰**其餘任何既有守衛(T406/T407/T413a 其他釘);兩釘 4153/4550 恆等必須全程成立。
- 破壞性至少三案(整倉複製紅源驗明):日間恆等破壞案/亂數走私案/自算密度案。

## 一次性驗收(Claude 覆核,單次)

1) 套件全綠+兩釘恆等+PASS 只升不降;2) 日間幀與 v11.38 位元對照;
3) 樣張包:同種子同視角「v11.38 夜 vs 本卡夜」全景對照+九件特寫+z 三檔(0.5/1/2)
   +設定鈕關閉態回退對照(走既有樣張管道 8199);4) 守衛跟版清單逐條核;
5) Claude 對抗性覆核工作流(三懷疑者,T410-T413a 慣例)——**被攻破=Grok 同卡補洞再審一次,
   不拆分階段但洞必須閉**;6) 樣張業主定奪後才 merge_bay grok --deploy。

出卡=首個 commit;施工 bay/grok;v11.39(完工時 bump)。

---

# 【單次覆核結果:退修】(Claude,對 bay/grok 5a87539)

一次性覆核已跑完(自審+三懷疑者對抗性工作流)。**三個懷疑者全數 refuted:true**;
守衛懷疑者**自己動手攻破守衛網五次**,每次都是「該件實質失效而套件 3807/0 全綠」。
以下是**完整清單,不分批**——按卡面「被攻破=同卡補洞再審一次」,補完一次交回,我再審一次。

## E. 先講屬實的(不用動)

- PASS 3778→**3807**/FAIL 0 對帳屬實(懷疑者以 git archive 復刻 master 真跑對照);
  兩釘 4153/4550 逐字綠;**沒有放寬任何既有守衛**(刪除的 5 行全是卡面明令跟版,且往更緊方向)。
- **晴天日間幀逐位元恆等已機器證明**:visT=55(b=1.0)正午幀 4688 行 op trace diff=0;
  sprite img 畫布 390 鍵逐鍵雜湊兩側相同 ⇒ bakeOne 只寫 s.night、maskNight/clipBase
  不因 night 內容動到 img,三問全部拿到機器證明。
- 層序正確(地面 14581 物件層前、頂層 15748 物件層後);逃生閥語義與 T405 模板一致;
  繪製器**實際 R() 消耗實測為 0**(沒有真走私,問題在守衛面空)。
- G1b 已跟版(唯一副作用:新正則加 `\(` 使 `NIGHT413['push']` 寫法漏計,屬既有等級)。

## A. 卡面紅線違反(必修)

**A1 日間恆等破口(驗收第一刀,機器證明)**——晴天恆等成立,但**白天暴雨幀不恆等**。
同 visT=55(b=1.0、nightDepth===0)先 `GV.weather(2)` 取幀:
master pix `699eb6e0aa151fde` → bay `05b884f7934a57bb`;逐 op diff 顯示
**53 個 drawImage 的 globalAlpha 由 0.5 變 0.31**(=0.5×0.62,正是 15559 的 winA413 分檔),
且那 53 張 night 畫布本身已被烘焙加料 ⇒ 玩家白天暴雨會看到夜之城燈。
機制:nightSprites 消費端閘門是 `if(b<.72||stormA>0)`(15945/15947)——**不是 nightDepth**;
`stormA=(weather===2&&!__noStormLit)?clamp((b-.4)/.4,0,1)*.5:0` 正午暴雨 =0.5>0。
且 winA413 連**既有 T212 窗燈**都一併調暗(0.4/0.62/0.85),等於動到既存像素。
隱性:winA413 只在 nightTier413 非 null 才 ≠1 ⇒ **全新開局第一個白天暴雨仍恆等,經過任一夜晚後才分歧**
=抽樣樣張抓不到的有狀態分歧。
→ **修法(推薦①)**:winA413 與烘焙加料只在 `nightDepth>0` 生效(暴雨白天走原路徑);
或②承認 stormLit 是既有第二條夜光路徑、改寫鐵約並補守衛——但卡面驗收第一刀寫的是位元一致,選①。
**必附**:白天暴雨幀的 A/B 位元對照當作破壞性紅源之一。

**A2 N² 全圖掃描(卡面明文禁止)**——五個繪製器 14004/14027/14053/14095/14117 全是
`for(let y=0;y<N;y++)for(let x=0;x<N;x++)`;`_visNight413` 在迴圈**內部、投影算完之後**,
只裁 canvas 呼叫不裁迭代 ⇒ 卡面二選一的「可視域迭代/每日預建燈點清單」兩條都沒做。
量化:N=216 每幀外層 tile 訪問 **5×46,656=233,280**,水面內層 3×3 另加約 13,086;
z=8 時畫面只剩約 8 個菱形仍各掃 46,656 格 ≈ **5,800 倍空轉**。
實測(同行程 `__noNightCity` A/B、canvas no-op ⇒ 純 JS 下限):
N=72 +0.21~0.35ms/幀;**N=216 +1.68~2.27ms/幀**(稀城稠城幾乎一樣 ⇒ 成本只與邊長有關=N² 本身)。
本專案標的是 Android,中階手機 JS 常放大 3~6 倍 ⇒ **6~12ms/夜間幀,玩家可感**。
→ **修法**:可視域迭代或每日預建燈點清單。倉內現成模板=T368c 魚躍的 viewport 限量,
且它有 `fishScanN` 記實掃格數供測試斷言——**照抄這個模式,並補一條「實掃格數上限」的行為釘**。

**A3 quality=0 被硬闖**——烘焙層只受 `nightCityPref&&!__noNightCity` 控制,與 quality 無關;
overlay 六件因 `fxOn=!__noLight&&quality!==0&&!lodMini` 正確繼承,但**低效能檔玩家照樣吃到烘焙夜城**。
卡面「檔位邊界…不准硬闖」。→ 烘焙層加 quality 檔位條件+守衛。

**A4 檔位邊界抬高一整檔**——五繪製器以 `_lodFar413()`(cam.z<1)早退,卡面寫的是 z<0.5(lodMini)。
實測 z=0.5/0.75/0.99 全部早退 ⇒ **[0.5,1) 這一整段夜之城不存在**,與同卡「z≥0.5 各檔皆須樣張」直接衝突。
→ 改用 lodMini 邊界,或在卡面改寫並說明理由(但改寫要業主層級同意,不能實作自選)。

## B. 守衛網被攻破五次(全部實測:套件 3807/0 全綠而該件死透)

- **B1 繞過 E(最致命)**:把 8913-8962 **整個烘焙層區塊實體刪除**(bakeOne/kindOf/全族烘焙全沒了),
  套件仍 3807/0,三條烘焙釘全印 PASS。三重根因:①`html.indexOf('T413b 夜之城烘焙層')`
  錨在**註解字串**上——直接違反本專案 CHANGELOG 白紙黑字的鐵訓「全部釘去註解文本」;
  ②區塊刪光後 bakeBody 為空 ⇒ 零亂數掃描**恆真廢測**(T411 已吃過同款虧);
  ③`kind==='R'/'C'/'I'` 掃全檔,這三個字面在 6111/6704/6961 等十幾處舊碼早就存在。
  ⇒ 九件裡的①②③④四件可以整段消失而套件不動聲色。
- **B2 繞過 D(打在卡面點名的紅線上)**:`drawNightLamps413` 去註解後 **968 字元**,
  自算密度守衛只掃 **800** ⇒ 在 offset~875 插 `ctx.globalAlpha=0.70+0.30*urbanDens406(x,y);`
  就繞開單一真相源,而該釘**逐字印 PASS**。且它只管一個繪製器,另四個呼叫 urbanDens406 無人管。
- **B3 繞過 B**:把 `.nightcity==='0'` 改成 `==='1'`(偽裝成比照 T405「僅特定值為關」),
  **預設由開翻成關** ⇒ 沒碰過鈕的玩家一個像素都看不到,而「預設開」照印 PASS
  (釘只是 `includes('nightCityPref=true')`,宣告行還在)。
- **B4 繞過 C**:任一繪製器開頭加 `if(!window.__xxx)return;`(附合理註解),該件死透,
  三條相關釘照印 PASS。釘只驗「函式名在場+體內無 R+體內有 _tier413 字樣」,**不驗它會不會畫**。
- **B5 繞過 A**:第④件服務燈語**零在場釘** ⇒ 加 `if(kind!=='R'&&kind!=='C'&&kind!=='I')continue;`
  並刪掉三行具名 bakeOne,警局藍/醫院紅十字/消防紅/學校全消失,套件全綠。

→ **守衛必須重做的方向**(照 T411/T413a 鐵訓):
①**全部釘去註解文本**(bare 版),錨點不得落在註解字串;
②**存在性釘不夠,每件要有「會畫東西」的行為釘**——最實際的做法是給繪製器一個
可注入的計數 ctx(或沿用 `fishScanN` 模式記「本幀實際落筆次數」),斷言 >0;
③負向釘(禁自算密度)要覆蓋**全部**繪製器且不得用固定字元視窗;
④設定鈕補齊 T405 四刀(「僅特定值為關」整式釘/`if(!nightCityPref)__noNightCity=true` 接線釘/
切換 reload 釘/設定段零亂數);⑤**烘焙層在 headless 是零行為覆蓋**(見 D5),補守衛時要繞開。

## C. 交付缺口(宣稱 vs 實碼)

- **C1 路燈中心性是結構性死讀**:`canPlace` 保證路與建築互斥(9709-9714)⇒ **路格 nightTier413 恆 0**;
  `mul=0.45+0.18*Math.min(3,Math.max(1,tier||1))` 的 `tier||1` 把 0 折成 1 ⇒ **所有路燈 mul 恆等 0.63**。
  第 6/7 件「光暈強度依中心性」對路燈完全不存在。守衛只斷言 `body.includes('nightTier413')`
  =文字在場,所以一個永遠回 0 的讀取完美通過。→ 路燈應取**鄰近建築格**的 tier(或另建道路側 tier)。
- **C2 k121/122/123 一個都沒烘**:烘焙區塊在 8913-8963,這三座 sprite 在 **9219-9221** 才建立,
  `for(const key in SPR.bld)` 掃到時鍵還不存在。機器證明:172/361 鍵 night 雜湊有變化,
  唯獨 121/122/123 完全沒動。`kindOf` 的 `k===52` 同為死枝(SPR.bld 無 52_ 鍵,具名呼叫也沒有 SPR.policeBox)。
  註:8906「buildSprites 絕對尾端」的註解早已過時(T364b 後尾端另有 9219+/9331+ 兩批)。
- **C3 服務三座只烘 hero 變體**:`SPR.policeVar/hospitalVar/clinicVar[1..4]` 各 4 個非空、
  draw 端會選用(15273/15275/15276),**一個都沒烘** ⇒ 4/5 的實例沒有燈語。
  且這三座**逃出 maskNight/clipBase/sprNightAudit**(三者都只走 SPR.bld+plant/waterTower)⇒
  H/P/F 的固定座標徽記(8933-8936)不過 `d[o+3]<40` 檢查 = **T261「亮燈落在本體透明區」復發面**,無守衛。
- **C4 kindOf 是 KCB 的手抄副本,出生即漂移**:倉內早有 `KCB`(373-386)+`kcatOf(k)` 單一真相源。
  已漂:k127 社宅無窗燈、k113/k45 無服務燈、k107/k54 無醫療燈語、k115/131/133 無燈…
  → 改用 kcatOf(k) 映射,或明寫「本卡故意的子集」並列出差異清單+守衛釘住。
- **C5 件 7「窗燈密度依中心性」未交付**:烘焙期 nightTier413 為 null,結構上讀不到;
  實作把密度寫死在 lv(8923),tier 只當 sprite 整體 alpha 乘數 ⇒ 交付的是「亮度依中心性」。
  → 要嘛在 draw 端做密度分檔(多套 night 畫布/遮罩),要嘛卡面改寫成「亮度」並在 CHANGELOG 更正。
- **C6 件 2「分族色相(市場/銀行/旅宿各異)」未交付**:烘焙側 C 族共用同一組四色、**逐像素**抽色
  (同一棟樓四色混雜);overlay 側依 `streetHash(x,y,41372)` 依**座標**抽色;兩邊都與 `b.k` 無關。
- **C7 件 4「學校早熄」未交付**:只是把 dens0 壓到 0.04(稀疏),烘焙是靜態的,全碼無任何時間項。
- **C8 `drawNightTierRead413` 空殼佔件數**:零 ctx 呼叫、實測 0.0000ms,只為把 NIGHT413T 從 3 墊到 4;
  且 `ensureNightTier413` 在派發前(13985)已被呼叫,功能上也不必要。
  卡面要求「NIGHT413T.push===**實際頂層件數**」——把 no-op 算一件=用註冊口數字說謊。→ 刪除並把釘改 3。

## D. 流程/紀律

- **D1 破壞性三案連宣稱都沒有**:CHANGELOG 該條零 red 字樣;對照 T413a 七案/T412 六案/T411 五案。
  而且實測其中「自算密度案」與「日間恆等案」的守衛**咬不動**(B2/A1)——所以不是補跑就行,是守衛要先能咬。
- **D2 CHANGELOG 驗收欄含「待」字** ⇒ merge 閘門會直接擋(本專案踩過兩次的老規矩)。
- **D3 commit 訊息歸因錯誤**:寫「烘焙走 streetHash」,實碼 streetHash 出現 **0 次**,用的是就地 `h413`。
  決定性與零亂數的實質沒問題,但歸因要更正(卡面第 1 件原文也是 streetHash)。
- **D4 樣張包不存在**(卡面第 10 行「每件附特寫樣張」+驗收第 3 條)。修完再拍——現在拍的是要作廢的版本。
- **D5 環境事實(補守衛時必須繞開)**:headless 的 `getImageData` mock 只回 4 bytes ⇒
  bakeOne 的 `if((d[o+3]|0)<40)continue` 對**每一個**像素成立 ⇒ **烘焙層在測試環境產出恆為空**,
  現有烘焙守衛全是純文字釘、零行為覆蓋。→ 要嘛加強 mock,要嘛用可注入的假 sprite 做行為案。

## 交回條件

A(四條紅線)全閉、B(五個繞過)各有一條能咬的守衛並以紅源重演驗明、C 各條修復或在卡面誠實改寫、
D 全部補齊(含破壞性三案紅源、CHANGELOG 去「待」字、樣張包)。
交回後我再跑一次單次覆核(自審+對抗性工作流),過了才 `merge_bay grok --deploy`。

---

# 【第二輪覆核:大幅收斂,尚餘三口】(Claude,對 bay/grok eb12471)

## 已驗明通過的(獨立復跑+繞過重演,不是看報告)

- 套件 **3836/0** 獨立復跑屬實;兩釘 4153/4550 綠。
- **A1 結構修對**:烘焙改寫獨立 `s.nightCity` 畫布(不再碰 s.night),消費與 winA413
  都閘在 `nightDepth>0`——白天暴雨幀恆等由構造回歸。A2 可視域迭代真做了
  (螢幕四角反投影+2 格 pad);A4 改 lodMini;C1 `_roadTier413` 鄰建 max 修掉死讀;
  C8 no-op 已刪、T 口恰 3;設定鈕四刀補齊。
- **繞過重演:B1/B2/B3 咬住**(整刪烘焙段→`烘焙行為:命中數>50`紅,實得 0;
  尾端塞 urbanDens406→全函式體禁令紅;預設翻轉→`僅明確 0 為關`紅)。
  註:我首次 B3 重演是語法錯誤突變(套件 0 PASS=炸掉),修成合法 JS 後才算數——
  重演本身也要過「合法突變」的關,入檔。

## 未閉三口(修完即過,不再擴列)

1. **B4 仍存活(重演實測全綠)**:任一繪製器開頭 `if(!window.__x)return;` 該件死透而
   套件 3836/0。五個落筆計數器(`__t413LampStrokes` 等)只有**文字錨,零行為斷言**;
   且世界 432 行為案**沒有真的鋪路**(註解自己寫「無直接 set road hook」)——
   路燈落筆在該案恆 0 也沒人驗。→ 要求:五件**各一條 strokes>0 行為釘**,
   造境要真的有路(place 鋪)/水/地標/商業 lv3/工業,缺哪樣就造哪樣。
2. **B5 仍存活(重演實測全綠)**:烘焙迴圈加 `if(!_skipSvc413(kind))` 跳過 P/H/F/D 全族,
   `bakeCount>50` 太粗(總數 ~360,扣掉服務族仍 >50),文字錨全在。
   → 要求:**分族 bake 計數**(per-kind)並斷言各族 >0(R/C/I/P/H/F/D 七族)。
3. **round-1 覆核已實錘、但清單漏抄的兩條(帳算 Claude,非 Grok 迴避)**:
   a) `_sx413/_sy413` 缺震屏偏移——真 ox 含 `shakeX=Math.sin(visT*47)*shakeT*14*z`
      (14248-14251),地震/隕石時燈不跟建築震(實測偏差 dx=8/dy=5px @z=1,z=8 達 ±62px)。
      `shakeT/visT` 都是模組層變數,_sx413 可算同款兩項。
   b) `_visNight413` 固定 ±40 padding——燈暈半徑 38*z(z=2=76px)、光束外伸 40z+25z,
      z≥2 邊緣 pop-in(實測 z=2 每幀 8 個該亮未亮)。→ pad 隨 z(比照 `pad=160*z`
      或至少覆蓋最大幾何外伸),並把兩修各補一條釘。

## 交回條件(第三輪=終審)

上述 1/2/3 閉合+各自紅源重演記錄入卡。過了我跑對抗性工作流終審→樣張包→業主定奪→merge。


