-# T407 — 塔高梯度;附「地面鋪裝」診斷撤回(TheoTown 觀感對標 第三刀)

## 一、先撤回一個我自己的錯誤診斷

我在 T406 收尾時把「地面過度鋪裝」列為剩餘兩刀之一,理由是業主實機圖整片灰白。
**量測後證實診斷錯誤,該卡當場作廢**:

- `__noPlaza` 是繪製期旗標,可即時 A/B。實測(seed301 AI 300 天、同視角)
  **切掉整個 T160 廣場層只改變 0.92% 的像素**(25,166 / 2,728,000),併排看幾乎一模一樣。
- 業主那張實機圖是**冬天雪景**(第2年346天、HUD 有雪花圖示),灰白是積雪不是鋪裝;
  TheoTown 對照圖是夏天。**跨季節比對造成的假事實。**

教訓:視覺類診斷必須先做可切換旗標的 A/B 像素量測,不能靠肉眼隔季比對。

## 二、真正剩下的差異:塔不向市中心聚集

跑到成熟期(seed301 800 天、pop 8110、29 座塔/巨廈)實拍:密度與 TheoTown 相當,
但**摩天樓散落全城,讀不出市中心**。根因(偵查 wf_abea8b7c D 組已記):
- 塔由 2×2 合併生成,**唯一的空間選擇力量是「哪裡有水管」**;
- AI 水管走 `%6===3` 均勻格網 ⇒ 全圖均勻供水 ⇒ 全圖均勻長塔;
- 原有的地價閘門已被 **T341c 明確移除**(實測高地價中心無水=閘門成空集)。

**塔的「位置」屬模擬,改它必動 pop ⇒ 兩釘必破、須重新釘值——這是業主層級的決定,本卡不做。**

## 三、本卡做的:塔高由中心性決定(與 T406 同款,零風險)

塔有 2 個變體:h=150(v0,水塔頂)與 h=168(v1,直升機坪),剪影 178 vs 196px。
目前 `v:ri(2)` 擲骰 ⇒ 高矮與位置無關。改用 T406 的 `pickV406`:

1. `VH406['33_1']=[178,196]`、`VH406['34_1']=[178,196]`(h+thw,thw=28),
   於 TWDEF 迴圈**內**記錄(與 RCI 同一手法:與實際建造同源、不另抄表)。
2. 合併點改 `v:pickV406(tk,1,x,y,ri(2))` —— `ri(2)` 照常求值 ⇒ 亂數消耗零位移;
   `pickV406` 對 2 元排名表同樣適用(`rank.length<2` 的回退門檻剛好不觸發)。
3. 塔的 `v` 與 RCI 同樣純視覺(TWDEF 只給牆色/屋頂型/高度;pop 用 TOWER_POP 常數與 v 無關)。

## 紅線

- 亂數消耗次數與順序零位移;**兩釘 4153/4550 恆等**(v 純視覺)。
- 不動塔的生成條件、不動 TOWER_POP/JOBS、不動存檔。
- `VH406['33_1']` 必須在 TWDEF 迴圈內記錄(不得另抄高度)。

## 驗收

1) 守衛:TWDEF 迴圈內記錄+合併點仍求值 `ri(2)`+塔排名表恰 2 項且單調;
2) 梯度實證:800 天 AI 城,塔的「鄰域密度前半 vs 後半」平均高度須有正向差;
3) 破壞性:合併點退回 `ri(2)`→紅;塔高度改抄常數→紅;
4) 套件全綠 + 兩釘恆等。

出卡=本 commit;施工 bay/kimi;v11.33。
