# T591 園區接新圖（業主定案 A）：36 類實驗線新圖放進主線 T574 園區

**發卡／施工**：業主 2026-09-26 看過三選一對照圖（`docs/tasks/t590-shots/T590-decision-campus-ABC.png`）後定案「我也傾向與A」——新圖放進主線園區、占地不變、剩下的地補停車／花園。同日另有指示「直接做吧，做完上GitHub」「需要讓我做決策的就多拍」。Claude 出卡＋施工。
**claim**：Claude；canonical master 直寫（T373 路徑），base `29159cc`／v11.199（T590 落地提交）。開工前 `git fetch origin` 驗兩向差 0；`fp_snapshot.py --check` 零差異；`merge_bay.py --status` 四車位 clean、receipt=match。驗證埠 8129+（真 Chrome、載入前設槽 3），不碰 8123。
**卡序**：接 T590 之後，串行（同碰 `index.html`、`test_fixde.js`）。
**合併權**：直寫 master，落地後 `merge_bay.py --publish` 與 `git push`。Claude 出卡＝施工同一方；另派同模型家族子代理做對抗式覆核，**不是第三方覆核，如實聲明**。

## 定位錨與問題

- T590 把 43 類實驗線新圖換進 `SPR.bld` 與家族，但其中 37 類新放置時帶 `bd.lot574`，繪製走 `lotSprite574 → bakeLot574` 程序化園區，不讀 `SPR.bld`。玩家新蓋的學校、醫院、博物館、機場……仍是 T574 的通用方盒。
- 主線園區比新圖原占地大：例如學校 1→3、機場 4→7、體育園區 3→7、國際機場 5→9（前者是實驗線／舊主線占地 `MSZ`，後者是 `LOT_PLAN574`）。A 案不改占地，新圖只佔園區一部分，其餘要補地面。
- k5 電廠的園區是 T585 剛做的 3×3 新電廠（`SPR.lot574` 預烘），本卡不動，所以是 36 類。k11 警察局、k12 醫院的新圖在家族（`SPR.police`／`SPR.hospital`），T590 的 `ported590()` 只看 `SPR.bld`，本卡用 `portedLot591()` 認家族。

## 業主決定

- 已定：A（占地不變，新圖放進園區）。
- **待業主看圖**：新圖放哪、剩下的地怎麼補。原型兩式（`docs/tasks/t591-shots/`）：
  - A1 置中：後兩帶種樹林，前左廣場、前右停車，前角進場道。**預設採 A1**（業主看過的 A 圖就是置中）。
  - A2 靠後：新圖貼後角，前方整片廣場＋停車，側邊樹林。
  業主若選 A2，只換一個常數。

## 做法

1. `bakeLot574` 在配方 `switch(k)` 之前加 T591 分支：`portedLot591(k)` 時，把新圖按原占地 m×m（`MSZ[k]||1`）以前角對齊貼進 n×n 園區，其餘地面用本函式既有的地面件（`ground／road／garden／tree／lamp／court`）加兩個新件（停車場、廣場）補滿；配置只由 (k,v,n,m) 的整數運算決定，零亂數。冬天改貼新圖的冬側版（T590 `winter590`）。
2. 本體以一個 `foot('main590')` 部件參與 `lotOrder574` 前後排序；夜圖先以本體輪廓挖空再貼新圖夜圖，前方部件的日圖照 T574 機制遮後方燈光。
3. 掛點換算成園區畫布座標：`flagAt→hooks.flag`、`radarAt→hooks.radar`、`door403→hooks.door`；繪製端旗子條件改成「T591 園區有 `hooks.flag` 就掛」；k65／k87 的寫死霓虹條同 T590 跳過（新圖夜圖自帶招牌）。
4. T591 烘出的園區物件帶 `__t590`，沿用 T590 對新圖的處理：不疊舊外框牆面層、不疊壓扁剪影影。
5. 園區變體：放置照舊寫 `v:0`；繪製期依格雜湊選 v0–v2（同 T590 `vdraw590`），不寫存檔。繪製與縮略圖預算（`lotFarFrame574`）用同一支 `lotV591()`，快取估算才不會少算。
6. 逃生閥：`?noT591=1`／`window.__noT591`（走回 T574 配方，變體回到 `bd.v`）。

## 允許觸碰

- `index.html`：`bakeLot574` 內 T591 分支；`t591On／portedLot591／lotV591`；繪製端 `lotSprite574` 呼叫處與 `lotFarFrame574` 的變體參數；旗子條件；霓虹跳過條件。
- `test_fixde.js`：新增 T591 守衛；T574 既有守衛凡是在驗 T574 配方本身（角色清單、G23d 夜光只落在窗燈色的像素台架等）改成在 T591 關閉下跑——配方仍是逃生閥路徑，要照樣守；逐條記錄。
- `docs/SPR_PINS.json`（預期零變更：園區按需烘焙，不在 1,548 鍵內）、`docs/ARCH.md`、`sw.js`（只經 bump）、`docs/CHANGELOG.md`、本卡、`docs/tasks/t591-shots/`。

## 禁區與停手線

- 不改占地、放置、存檔格式、`LOT_PLAN574`。
- 不動 k5 電廠園區、k22／53 農場（T585 生長期）。
- 六哨兵逐位恆等；園區 LRU 不得超過 T574 硬帽（128 組／12M 像素）。

## 驗收

- verify.py ALL GREEN、PASS 不降、六哨兵恆等；工具鏈 OK；CRLF 0。
- 36 類園區真 Chrome 日、夜、冬各烘一次：零例外、`cycles574` 全 0；夜光只落在新圖本體可見處或燈頭上（逐像素數）。
- 真城實拍：新放置的園區日夜冬、四方向；業主看圖定稿 A1／A2。

## 回滾

`git revert <T591 commit>`；或 `?noT591=1`／`window.__noT591=true`。

## 施工紀錄

2026-09-26，Claude（Opus 5.5）施工，v11.199→v11.200。

**落地內容**
- `bakeLot574` 在配方 `switch(k)` 之前加 T591 分支（配方改成 `if(!t591)switch(k){`）：`portedLot591(k)` 且該款是 T590 新圖（`__t590===1`）時，新圖按原占地 m×m（`MSZ[k]||1`，不超過 n−1）以前角對齊 1:1 貼進 n×n 園區、置中（A1）。其餘地面：後兩帶樹林、右下帶停車（離本體右立面留 .2 格，新圖外溢的車、雨棚、圍籬不被蓋；太窄改種樹）、左下帶前廣場（大廣場有水池與花台）、前角進場道（太淺不畫）；k18 港口的前左帶改成港池。冬天貼新圖的冬側版（T590 `winter590`）。
- 樹與車照新圖語彙重畫（`tree591`：1px 深色描邊、三層綠冠，冬天綠冠加頂部雪帽；車：描邊＋1px 擋風條，冬天車頂積雪），路燈只放在新圖左立面的左側。
- 本體以一個 `main590` 部件參與 `lotOrder574` 排序；夜圖先以本體輪廓挖空再貼新圖夜圖。掛點換算成園區畫布座標：`flagAt→hooks.flag`、`radarAt→hooks.radar`（k19）、`door403／hen403→hooks.door／field`（k23），另有 `hooks.aviation`、`hooks.body591`。
- 輔助：`t591On`（網址 `?noT591=1` 只判一次，另有執行期 `window.__noT591`）、`portedLot591`（k5 不算；k11／k12 認家族）、`lotKey591`（精靈快取與掛點快取共用的鍵尾：關閥 `_n591`、A2 出樣 `_b591`）、`lotV591`（只對放置寫死 v:0 的類按格雜湊選款，與 T590 同一支雜湊）。
- 繪製端：`lotSprite574(bd.k,lotV591(...))`；`lotFarFrame574` 用同一個變體估算；遠景保留貼新圖園區的變體（配方園區照舊合併成 v0）；旗子條件加「T591 園區有 `hooks.flag` 就掛」；k65／k87 貼新圖的園區跳過寫死霓虹條；施工升起期（age 4–8）不畫旗子與雷達。
- 貼了新圖的園區物件帶 `__t590`，沿用 T590 對新圖的處理（不疊舊外框牆面層與壓扁剪影影）。
- 檔案 3,779,513→3,789,049 bytes（+9,536），行數 41,920→42,023。
**真 Chrome 稽核（scratch 副本、槽 3、埠 8211+）**
- 36 類 × v0–v2 × 日／冬共 208 次直接烘焙：零例外、`t591` 全真、`cycles574` 全 0、旗子掛點有無與新圖 `flagAt` 一致。
- 夜光逐像素：夜圖亮點只落在「新圖本體仍可見處」或燈頭上，穿透 0；另有 36 個像素（16 次烘焙）是實驗線新圖自己在輪廓外的窗燈光暈，在實驗線同樣落在地面上，屬原設計。
- 真放置（`GV.place`）36 類園區（k18 港口外緣補水面），日／夜／冬／旋轉 90° 實拍，前後兩輪 `__errLog` 皆 0。
- 像素指紋 1,548 鍵零差異（園區是按需烘焙，不在指紋內），`--check` 再跑零差異。

**業主看圖**：`docs/tasks/t591-shots/`。`T591-compare-{day,night,winter}.png` 是左現況 T574 配方（`?noT591=1`）、右 T591；`T591-decide-A1-vs-A2.png` 是 A1 置中（預設）與 A2 靠後的真放置對照，請業主定稿；`T591-check.js` 是稽核與拍攝腳本。2×2 園區兩式幾乎一樣，差別在大園區。

**既有守衛調整（逐條）**
1. T574 G8c（園區不得 drawImage）：原本唯一例外是遠景縮圖；新增第二個例外，只放行 T591 三行貼圖（剝字串後原文），另加 T591 G1e 未剝字串的整行原文釘（覆核抓到剝字串後改合成模式不會紅）。其餘貼圖照舊拒收。
2. T574 G23c／G23d、G22l–n、G15 的購物中心案、T580 `bake580`：這些驗的是 T574 配方本身（主樓／側翼地腳、配方窗燈夜光契約、沒座標合成物件的快取算術、配方霓虹、配方細節件標記色）。移植類已貼新圖，配方只剩逃生閥路徑，所以在 `window.__noT591=true` 下照樣守，全部 try/finally 還原；對應的 T591 行為另有守衛（G2、G4、G10、G15）。
3. T574 G22g（遠景合併變體鍵）：改在 T591 關閉下數，因為貼新圖的園區遠景刻意保留變體（T591 G7）；合併是配方色票變體的契約。
4. T574 測試橋 `drawOrder`／`frame`／`nightFrame`：改用 `lotV591` 取與繪製同一個變體（原本用存檔的 v，對不上繪製端的格雜湊款）。
5. T590 G5a 旗子原文釘：跟著新條件更新（T591 園區讀 `hooks.flag`、施工升起期不畫）；G5e 霓虹原文釘同理。

**新增 T591 守衛**：G1a–e（配方只在沒貼時跑、繪製與預算同變體、快取與掛點快取同鍵尾、網址閥原文、貼圖三行整行原文）；G2（36 類「每一個真的會出現的款」日冬各烘一次、錄下每一筆貼圖：來源是該款自己的圖或雪景、三筆的畫布與合成模式、位置由園區地面錨點獨立算、旗／雷達／門／雞群掛點換算、本體在部件排序裡且前後關係對；實烘 260 次）；G2f（補地內容依新圖四周剩幾格分級的樹、車、燈下限）；G4a–f（關閥走回配方、快取與掛點快取關→開→關不串、網址 `noT591` 真的判得到）；G5a–c（k5 不移植、非移植類走配方、某款不是新圖時走回配方）；G6a–d（只對放置寫死 v:0 的類選款、其餘照存檔、關閥照存檔）；G7a–b（遠景保留變體／關閥照舊合併）；G10（像素台架：以已知遮罩的合成新圖代替 k40 v0，三顆窗燈全數貼上且落在新圖可見處、零雜光）；G15（購物中心貼新圖後不疊霓虹）。

**覆核（工作流：五個維度各一個審查代理＋各一個預設反駁的驗證代理，同模型家族；未經第三方覆核，如實聲明）**：32 條，驗證後 29 條成立、3 條被駁回。成立的合併後處理如下：
1. 遠景把變體壓回 v0，縮放跨過 1 就整棟換款 → 遠景保留貼新圖園區的變體（G7）。
2. 掛點快取鍵不分 T591 開關，執行期關→開→關後夜間霓虹每幀丟例外 → 精靈快取與掛點快取共用 `lotKey591`（G4d）。
3. `lotV591` 沒看 `vark590`，牧場的牛被改成羊／馬、k55 露出 T590 刻意不顯示的款 → 只對放置寫死 v:0 的類選款（G6b）。
4. 貼圖位置、掛點換算、本體排序、合成模式、變體覆蓋、補地內容、網址閥、k5、非新圖款全無守衛或空驗 → G2 重寫、G2f、G4f、G5a、G5c、G10、G1d–e。
5. 美術：路燈立在新圖入口與前角、停車格緊貼右立面、港口的船擱在廣場上、2×2 右前帶只剩空水泥與碎路、車像垃圾桶、套件樹無描邊且冬天全白與新圖綠樹不協調、大園區大片空廣場 → 全部照上面「落地內容」改掉，實拍重拍。
6. 施工升起期旗子與雷達懸空（T574／T590 本來就有，T591 擴大範圍）→ age 4–8 不畫。
7. A2 出樣旗標不在快取鍵裡 → 併入 `lotKey591`。
8. 近景預算對 vark 類變成最多三倍，大螢幕上較早轉遠景 → 不改碼，如實記錄；遠景現在保留變體，所以轉遠景時不再整片換款。
被駁回的三條：A2 旗標的另一個描述（與第 7 條重複且已修）、k11／k12 只顯示 v0（它們的 v 是放置時選的，走存檔的 v 本來就有變化）、高新圖後方路燈像裝在屋頂上（既有路燈層，品味問題）。
**紅源（整倉複本、不含 .git，三份複本並行，逐案單一突變）**
| 突變 | 結果 | 首紅 |
|---|---|---|
| m1 整段不貼新圖 | exit 1 | T591 G2 |
| m2 園區不選款 | exit 1 | T591 G6a |
| m3 冬天貼夏圖 | exit 1 | T591 G2（貼圖來源） |
| m4 旗子掛點不加本體偏移 | exit 1 | T591 G2（掛點） |
| m5 警察局／醫院只貼本體、不認款 | exit 1 | T591 G2（貼圖來源） |
| m6 掛點快取不帶鍵尾（拿掉 G1c 原文釘） | exit 1 | T591 G4d |
| m7 本體不挖空夜圖（拿掉 G1e、G8c 原文釘） | exit 1 | T574 G8c |
| m8 貼到後角 | exit 1 | T591 G2（位置） |
| m9 本體不進部件排序 | exit 1 | T591 G2（排序） |
| m10 合成模式改 source-atop（拿掉 G1e 原文釘） | exit 1 | T591 G2（合成模式） |
| m11 lotV591 不看 vark590 | exit 1 | T591 G6b |
| m12 遠景又壓回 v0 | exit 1 | T591 G7a |
| m13 網址閥失效（拿掉 G1d 原文釘） | exit 1 | T591 G4f |
| m14 k5 也當移植類 | exit 1 | T591 G5a |
| m15 不檢查新圖標記就貼 | exit 1 | T591 G5c |
| m16 樹林不種樹 | exit 1 | T574 G12d（小園區部件少於 3） |
| m16b 同上，另拿掉 G12d 的部件數下限 | exit 1 | T591 G2f |
| m17 關閥精靈快取鍵不分（拿掉 G1c 原文釘） | exit 1 | T591 G4c |
| 控制組（無突變） | exit 0 | — |

m7 如實說明：現行配置裡本體後方沒有會發光的部件，所以拿掉「先挖空」在行為上看不出差別，由 T574 G8c 的原文閘擋住；貼新圖後的夜光遮擋另有真 Chrome 逐像素稽核與 G10 台架。m16 的首紅是 T574 既有守衛，另跑 m16b 確認目標釘 G2f 也會紅。

**待決（留給業主）**
- A1 置中（預設）或 A2 靠後：看 `T591-decide-A1-vs-A2.png`；選 A2 只改一個常數。
- 大園區的補地內容（樹林／停車／廣場的比例）屬美術定稿，業主看圖可再調。
- 冬天新圖屋頂上的主線雪帽疊層（既有 T183 冬季屋頂積雪層）在高樓立面上偏白，屬既有繪製層，T591 未動。

**收束**：六哨兵 seed301 780／seed301m 2297／seed7 117／seed7m 77／seed22 639／seed22m 14523 逐位恆等；`verify.py` ALL GREEN（PASS=12297、CRLF=0、11.200 雙版本同步、ARCH 錨點新鮮）；`python -B -m unittest tools.test_toolchain` 63 tests OK；`git diff --check` 乾淨。
