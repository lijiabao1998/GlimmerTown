# T566 連棟派對牆加強（視覺隱形 → 真連棟）

**日期**：2026-08-28　**發卡**：Kimi　**施工**：Kimi（業主全鏈授權：「出加強卡……你出卡，你施工，你迭代，即便如此，也應嚴格遵守施工紀律」；美術定稿權在業主，樣張收尾交）
**前案**：T565（v11.180 已併 master `7f4ace3`）——A/B 樣張業主已閱、無異議，唯派對牆一件經 z8 單縫裁決判定「有落筆、視覺隱形」。

## 0. 病灶（樣張實錘）

T565 的派對牆是 **5×z px 檐口對檐口細帶，在先畫棟的 pass 落筆**——後畫的鏡頭側鄰棟
sprite 把條帶大半覆蓋，殘量只有細絲：z3 凍幀 A/B 像素差分在住宅排縫處≈0
（差異全數來自計程車與廠區圍籬），z8 才見帶狀差異。
計數器證明筆筆有畫（`__t565Wall` 持續累進），是**被遮罩不是沒畫**。

## 1. 修法（同批縫、換手臂、蓋頂層）

`drawPartyWall565` 內加第二款——**接縫立牆條**：

- **換手臂**：現款橋帶畫在 `off[1]>0`（鄰棟更晚畫＝會蓋我）；立牆條畫在 `off[1]<0`
  （鄰棟已畫完＝我蓋它）——同一物理縫，由鏡頭側後棟在自己的 pass 收尾，深度序正確。
- **密度閥沿用同一顆** `streetHash(min x,min y,56511)<.78`：選縫集合與 v11.180 完全相同，
  街廓記憶連續、可對照。
- **幾何**：本棟靠鄰側牆緣起一條垂直牆條，自檐口（~.835h）落至牆基（~.99h），
  寬 ~.10w（z1≈7px），色＝既有 tone 三色表；靠縫側 1px 暗描邊＋頂帽線，
  視覺上把兩棟之間的縫填成連續牆面。
- **資格閘原樣**（k1 lv1、1×1、age≥9、z≥1、`__noAdj565` 逃生閥）——只動筆不動閘。

## 2. T523 預算對策（7474/7500 只剩 26 筆餘量）

立牆條**批次化單路徑**：全棟所有縫的矩形併入一條 `Path2D` 級 path，fill 一次、stroke 一次
（T565 預算戰同款手法）；若仍超支，**等量替換**——被遮罩的舊檐口橋帶降權或刪除
（它的視覺貢獻≈0，刪了不損畫面）。預算釘 7500 紅線不動。

## 3. 驗收計畫（test_fixde.js 檔尾 T566 塊）

- **G1 立條行為釘（真造境）**：`__t384Bld(1,x,y)` 兩棟 lv1 相鄰 → `__t565Fin>0`；
  拆一棟 → 歸 0；孤棟 → 0（沿用 G3 造境 (36,36) 與雜湊釘 0.539）。
- **G2 短路釘**：`__noAdj565` 下 `__t565Fin` 恆 0；lodFar 下同。
- **G3 預算釘**：T523 census 不升破 7500（施工紀錄記實得值）。
- **G4 六哨兵位元恆等＋兩釘（seed301=4153／seed22=4550）不動**——純 draw-time，
  烘焙零觸碰 ⇒ **指紋零漂移**（本卡無白名單宣告，fp 全鍵逐位不動）。
- **紅源一案**：立牆條段合法 no-op → G1 咬中（斷言名＋實得值入紀錄）。
- **樣張**：8125 同種子同視角 A/B（z3 連棟排＋z8 單縫），visT 凍幀正午相位，
  像素差分報告差異座標——目標：住宅排縫處有肉眼可辨的連續牆面，收尾交業主人眼軌。

**明確不動**：T565 三款本體（橋帶僅可能降權/刪除，不動資格閘）；地墊／圍籬；
模擬層；烘焙層；TheoTown 素材檔。

## 4. 回滾

單卡單提交鏈，`git revert` 回 v11.180；運行期 `__noAdj565` 照舊一刀全關。

## 5. 施工紀錄

**落點**：`drawPartyWall565`（index.html 18814–18847 起，v11.181 態）；掛點 17704 行不動。
橋帶臂整撤（`__t565Wall` 不再累加＝同族病釘 G2b 防回流），立牆條臂由 `off[1]<0` 側落筆。

**六輪樣張迭代（全程 A/B 像素差分，閾值 >16、20px 格叢集，計程車叢集排除）**：

| 輪 | 改動 | 裁決 |
|---|---|---|
| v1 | 立牆條 hw=.055w、中性灰泥三色表 | 含蓄：整條僅縫線 ~8px 可辨（on=(134,129,121) 蓋 off=(88,85,79)） |
| v2 | hw→.09w（總寬 .18w） | 過矯正：z8 半幅側牆被平塗成異物貼片，抹掉牆面紋理 |
| v3 | 色調改變體牆族派生（v0 奶油 #c9ad82／v1 暖陶 #cf9862／v2 灰綠 #9db49a） | 貼片感減但仍太寬太平 |
| v4 | 補卡面規格的靠縫側 1px 暗描邊 | **T523 census 7528 破紅線**（+2 筆/縫×18 縫） |
| v5 | 矩形改 fillRect（1 筆/縫）＋帽線描邊連續折線共享 moveTo | census 7420/7500，比施工前 7474 便宜 54 筆 |
| v6 | 牆基 .995h→.90h | 修掉 z3 排尾棟懸空色柱（剪影在 .20/.80w 骨位早收）；**定稿** |

卡面原文「寬 ~.10w（z1≈7px）／落至 ~.99h」中，~.99h 係未計剪影斜收的筆誤，實作收 .90h；
寬度 v2 試過 .18w 後退回卡面 ~.11w——含蓄的真病根是色調對比不足，不是寬度。

**預算實得**：T523 census **7420/7500**（施工前 7474，淨 −54；紅線不動）。
**守衛清單**：T566 G0 段掃錨／G2 計數器在場／G2b 橋帶計數器已撤／G2c 落筆臂方向釘；
行為主釘併入 T565 G3 造境塊（fin>0／孤棟 0／__noAdj565 全關恆 0／lodFar 短路）。
**紅源**：立牆段插 `if(z>=1)return 0`（合法語法、文字錨全留）→ T566 G1 咬中，實得 fin=0；
突變後位元備份還原（sha256 恆等）。整倉複本在系統暫存區比車位慢一倍（300s 跑不完），
改在車位本體「備份→突變→跑→還原」＝等價流程，如實記。
**verify 實數**：PASS=6333 exit=0、七閘全綠（ARCH 地圖曾咬行號漂移，arch_map --fix 補錨後綠）。
**指紋**：純 draw-time、烘焙零觸碰 ⇒ 六哨兵位元恆等（verify 實測 seed301=6801／seed301m=640249／seed22=3423／seed22m=1461／seed7=260／seed7m=50 全匹配）、像素指紋零漂移，無白名單宣告。
**樣張差分座標**（v6 定稿組，1440×900）：z3 連棟排差異叢集沿兩排住宅縫位分布
（(340,540)-(400,600)、(440,580)-(500,660)、(540,620)-(580,700)、(620,680)-(680,760)、(720,720)-(780,800) 等），
z8 單縫主叢集 (740,680)-(880,900)；左緣與下緣另有計程車/道路動態叢集（既有行為，非本卡）。
樣張包 8 張＋紅標疊圖 3 張存 `安卓探索\t566-shots\`，交業主人眼軌；色調表三值驅動可逐值回調。
