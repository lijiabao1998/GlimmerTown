# T380a — L1 守衛的逐變體斷言（補 T380 殘留缺口）

狀態：**待施工（任一方皆可，非 T380 作者／非 T380 施工者最佳）。**
發卡：Claude（T380 二輪覆核時實測發現，未阻擋 T380 合併）
前置：master `4b35ffe`（v11.2，PASS 3180 / 0 FAIL）；三車位 `ahead=0 behind=0 clean`；`receipt=match`。
規模：極小。預期改動 `test_fixde.js` 一條斷言 ＋ `index.html` 兩個變體各加一個字面矩形。

---

## 一句話

T380 的 L1 守衛用 regex 抓 `fillRect(ax±N,ay±N,w,h)`，**x 與 y 都必須是字面量**；
而 v5／v7 的招牌是迴圈算座標（`ax-24+i*12`、`ay-54+r*3`），**L1 一個都看不到**。
self-check 是「總數 ≥24」而非逐變體，所以兩個變體歸零仍然過。

---

## 證據（實測，非推論）

L1 失敗訊息自己就印出逐變體的可見矩形數（第二個數字）：

```
v0:0/4 v1:0/4 v2:0/3 v3:0/2 v4:0/2 v5:0/0 v6:0/1 v7:0/0
v8:0/1 v9:0/1 v10:0/3 v11:0/2 v12:0/3 v13:0/4 v14:0/4 v15:0/2
```

**`v5:0/0` 與 `v7:0/0`** —— 守衛知道那兩個是 0，但沒有任何斷言檢查它。

破壞性注入（在 bay 的完整檔案集上跑，避免 T371b fail-fast 遮蔽）：

| 注入 | 結果 |
|---|---|
| **v5 招牌 `ay-52` → `ay-16`（搬進田心 y=132 正中央）** | **全綠 —— L1 沒擋住** |
| v0 招牌 `ay-52` → `ay-16` | 紅：`T379 L1：k22 精細田蓋掉建築 138 px，超過門檻 60：v0:138/4 …` |
| 把所有 `//sig` 改成 `//x` | 紅：`T380 L1：建築(//sig)矩形總數應 ≥24（每變體至少 1 招牌），實得 0` |

第三條的訊息寫「**每變體至少 1 招牌**」，但實際檢查是總數 —— **訊息與檢查不符**，
這是本專案定義的守衛缺陷（COLLAB「覆核的盲區」：卡面每一條「不得」都要有對應檢查項）。

**T380 的美術本身是正確的**（覆核已實測：招牌全在 y90~105、田 y108~148，y 區間不相交，
16 變體落在田內 0 px，含迴圈畫的）。本卡修的是**未來回歸風險**，不是現存缺陷。

---

## 要改什麼（兩處）

### 1. `test_fixde.js` — 把 self-check 改成逐變體

現況（總數）：

```js
assert(sigTotal >= 24, 'T380 L1：建築(//sig)矩形總數應 ≥24（每變體至少 1 招牌），實得 ' + sigTotal);
```

改成逐變體，並在訊息裡列出歸零的變體：

```js
const blind = perSig.map((n, v) => n === 0 ? 'v' + v : null).filter(Boolean);
assert(blind.length === 0,
  'T380a L1：每個變體都必須有 ≥1 個 L1 可解析的 //sig 矩形（x/y 皆字面量），' +
  '否則該變體的招牌不受守衛保護。歸零：' + blind.join(',') +
  '（修法：給該變體加一個字面座標的基床矩形，見 v0 的 //sig 基床）');
```

（`perSig` 就是 L1 訊息裡已經在算的那個逐變體矩形數，直接沿用，不要另寫一份幾何。）

### 2. `index.html` — v5／v7 各加一個字面座標的基床矩形

照 v0 的既有寫法（v0 已經有）：

```js
sg.fillStyle='#5a6a34';sg.fillRect(ax-28,ay-54,52,10); //sig 基床
```

v5（果箱列）與 v7（草莓壟）各補一個能罩住自己招牌的基床，**座標必須是 `ax±N,ay±N` 字面量**。
基床本身也是美術（v0/v10 的基床看起來就是田床/水田面），所以這不是為測試而加的死像素。

---

## 允許觸碰

- `test_fixde.js`：T380 L1 的 self-check 那一條（本卡明示授權改寫）
- `index.html`：v5／v7 兩個變體塊各加一行 `sg.fillRect(...)`；**不動 `F22`、不動其他變體**
- `docs/CHANGELOG.md`：一條
- 本卡狀態行與施工記錄

**不升版號**（純守衛強化＋兩個像素基床）。若因此需要升版，改用 `python tools/bump.py`
並記得 `docs/ARCH.md` 的版本字要同步（T371b 會咬 —— T374／T379 各犯一次）。

## 禁止觸碰

1. `F22` 表（16 筆 `[68,144,40,20]`）不得動。
2. 每個變體塊仍須恰好一個 `plate(g,ax,ay,'#8a9a5a',2)`（鐵律2：`plate` 吃 `spriteTexRand` 78 draws）。
3. 新增的基床必須整個落在 **y ≤ 105**（田是 y108~148），且不得新增色票
   —— 用該變體已有的顏色（v5 `#a8763a`／v7 `#4a8a3a` 之類）。
4. L1 的幾何計算本體、K4、K1、T274 harness 一律不動。
5. 不得放寬門檻（≤60）來讓事情過關。

---

## 不變量

1. L1 建築覆蓋仍為 **0**；逐變體可見矩形數全部 **≥1**。
2. 辨識度不得下降：兩兩相異像素的 `最相似對 ≥352`、`中位 ≥913`、`相異<300 的對數 = 0/120`
   （覆核者在 atlas 全鏈實測的 v11.2 基線；**注意 Node 重放會低估**，
   施工者若用 Node 重放請自報方法，數字對不上不代表退步）。
3. 套件八色、地墊 130/258/194、指紋 diff 只含 k22 家族 —— 皆不得變動。
4. PASS 不得低於 1902 且 0 FAIL（機器底線；T380 實得 3180）。

## 驗收清單

1. **破壞性證明**：把 v5 招牌的 `ay-N` 改成 `ay-16` ⇒ **L1 必須紅並指名 v5**（這是本卡存在的理由）。
2. 把新加的基床拿掉 ⇒ 逐變體斷言必須紅並列出 v5（或 v7）。
3. 還原後全綠；`node test_fixde.js` 0 FAIL、`python tools/verify.py` ALL GREEN。
4. atlas 目視 v5／v7 兩張：基床要看起來像美術，不是浮在空中的色塊。
5. 量不變量 2 的三個辨識度數字，寫進卡面。

## 回滾

單一 commit，`git revert`。純守衛與兩個像素基床，不影響存檔與模擬。

## 阻塞規則

- 若加了基床卻讓某變體的招牌被田蓋到（L1 > 0）⇒ 停手回報。
- 若辨識度三個數字任一下降 ⇒ 停手回報。

---

## 交接資訊

- master HEAD：`4b35ffe`（v11.2，PASS 3180）
- 錨點：`test_fixde.js` 搜 `T380 L1`；`index.html` 搜 `//sig 箱`（v5）、`//sig 壟`（v7）、`//sig 基床`（v0 範例）
- **跑注入測試時請複製整組檔案**（`index.html`／`test_fixde.js`／`sw.js`／`docs/ARCH.md`／`docs/CHANGELOG.md`）到測試目錄，
  只複製 index.html 會讓 T371b 版本守衛先咬、把 L1 遮住（覆核時踩過）。
- 相關：[T380 卡面](T380-k22大農場逐變體重構圖與精細田放大.md)、
  [T380 覆核記錄與出卡更正](T380-覆核記錄與出卡更正.md)
