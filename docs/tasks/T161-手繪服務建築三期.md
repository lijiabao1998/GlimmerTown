# T161 手繪服務建築三期（剩餘服務設施）
版本：v4.4 ｜ 難度：★★★ ｜ 前置：T148（HERO_PIX）｜ 建議模型：Sonnet

## 目標
把手繪像素管線推到尚未手繪的服務設施 8 座：診所(k=13)、圖書館(k=14)、郵局(k=15)、墓園(k=16)、救護站(k=28)、回收中心(k=29)、高級消防(k=30)、污水廠(k=27)。手繪英雄建築累計推向 29 座。

## 紅線
像素逐格原創，畫風與既有一致（64×32、2:1）。**嚴禁參考/描摹 TheoTown 或任何既有遊戲**，summary 聲明。

## 定位錨
- T148 `HERO_PIX`/`drawHeroPix`（沿用格式不改）；`buildSprites` 絕對尾端（接 T160 之後）；`__noHero3` 開關
- 各 k 的既有 SPR 鍵慣例（單一鍵 `SPR.xxx` 或 `k_1_0`）、footprint/錨點

## 允許觸碰
- `HERO_PIX` 追加 8 座；覆蓋既有程序化版；沿用管線加 night 窗燈與冬季雪簷。
- 識別特徵明確：診所綠十字小樓、圖書館列柱書卷、郵局郵筒卡車、墓園墓碑柏樹、救護站紅新月車庫、回收中心分類桶輸送帶、高級消防雙塔雲梯、污水廠沉澱池管線。

## 硬性不變量
- 只替換這 8 個鍵；其餘 SPR 鍵 checksum 位元一致（`__noHero3` 證明）。
- footprint／錨點與替換前逐位元組一致（實機放置驗不錯位）。
- night 層與冬季版同步；入冬零錯誤。

## 本卡驗收（含 T160 教訓：先確認建築在畫面）
1. 三件套（node --check / test_fixde 380+ / 瀏覽器零錯誤）。
2. **驗證前務必**：放建築後 GV.center 對準＋z≥1，確認該建築確實渲染在畫面（getImageData 該建築螢幕框非草地）再做 checksum/像素比對——否則測不出屬場景錯非 bug。
3. `__noHero3` 開時位元一致；關時 8 鍵 checksum 改變、非透明像素合理。
4. 8 座實機放置對齊、拆除整棟消失、小地圖色正確、夜燈＋入冬雪化正常。

## 回滾
git checkout -- index.html
