# FIX-B 亂數流對齊（恢復 v2.0 sprite 基線一致）
難度：★★★ ｜ 建議模型：Opus ｜ 審查確認：T29/T30/T35/T36 位移了下游所有 sprite 的亂數流

## 目標
按 ARCH §5.1 紀律恢復對齊：既有 v2.0 的全部 SPR 鍵位（夜窗圖樣、點綴）與基線位元一致；
新增內容的生成移到不擾動位置。修完以 T28 式逐鍵指紋回歸證明。

## 施工方案（審查團裁定的 A 案：零下游擾動搬移）
1. **T29 道路**：道路 sprite 生成恢復「基線 rand 消耗」——rc=2（支路）在原位置生成（保持舊版 16 次 speck 的消耗量與順序），rc=1/3/4 三級的生成整段搬到 buildSprites **尾端**。
2. **T30 高密度住宅**：DRAFTS 淨增行 (1,3,4) 的建造任務從排序主流程摘出，在 buildSprites 尾端單獨建造（查表→plate→isoBox→parts→outline→註冊，同引擎函數可重用；若引擎不便重入，允許在尾端以顯式呼叫重建該一鍵）。
3. **T35/T36 圖書館/郵局**：兩座 sprite 生成區塊整段搬到尾端（水塔/警察/醫院/診所/墓園已在尾端或不耗 rand，不動）。
4. 搬移時**不得改動任何生成內容本身**，只動位置；注意搬移後區塊間的變數作用域（各區塊已用獨立 {} 塊）。

## 驗收（核心：像素回歸）
1. 以固定種子重建兩次（改前基線取 git 6773675 版在瀏覽器跑出的指紋——用 `git show 6773675:index.html > /tmp/base.html` 起本地服務對照），對全部 v2.0 既有 SPR.bld/park/plant/tree/road/bridge 鍵做 toDataURL 雙雜湊（djb2+sdbm）比對：**既有鍵 100% 一致**。
2. 新增鍵（rc=1/3/4 道路、1_3_4、圖書館、郵局）存在且渲染正常（放置實測+像素取樣）。
3. VERIFY.md 冒煙 + 零錯誤。
4. CHANGELOG 記錄回歸結論（N/N 鍵一致）。

## 回滾
git checkout -- index.html
