# T454 最低工資（城市科學十卡 #4）

**日期**：2026-08-14　**發卡**：Claude（業主 goal：前沿經濟學/社會學研究進玩法）
**藍圖**：T451 卡面 §0。第四張政策卡，走 `SCI451` 基座（指南零改動）。

## 0. 實證依據（已聯網核實，含爭議如實記）

Card & Krueger (1994, *AER*)——1992 年 4 月紐澤西州最低工資 $4.25→$5.05，
以賓州速食店為對照組的差異中之差異（DiD）自然實驗（331+79 家店、前後兩輪普查）：

- **教科書預測被推翻**：就業沒有下降——相對賓州反而微升（DiD 估計 +2.75 全職當量）。
- **爭議如實記**：Neumark & Wascher 用薪資冊資料質疑；三十年論戰後，近期共識（Cengiz 等
  2019 QJE 一系）認為溫和水準的最低工資就業效應接近零。本卡科學頁寫明論戰存在。

## 1. 遊戲效應（三接點）

`SCI451` 新增 `minWage454`，政策 `pol.minWage454` **預設 false**：

| fx | 值 | 對應 | 接點 |
|---|---|---|---|
| `emplMul` | 1 | **發現本身寫成常數**：就業 ×1.00＝零顯著就業流失 | `jobs` 總和式之後 ×emplMul（開啟時也恆等——這正是這條研究的主張） |
| `bizTaxMul` | .97 | 商家承擔工資成本、利潤微縮 | k2 商業稅與 k3 工業稅 `v2` ×.97，差額帳列 `fin.mwCost` |
| `happyVal` | +.015 | 低薪勞工實質所得上升 | 幸福因子列（租金管制旁） |

- 對帳：`GV.sci451` 擴 `mwCost`/`mwOn`；G4 行為對帳 0→>0→0（同型第四次）。
- 誠實簡化（寫進科學頁）：只作用一般商業 k2 與工業 k3；購物中心/塔樓等大型業態 v1 不受影響。
- `emplMul:1` 是刻意的：把「null result」直接接進 jobs 式，開關前後 jobs 恆等本身就是研究結論的展示。
- G1c 回歸釘擴充：T452 兩條＋T453 三條效應原文必須原樣保留。

## 2. 驗收計畫

守衛 T454 塊：G1 表項；G1b 三接點讀表；G1c 前兩卡原文保留；G2b 切片禁手抄 `.97`（opacity
白名單沿用）；G3/G3b 預設 false＋白名單；G4 行為對帳（seed 4541、90 天 AI 城）＋**G4d 就業恆等**
（開關前後 jobs 相等＝emplMul=1 的行為驗證）。紅源三案。真瀏覽器：四筆齊、⭕→✅、mwCost 真扣帳。
三釘位元恆等；`verify.py` ALL GREEN。

---

## 3. 施工紀錄（Claude 全鏈；起點 master `f7c8766` → v11.86，PASS 4,997）

### 3.1 落地（十一個接點，指南零改動）

`SCI451` 新增 `minWage454`（含論戰句原文）、`pol` 初始 false、`jobs` 總和式後 ×`emplMul`
（=1，null result 寫成常數）、k2 商業稅與 k3 工業稅各插 `mw454`/`mw454i` 成本記帳
（合計 `fin.mwCost`）、幸福因子列、面板 checkbox `polMw454`＋`polToggle`、`GV.pol` 白名單、
`GV.sci451` 擴 `mwCost`/`mwOn`/`jobs`。第四筆自動長出。

### 3.2 T386a 字串釘跟版（本卡撞到的既有絆線）

k2/k3 稅式插入 mw454 把 T386a 釘的「`sq(...)；income+=`」連續原文切開 ⇒ 套件紅。
釘的意圖（sq 效果因子必須仍落在兩條稅式上）不變、原文合法演進 ⇒ **needle 跟版＋理由註記**
（T343c 探針跟版同款）。這正是 T453 發明 G1c 的鏡像場景：上一張的釘擋住這一張的手，
差別只在這次是「合法演進要跟版」而不是「改壞要擋」。

### 3.3 G4d 的設計說明（為什麼不是跨 tick 全恆等）

`emplMul=1` 理論上開著 tick 也恆等，但幸福 +.015 會合法改變 cityHappy → 住宅生長 roll →
建築存量 → jobs。跨 tick 比較會被這條合法路徑汙染成 flaky 釘（T444 教訓：釘要行為化且穩定）。
故 G4d 取「開關瞬間（未 tick）jobs 不動」＋ G1 鎖 `emplMul` 恰為 1 ＋ G1b 鎖 ×emplMul 接線，
三件合起來＝null result 的完整絆線。

### 3.4 真瀏覽器驗收

四筆齊、Card & Krueger 引用與論戰句（Neumark）在頁上、⭕→✅ 回寫、開關瞬間 jobs 不動=true、
tick 一天 `mwCost=2.668` 真扣帳、殘留 0。

### 3.5 紅源（四案，整樹複本）

| 紅源 | 結果 |
|---|---|
| ① `pol` 預設改 true | **G3 紅** |
| ② k2 硬編碼 .97 | **G1b 紅** |
| ③ 科學頁手抄「×0.97」 | **G2b 紅** |
| ④ **emplMul 竄改成 .95**（把 null result 改成教科書預測） | **G1 紅**——引用的靈魂有絆線 |

### 3.6 驗收

三釘位元恆等；`tools/verify.py` ALL GREEN、PASS 4,987 → **4,997**；引用聯網核實、論戰如實記。
