# T454 最低工資（城市科學十卡 #4）

**日期**：2026-08-14　**發卡**：Claude（業主 goal：前沿經濟學/社會學研究進玩法）
**藍圖**：T451 卡面 §0。第四張政策卡，走 `SCI451` 基座（指南零改動）。

## 0. 實證依據（已聯網核實，含爭議如實記）

Card & Krueger (1994, *AER*)——1992 年 4 月紐澤西州最低工資 $4.25→$5.05，
以賓州速食店為對照組的差異中之差異（DiD）自然實驗（331+79 家店、前後兩輪普查）：

- **教科書預測被推翻**：就業沒有下降——相對賓州反而微升（DiD 估計 +2.75 全職當量）。
- **爭議如實記**：Neumark & Wascher 用薪資冊資料質疑；三十年論戰後，近期共識（Cengiz 等
  2019 QJE 一系）認為溫和水準的最低工資就業效應接近零。本卡科學頁寫明論戰存在。

## 1. 遊戲效應（三接點）

`SCI451` 新增 `minWage454`，政策 `pol.minWage454` **預設 false**：

| fx | 值 | 對應 | 接點 |
|---|---|---|---|
| `emplMul` | 1 | **發現本身寫成常數**：就業 ×1.00＝零顯著就業流失 | `jobs` 總和式之後 ×emplMul（開啟時也恆等——這正是這條研究的主張） |
| `bizTaxMul` | .97 | 商家承擔工資成本、利潤微縮 | k2 商業稅與 k3 工業稅 `v2` ×.97，差額帳列 `fin.mwCost` |
| `happyVal` | +.015 | 低薪勞工實質所得上升 | 幸福因子列（租金管制旁） |

- 對帳：`GV.sci451` 擴 `mwCost`/`mwOn`；G4 行為對帳 0→>0→0（同型第四次）。
- 誠實簡化（寫進科學頁）：只作用一般商業 k2 與工業 k3；購物中心/塔樓等大型業態 v1 不受影響。
- `emplMul:1` 是刻意的：把「null result」直接接進 jobs 式，開關前後 jobs 恆等本身就是研究結論的展示。
- G1c 回歸釘擴充：T452 兩條＋T453 三條效應原文必須原樣保留。

## 2. 驗收計畫

守衛 T454 塊：G1 表項；G1b 三接點讀表；G1c 前兩卡原文保留；G2b 切片禁手抄 `.97`（opacity
白名單沿用）；G3/G3b 預設 false＋白名單；G4 行為對帳（seed 4541、90 天 AI 城）＋**G4d 就業恆等**
（開關前後 jobs 相等＝emplMul=1 的行為驗證）。紅源三案。真瀏覽器：四筆齊、⭕→✅、mwCost 真扣帳。
三釘位元恆等；`verify.py` ALL GREEN。
