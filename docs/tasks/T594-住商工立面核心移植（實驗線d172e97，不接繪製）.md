# T594 住商工立面核心移植（實驗線 d172e97 超街區生成器，只移植＋對照檯，不接繪製）

**發卡／施工**：業主 2026-09-26「下一步吧，你很棒完全沒問題的」。住商工（k1–3）換新圖要分期，怎麼上畫面等業主回決策單（https://claude.ai/artifact/Ka5ZHv36qLLcW71CUysnah ，七題）；本卡先做**不需要任何決策**的地基：把實驗線的立面生成器原樣搬進主線、逐位證明跟實驗線一樣，但**不接到任何繪製路徑**，畫面零變化。Claude 出卡＋施工。
**claim**：Claude；canonical master 直寫（T373 路徑），base `75b5340`／v11.202。來源釘在實驗線 `origin/main` **d172e97**（2026-09-26，T681 之後；開工前 fetch 確認兩邊都沒有新提交）。驗證埠 8129+（真 Chrome、載入前設槽 3），不碰 8123／8199。
**合併權**：直寫 master，落地後 `merge_bay.py --publish` 與 `git push`。Claude 出卡＝施工同一方，未經第三方覆核，如實聲明。

## 定位錨

- 實驗線的住商工不是靜態精靈：`getBlockSprite547(k,lv,bw,bh,v)` 在繪製時依街廓大小（bw×bh）與款（v 0–11）現場生圖並快取；核心 `makeBlockSprite547` 畫地坪、量體、屋頂、窗，符合原型（`ARCHE568`）的再交給十支英美立面繪製器（`window.__facade577` 登記表，七個獨立 `<script>`）。
- 主線只有一個 `<script>`（主 IIFE），而且沒有這套東西；主線住商工是 T3xx 系列的靜態 `SPR.bld` 圖，由 T590 之前的程序圖產生。
- T593 已從實驗線 15ac877 移植了 `metroHash／metroRand／metroPalette／metroFacade／isoBox／windows／winShape570`（593 尾碼）；d172e97 的同名函式與 T593 版逐字相同（比對過，註解除外），直接沿用。
- 相依閉包（acorn 作用域分析、只追真正指向頂層宣告的引用）：51 個宣告要移植，5 個沿用 T593，`cv／shade／clamp` 與主線逐字相同直接用主線的。
- 盤點時發現 T593 的一個漏洞：`metroPalette593` 的工業分支（k===3）引用 `IND_ROOF593／IND_WALL593`，但主線從未宣告；T593 只替 105／106 生圖所以碰不到，T594 的工業會走到，要一起補。

## 做法

1. 在 T593 區塊之前插入 T594 區塊：
   - 51 個宣告原樣搬入，實驗線 T 號一律換成 594（`makeBlockSprite547→makeBlockSprite594`、`ARCHE568→ARCHE594`…；沒有 T 號的加 594）；改名只改真正指向這些宣告的識別字（作用域分析），區域變數與字串不動——亂數種子字串 `'block557:'／'roof559:'` 照實驗線原文，才能逐位相同。
   - 七支立面繪製器原樣內嵌成主 IIFE 裡的小 IIFE，只把 `window.__facade577` 換成本區塊的 `FACADE594`；剝掉實驗線自測（`gptCornerShopSelftest001`、`shopTallSelftest680`、`mewsSmokeSelftest671`、`mewsBaysSelftest681`、`ukMewsSelftestGROK2`，都依賴實驗線的 `GV.block559`）。
   - 核心讀 `window.GV.art574` 改讀本區塊的 `A594`（實驗線 art574 的子集：`shade／lerp2／paraPt559／fillPara547`，繪製器只用這四個）。
   - 實驗線的 `window.__no*`／`__facadeForce577` 閥門原名保留（主線原本沒有這些名字，預設全關＝實驗線預設畫法；照 T593 慣例）。
2. `IND_ROOF593／IND_WALL593` 原樣補進 T593 區塊（`metroPalette593` 之前）。
3. 不接繪製：任何繪製路徑都不呼叫 `getBlockSprite594`；開機不生圖；`SPR` 零變化。

## 允許觸碰

- `index.html`：新增 T594 區塊；T593 區塊補兩個常數陣列。其他一行都不改。
- `test_fixde.js`：新增 T594 守衛與測試橋（`window.__t594`，只在測試注入）。
- `docs/ARCH.md`、`sw.js`（只經 bump）、`docs/CHANGELOG.md`、`docs/LINEAGE.md`（記錄移植來源與之後的同步債）、本卡、`docs/tasks/t594-shots/`。
- `docs/SPR_PINS.json`：**不准變**（本卡不動任何精靈）。

## 驗收

- verify.py ALL GREEN、PASS 不降、六哨兵恆等；工具鏈 OK；指紋零差異；CRLF 0。
- 真 Chrome 對照檯：主線 `makeBlockSprite594` 與實驗線 d172e97 `GV.block559.make` 在 k1–3 × lv1–3 × v0–11 × 16 種街廓（bw、bh 各 1–4）共 1,728 張上，日圖、夜圖 CRC32、尺寸、錨點、冒煙點、立面名**逐位相同**。
- 守衛：零世界亂數（生圖時世界 `R` 與 `Math.random` 呼叫數 0）、零例外、沒接繪製、十個立面都登記、快取鍵分得開 v 與 v+6、T593 補的兩個陣列可用。

## 回滾

`git revert <T594 commit>`（本卡不改畫面，回滾也不改畫面）。

## 施工紀錄

2026-09-26～27，Claude（Opus 5.5）施工，v11.202→v11.203。

**落地內容**
- `index.html` 在 T593 區塊之前插入 T594 區塊（2,660 行）：實驗線 d172e97 住商工街區精靈核心的相依閉包 51 個宣告（`getBlockSprite594／makeBlockSprite594／ARCHE594／roofKit594／lotFill594／occWrap594…`）＋七支立面繪製器（十個立面：ukTerrace、ukSemi、ukVictorian、ukMansion、ukHighStreet、ukCornerShopGPT001、usBrownstone、usPrewar、usMainStreet、ukMews），登記在本地 `FACADE594`；核心的 `ctx.A` 改讀本地 `A594`（shade／lerp2／paraPt559／fillPara547）。
- 抽取器用 acorn 做作用域分析：只改真正指向頂層宣告的識別字，區域變數（例如 `ptb653` 裡的區域 `R`、繪製器的 `R=Math.round`）與字串（亂數種子 `'block557:'／'roof559:'`）一字不動；沿用的 T593 函式逐字比對（註解除外）相同；七支繪製器的自由名只剩瀏覽器全域與 `FACADE594`；主線全檔自由名掃描只剩瀏覽器全域（和一個與本卡無關的舊問題，見下）。
- 補 T593 漏宣告：`IND_ROOF593／IND_WALL593` 原樣補在 `metroPalette593` 之前（T593 只替 105／106 生圖，走不到工業分支；T594 的 k3 會走到）。
- 不接繪製：區塊外沒有任何地方引用 T594 的名字；開機不生圖；像素指紋 1,552 鍵零差異。`docs/SPR_PINS.json` 只有版本欄隨 bump 從 11.202 改成 11.203（T549 G2b 要求基線版本等於 GAME_VER，T592 起每次 bump 都這樣）；卡面「允許觸碰」寫「不准變」是寫得太死，實際意思是鍵與指紋不准變，如實更正。
- 檔案 43,181→45,844 行。

**真 Chrome 對照檯**（`docs/tasks/t594-shots/px594.js`、結果 `px594-summary.json`）：主線 `makeBlockSprite594` 與實驗線 d172e97 `GV.block559.make` 在 k1–3 × lv1–3 × v0–11 × bw,bh 1–4 共 **1,728 張上逐位相同**（日圖／夜圖 CRC32、尺寸、錨點、冒煙點、立面名、牆框、遮擋框），兩邊 console.error 都是 0。立面名分布：核心三式 452／436／452，十個立面 GPT 街角店 32、英式高街 80、紅磚公寓 21、馬廄 7、半獨立 21、連棟 14、白灰泥聯排 32、布朗石 21、美式主街 96、戰前公寓 64。接觸表：`T594-grid-day／night.png`（2×2 街廓 108 格）、`T594-fac-day／night.png`（十個立面各一張）。

**新增 T594 守衛**
- G0 區塊緊接在 T593 之前；G1a 頂層宣告恰 53 個全帶 594、繪製器段恰七個 IIFE；G1b 區塊外零引用（連 `obj.名字` 也算）＝沒接繪製。
- G2 恰登記十個立面；G2b 區塊內不再讀寫 `window.__facade577`／`window.GV`、沒有自測殘留；G2c 區塊不往全域掛東西（window 寫入只准實驗線的擦燈計數器 `__t653Erased`，不得有 `window[…]`／`globalThis`／`self.`／`GV.`／`var`）。
- G7a／G7b T593 補宣告各一次、工業配色取得到（排在全格生圖之前，漏宣告時先在這裡紅）。
- G3 A594 恰四個成員，繪製器只讀這四個。
- G4a–c Node 全格 1,728 張生得出來、形狀對；console.error 0；立面名分布等於實驗線規格（另抄一份）。**G4d 畫布呼叫指紋**：全格生圖時把每張的畫布呼叫（方法＋參數＋當下填色／線色／透明度／合成／線寬）錄成 FNV-1a 指紋 `a6a045b9`（6,372,883 次呼叫），並釘各立面的呼叫數；繪製器少畫、改畫、改吃別條亂數串流，指紋就變。這是 Node 端「原樣移植」的釘，實驗線重新移植或共用函式改動時要重新簽名並重跑真 Chrome 對照檯。
- G5 生圖期間世界 `R`、`spriteTexRand`、建圖串流 `rand`、`Math.random` 呼叫數 0，且量測期間沒被換掉；G5b 原文：區塊不得呼叫 ri／vri／瀏覽器亂數／spriteTexRand，局部亂數恰兩條 metroRand593、種子字串照實驗線；G5c 核心段沒有 `R(`，七個繪製器 IIFE 開頭都以 `const R=Math.round` 遮蔽世界 R。
- G6 快取同鍵同物件、v 與 v+6 分開；G6b 區塊讀的 33 個閥門，除五個實驗線原本就不進鍵的（`__noBay604／__noChimVis613／__noFacadeSmoke613／__noLeftLight541／__noSpecies601`）以外都在快取鍵裡。
- G8a–c 逐閥門掃：32 個布林閥門逐一打開、`__facadeForce577` 逐一強制十個立面，縮小格共 6,048 張：零例外、零世界亂數、繪製器零吞例外。
- 測試橋 `window.__t594` 只在測試注入；`rOn` 量測期間把三條世界串流包一層計數、結束還原並回報有沒有被換掉。量測前後的 console.error／Math.random／document.createElement／`__noOccWin653` 都在 try 裡設、finally 裡先還原 console.error。Node 樁沒有 `CanvasRenderingContext2D`，G4／G8 在 T653 遮窗包裝關閉下跑；遮窗在內的像素由真 Chrome 對照檯承接。

**既有守衛調整（判準放寬，逐條記錄）**
1. T445 G1 裸等距式白名單：精確增列 `blockCorners594`、`makeBlockSprite594` 兩個具名函式（街廓四角與畫布四角，都是單張精靈畫布內的格內座標）；不以 594 後綴泛放行。命中的四段原文逐字釘死（`(bw-bh)*32`、`(bw+bh)*16` 各兩次），不只釘數目。
2. T438 G2 繪製區零共用亂數：T594 區塊內的 `drawShops／drawHotel／drawCornerShop001` 是立面繪製器（烘焙街區精靈，不是逐幀繪製），所屬 IIFE 開頭 `const R=Math.round` 遮蔽世界 R，裡面的 `R(` 是取整。這三個具名函式從繪製區排除，排除名單恰這三個、都必須在 T594 區塊內，且所屬 IIFE 開頭（剝掉註解後）有 `R=Math.round`（新增 T438 G0c）；零世界亂數改由 T594 G5／G5c／G8b 承接。

**對抗覆核**（兩個獨立代理人，唯讀）
- 移植忠實度：自建作用域解析器逐 token 比對實驗線與主線，差異恰是卡面宣稱的 13 處（兩行新宣告、兩處介面替換、七個登記行、兩段自測刪除），加上版本號；同名撞名、繪製器自由名落到主線頂層名、閥門分支漏改名、自測刪掉必要程式：都沒有。提醒一件接線時的事，記在下面。
- 守衛對抗：第一版守衛有六種錯誤改法能全綠——繪製器畫空白、繪製器落到全域建圖串流 `rand`、在區塊裡掛 `window` 別名或從外面用 `window.xxx594` 偷接、閥門分支用世界 R、快取鍵漏一個閥門；另有守衛本身的毛病（量測前的包裝不在 try 裡、`rOn` 還原時不查有沒有被換掉、G4a 對 null 取屬性、T438 用未剝註解的行、T445 只釘數目）。上面的 G2c、G4d、G5 擴充、G5c、G6b、G8 與兩條既有守衛的收緊就是針對這些；覆核提出的改法全部寫成第二輪突變，見下表。
- 覆核也指出 bump 之後 `SPR_PINS.json` 版本欄沒跟上（T549 G2b 紅），已重跑 fp_snapshot（鍵與指紋零差異，只改版本欄）。

**紅源（整倉複本、不含 .git，逐案單一突變）**
第一輪（三份複本並行）：
| 突變 | 結果 | 首紅 |
|---|---|---|
| m1 繪製端偷接 getBlockSprite594 | exit 1 | T594 G1b |
| m2 核心塞瀏覽器亂數 | exit 1 | T594 G5（Math.random 1,728 次） |
| m3 核心改用世界 R | exit 1 | T594 G5（R 107,183 次） |
| m4 漏登記一支立面 | exit 1 | T594 G2 |
| m5 A594 少 lerp2 | exit 1 | T594 G3 |
| m6 馬廄繪製器丟例外（核心吞掉、退回核心畫法） | exit 1 | T594 G4b（console.error 7 次） |
| m7 快取鍵 v 與 v+6 共用 | exit 1 | T594 G6 |
| m8 拿掉 T593 補宣告 | exit 1 | T594 G7a |
| m9 facadeFor 回讀 window.__facade577 | exit 1 | T594 G2b |
| 控制組（無突變） | exit 0 | — |

第二輪（守衛收緊後，四份複本並行；m1–m9 重跑＋覆核的全綠改法 r1–r8）：
| 突變 | 結果 | 首紅 |
|---|---|---|
| m1 繪製端偷接 getBlockSprite594 | exit 1 | T594 G1b |
| m2 核心塞瀏覽器亂數 | exit 1 | T594 G5 |
| m3 核心改用世界 R | exit 1 | T594 G5（R 107,183 次） |
| m4 漏登記一支立面 | exit 1 | T594 G2 |
| m5 A594 少 lerp2 | exit 1 | T594 G3 |
| m6 馬廄繪製器丟例外 | exit 1 | T594 G4b |
| m7 快取鍵 v 與 v+6 共用 | exit 1 | T594 G6 |
| m8 拿掉 T593 補宣告 | exit 1 | T594 G7a |
| m9 facadeFor 回讀 window.__facade577 | exit 1 | T594 G2b |
| r1 繪製器畫空白（drawShops 早退） | exit 1 | T594 G4d（呼叫數 6,372,883→6,314,078） |
| r2 drawHotel 落到全域建圖串流 rand | exit 1 | T594 G5（rand 3,147 次） |
| r3 區塊內把生成器掛到 window | exit 1 | T594 G2c |
| r4 繪製端經 window.getBlockSprite594 偷接 | exit 1 | T594 G1b |
| r5 閥門分支用世界 R（先取別名躲過原文掃描） | exit 1 | T594 G8b（R 144 次） |
| r6 快取鍵漏 __noShopTall680 | exit 1 | T594 G6b |
| r7 GPT 繪製器拿掉 R=Math.round、只在註解留字 | exit 1 | T438 G0c |
| r8 T445 換式：N 角改寫、另加一條世界座標式（數目仍是 4） | exit 1 | T594 G445 原文釘 |
| 控制組（無突變） | exit 0 | — |

第二輪起初 m3、r2 的首紅是 G4d 指紋（亂數換了、畫出來的東西也跟著變），攔得到但診斷不準；把 G5 挪到 G4d 前面後重跑，兩案都改在 G5 紅，指出是哪條串流。r3 第一版把別名和下一個函式寫在同一行，意外先弄壞 G1a 的第 0 欄計數；改成獨立一行重跑，首紅是目標 G2c。十七案首紅都是目標釘，控制組在最終守衛順序下 exit 0。

**接線時要注意（留給之後接上畫面的卡）**
- `IND_ROOF593／IND_WALL593` 是 `const`，位置在 T593 區塊裡；主 IIFE 跑到那一行之前就替工業生圖會撞 TDZ（開機完成後呼叫沒事）。
- T653 遮窗包裝要真 Canvas；Node 樁沒有 `CanvasRenderingContext2D`，Node 守衛都在遮窗關閉下跑。
- 快取鍵帶實驗線閥門旗標；有五個閥門實驗線原本就不進鍵（G6b 名單），接線後若開放玩家切這些閥門，要先補進鍵或清快取。
- 實驗線的 `window.__facade577` 可以從主控台插拔立面；主線 `FACADE594` 是區域的，主控台動不到（覆核提醒，照設計）。

**順手發現（不在本卡範圍）**：主線 `inspect()` 的墓園分支（約 21579 行）讀 `cemeteries`，但它只是 `updateStats` 裡的區域變數，點墓園時應會丟 ReferenceError。已開成獨立任務，另一個 session 認領為 T595（分支 `fix/t595-cemetery`，會在 T594 之後 rebase、版本 11.204）。

**收束**：六哨兵 seed301 780／seed301m 2297／seed7 117／seed7m 77／seed22 639／seed22m 14523 逐位恆等；`verify.py` ALL GREEN（PASS=12347、CRLF=0、11.203 雙版本同步、ARCH 錨點新鮮）；`python -B -m unittest tools.test_toolchain` 63 tests OK；`fp_snapshot --check` 零差異；`git diff --check` 乾淨。
