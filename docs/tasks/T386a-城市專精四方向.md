# T386a — 城市專精四方向(工業港城/綠色城市/教育科技城/交通樞紐)

狀態:**已出卡,待施工(自主 loop R6)。**
發卡:業主 loop 直令(GPT 順位 3 的專精半邊;T386b 合約已隨 v11.11 上線)。
出卡+施工+自審:Claude(業主 loop 授權)。
前置:master `58dbddc`(v11.11,PASS 3415);四路偵查(wf_64aa0277)打底。
路徑:bay-kimi,bump 11.12,merge --deploy。

> 風險披露:本卡動稅式(T383b 稅收窮舉守衛區、七八層乘數鏈)——紅線=**未選專精逐位元恆等**
> (tq() 零解鎖恆等同構,兩釘+雙重播機器證明)。

---

## 一句話

城市 Lv.6 起可**永久**四選一專精(dozeArm 兩擊確認,選定即鎖),每向 2 正 1 負效果
全掛既有白名單計算點(`sq()` 三元 helper=tq() 同構,未選恆 ×1/+0);
載體=獨立單字串存檔欄 `spec386`(偵查裁決:pol 可逆語義不合、tech343 互斥幾何不容)。

## 設計決策(七條)

1. **效果表**(全部行內併乘/併加在既有 tq() 同行或同式;保守幅度):
   | 方向 | 正效果 | 代價 |
   |---|---|---|
   | ind 🏭 工業港城 | 工業稅 ×1.06(12254)+造船港金 ×1.25(11717 尾乘) | 起火 ×1.08(12072) |
   | green 🌿 綠色城市 | 起火 ×0.90+觀光 ×1.15(11696 round 內乘) | 工業稅 ×0.92 |
   | edu 🎓 教育科技城 | EDU 場 ×1.08(10650)+研究速度 +1(1003) | 政策日費 +$12(12259) |
   | hub 🚉 交通樞紐 | 運量 ×1.12(13486)+商業稅 ×1.03(12252) | 建造費 ×1.05(9702) |
   **被否決:綠色城市掛污染場**——stampPolSrc 是 ±對稱增量蓋印,跨選定切換會留殘值;
   recomputePol 尾乘需切換時全量重建,v1 以起火/觀光組合替代(記卡,日後另議)。
   edu 選定時觸發 rebuildCov(TECH_EDU343 先例——EDU 乘數變了場要重建)。
2. **helper**:`const sq=(id,on,off)=>spec386===id?on:off;`(tq() 985 同構);
   **sq( 呼叫數釘死斷言**(9 個效果點,test 靜態 count pin=防靜默刪點)。
3. **狀態**:`let spec386='';` 白名單 SPEC386={ind,green,edu,hub}(名/圖標/效果文案);
   save `if(spec386)data.spec386=spec386`(零狀態零 bytes);load 一行白名單驗型
   (非法→'' 整欄棄用不拋);newWorld 成對歸零(鐵律7);GV.spec386()/specPick386(i)。
4. **永久性**:`specPick386` 若 `spec386` 已有值 return false(一行鎖);
   **兩擊確認**=dozeArm 先例(第一擊武裝+警告 toast+3 秒窗,第二擊生效+金 toast;
   specArm386 純 UI 模組變數,面板重繪即清=不殘留);**不用原生 confirm**(凍結 rAF)。
5. **門檻**:rankIdx+1>=6 且 diff!==3(沙盒不可選——同委託慣例一條規則到底,記卡)。
6. **UI**:委託 tab 頂部「🏙️ 城市方向」區(**插在全部早退分支之前**=任何狀態可見,
   偵查風險 6);未選=四顆 `bSpecPick386_0..3`(未達門檻 dim);已選=選中 on 樣式+
   其餘三向 ⊗ 已封鎖列(tech343 呈現先例,不隱藏);城市 tab rankHtml 行尾附專精圖標
   (一行);選定 toast 金=自動落通知中心(永久紀錄)。四新 id 入 test 白名單。
7. **T383b 同步**:稅式加乘數後,窮舉守衛與兩釘在「未選」路徑天然不動;
   選定路徑的稅式正確性以「同 seed 對照城 fin.taxI/taxC 比值≈效果值」專項驗。

## 允許觸碰

`index.html`(SPEC386/spec386/sq/9 效果點/save/load/newWorld/委託 tab 區/rankHtml 一行/
GV 鉤子+bump 11.12)、`sw.js`、`test_fixde.js`(白名單 4 id+新斷言+橋)、`docs/ARCH.md`
(版本行+§5 符號句+§9 鐵律7 列)、`docs/CHANGELOG.md`、T386b 卡(補「a 完成後 spec gate」
一行說明可選)、本卡、帳本。

## 禁止觸碰

R()/ri()/rand/Math.random 消耗零新增;income+=;POL 場;happyParts(固定 50 項不可 push);
aiStep(AI 不選專精);存檔僅 spec386 一欄;tools/;其他車位。

## 不變量

1. **未選專精位元恆等**:兩釘原值+雙重播 bytes 全等(套件重跑=機器證明)。
2. 零狀態存檔零新鍵;PASS 只增(3415 起);verify ALL GREEN 11.12;T383b/T385/T386b 守衛全綠。

## 驗收清單

1. sq 呼叫數 count pin;未選恆等(套件重跑)。
2. 效果專項:同 seed 兩城(ind 專精 vs 未選)synthetic 工業城 step→fin.taxI 比值≈1.06;
   edu 城 techSpeed343 差 1;hub 城運量比值≈1.12(三向抽驗,green 以起火機率式靜態驗)。
3. 兩擊:第一擊不生效(armed)、第二擊生效、重繪清 armed;永久:已選再 pick=false。
4. 門檻:rank<6/沙盒 拒;驗型:非法字串→'' 且不拋;存讀往返;零狀態零鍵。
5. UI:綁定真實性(白名單)、任何狀態面板含「城市方向」區、零壞值。
6. 破壞性:刪一個 sq 效果點→count pin 紅;白名單刪 id→綁定紅;基線→綠。
7. 閘門+真瀏覽器 8125 槽3(四選卡渲染/兩擊流程實測/console 0)。

## 回滾

單 bay commit revert;未選玩家零可見面;已選玩家舊版載入時未知欄位天然忽略。

---

## 施工記錄(施工後填)
