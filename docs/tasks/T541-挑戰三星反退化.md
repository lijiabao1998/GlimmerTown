# T541 挑戰三星反退化（T538 判例的同型收尾）

**日期**：2026-08-20　**發卡**：Claude（業主 2026-08-20 全權授權延續；T538 判例同型）

## 0. 依據

`star3` 挑戰＝三個挑戰裡**獎金最高**（$2,500 vs pop1k $1,500／day200 $2,000），
判定卻是 `bestStar>=3`——筆記七實測迷你城**第 19 天**就到 3 星（pop 183），
起始資金 $3,000 ⇒ 第 19 天白拿 $2,500 ＝ 早期資本近乎翻倍的免費午餐。
與 sc_starlight 同一個退化策略；T538 已裁決同型修法（當前星＋人口門檻、文案揭露）。

## 1. 落地
1. 判定改 `cityStar>=3 && pop>=CH_STAR3_POP541`（具名常數 500；用**當前星**＝T538 同理）。
   500 的量級：拓荒立鎮 150 天目標 600 ⇒ 挑戰（無時限）500 人＋3 星是合理的中期目標；
   與 pop1k（純人口 1000／$1,500）比＝軸不同、價更高、難度相稱。
2. 按鈕文案揭露門檻：「🏁 挑戰：三星城市·500人」（T442/T538 揭露紀律）。
3. 挑戰無 GV 鉤子 ⇒ 補 `GV.challenge541(id,bonus)` 供守衛造境（純測試面）。

## 2. 驗收計畫
- G1 源釘：判定必須是「當前星＋具名常數門檻」形態；`bestStar>=3` 不得殘留於 star3 分支。
- G2 行為：迷你城（AI 40 天、star 已達 3）掛上挑戰跑 10 天 ⇒ **不得發獎**；
  pop 門檻達標後（造境）發獎且恰 +$2,500。
- G3 文案：按鈕 label 含常數數字（交叉一致）。
- 六哨兵位元恆等（challenge 預設 null）。
- 紅源：①判定退回 bestStar（G1）②門檻亂調（G3 交叉釘）③文案忘改（G3）。

---

## 3. 施工紀錄（2026-08-20 收）

**版本** v11.167　**PASS** 6,011 → 6,017　**master** 待填　**六哨兵位元恆等**（challenge 預設 null）

### 落地
- 判定 `bestStar>=3` → `cityStar>=3 && pop>=CH_STAR3_POP541`（具名常數 500；當前星＝T538 同理）。
- 按鈕文案「🏁 挑戰：三星城市·500人」——**由常數組出**（T442 紀律）。
- `GV.challenge541(id,bonus)` 造境鉤（挑戰原本只有開始畫面按鈕一個入口，守衛需要）。

### 行為證明
45 天迷你城（star 3、pop 322＝**修前白拿 $2,500 的狀態**）掛上挑戰跑 10 天 ⇒ **不發獎**
（資金變化只有正常經濟活動）；六哨兵位元恆等。

### 守衛自捕一件
G1b 第一版 200 字窗口把**我自己剛寫的 T541 註解**（內含「bestStar」四字）掃進去＝守衛咬自己的
說明文字——**T538「窗口裡只有你要驗的東西」教訓當場複習**，窗口切到行內註解前才綠。

### 紅源三案（全紅）
①判定退回 bestStar → G1b／②門檻亂調成 1 → G1 常數釘（設計絆線）／③文案改手抄 → G3。
