# T575 健康度棘輪（分階段修・階段一：先把偵測器立起來）

**日期**：2026-09-03　**發卡／施工**：Claude（業主 09-03 明示「分階段修，開始吧」）
**base**：`7cb0dcd`／v11.188　**車位**：`bay/kimi`
**性質**：純守衛與文件。**不動 index.html、不動 tools/、不動六哨兵值、不動任何既有斷言。**

## 0. 病灶：釘問錯了問題

業主 09-03 定調：「舊釘對目前的高速迭代已經不足以滿足內容增長」。本卡把這句話變成程式。

六哨兵（`seedPin444`）是**混沌系統終點的精確值**釘。它同時被當成兩件事用：

- **決定性**——同碼同種子必得同結果（抓亂數流位移）。這個目的今天仍然成立，精確恰等是對的形狀。
- **健康度**——AI 城會長大。大家以為有，其實從來沒有。

高速迭代下兩者命運相反：任何內容變動都推動終點值，釘每卡必響，唯一處置是重釘。
**一個永遠響、永遠重釘的釘只記錄歷史，不偵測任何東西。**

T574 是實例：seed301 400 天人口 6801→288（−96%）、12 種子 400 天 pop<500 由 4/12 惡化到 8/12。
Codex 卡面如實揭露，業主授權重釘，六哨兵隨即「吻合」——決定性回來了，健康度的屍體被一起埋掉。
**系統沒有一個位置能讓「8/12 崩城」擋住合併。**

### 進場探針（本卡覆核者親跑，單變數跑檯，seed301／400 天／標準難度／AI 開）

| 組別 | 內容 | pop@400 | 棟數 | 種類 | 路 | k5 電廠 |
|---|---|---:|---:|---:|---:|---:|
| A | 舊版 v11.186 | 6801 | 1065 | 74 | 726 | 4 |
| B | 現況 v11.188 | 288 | 309 | 21 | 175 | 0 |
| C | 新碼＋占地全回 1×1 | 6302 | 1073 | 70 | 716 | 4 |

A、B 精確復現官方兩組釘（6801/640249、288/4779）＝跑檯忠實。
**C 證明崩塌不是新程式路徑壞掉，而是為「建築＝一格」校準的 AI 常數在 3×3 世界失準。**
終局地圖仍有 3,567 空格、2,541 個合法 3×3 落點，但第 150 天起**接得上路網的合法 3×3 落點恆為 0**。
AI 常數失配的修復是**階段二**，本卡不碰。

## 1. 手法：單向地板（棘輪），與 PASS 棘輪同形

本專案其他的釘全都不是點值，而且都運作良好：

| 既有釘 | 形狀 | 內容增長時 |
|---|---|---|
| fp／SPR_PINS 1,383 鍵 | 逐項可稽核 diff | 不斷；產出「變更 N 鍵」供對帳 |
| T523 落筆 7500 | 天花板 | 只在真的越線才響 |
| PASS 棘輪 | 單調不減 | 只增不減，永不誤報 |
| ARCH 行數 ±10% | 帶 | 內容長大不響 |

**六哨兵是唯一一個用混沌終點精確值的，也是唯一一個註定每卡必斷的。**
本卡不改它（決定性職責保留），而是在**同一次模擬**上疊加一組單向地板：

1. `docs/HEALTH-PINS.json`——健康度地板的**單一來源**，含每項的 `floor`、`measured`（立釘當日實測）、
   `target`（v11.186 舊版實測，標示我們掉了多少）、`history`（每次調整的日期／方向／授權原話）。
2. `test_fixde.js` 新增 `healthFloor575()`：斷言 `actual >= floor`。
   **一律取用既有三顆哨兵城的同一次 400 天模擬**（量 `GV.stats()`，零額外模擬成本、零額外亂數）。
3. 量測項（全部來自 `stats()`，零掃描）：`pop`、`buildings`、`roads`、`zones`、`poweredBld`。

**為什麼地板設在 T574 之後的谷底而不是舊版高點**：地板設在 6801 會讓套件今天就紅、擋住一切合併，
那不是偵測器是路障。棘輪的語意是「**從今天起不准更差**」；階段二每修好一段就上調一次地板，
`target` 欄把 6801 這個目標留在檔案裡不讓它被忘記。

### 防止靜默下調

地板值只寫在 JSON、不寫在 test 檔（避免重釘時順手改掉）。三道守衛：

- **結構釘**：JSON 必須存在、三顆種子齊全、每項有 floor/measured/target/history。
- **溯源釘**：每個 `floor` 必須等於該項 `history` 最後一筆的 `value`——**下調地板必須留記錄才能通過**。
- **行為釘**：地板值真的被讀入並用於斷言（把 JSON 的 floor 調高一格必須立刻紅）。

## 2. 明確不動

`index.html` 一個位元不改（故無 bump／無 fp 變更／無 ARCH 行數變更）；`tools/` 不動
（**業主未授權碰閘門程式，本階段不需要**）；`seedPin444` 六個 expect 值不動；
既有任何斷言不刪不改；12 種子面板因套件 900 秒限時**不入套件**（記於 JSON 的 `panel_reference` 供階段二用）。

## 3. 驗收

- **G1 結構／溯源**：JSON 三種子齊全、欄位完整、floor === history 末筆 value。
- **G2 地板生效**：三顆哨兵城各五項 `actual >= floor` 全綠，並印 `HEALTHPIN` 行供人眼掃。
- **G3 真紅源（本卡的核心證明）**：把 runtime 突變成「更嚴重的崩塌」（`LOT_PLAN574` 的 plant 由 3 改 5），
  健康度地板**必須紅**——證明這根釘若在 T574 當時就存在，它會擋下那次合併。
- **G4 調高地板必紅**、**G5 抽掉 history 末筆必紅**、**G6 刪 JSON 種子條目必紅**。
- 全套 `verify.py` ALL GREEN、PASS 只增不減、toolchain 63 例全綠、六哨兵原值不動。

## 4. 順帶收掉的文件謊言

`docs/ARCH.md` 現況行本來就要因 PASS 變動而改；它目前寫著
「候選 v11.188…STOP: BROWSER_VERIFICATION…僅車位候選、退修未commit、未經非作者覆核、未合併／部署」，
但 master 早已是 7cb0dcd 且部署收據 `source_oid=7cb0dcd`（2026-08-31 13:38:59）。
`arch_map.py` 只驗表格錨點行號、T371b 只驗行數與版本字串，**沒有守衛讀這段散文**，所以閘門擋不住。
本卡把該行改成與 master 實況相符，並如實記錄它曾經失準。

## 5. 回滾

單卡單提交 revert；本卡零 runtime 差異，回滾不影響玩家端任何位元。

## 6. 階段路線（本卡之後）

- **階段二 AI 空間校準**（T576+）：逐顆失配常數修，每顆用單變數跑檯量效果，
  每修好一段就上調 HEALTH-PINS 地板。已量到的槓桿：電廠選用資金門檻（6.5×）、
  電廠接路半徑（2.2×）、電力類占地（9.6×）；`lotRoadLink574` 補路支線目前只接了地熱一個呼叫點。
- **階段三 文件欠帳**：帳本補記 T573/T574、GV-HOOKS 補 `__t574` 橋、T552 U+0008 失效正則
  （**觸碰既有斷言，需業主授權**）。
