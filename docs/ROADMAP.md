# 長期路線圖（ROADMAP）— v1.0 → v2.0 時代的規劃

> # ⚠️ 現況與本文件落差極大（T371 標註）
>
> 本文件最後更新於 v4.2 時代、最大卡號 T155。**現況是 v10.4、卡號至 T371**。
> 下面 v1.x～v4.x 的版本規劃**已全部走完**，保留作歷史記錄；
> 這份檔案現在的價值是「當時怎麼想的」，不是「接下來做什麼」。
>
> **現行待辦看這裡**（T371 當下實況，做完就從這裡劃掉）：
>
> | 待辦 | 出處 | 狀態 |
> |---|---|---|
> | T369／T369 退修／T369.1 的非作者覆核 | CHANGELOG 前三條 | 真欠，Kimi 待命 |
> | 簽核守衛（驗收欄具名覆核者＋master 不留「待」字＋攔作者自簽） | T371 提案 | 待現存條目清完 |
> | 編碼工具卡（`run_cmd` 帶 PYTHONIOENCODING/PYTHONUTF8＋故障注入） | T370 合併首輪被擋的根因 | 未開卡 |
> | 旋轉系統七處未補（地鐵 overlay 整層未過 w2v 最嚴重） | T367 卡面預期未實作的部分 | 未開卡 |
> | 空轉守衛清理（`test_fixde.js:413` 等開關無鑑別力） | T371 審計 | 未開卡 |
> | 大農場四塊作物 patch 浮空 22px／98.3% 出界 | CHANGELOG T353 | 已量測未修 |
> | k112 等大草坪建築無冬季相 | CHANGELOG T355 | 已量測未修 |
> | T343 科技樹（HOI4 國策樹形態） | 設計完成 | 未開工 |
>
---


> **功能對標總目錄見 `THEOTOWN-PARITY.md`**（TheoTown 有啥我們原創做啥，含逐項實作思路與版本歸屬）。
> 本文件管「執行順序與出卡」，對標文件管「做什麼與為什麼」。
> ~~施工模型分工：Sonnet 5（★~★★）、Opus 4.8（★★★與修復）、Kimi（外部按 SOP）；Fable 只出卡與把關。~~
> **T371 更正**：分工早已改為四方協作（Claude／Kimi／Codex／Grok），
> 且權責三分離（發卡只有業主／施工四方皆可／合併四方皆可但作者不自合）。權威見 `docs/COLLAB.md` 第八節。

> 原則：**近期卡寫死、遠期寫方向。** 任務卡必須對著「當時的代碼」撰寫才可靠，
> 所以每個版本開始前，回來讓強模型（Claude/Opus）按本文件末尾的模板出下一批卡。
> 排序邏輯：純增量內容 → 便利功能 → 新系統 → 世界擴展。風險低的先做，讓弱模型練手。

> **進度（2026-07-21 v4.2 收官更新）**：v1.1 ✅｜v1.2 ✅｜v1.3 ✅｜v1.4 ✅｜v1.5 ✅｜**v2.0 ✅**（tag `v2.0`）
> ｜T29–T61 ✅｜FIX-A/B/C/D/E/G/H/I/J ✅｜T39–T60 批次（部分，見 CHANGELOG）｜**T62–T100 v3.0 全部完成 ✅**（`GAME_VER=3.0`，逐卡實機冒煙通過：node --check 語法通過、test_fixde.js 380/380 PASS、瀏覽器 newWorld→鋪路分區電廠→tick 30 天不拋錯、Console 零錯誤，T100 收官複查）
> ｜**T101–T105 v3.1 圖鑑擴充波全部完成 ✅**（`GAME_VER=3.1`，住宅/商業/工業 4→8 變體、公園 3→6、樹 4→7、裝飾 3→8，逐卡驗證三件套全綠，T105 收官複查）
> ｜**T106–T115＋FIX-K~N v3.2 縮放軸＋玩法深度波全部完成 ✅**（`GAME_VER=3.2`，縮放範圍/縮放體感/遠景LOD 三卡＋RCI需求模型/污染擴散場/幸福歸因/升級多因子門檻/歷史曲線/通知中心/沙盒難度 七卡＋四項可重現性修復，逐卡驗證三件套全綠，T115 收官複查）
> ｜**T116–T122 v3.3 災害擴充＋地形＋政策＋交通波全部完成 ✅**（`GAME_VER=3.3`，龍捲風/隕石/暴亂三新災害＋地形筆刷＋法規開關＋服務車輛出勤框架＋公車路線 七卡，逐卡驗證三件套全綠，v3.3 收官複查）
> ｜**T123–T125 v3.4 住宅財富分級波全部完成 ✅**（`GAME_VER=3.4`，THETA 波三卡：資料判定（`judgeWealth`/`bld.we`）→經濟接入（稅收/幸福係數＋30天漸變重判＋統計占比面板）→外觀呈現（貧/富 sprite 色變＋小地圖微調），逐卡驗證三件套全綠，v3.4 收官複查）
> ｜**T126–T128 v3.5 建築細節與遮擋波全部完成 ✅**（`GAME_VER=3.5`，IOTA 波三卡：放置遮擋修復（玩家實測反饋，工具/選中建築前方半透明可透視）→摩天樓合併（2×2 住宅/商業塔 k=33/34，沿用多格建築架構機率合併）→建築細節密度提升（`detailPass` 屋頂雜物+立面件疊加），逐卡驗證三件套全綠，v3.5 收官複查）
> ｜**T129–T131 v3.6 交通壅堵＋地價＋資訊圖層波全部完成 ✅**（`GAME_VER=3.6`，KAPPA 波三卡：交通壅堵模擬（`roadLoad` 衰減式移動平均車流，接線車速折減/道路熱力繪製/幸福壅堵懲罰）→地價系統 LAND（服務覆蓋/景觀/污染/犯罪綜合場，接線生長機率/財富評分/住宅稅收/小地圖地價視圖）→資訊圖層與城市顧問（小地圖視圖擴為六種循環＋`cityAdvisor()` 彙總九類候選輸出 3-5 條建議、可點擊跳鏡頭），逐卡驗證三件套全綠，v3.6 收官複查）
> ｜**T132–T135 v3.7 場景戰役＋城市等級＋成就鏈＋編輯器波全部完成 ✅**（`GAME_VER=3.7`，LAMBDA 波四卡：場景／任務戰役模式（`SCENARIOS` 8 關七種目標型別＋選關選單＋通關持久記錄）→城市等級階梯（`RANKS` 26 級＋`cityPoints`/`rankIdx` 只升不降晉升）→成就擴充階層鏈（`ACHS` 8→34 條，含銅銀金三階鏈與隱藏成就＋`showAch()` 面板）→地圖/場景編輯器＋分享碼（`editMode` 免費建造＋目標設定 UI＋擴充版分享碼 `SHARE_VER='GVX1:'` 匯出入自製場景），逐卡驗證三件套全綠，v3.7 收官複查，超越計分卡判準 1-4 全部達成）
> ｜**T136–T139 v3.8 世界／規模波全部完成 ✅**（`GAME_VER=3.8`，MU 波四卡：真四季循環（`season()` 春/夏/秋/冬四值，農場產量／電力需求壓力／觀光係數三系統效果聯動，基準季=春退化為既有冬季二元行為等價）→災害擴充 6→10 種（瘟疫/乾旱/暴風雪沙暴/工業洩漏，皆因果觸發，`GV.disasters()` 可枚舉）→文化娛樂建築（k=35-41 博物館/劇院/水族館/動物園/遊樂園/電影院/圖書總館，建築型別 34→41）→民生科研建築（k=42-48 市政廳/法院/會展中心/研究院/氣象站/植物園/綜合醫院，建築型別 41→**48**），逐卡驗證三件套全綠，v3.8 收官複查，超越計分卡判準 10/11/12 全部達成，世界/規模軸 3/3 全數達標）
> ｜**T140–T142 v3.9 深度軸波全部完成 ✅**（`GAME_VER=3.9`，NU 波三卡：資源供應鏈（地圖稀缺資源層 `RESOURCE` 油/礦→開採建築 k=49-51 油井/礦場/太空研究中心抽取為 `supplies` 存量→工業消耗鏈提稅→終局 megaproject 消耗換大額回饋，四級鏈，建築型別 48→**51**）→通勤/就業 agent（`computeCommute()` 多源 BFS＋叢集快取，同時接線壅堵場 `roadPass` 與財富評分 `judgeWealth` 兩處）→教育場 `EDU`（比照 COV/POL/LAND 家族設計，學校/大學/圖書館覆蓋疊加，接線財富與高階工業產出），逐卡驗證三件套全綠，v3.9 收官複查，超越計分卡判準 7/8/9 全部達成，深度軸 5 項判準中 3 項全數達標（5、6 沿用 KAPPA 部分完成狀態））
> ｜**T143–T145 v4.0 UI/氛圍收官波全部完成 ✅**（`GAME_VER=4.0`，XI 波三卡：可視化因果追溯（T143，`traceOverlay`＋幸福構成/城市顧問面板可點擊追溯來源格）→環境音動態分層混音（T144，`AmbientBus` 街噪/人聲/機械/鳥鳴四層依交通/人口/工業/公園指標即時混音）→判準5-6誠實複審＋急救延遲聯動補全（T145，`updDispatch` 補接 `roadLoad` 速度折減，服務車輛壅堵下延遲到場但終究必達），逐卡驗證三件套全綠，v4.0 收官複查，超越計分卡判準 15/16 達成、5/6 由複審升級為 ✅，深度軸 5/5 全數達成）
> ｜**T146–T150 v4.1 美術收官波全部完成 ✅**（`GAME_VER=4.1`，OMICRON 波五卡：街道級細節層（T146，`SPR.roadDeco`＋座標決定性 `streetHash`，人行道邊石/斑馬線/標線/街道設施/停放車/行人）→建築細節層二期（T147，`detailPass2()` 風化/招牌/牆面痕跡/屋頂二期/陽台雜物）→手繪像素英雄建築（T148，`HERO_PIX`＋`drawHeroPix` 解碼，四座地標建築逐格原創手繪，本作首例手繪素材混合）→光影與氛圍（T149，地面光暈/方向性陰影/晨昏色調三層動態渲染，正午恆等於改動前）→變體大擴充（T150，住宅/商業/工業 8→12 變體、樹 7→10、公園 6→9），逐卡驗證三件套全綠＋各配 `__noXXX` 開關零位移證明，v4.1 收官複查，`index.html` 體積 +23.7%／`buildSprites` 生成耗時 14.0ms→17.4ms／`SPR` 鍵數三口徑皆上升，純美術投入不影響超越計分卡判準狀態）
> ｜**T151–T155 v4.2 美術收官波（二期）全部完成 ✅**（`GAME_VER=4.2`，PI 波五卡：手繪像素英雄建築二期（T151，沿用 T148 `HERO_PIX`/`drawHeroPix` 管線，消防局/學校/體育場/警局/醫院/博物館/劇院/機場 8 座服務建築手繪化，`HERO_PIX` 鍵數 6→14）→動畫細節層（T152，`SPR.flag`/`parkFtn`/`radar`/`windRotor`/`warnBlink`/`bird` 六組多幀 sprite＋樹梢搖曳/水面波光，`animOn` 統一開關，全程零 `R()`/`ri()` 消耗）→地形與自然美術（T153，草地紋理/自然散佈物/岸邊漸變帶/懸崖岩層紋理，`streetHash` 決定性挑選，`__noNature` 零位移證明）→天氣視覺升級（T154，雨落地水花/濕地反光/積雪漸進堆積/低機率霧天/暴風雪雪流線條，`wxUp` 統一開關，過程中修正一處暴風雪線條座標取模負數缺陷）→載具與人物變體（T155，廂型車/卡車/機車/救護車/三形態行人，`streetHash` 決定性選型，並補齊 T119 救護車自建立以來從未接上 `draw()` 繪製之缺口），逐卡驗證三件套全綠＋各配 `__noXXX` 開關零位移證明，v4.2 收官複查，`index.html` 體積較 v4.1 基準再 +22%／`buildSprites` 生成耗時同機同法均值 13.4ms→15.6ms／`SPR` 頂層家族 50→69、`SPR.bld` 256 不變（本波以覆蓋/多幀擴充為主）、遞迴葉節點（實際生成畫布數）916→1047，純美術投入不影響超越計分卡判準狀態）
> 剩餘候選：視角四向旋轉、多城市 regions、地面層離屏快取深化（T96 已做基礎版，深化仍可選）；超越計分卡判準 13/14（城市顧問/小地圖資訊圖層，KAPPA／T131 狀態沿用未重新稽核）與元判準 17 最終覆核，留待下一輪

## v1.1 內容與手感 ✅ 已完成 — 全部低風險純增量

| 卡 | 標題 | 難度 | 依賴 |
|----|------|------|------|
| T01 | 建築外觀第 4 變體 | ★ | 無 |
| T02 | 地圖裝飾物（岩石/花叢/蘆葦） | ★★ | 無 |
| T03 | 里程碑擴充＋成就系統 | ★★ | 無 |
| T04 | 收支明細統計面板 | ★★ | 無 |
| T05 | 環境音（晝鳥鳴/夜蟲鳴）＋三段音效開關 | ★★ | 無 |

## v1.2 品質與便利（開始前回來出卡）

- **小地圖**：右上角 144×144 canvas，畫地形色塊＋視窗框，點擊跳轉。低頻重繪（每 2s＋建設後）。
- **多存檔槽**：3 槽選擇 UI＋「匯出/匯入存檔字串」（方便手機備份）。存檔鍵改 `glimmerville.v1.slot{n}`，舊鍵自動遷入槽 1。
- **拆除保護**：Lv2 以上建築拆除需二次確認 toast。
- **觸控打磨**：長按（500ms）任意格＝檢視；工具列當前工具高亮加大；HUD 在 360px 寬換行驗證。
- **遊戲內說明頁**：❓ 按鈕彈出操作說明（開始畫面那段搬進來＋補充）。

### 併入候選（源自另一 AI 會話的構想，素材保存在 attic/，出卡時評估）

- **撤銷系統**（Ctrl+Z、退款、上限 40 步）：列入 v1.2 出卡評估。
- **匯出/匯入分享碼**（RLE+base64）：已涵蓋於 v1.2「多存檔槽」卡。
- **小鎮名字生成器**：attic/ 有 char-RNN 權重（118KB，太重、違反單檔輕量原則）；
  出卡時改評估「詞庫隨機組合」方案（前綴×後綴查表，幾百 bytes 即可）。
- **海拔與河流地形**（elev 0-3、下坡河流演算法、內陸懸崖）：大工程，移列 v1.4；
  需強模型出詳細卡並將存檔升 v2。

## v1.3 新系統（每個系統一張卡，互相獨立）

- **火災**：工業/老舊住宅小機率起火 → 蔓延 → 燒成廢墟格（可推平）。點擊滅火（收費）。
- **消防局**：新建築（服務類 k=6 起），半徑 8 內自動滅火；維護費。※先做火災再做消防局。
- **學校**：半徑內住宅升級機率 +50%，就業 +10。
- **貸款**：銀行面板，借 2000 分 20 天還 2400；同時只能一筆；破產保底改為提示貸款。
- ⚠️ 每個新建築種類都要：KNAME/LVNAME、COST、工具列按鈕、sprite（照 §5 管線）、canPlace/doPlace、tick 效果、存檔 bl 相容（k 值新增即可，格式不變）。

## v1.4 世界擴展

- **地形工具**：種樹 $5、填草 $20（沙→草）；美化玩法。
- **場景模式**：3 個挑戰關（如「200 天內人口 1000」「起始資金 800 翻十倍」），完成給稱號。場景定義用資料表驅動，不寫死邏輯。
- **公路升級**：高速路（車速×2、造價×3、不吸引住宅）——動 mask 渲染與 updCars，中高風險，放最後。

## v1.3 追加（對標城市服務，見 PARITY §二）
- 供水網路、警察+犯罪、醫療、教育（各一張卡，覆蓋半徑模式統一實作）

## v1.4 追加（對標世界與視覺）
- 天氣（雨/雪/閃電）、冬季換皮幀、垃圾處理、道路裝飾、高速路、體育場（首個 2×2 多格建築）

## v1.5 安卓化收尾

- **PWA**：manifest.json＋service worker 離線快取（此卡例外允許新增 2 個檔案）；「加到主畫面」全螢幕圖示。
- **效能巡檢**：低階手機實測；必要時地面層改離屏分塊快取（大工程，需強模型出詳細卡）。
- **打包 APK（可選）**：用 Capacitor 或 TWA 包殼上手機。此步在電腦上做，不是改遊戲代碼。

## v2.0 架構升級（需 Fable 詳細出卡）
- **資料驅動 drafts 重構**（PARITY §四：內容＝數據，之後加建築只改資料表）
- 攝影機四向旋轉評估、公車系統、多城市 regions 評估


## v3.0 收官批次（T62–T100）✅ 全部完成（2026-07-19 T100 收官驗收）

> 總表：`docs/tasks/T62-T100-總表.md`；單卡：`docs/tasks/T62-*.md` … `T100-*.md`
> 施工目標：收斂 T39–T60 半成品 + 品質/災害/政策/車隊/安卓手感 → 標記 **v3.0**
> 收官複查（T100）：39 張卡（T62–T100）逐一在 CHANGELOG 留有驗收記錄；T100 再跑一次標準三件套
> （node --check／test_fixde.js 380/380／瀏覽器 newWorld→鋪路分區電廠→tick 不拋錯＋零 Console 錯誤）全綠，版本標記 `GAME_VER='3.0'` 與 README/ARCH 一致。

| 段 | 卡 | 主題 |
|----|----|------|
| 工具與交通動畫 | T62–T66 | 分類工具列、鐵路/港/輕軌/救護車動畫 |
| 系統閉環 | T67–T73 | COV 擴充、政策、地震洪水、廢棄、住房、區域摘要 |
| 交通與經濟 | T74–T87 | 擁堵/單行/號誌/辦公/觀光/食物/再生能源/污水回收/高級服務 |
| 模式與打磨 | T88–T95 | 挑戰、統計、小地圖、說明、設定、存檔元資料、提示、平衡 |
| 效能與收官 | T96–T100 | 快取、品質檔、GV、ARCH、v3.0 標記 |

## v3.1 圖鑑擴充波（T101–T105）✅ 全部完成（2026-07-19 收官驗收）

> 目標：不動系統邏輯，純擴充既有分類的視覺變體值域，充實圖鑑豐富度。
> 單卡驗收記錄見 `docs/CHANGELOG.md`；逐卡皆通過標準三件套（node --check／test_fixde.js 380/380／瀏覽器實機 newWorld→放置→存讀檔→零 Console 錯誤），並以雙份程式碼比對法確認既有 sprite 的共用亂數流未被位移。

| 卡 | 主題 | 變化 |
|----|------|------|
| T101 | 住宅圖鑑擴充 | `PAL[1]`/`DRAFTS(k=1)` 4→8 變體 |
| T102 | 商業圖鑑擴充 | `PAL[2]`/`SIGNC`/`DRAFTS(k=2)` 4→8 變體 |
| T103 | 工業圖鑑擴充 | `PAL[3]`/`DRAFTS(k=3)` 4→8 變體 |
| T104 | 公園與樹木圖鑑擴充 | `SPR.park` 3→6、`SPR.tree` 4→7（含冬季 parkW/treeW 同步） |
| T105 | 裝飾圖鑑擴充 | `SPR.deco` 3→8（新增石燈籠夜光、噴泉 2 幀動畫、小雕像、花壇、告示牌） |

收官複查（T105）：版本標記 `GAME_VER='3.1'`，node --check 語法通過、test_fixde.js 380/380 PASS、瀏覽器 8186/8187（槽3）確認 title 顯示 v3.1、舊 3.0 存檔正常載入、零 Console 錯誤。

## v3.2 縮放軸＋玩法深度波（T106–T115＋FIX-K~N）✅ 全部完成（2026-07-19 收官驗收）

> 目標：DELTA 波（三卡）把攝影機縮放軸擴展為可平滑操作的連續體感；EPSILON 波（七卡）把既有黑箱數值（需求/污染/幸福/升級）攤開成玩家看得懂、能決策的系統，並補歷史回顧與難度分層。單卡驗收記錄見 `docs/CHANGELOG.md`；逐卡皆通過標準三件套（node --check／test_fixde.js 380/380／瀏覽器實機 newWorld→放置→存讀檔→零 Console 錯誤），高風險卡（T106-T108 縮放、T110-T111 黑箱重構）額外做像素/數值回歸位元比對確認零漂移。

### 縮放軸（DELTA，T106–T108）

| 卡 | 主題 | 變化 |
|----|------|------|
| T106 | 縮放範圍擴展 | `cam.z` 由整數 1..4 改乘法階梯 `ZOOMS=[0.25,0.35,0.5,0.7,1,1.4,2,2.8,4]`（√2 步進）＋`zoomIdx()`；`clampCam` 遠檔位邊界放寬 |
| T107 | 遠景 LOD | `z<1`／`z<0.5` 兩級裁剪繪製（地面實色菱形、車/煙個體不畫、建築退化色塊），只裁「畫」不裁「算」 |
| T108 | 縮放體感 | `zoomAnim` 換檔緩動（~150ms easeOutQuad）＋捏合連續縮放（`pinchBase` 比例式）＋單擊/雙指雙擊快捷縮放 |

### 玩法深度（EPSILON，T109–T115）

| 卡 | 主題 | 變化 |
|----|------|------|
| T109 | RCI 需求模型 | `dem[1/2/3]` 三值公式升級（就業盈餘×幸福加權／人口商業格數比／商業貨源缺口），HUD 需求條改逐條數值 tooltip |
| T110 | 污染擴散場 | 新增 `POL`/`POLBASE`/`POLTREE` 場家族（工業/電廠/垃圾場/機場為源，半徑衰減，樹木減免），接入幸福公式與小地圖熱度視圖 |
| T111 | 幸福歸因面板 | 幸福計算拆成 `happyParts`/`happyAgg`，統計面板新增「幸福構成」明細＋最負項標紅對症文案 |
| T112 | 歷史曲線 | `hist` 環形緩衝（365 天）記錄 money/net/pop/happy，統計面板新增折線圖＋30/90/365 天範圍切換 |
| T113 | 升級多因子門檻 | lv1→2 需警局覆蓋、lv2→3 需學校覆蓋＋低污染，`inspect()` 新增「🔒 升級還缺」缺失清單 |
| T114 | 通知中心 | `toast()` 帶座標事件寫入 `log` 日誌，HUD 🔔 按鈕＋未讀紅點，面板可點擊項跳轉鏡頭 |
| T115 | 沙盒與難度 | 開始畫面難度列（簡單/標準/困難/沙盒），`diff` 全域控制起始資金/災害機率/沙盒免費建造與稅收維護費短路 |

### 可重現性修復（FIX-K~N，隨波同批發現同批修復）

| 卡 | 主題 |
|----|------|
| FIX-K | `newWorld()` 補天氣/災害/升星殘留狀態重置（同種子跨分頁不可重現） |
| FIX-L | `load()` 補天氣/災害殘留狀態重置（同存檔跨分頁不可重現） |
| FIX-M | `load()` cam.z 就近落 T106 階梯＋`newWorld`/`load` 清殘留 `zoomAnim`（T108 已知限制收尾） |
| FIX-N | `newWorld()` 內建 `hist`/`log`/`unread` 成對重置（鐵律7，避免🆕殘留歷史曲線/通知日誌） |

收官複查（T115）：版本標記 `GAME_VER='3.2'`，node --check 語法通過、test_fixde.js 380/380 PASS、瀏覽器（槽3）確認 title 顯示 v3.2、舊 3.1 存檔正常載入、零 Console 錯誤。

## v3.3 災害擴充＋地形＋政策＋交通波（T116–T122）✅ 全部完成（2026-07-19 收官驗收）

> 目標：ZETA 波（三卡）補齊龍捲風/隕石/暴亂三種新災害，把「五種災害」擴充為完整六種；ETA 波（四卡）補地形編輯筆刷、非稅收城市法規開關、服務車輛出勤框架（消防車/垃圾車接上既有救護車派遣抽象）、公車路線系統（交通從調味到策略）。單卡驗收記錄見 `docs/CHANGELOG.md`；逐卡皆通過標準三件套（node --check／test_fixde.js 380/380／瀏覽器實機 newWorld→放置→存讀檔→零 Console 錯誤），高風險卡（T119 派遣抽象重構、T121 地形函式抽出）額外做位元/數值回歸比對確認零漂移。

### 災害擴充（ZETA，T116–T118）

| 卡 | 主題 | 變化 |
|----|------|------|
| T116 | 龍捲風災害 | 新全域 `tornado`（移動實體，`{x,y,dx,dy,life}`，不進存檔），逐日沿方向前進 1 格摧毀建築/樹木，`SPR.tornado[0..2]` 三幀漏斗雲動畫 |
| T117 | 隕石災害 | 共用函式 `meteorStrike(cx,cy)`，中心格轉新 tile 欄位 `crater`（與 `ruin` 互斥），存檔新增可選欄位 `ctr`（per-tile 字元串），坑期間每日對鄰域擲火 |
| T118 | 暴亂事件 | 新全域 `riot`（`{cells,days}` 或 null，存檔可選欄位 `riot`），高犯罪住宅簇觸發，住宅停稅＋封路，警局覆蓋機率自然平息，`showStats` 鎮壓按鈕 $200 |

### 地形與政策（ETA，T119–T122）

| 卡 | 主題 | 變化 |
|----|------|------|
| T119 | 服務車輛出勤框架 | 抽象函式 `updDispatch(arr,dt,speed,cap,sts,cand,onArrive)`（from/target/onArrive 三段式），原救護車邏輯原樣搬入零回歸；消防車/垃圾車接上同一框架，垃圾車另新增 `computeGarbLocal()` 路距懲罰 |
| T120 | 公車路線系統 | 新全域 `busRoutes`（固定 3 條、每條最多 8 站，存檔可選欄位 `bus_rt`），路線工具＋沿站序往返行駛（`rbuses`/`updRouteBuses`），路線覆蓋住宅折抵小車生成率 |
| T121 | 地形編輯筆刷 | 挖水/填土/抬升削平三工具，`computeFoam()`/`computeElMask()` 逐格演算抽成 `recalcFoamAt(x,y)`/`recalcElMaskAt(x,y)`（結構性重構、零邏輯改動，位元回歸驗證），1×1/3×3 筆刷 |
| T122 | 城市法規開關 | `pol` 政策物件擴四布林 `curfew`/`recycle`/`tourPromo`/`ecoReg`（非稅收槓桿：犯罪率/垃圾產量/觀光稅/電廠容量），全關回歸基準 30 日序列逐值比對零漂移 |

收官複查（v3.3）：版本標記 `GAME_VER='3.3'`，node --check 語法通過、test_fixde.js 380/380 PASS、瀏覽器（槽3）確認 title 顯示「微光小鎮 v3.3」、舊 3.2 存檔（缺 `ctr`/`riot`/`bus_rt`/`pol` 四法規欄位）正常載入且默認值正確（`busRoutes` 三條空路線、`riot`/`crater` 全域函式存在），推進 100 天不拋錯，零 Console 錯誤，全程未觸碰 s1/s2。

## v3.4 住宅財富分級波（THETA，T123–T125）✅ 全部完成（2026-07-19 收官驗收）

> 目標：把住宅從單一同質類別拆出「貧/中/富」三級財富軸，讓城市服務覆蓋（警局/學校/醫院/公園）與污染/犯罪環境真正影響居民階層分布，並讓分層在稅收、幸福、外觀三處都看得見、有反饋。三卡單向依賴（資料→經濟→外觀），單卡驗收記錄見 `docs/CHANGELOG.md`；逐卡皆通過標準三件套（node --check／test_fixde.js 380/380／瀏覽器實機 newWorld→放置→存讀檔→零 Console 錯誤），並對「全城中產」基準做位元/數值回歸比對確認零漂移。

| 卡 | 主題 | 變化 |
|----|------|------|
| T123 | 財富分級（一）資料與判定 | 新函式 `judgeWealth(x,y)`（服務覆蓋計數－污染－犯罪加權評分，`score>=2`→2富／`score>0`→1中／其餘→0貧），`bld.we` 於住宅新建與升級時寫入；`WEALTH_NAME`/`WEALTH_ICON` 徽記；`inspect()` 顯示財富徽記；GV 新增 `wealthAt(x,y)`；存檔 `bl` 尾端第 8 位可選欄位（僅非中產才落盤），舊檔缺欄位缺省 1 中 |
| T124 | 財富分級（二）經濟接入 | `WEALTH_TAX=[0.6,1,1.5]`／`WEALTH_PEN=[0.5,1,2]`（貧/中/富）接入住宅稅收與 `happyParts` 空氣污染/犯罪兩項幸福懲罰；每 30 天全城住宅重判財富級（`judgeWealth` 與現值比較，一次最多漸變一級）；`showStats` 新增「🏚️ 貧X%　🏠 中X%　🏡 富X%」人口占比面板 |
| T125 | 財富分級（三）外觀呈現 | `buildSprites` 絕對尾端為 24 組 k=1 `(lv,v)` 各生成貧/富色變（鍵名 `1_lv_v_w0`/`1_lv_v_w2`，共 48 鍵：富＝提亮牆/屋頂色＋天線＋小花圃，貧＝降飽和牆/屋頂色＋牆面補丁），新函式 `wealthSpr(baseKey,we)` 缺鍵/壞值一律回退中產；`draw()` 住宅選鍵改呼叫 `wealthSpr`；`drawMini()` 小地圖依 we 微調明度；中產外觀與既有 `SPR.bld['k_lv_v']` 鍵位元零改動 |

收官複查（v3.4）：版本標記 `GAME_VER='3.4'`，node --check 語法通過、test_fixde.js 380/380 PASS、瀏覽器（槽3）確認 title 顯示「微光小鎮 v3.4」、`GV.ver()` 回傳 `'3.4'`；舊 3.3 存檔（bl 記錄缺第 8 位 we 欄位）正常載入，住宅財富級正確缺省為 1 中（人工構造「v3.4 記錄含 we=0 貧」→「截短還原 v3.3 格式」雙向往返驗證，載入前後皆不拋錯）；`GV.advanceN` 推進 100+ 天不拋錯，零 Console 錯誤，全程未觸碰 s1/s2。

## v3.5 建築細節與遮擋波（IOTA，T126–T128）✅ 全部完成（2026-07-19 收官驗收）

> 目標：三卡皆源自玩家實測反饋與視覺打磨——放置/選中時前方建築遮擋看不到目標格（T126）、住宅/商業長期發展缺乏「地標感」（T127 摩天樓合併）、建築外觀密度偏低顯得單薄（T128 屋頂雜物/立面件）。三卡互相獨立、各自純增量。單卡驗收記錄見 `docs/CHANGELOG.md`；逐卡皆通過標準三件套（node --check／test_fixde.js 380/380／瀏覽器實機 newWorld→放置→存讀檔→零 Console 錯誤），T127（多格建築合併）與 T128（sprite 尾端疊加）額外做位元回歸/像素越界比對確認零漂移。

| 卡 | 主題 | 變化 |
|----|------|------|
| T126 | 放置遮擋修復 | 建造/分區/拆除工具啟用中或檢視工具選中建築後，遮擋目標格/選中建築的前方物件半透明可透視（`ctx.globalAlpha=.35`）；新全域 `selTile`（選中建築格，`inspect()`/`hideInfo()` 維護）；無工具無選中時 `occTargets` 為空、逐像素零改動 |
| T127 | 摩天樓合併 | 2×2 全 lv3 同類分區住宅/商業建築，滿足供電供水無火＋城市快樂度/需求雙門檻，每日機率合併為 k=33 住宅摩天樓／k=34 商業摩天樓（沿用 v3.0 多格建築 root+ref+sz 架構），`TOWER_POP=248`/`TOWER_JOBS=175`（×1.15 加成） |
| T128 | 建築細節密度提升 | `buildSprites` 絕對尾端 `detailPass()` 疊加屋頂雜物（空調外機/通風管/儲物箱/太陽能板）與立面件（陽台欄杆/窗台花箱/曬衣繩/店招燈箱），密度依 `DENS[lv]` 遞增，`window.__noDetail` 除錯開關，不新增 `SPR` 鍵、不越出建築剪影 |

收官複查（v3.5）：版本標記 `GAME_VER='3.5'`，node --check 語法通過、test_fixde.js 380/380 PASS、瀏覽器（槽3）確認 title 顯示「微光小鎮 v3.5」、`GV.ver()` 回傳 `'3.5'`；舊 3.4 存檔（bl 記錄無 k=33/34 摩天樓、無 `selTile`/`detailPass` 相關存檔欄位——本波三卡皆不新增存檔欄位）正常載入，零 Console 錯誤，`GV.advanceN` 推進 100+ 天不拋錯，全程未觸碰 s1/s2。`docs/ARCH.md` 補齊 T126 放置遮擋透明機制段落（先前 T127/T128 個別卡驗收時已自行補入 ARCH，T126 遺漏於本次收官補齊，只增不改舊文）。

## v3.6 交通壅堵＋地價＋資訊圖層波（KAPPA，T129–T131）✅ 全部完成（2026-07-20 收官驗收）

> 目標：把交通從「純動畫」升級為會回饋模擬的壅堵場，把地價從隱性概念變成統一環境因子的可視場，並用一組小地圖疊加視圖＋城市顧問面板把散落的模擬場（COV/POL/LAND/財富/壅堵/犯罪）彙總成玩家看得懂、能決策的資訊層。三卡單向依賴（壅堵場→地價場讀壅堵→資訊圖層讀壅堵+地價+既有場），單卡驗收記錄見 `docs/CHANGELOG.md`；逐卡皆通過標準三件套（node --check／test_fixde.js 380/380／瀏覽器實機 newWorld→放置→存讀檔→零 Console 錯誤），T129/T130（新運行時場接入生長/稅收/幸福公式）與 T131（彙總層）皆額外做位元回歸（全空地圖／有道路無建築／自然生長三種情境，同種子對照改動前後 `GV.stats()`＋全圖 tile 雜湊逐位元組比對）確認零漂移。

| 卡 | 主題 | 變化 |
|----|------|------|
| T129 | 交通壅堵模擬 | 新運行時場 `roadLoad`（`Float32Array`，衰減式移動平均車流）＋日累加器 `roadPass`（`Uint16Array`，`updCars` 切格瞬間遞增，皆不入存檔）；`ROAD_CAP`/`ROAD_JAM_K` 接線車速折減（`load≤cap` 恆等 ×1）、道路熱力繪製（`load>cap` 疊黃→紅）、`happyParts` 新增壅堵懲罰項；新函式 `congestNear(x,y,r)`；GV 新增 `loadAt`/`roadCapAt`/`setLoadAt` |
| T130 | 地價系統 LAND | 新場 `LAND`/`LANDBASE`（比照 COV/POL 家族設計，128 中性基準，不入存檔）；`landStaticAt` 綜合服務覆蓋＋景觀（僅 `COV.park`）－污染－犯罪；`landCongestAt` 讀 T129 `roadLoad` 疊加動態壅堵懲罰；四處接線：生長機率／`judgeWealth` 財富評分／住宅稅收／小地圖地價視圖（LAND=128 時四處係數精確恆等 ×1／+0）；GV 新增 `landAt` |
| T131 | 資訊圖層與顧問 | 小地圖視圖由三段擴為六段循環（一般/地價/污染/壅堵/犯罪熱點/服務覆蓋），新常數 `MINI_VIEW_NAME`/`MINI_VIEW_ICON`；新函式 `cityAdvisor()`（`showStats()` 開啟時單次全圖掃描，不掛 tick/draw）彙總服務缺口/污染熱點/犯罪熱點/壅堵熱點/RCI 缺口/財政趨勢九類候選，排序取前 3-5 條、最嚴重標紅、帶座標可點擊跳鏡頭，空清單回退空態文案 |

收官複查（v3.6）：版本標記 `GAME_VER='3.6'`，node --check 語法通過、test_fixde.js 380/380 PASS、瀏覽器（槽3）確認 title 顯示「微光小鎮 v3.6」、`GV.ver()` 回傳 `'3.6'`；舊 3.5 存檔（bl 記錄無 T129-T131 相關可選欄位——本波三卡皆不新增存檔欄位，`roadLoad`/`roadPass`/`LAND`/`LANDBASE` 純運行時場，`newWorld`/`load` 皆歸零／重建）正常載入，零 Console 錯誤，`GV.advanceN` 推進 100+ 天不拋錯，全程未觸碰 s1/s2。

## v3.7 場景戰役＋城市等級＋成就鏈＋編輯器波（LAMBDA，T132–T135）✅ 全部完成（2026-07-20 收官驗收）

> 目標：LAMBDA 波聚焦「模式軸」，一次性打齊超越計分卡判準 1-4（場景/任務戰役、地圖/場景編輯器＋分享碼、城市等級階梯、成就銅銀金階層鏈）——把微光小鎮從「單一沙盒模擬」擴展成「有勝負判定的戰役關卡＋長線進度曲線＋玩家自製內容」的完整遊戲模式集合。四卡大致單向依賴（T132 場景框架→T133 等級讀城市指標／T134 成就交叉 T132 通關記錄與 T133 等級→T135 編輯器複用 T132 場景判定管線＋擴充版分享碼），單卡驗收記錄見 `docs/CHANGELOG.md`；逐卡皆通過標準三件套（node --check／test_fixde.js 380/380／瀏覽器實機 newWorld→放置→存讀檔→零 Console 錯誤），四卡皆額外做位元回歸（Node harness 對照改動前 HEAD 版本，固定種子多情境跑 30+ 天，`GV.stats()`＋全圖 tile 雜湊逐位元組比對）確認對既有模擬零副作用。

| 卡 | 主題 | 變化 |
|----|------|------|
| T132 | 場景／任務戰役模式 | 新 `SCENARIOS` 資料表（8 關，pop/cash/star/happy/debt/disaster/coverage 七種目標型別，含開場/勝利/失敗敘事文案）；新全域 `scenario`（進行中場景）/`pendingScenario`（選關暫存）/`scDone`（已通關 id 集合，落 localStorage 獨立鍵，跨局/跨存檔持久）；開始畫面新增「🎯 戰役」選關選單（依 `scDone` 算鎖定/已通關/可選三態）；`tick()` 新增場景判定區塊（`scenarioGoalMet`/`scenarioFailMet`），達標發獎勵金＋寫入 `scDone`＋勝利 toast，逾期未達標則失敗 toast；存檔新增可選欄位 `sc` |
| T133 | 城市等級階梯 | 新 `RANKS` 資料表（26 級，`{name,threshold[,unlock]}` 門檻嚴格遞增 0~29750，4 級掛未來版本解鎖文案）；新全域 `rankIdx`（只升不降的晉升進度）/`cityPoints`（不進存檔，`tick()` 每日重算：人口＋幸福＋服務覆蓋率加權彙總）；新函式 `computeCityPoints()`（唯讀掃描，不消耗 `R()`）；`tick()` 新增等級 while-ratchet 晉升 toast「🏙️ {鎮名}升至 Lv.{N} {級名}！」；`showStats()` 新增等級進度條區塊；存檔新增可選欄位 `rk`；預留 `unlockRank` 建築解鎖掛點框架（本卡未鎖任何既有建築，全部 34 種在任何等級皆可放） |
| T134 | 成就擴充階層鏈 | `ACHS` 由 8 條擴至 34 條（原 8 條 id/nm/t 逐字未動）；新增 3 條銅／銀／金三階鏈（`metro` 人口 1,000/10,000/100,000、`civic` 城市等級 Lv.6/13/26 交叉 T133 `rankIdx`、`svc` 服務設施種類數 6/11/16）與 17 條單一成就（含 7 條隱藏成就，`saga1`/`saga_all` 交叉 T132 `scDone` 場景戰役通關記錄），共 7 個分類；新 `ACH_META`（id→顯示中繼資料，不動原始 8 條資料）；新成就面板 `showAch()`（🏆 按鈕，分類顯示／雙層進度條／銅銀金徽記／隱藏成就未解鎖顯示？？？占位）；存檔格式零變更（`ach` 仍是既有 id 陣列格式，34 條定義天然向後相容） |
| T135 | 地圖／場景編輯器＋分享碼 | 新編輯器模式旗標 `editMode`（`newWorld`/`load` 成對重置為 false，鐵律7；複用既有 T115 沙盒 `diff=3` 短路免費建造，不新增經濟分支）；開始畫面新增「🛠 編輯器」入口＋ HUD「🛠 目標」按鈕開 `showEditor()` 面板（6 種目標型別、起始資金／期限天數設定、匯出／離開）；分享碼機制擴充新版本前綴 `SHARE_VER='GVX1:'`（帶目標包 `egoal`，`importShareCode` 匯入時剔除 `egoal` 才寫回常規存檔，硬性不變量：編輯器產物不侵入常規存檔；舊版無前綴分享碼天然向後相容）；匯入自製場景後 `tick()` 場景判定區塊改讀 `scenario.custom?customScenario:SCENARIOS.find(...)` 複用 T132 同一套判定/toast 管線，達標/逾期後不寫入官方 `scDone`；存檔本體（`save()`/`load()`）零改動 |

收官複查（v3.7）：版本標記 `GAME_VER='3.7'`，node --check 語法通過、test_fixde.js 380/380 PASS、瀏覽器（槽3）確認 title 顯示「微光小鎮 v3.7」、`GV.ver()` 回傳 `'3.7'`；舊 3.6 存檔（`bl` 記錄無 `sc`/`rk` 欄位——`scenario` 缺欄位回一般模式、`rankIdx` 缺欄位由當前城市狀態即時算出合理等級（不因缺欄位降級 Lv.1）、`ach` 沿用既有已解鎖集合、34 條新成就定義未誤標舊檔已解鎖）正常載入，零 Console 錯誤，`GV.advanceN` 推進 100+ 天不拋錯，全程未觸碰 s1/s2。超越計分卡（`docs/SURPASS.md`）判準 1（場景/任務戰役）、2（地圖/場景編輯器＋分享碼）、3（城市等級 ≥24 級）、4（成就 ≥30 含銅銀金）全部達成，模式軸 4 項判準全通過。

## v3.8 世界／規模波（MU，T136–T139）✅ 全部完成（2026-07-20 收官驗收）

> 目標：MU 波聚焦「世界／規模軸」，一次性打齊超越計分卡判準 10-12（真四季、災害 ≥10、建築機制相異型別 ≥48）——把微光小鎮的世界模擬從「二元冬季濾鏡＋六種災害＋34 種建築」擴展為「系統聯動四季＋十種因果災害＋48 種機制相異建築型別」的完整世界規模。四卡大致單向依賴（T136 四季基礎→T137 災害擴充讀 T136 `season()` 作季節性觸發條件→T138/T139 建築型別擴充彼此獨立、皆延續既有多格建築架構，T139 前置 T138 同架構），單卡驗收記錄見 `docs/CHANGELOG.md`；逐卡皆通過標準三件套（node --check／test_fixde.js 380/380／瀏覽器實機 newWorld→放置→存讀檔→零 Console 錯誤），四卡皆額外做位元回歸（Node harness 對照改動前基準版本，固定種子多情境跑 100-500+ 天，`GV.stats()`＋全圖 tile 序列化雜湊比對）確認對既有模擬零副作用。

| 卡 | 主題 | 變化 |
|----|------|------|
| T136 | 真四季循環 | 新函式 `season()`（由 `day` 導出 0-3 春/夏/秋/冬，`season()===3` 與既有 `inWinter()` 精確同義），三系統效果乘數表 `FARM_SEASON_MULT`/`POWER_SEASON_MULT`/`TOUR_SEASON_MULT`（皆以春＝基準季、乘數精確為 1）接線農場產量／供電容量壓力／觀光係數；`buildSprites` 尾端 `tintCanvas()` 疊色衍生 `SPR.grassS/A`／`SPR.treeS/A` 四季地面色與樹色調；`AudioComposer.pickMood()` 新增 `summer`/`autumn` 情緒分支；存檔零新增欄位（season 純由 day 導出） |
| T137 | 災害擴充至十種 | 新增瘟疫（`plague`，住宅簇密度+醫療覆蓋缺口因果觸發，健康設施平息，`plagueBtn` 隔離）、乾旱（`drought`，夏季觸發，壓供水上限＋抬火險，季末自動解除）、暴風雪/沙暴（`blizzard`，冬/夏觸發，全屏疊色＋癱瘓車速）、工業洩漏（`leakDays`，工業起火因果觸發，`startLeak` 疊加 POL 尖峰持續 6 天）；`GV.disasters()` 列舉十種災害＋開關狀態；存檔新增可選欄位 `plague`，`drought`/`blizzard`/`leakDays` 皆運行時狀態不入存檔 |
| T138 | 文化娛樂建築 | 新增 k=35-41 共 7 種（博物館2×2/劇院2×2/水族館2×2/動物園3×3/遊樂園3×3/電影院1×1/圖書總館2×2），建築機制相異型別 34→41；沿用既有多格建築 root+ref+MSZ 通用機制零改動；博物館/劇院/電影院＝COV 覆蓋半徑驅動 happyParts＋觀光係數，水族館/動物園/遊樂園＝純觀光強係數，圖書總館＝COV 半徑12 大範圍教育加成（`glBoost`，融入既有 `eduBoost` 鏈）；經濟每個新 k 顯式 else-if 分支（防幽靈稅） |
| T139 | 民生科研建築 | 新增 k=42-48 共 7 種（市政廳2×2/法院2×2/會展中心3×3/研究院2×2/氣象站1×1/植物園3×3/綜合醫院3×3），建築機制相異型別 41→**48**（判準 12 達標）；七種效果各自機制相異——市政廳全城稅收微增 `civicMul`、法院降犯罪配合警局、會展中心非連續觀光脈衝（`day%20===0`）、研究院科技加成不限住宅（`instituteBoost`）、氣象站季節/災害預警 toast、植物園大範圍幸福+觀光、綜合醫院半徑18超大覆蓋（比照 T95 教訓刻意壓低效益/成本比，造價2800/維護16 遠高於醫院600/5，避免碾壓既有醫療建築） |

收官複查（v3.8）：版本標記 `GAME_VER='3.8'`，node --check 語法通過、test_fixde.js 380/380 PASS、瀏覽器（槽3）確認 title 顯示「微光小鎮 v3.8」、`GV.ver()` 回傳 `'3.8'`；舊 3.7 存檔（`bl` 記錄無 `plague` 欄位、無 k=35-48 任何新建築、`season` 純由 day 導出無需欄位——本波僅 T137 新增 `plague` 一個可選欄位，`load()` 缺欄位視為無瘟疫、`drought`/`blizzard`/`leakDays` 皆運行時狀態自然歸零、`scenario`/`rankIdx`/`ach`/`editMode` 等既有 v3.7 欄位不受影響）正常載入，零 Console 錯誤，`GV.advanceN` 推進 100+ 天不拋錯，全程未觸碰 s1/s2。超越計分卡（`docs/SURPASS.md`）判準 10（真四季，農業/能源/觀光三系統效果）、11（災害 ≥10 皆因果觸發）、12（建築機制相異型別 ≥48）全部達成，世界/規模軸 3 項判準全通過（3/3）。

## v3.9 深度軸波（NU，T140–T142）✅ 全部完成（2026-07-20 收官驗收）

> 目標：NU 波聚焦「深度軸」，補齊超越計分卡判準 7-9（資源供應鏈 ≥3 級＋地圖稀缺資源、通勤/就業 agent 影響壅堵與財富、模擬場層數 ≥22）——把微光小鎮從「城市服務覆蓋場（COV/POL/LAND）已具雛形」推進到「有真正資源稀缺約束、通勤成本回饋壅堵與貧富、勞動力素質分層」的深度模擬。三卡大致獨立、皆延續既有場家族設計慣例（T140/T141/T142 分別比照 COV/POL/LAND 與 T119/T129 場/agent 先例），單卡驗收記錄見 `docs/CHANGELOG.md`；逐卡皆通過標準三件套（node --check／test_fixde.js 380/380／瀏覽器實機 newWorld→放置→存讀檔→零 Console 錯誤），三卡皆額外做位元回歸（Node harness 對照改動前基準版本，固定種子多情境跑 30-500+ 天，`GV.stats()`＋全圖 tile 序列化雜湊比對）確認對既有模擬零副作用。

| 卡 | 主題 | 變化 |
|----|------|------|
| T140 | 資源供應鏈 | 新地圖稀缺資源層 `RESOURCE`（油/礦，種子確定性鋪設地理集中斑塊，不入存檔）＋開採建築 k=49油井/50礦場/51太空研究中心（3×3），建築機制相異型別 48→**51**；四級資源鏈——資源層→開採抽取 `supplies` 存量（`RDEP` 耗損稀疏可選存檔欄位 `rdep`）→工業消耗鏈提升工業稅收（`indSupplyMul`）→太空研究中心週期性消耗 `supplies` 換大額回饋（`MEGAPROJECT_REWARD`），對標 TheoTown 油/礦→Supplies→DSA 判準7 |
| T141 | 通勤/就業 agent | 新函式 `computeCommute()`（每 `COMMUTE_PERIOD` 天多源 BFS＋叢集快取，控制大城效能），住宅依路距最近就業區（商業/工業分區）分「過長／不可達」兩級懲罰，同時接線兩處：貢獻 `roadPass`（與 T129 壅堵場耦合）＋`judgeWealth` 新增 `commuteTerm`（財富評分），滿足判準8「通勤同時影響壅堵與財富」 |
| T142 | 教育場 EDU | 新場 `EDU`（比照 COV/POL/LAND 家族設計，學校/大學/圖書館覆蓋加權疊加，`rebuildCov` 尾端全量重建、不入存檔），接線 `judgeWealth`（受教勞動力→易判富）與高階工業（lv3）產出品質係數；補上模擬場層數判準9所需的最後一層 |

收官複查（v3.9）：版本標記 `GAME_VER='3.9'`，node --check 語法通過、test_fixde.js 380/380 PASS、瀏覽器（槽3）確認 title 顯示「微光小鎮 v3.9」、`GV.ver()` 回傳 `'3.9'`；舊 3.8 存檔（`bl` 記錄無 k=49-51 任何新建築、無 `sup`/`rdep` 欄位——本波僅 T140 新增此兩個可選欄位，`load()` 缺欄位視為供應品 0／全數未耗損，`RESOURCE`/`commuteLoad`/`EDU` 等運行時場皆由種子/COV 自動重建，`plague`/`scenario`/`rankIdx`/`ach`/`editMode` 等既有欄位不受影響）正常載入，零 Console 錯誤，`GV.advanceN` 推進 100+ 天不拋錯，全程未觸碰 s1/s2。超越計分卡（`docs/SURPASS.md`）判準 7（資源供應鏈 ≥3 級＋地圖稀缺資源）、8（通勤/就業 agent 影響壅堵與財富）、9（模擬場層數 ≥22）全部達成，深度軸（判準5-9）由 0/5 推進至 3/5 全數達成（5、6 沿用 KAPPA／v3.6 部分完成狀態，本波未重新稽核）。

## v4.0 UI／氛圍收官波（XI，T143–T145）✅ 全部完成（2026-07-20 收官驗收）

> 目標：XI 波聚焦「UI／氛圍收官」，補齊超越計分卡判準 15-16（可視化因果追溯、環境音動態混音），並回頭對深度軸判準 5-6（KAPPA／T129-T130 遺留的「持久場接線是否完整」）做一次誠實複審——把微光小鎮的收官波從「新系統堆疊」轉為「補完既有系統的承諾缺口」。三卡大致獨立（T143/T144 各自純增量疊加層，T145 為回頭複審＋最小補接），單卡驗收記錄見 `docs/CHANGELOG.md`；逐卡皆通過標準三件套（node --check／test_fixde.js 380/380／瀏覽器實機 newWorld→放置→存讀檔→零 Console 錯誤），T145 額外做位元回歸（Node harness 對照改動前基準版本，同種子含救護站+道路+存讀檔注入生病住宅情境推進 23 天，`GV.stats()` 全欄位逐值比對）確認補接的速度折減公式在無壅堵時精確恆等 `×1`、零回歸風險。

| 卡 | 主題 | 變化 |
|----|------|------|
| T143 | 可視化因果追溯 | 新全域 `traceOverlay`＋`FACTOR_TRACE`/`traceFactor(factor)`/`svcGapTiles(f)`，「幸福構成」負值因子行與「城市顧問」五項服務覆蓋不足建議皆變可點擊，`draw()` 新增脈動半透明菱形疊圖高亮來源格，GV 新增 `trace`/`traceOverlay`/`clearTrace` |
| T144 | 環境音動態分層混音 | 新 `AmbientBus`（街噪/人聲/機械/鳥鳴四層程序化噪聲/振盪源），增益依 `roadLoad`/`pop`/`achStats.nI`/`COV.park` 等既有指標即時線性斜坡跟隨，疊加於既有 `AudioComposer` BGM 之外不取代，GV 新增 `ambientLevels()` |
| T145 | 判準5-6誠實複審＋急救延遲聯動補全 | 複審發現判準5「急救到場延遲與壅堵聯動」一項未落地——`updDispatch`（救護車/消防車/垃圾車共用派遣邏輯）原未讀取 `roadLoad`；最小補接比照 T129 折減公式，服務車輛壅堵下延遲到場（實測延遲 3.16 倍）但終究必達目標；判準6（地價場 LAND 四處接線）逐一核對現版程式碼仍完整存在，純稽核無程式碼改動；GV 新增 `svcResponseSample()`/`svcResponseReset()` |

收官複查（v4.0）：版本標記 `GAME_VER='4.0'`，node --check 語法通過、test_fixde.js 380/380 PASS、瀏覽器（槽3）確認 title 顯示「微光小鎮 v4.0」、`GV.ver()` 回傳 `'4.0'`；舊 3.9 存檔（`bl` 記錄與 `sup`/`rdep` 等既有欄位皆不受本波影響——T143 `traceOverlay` 純 UI 狀態不入存檔、T144 `AmbientBus` 純音訊旁路不入存檔、T145 `svcSamples` 純運行時觀測不入存檔，三卡皆存檔格式零變更）正常載入，零 Console 錯誤，`GV.advanceN` 推進 100+ 天不拋錯，全程未觸碰 s1/s2。超越計分卡（`docs/SURPASS.md`）判準 15（可視化因果追溯）、16（環境音≥3層動態混音）由 T143/T144 落地、本次收官正式標記達成 ✅；判準 5、6（交通壅堵持久場急救聯動／地價場四處接線）由 T145 複審升級為 ✅，深度軸（判準5-9）達 5/5 全數達成；17 項判準中已有 **14 項**（1-12、15、16）明確標記達成，僅餘 UI 軸判準 13、14（KAPPA／T131 狀態沿用未重新稽核）與元判準 17 最終覆核，留待下一輪處理，本輪暫不逕行宣稱「整體超越」。

## v4.1 美術收官波（OMICRON，T146–T150）✅ 全部完成（2026-07-21 收官驗收）

> 目標：OMICRON 波聚焦「美術投入」——在 XI 波（v4.0）完成模擬深度/透明度/氛圍三軸收官之後，回頭把畫面本身的密度與品質推上一個台階：街道級細節層、建築細節二期（風化/招牌/生活痕跡）、四座手繪像素英雄建築（本作首例「手繪素材混合」）、動態光影與氛圍、RCI/樹/公園變體大擴充。五卡皆為純疊加層或純規模擴充（不改模擬公式、不改玩法規則），彼此定位錨不重疊（T146 地面層／T147 建築立面二期／T148 四座地標建築／T149 動態光影／T150 變體規模），T150 前置隱含依賴 T148（絕對尾端生成序須排在英雄建築之後），其餘四卡互相獨立，單卡驗收記錄見 `docs/CHANGELOG.md`；逐卡皆通過標準三件套（node --check／test_fixde.js 380/380／瀏覽器實機 newWorld→放置→存讀檔→零 Console 錯誤），每卡皆配一顆 `__noXXX` 除錯開關（`__noStreet`/`__noDetail2`/`__noHero`/`__noLight`/`__noVar12`）證明「關閉時全 SPR 鍵/渲染路徑與改動前位元一致」，T149 額外要求「正午/日間無天氣時渲染逐像素與改動前一致」之硬性不變量（動態光影層設計上日間強度恆為 0）。

| 卡 | 主題 | 變化 |
|----|------|------|
| T146 | 街道級細節層 | 新增 `SPR.roadDeco` 家族＋純函式 `streetHash(x,y,salt)`（座標決定性雜湊，不消耗 `R()`），`buildSprites` 絕對尾端＋`draw()` 道路繪製尾端疊加：人行道邊石、斑馬線、車道標線（`rc≥3`）、街道設施小件（消防栓/郵筒/長椅/垃圾桶/夜光候車亭）、路邊停放車（複用 `SPR.car`）、行人點綴；密度依道路等級與鄰近建築等級遞增，併入 `groundCache` 重繪路徑，LOD 沿用 T107 分級 |
| T147 | 建築細節層二期 | 新函式 `detailPass2()`（接在 T128 `detailPass()` 之後的二次疊加，物理上置於 buildSprites 真正絕對尾端）：風化層（污漬/鏽點/苔痕，依財富/等級調密度）、商業招牌（遮陽棚＋霓虹字條，night 層發光）、牆面海報/塗鴉（僅貧中產低層）、屋頂二期（菜園/衛星天線/儲水槽變體）、陽台雜物二期；`wallSafeY()` 新增通用安全頂端 y 計算 |
| T148 | 手繪像素英雄建築 | 新增 `HERO_PIX` 資料表＋`drawHeroPix(key,g,ng)` 解碼函式，四座地標級建築（k=24 地標／k=32 大學／k=42 市政廳／k=51 太空研究中心）像素資料逐格原創手繪、以調色板索引字串編碼，覆蓋同鍵取代原程序化版本，footprint/錨點完全一致；本作首例「手繪素材混合」 |
| T149 | 光影與氛圍 | `draw()` 新增三類動態光影層（不烘進 `groundCache`）：路燈/裝飾光源地面光暈、建築方向性陰影＋窗燈地面光暈、日出日落色調（`daylight()` 新增 `ph` 相位鍵分流冷藍/暖橙兩檔）；統一開關 `fxOn` 受 T107 LOD 與 T97 效能檔低檔門檻管制，正午/日間強度設計上恆為 0 |
| T150 | 變體大擴充 | `PAL[1]/[2]/[3]` 8→12 組配色、`DRAFTS` 對應新增 v8-11；住宅/商業/工業生長與升級 `ri(8)→ri(12)`（逐 k 判斷）；樹種 7→10、公園 6→9，冬季/四季陣列同步擴充且長度一致；新變體財富色變與 detailPass/detailPass2 覆蓋同步涵蓋；存檔 `v` 值域擴大但格式不變，舊檔（v≤7）正常載入 |

收官複查（v4.1）：版本標記 `GAME_VER='4.1'`（`grep -n "GAME_VER="` 確認全檔僅 `index.html:257` 一處字面賦值），node --check 語法通過、test_fixde.js 380/380 PASS、瀏覽器（槽3）確認 title 顯示「微光小鎮 v4.1」、`GV.ver()` 回傳 `'4.1'`；舊 4.0 存檔（本波五卡皆存檔格式零變更——T146/T149 純繪製疊加層、T147/T148 純繪製替換、T150 僅 `v` 值域擴大但 `bl` 元組格式不變，`plague`/`sup`/`rdep`/既有欄位皆不受影響）正常載入，零 Console 錯誤，`GV.advanceN` 推進 100+ 天不拋錯，全程未觸碰 s1/s2。**美術投入量化**（本波三件套外新增之量測項，供「規模擴張是否留下可驗證痕跡」存證，方法與數字見 `docs/CHANGELOG.md` v4.1 收官行）：`index.html` 檔案體積（v4.0 收官基準→v4.1，git 內容位元組數口徑）486,036→601,293 bytes（+115,257 bytes，+23.7%）；`buildSprites()` 總生成耗時（Node headless mock canvas 環境，同機同法連續 5 次取樣）均值 14.0ms→17.4ms；`SPR` 鍵總數三種口徑——頂層家族 49→50（新增 `roadDeco`）、`SPR.bld` 二級鍵 196→256（+60，T150 變體擴充為主）、遞迴葉節點（實際生成畫布總數）737→915（+178，+24.2%）。誠實凍結區維持不變：本波純美術/氛圍投入，未新增判準相關的模擬深度/玩法系統，`docs/SURPASS.md` 17 項判準狀態不因本波變動（美術素材總量、插件生態、多人、region、四向旋轉，仍在誠實凍結區不宣稱超越範圍）。

## v4.2 美術收官波（PI，T151–T155）✅ 全部完成（2026-07-21 收官驗收）

> 目標：PI 波是 OMICRON（v4.1）之後的美術投入延續二期——把手繪像素英雄建築從 4 座地標擴到服務建築、給城市加上會動的細節（旗幟/噴泉/雷達/風車/警示燈/鳥群/樹梢搖曳/水面波光）、補齊地形與自然美術（草地紋理/自然散佈物/岸邊漸變/懸崖岩層）、把天氣從「色調疊加」升級為「有實體感」（水花/反光/積雪/霧/暴風雪線條）、最後讓街上的車與人不再千篇一律。五卡皆為純疊加層、純覆蓋替換或純規模擴充（不改模擬公式、不改玩法規則、不改存檔格式），彼此定位錨不重疊（T151 服務建築立面／T152 動態掛點與天空層／T153 地面自然紋理／T154 天氣實體效果／T155 載具與行人個體），T155 依賴 T146 既有街道細節層的行人點狀繪製結構（重構而非新增），其餘四卡互相獨立，單卡驗收記錄見 `docs/CHANGELOG.md`；逐卡皆通過標準三件套（node --check／test_fixde.js 380/380／瀏覽器實機 newWorld→放置→存讀檔→零 Console 錯誤），每卡皆配一顆 `__noXXX` 除錯開關（`__noHero2`/`__noAnim`/`__noNature`/`__noWx`/`__noVeh`）證明「關閉時全 SPR 鍵/渲染路徑與改動前位元一致」。

| 卡 | 主題 | 變化 |
|----|------|------|
| T151 | 手繪像素英雄建築二期 | 沿用 T148 `HERO_PIX`/`drawHeroPix` 管線，新增 8 座服務建築手繪像素資料：消防局/學校/體育場（僅覆蓋 v0）、警局/醫院（單鍵 `SPR.police`/`SPR.hospital`）、博物館/劇院/機場，footprint/錨點與替換前逐位元組一致，`HERO_PIX` 鍵數 6→14 |
| T152 | 動畫細節層 | `SPR.flag`/`parkFtn`/`radar`/`windRotor`/`warnBlink`/`bird` 六組多幀 sprite（旗幟/噴泉水花/雷達碟面/風車葉片/警示燈/鳥群），純數學公式生成零亂數消耗；`draw()` 新增建築掛點動畫／樹梢搖曳／水面波光／鳥群四類動態層，皆不烘進 `groundCache`，統一開關 `animOn` |
| T153 | 地形與自然美術 | 草地紋理疊加（4變體×四季）、自然散佈物（花/雜草/石頭/倒木，`streetHash` 決定性挑選僅落空草地）、岸邊漸變帶＋河床石礫（`!lodFar` 才畫）、既有懸崖邊緣原地補畫水平岩層線；全部像素原創構圖 |
| T154 | 天氣視覺升級 | 雨落地水花＋濕地反光（衍生既有 `rainDays` 換算，非新增全域）、積雪漸進堆積、霧（新增獨立機制 `fog`，比照 T137 慣例不進存檔，低機率決定性觸發）、暴風雪橫向雪流線條、閃電補光加強；統一開關 `wxUp`；過程中修正暴風雪線條座標對負數取模處理有誤的實作缺陷 |
| T155 | 載具與人物變體 | 廂型車/卡車/機車 3 新型＋救護車＋行人 3 形態×2 走路幀；車型/人物挑選一律用 `streetHash` 決定性雜湊（非 `ri()`），寫入後終生不變；發現並補齊 T119 救護車自建立以來從未接上 `draw()` 繪製之缺口 |

收官複查（v4.2）：版本標記 `GAME_VER='4.2'`（`grep -n "GAME_VER="` 確認全檔僅 `index.html:257` 一處字面賦值），node --check 語法通過、test_fixde.js 380/380 PASS、瀏覽器（槽3）確認 title 顯示「微光小鎮 v4.2」、`GV.ver()` 回傳 `'4.2'`；舊 4.1 存檔（本波五卡皆存檔格式零變更——T151 純繪製替換、T152/T153 純繪製疊加層、T154 `fog`/`rainDays` 皆純運行時狀態、T155 `vt`/`vc`/`bt`/`pt` 皆運行時載具物件欄位）正常載入，`GV.load()` 回傳 `true`、`GV.ver()` 仍正確回傳 `'4.2'`（存檔內舊 `gameVer` 標記不影響程式碼版本常數），讀檔前後 `GV.stats()` 核心欄位（money/day/pop/jobs/roads/buildings/poweredBld/happy/dem/zones/ruins/fires/cars/buses）逐值完全相等，地標/服務手繪建築（school/hospital/landmark）核心欄位（k）保留，零 Console 錯誤，`GV.advanceN` 推進不拋錯，全程僅 `.slot`/`.s3`/`.s3_bak` 有寫入，`.s1`/`.s1_bak`（使用者既有存檔）未被觸碰、`.s2` 全程不存在。**美術投入量化**（本波三件套外新增之量測項，方法與數字見 `docs/CHANGELOG.md` v4.2 收官行）：`index.html` 檔案體積（git 內容位元組數口徑）v4.1 基準 601,293→v4.2 現版 736,002 bytes（+134,709 bytes，+22.4%；on-disk CRLF 口徑 609,818→745,073 bytes，+135,255 bytes，+22.2%，兩口徑百分比一致互證）；`buildSprites()` 總生成耗時（Node headless mock canvas 環境，同機同法連續 5 次取樣，取暖機後均值）13.41ms→15.59ms；`SPR` 鍵總數三種口徑——頂層家族 50→69（+19，新增 `flag`/`parkFtn`/`radar`/`windRotor`/`warnBlink`/`bird`/`natGrassTex` 系列/`natFlower`/`natWeed`/`natRock`/`natLog`/`shoreBlend`/`natPebble`/`vehTypes`/`ambulance`/`ped`）、`SPR.bld` 二級鍵 256→256（不變，T151 為覆蓋既有鍵而非新增）、遞迴葉節點（實際生成畫布總數，僅計真正 canvas 物件不含 w/h/ax/ay 等中繼資料）916→1047（+131，+14.3%）；`HERO_PIX` 手繪建築鍵數 6→**14**（+8，T151 新增 8 座服務建築）。誠實凍結區維持不變：本波純美術/氛圍投入，未新增判準相關的模擬深度/玩法系統，`docs/SURPASS.md` 17 項判準狀態不因本波變動。

## 永久凍結區（任何版本都不做，避免弱模型自作主張）

- 不換渲染技術（不上 WebGL/Pixi/Three）
- 不拆檔案、不上構建工具（v1.5 PWA 的兩個新檔除外）
- 不加多人/聯網功能
- 不改核心迴圈雙軌設計
- 不用任何外部素材（版權紅線：本專案賣點就是 100% 原創程式化美術）

---

## 任務卡模板（出新卡時複製此結構）

```markdown
# T?? 標題
版本：v1.? ｜ 難度：★~★★★ ｜ 前置：T??或無 ｜ 建議模型：Kimi/DeepSeek

## 目標
（一句話，做完後玩家能看到什麼）

## 允許觸碰
（精確到節號＋函數名；除此之外一行不准動）

## 禁區
（本卡特別容易誤傷的地方，點名列出）

## 實作步驟
（編號步驟。每步給「定位錨」＝原檔可搜尋的一行原文，
 說明在其上/下/內插入或替換什麼。關鍵代碼直接寫全。）

## 存檔相容性
（無變更 / 新增可選欄位怎麼容錯）

## 本卡驗收
（可執行的具體步驟＋預期結果，能用 GV 的就用 GV）

## 回滾
git checkout -- index.html
```

## 出卡時的自查清單（給強模型）

- [ ] 每個定位錨在當前代碼裡唯一且存在
- [ ] 允許觸碰範圍最小化；與其他未完成卡零交集
- [ ] 驗收可由弱模型自己執行（除視覺項）
- [ ] 存檔相容性寫明
- [ ] 弱模型不需要做任何設計決定
