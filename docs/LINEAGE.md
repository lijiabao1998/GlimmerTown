# LINEAGE.md — 版本線分岔與清查協定（T420 起）

業主決定（2026-08-08）：**從 T420 起，迭代分成兩條線。**

- **桌面線（主線）**＝本倉庫 `C:\dev\glimmer-town` ＋ 四個車位（kimi/codex/grok/deepseek）
  ＋ 玩家部署目錄 `C:\Users\Leon1\OneDrive\Desktop\安卓探索\glimmer-town`（埠 8123）。
  **本 code 環境此後傾向桌面線。** T421 起的卡都是桌面線交付。
- **移動線**＝業主在 chat session 由 T420 衍生出的移動版本，**在本倉庫之外維護**，
  **定期回到本倉庫清查**（reconciliation）。本倉庫不持有它的原始碼。

---

## 1. 分岔點（唯一錨點，清查時一律以此為基準）

| 項目 | 值 |
|---|---|
| 卡號 | **T420**（ART-LOOP 第二特許，三輪 R01/R02/R03 已上線） |
| master OID | **`40c045c89b404a85dcd6183a312533e889b51bff`** |
| git tag | **`fork/mobile-T420`**（就是為清查而打的，永久可尋址） |
| `GAME_VER` / `APP_VER` | **11.46** |
| PASS | **4,307**（`exit 0`，`verify.py` ALL GREEN） |
| `index.html` | **1,848,045 B**（UTF-8）／**20,229 行**（Python 數 `\n`） |
| 畫布台帳 | `sprAtlas356` 可達 **1,756**（img 1,349＋night 407）＋ `nightCity` **203** ＝ **1,959 張**／**21,645,447 px** |
| atlas | entries **1,374** ／ families **116** ／ skipped **37** |
| 兩釘 | seed301 400d `pop===4153` ／ seed22 `pop===4550` |

> **為什麼要打 tag**：一個月後「從 T420 分出去的」這句話不足以做 diff。
> `fork/mobile-T420` 讓 `git diff fork/mobile-T420..master` 永遠可用，不受後續 rebase／squash 影響。

---

## 2. 移動線回來清查時，跑這五條

1. **文字面 diff**：`git diff fork/mobile-T420..master --stat`
   ＋ `git log --oneline fork/mobile-T420..master`（桌面線新增了哪些卡）。
2. **卡面敘事**：`docs/CHANGELOG.md` 從分岔點之後新增的每一條（一卡一行，可 grep 成單一結果）。
   每條的「驗收:」欄記的是**桌面線**的實測值。
3. **資源台帳差**：用 `docs/tools/art_diff.html` 的 `canvasCensus()`（量法寫死在程式碼裡）
   對「移動線版本」與「桌面線 master」各跑一次，比對本文件第 1 節那六個指標。
   ——這是判斷「桌面線的成長會不會壓垮移動端」的唯一可信量法；**不要各自手寫量法**（T419 血淚：
   同一份 BASE 在文件裡出現過三組互不相容的 canvas 數字）。
4. **兩釘**：移動線若動過 `tick`／經濟／AI／RNG 基元，兩釘必然漂移；先確認兩釘是否仍為 4153／4550，
   不同就先定位是哪張卡的行為改動，再談合併。
5. **存檔相容**：固定城三配方 `rawSave()` 逐位元比對（配方寫在 T420 卡面第 7 節）。
   存檔格式若在任一線被改過，清查必須先解決存檔遷移，其他都是次要。

---

## 3. 桌面線在清查前要維持的紀律（讓清查便宜）

- **(B) 凍結線的恆等守衛不能拆**：畫布張數／總像素／atlas entries／raw-save bytes ——
  這四類在「只在既有畫布上加蓋」的施工面上數學上不會動，每張美術卡只需一條恆等守衛
  （見 T420／T421 卡面第 6/7 節的兩張表）。**它們同時是移動線的體檢項**。
- **凡會推動 (B) 的卡，CHANGELOG 條目必須明寫增量**（例如「canvas +N 張／+M px」）。
  移動線清查時只要 grep 這些條目就知道要付多少代價，不必重跑全部量測。
- **2.5 MiB 黃線（`index.html`）現已用 70.5%**。它原本是照移動端顧慮定的；
  桌面線若要放寬，**必須等第一次清查、由業主決定**——現在放寬等於替移動線預先記一筆債。
  （這是本文件唯一的待決事項，記在此處而不是散在卡面裡。）

---

## 4. 兩線的權責

| | 桌面線 | 移動線 |
|---|---|---|
| 原始碼位置 | 本倉庫（`C:\dev\glimmer-town`） | 本倉庫之外（業主的 chat session 環境） |
| 出卡／覆核／合併 | 照既有流程（`docs/COLLAB.md`、`tools/merge_bay.py`） | 不走本倉庫流程 |
| 玩家部署 | `安卓探索\glimmer-town`（埠 8123） | 不在本倉庫管轄 |
| 清查發起 | 被動配合 | **由移動線定期發起** |

移動線帶回來的改動若要進桌面線，走正常流程：進某個車位 → 非作者覆核 → `merge_bay` 合併。
**不得直接寫 master**（單寫者鐵律不因分支而放寬）。

## 4. 卡號衝突記帳（T426 起）

移動線與桌面線自 T420 分岔後**各自沿用同一卡號序列**：移動線 T420–T432 為 GPT 微雕美術波（街角微敘事/生活痕跡/活動口袋/夜間街區/區域材質/屋頂考古/立面記憶/街道邊界/地面痕跡/微型人物/材質反應/夜間微光/視覺噪音），桌面線 T420–T425K 為 ART-LOOP 特許/Grok 特許/遠景夜城LOD/UI 現代化/TheoTown 三鍵/購物中心系列——**雙方 T421–T425 是不同內容**。

處理規則：合併以「桌面線續編號」為準；T426 起桌面線從移動線移植的內容（T482 T453 Startup Pipeline）在桌面線記為 T426，CHANGELOG 內標註「源自移動線 T4xx」；不重編移動線卡號（移動線卡面敘事保持原樣，以 482 檔為移動側唯一真相）。
