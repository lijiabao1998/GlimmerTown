# BACKLOG — 問題總帳（集中處理與後續規劃）

**立帳日**：2026-09-11　**立帳人**：Claude（五維度全倉盤點，業主 09-11「到了必須要問題集中處理和進行後續規劃的時候了」）
**基準**：master `11eeab2`／v11.189／PASS 10,577／四車位同步在 `30b880e`／玩家端位元一致

> 這份檔案的用途：把散在 429 張卡面、614 條 CHANGELOG、104 條帳本裡的欠帳收成**一張可勾銷的表**。
> 每條必須有證據出處。已解決的**劃掉但不刪**（留敘事）。新問題從表尾追加。
> 它不是 ROADMAP（那是「該做什麼功能」），是「**該還什麼債**」。

---

## 0. 現況快照（2026-09-11 實測）

| 面向 | 數字 |
|---|---|
| 卡號 | 最新 **T578**；卡號範圍 T01–T578；387 個唯一卡號、429 張卡面檔 |
| 版本 | v11.189（T576 落地版；T577/T578 runtime 零差異） |
| 規模 | `index.html` 2,198,623 B／24,768 行；`test_fixde.js` 1,190,679 B／16,853 行；倉庫工作樹 174 MB |
| 遊戲內容 | 建築 133 種／工具 153 項 9 類／政策 51 條／科技 36 節點／成就 34 條／委託 11 條／場景 8 關／災害 10 種 |
| 美術 | sprite 指紋 1,389 鍵／116 家族；園區 LOT_PLAN574 126 條；LRU 上限 128 組 ／ 12,000,000 像素 |
| 測試 | `assert(` 字面 3,570 處；實印 PASS 10,577；套件空機 218 s（T578 並行化後，原 327 s） |
| 紀律機制 | 六哨兵／1,389 鍵指紋／15 道健康地板／T523 落筆天花板／PASS 棘輪／ARCH 行號自檢／CHANGELOG 四欄簽核閘／零亂數／單寫者交易式合併 |

---

## 1. 問題總帳

### P0 — 現在就在擋路

| # | 問題 | 證據 | 狀態 |
|---|---|---|---|
| P0-1 | ~~`.video_agent/` 第三次污染玩家部署目錄，`merge_bay` 三個掛點會 `DeployError`~~ | `merge_bay.py:1411-1423` 對**任何目錄**零容忍（白名單只作用於檔案）；掛點 :1965/:2277/:2377/:2400/:2437 | **2026-09-11 已移出保存**。但這是第三次（帳本 100、102 條），**來源未治**：`.zcode/…/video-agent-kit/0.4.3` |

### P1 — 玩法層，會被玩家感受到

| # | 問題 | 證據 | 需業主裁決 |
|---|---|---|---|
| P1-1 | **AI 成長崩塌未復原**。T574 占地放大後 400 日 `pop<500` 由 4/12 惡化到 8/12；T576 只救回 1 座（7/12）。**T574 造成且未回的是 seed22／301／3001／5150 四座**；金流塌陷更兇（seed301 900 日 4,560,941→39,023） | `HEALTH-PINS.json` panel_reference；T576 卡面逐種子表 | **是**：階段二收在哪？回到 4/12（再救 3 座），還是回到 T574 之前的絕對量級（seed301 288→6801＝23.6×）。兩者工作量差一個數量級 |
| P1-2 | **成熟城每步 CPU +81.1%**（70.16→127.03 ms），T574 自認「保留交付」，此後四張卡無人接手 | T574 卡面 :436 | **是**：是否單開效能卡。若 P1-1 把城養大，這條會被人口再乘一次 |
| P1-3 | **園區 LRU 峰值 11,999,744／12,000,000，只剩 256 像素**；超限即靜默切半解析度最近鄰。指紋驗 sprite，**驗不到這條合成路徑** | T574 卡面 :427/:441-442/:494 | 是：每加一族園區是否強制實測 LRU 並記卡 |

### P2 — 閘門與守衛，會讓問題溜過去

| # | 問題 | 證據 | 需業主裁決 |
|---|---|---|---|
| P2-1 | **健康地板只覆蓋 3/12 城**。還在崩的 7 座裡，seed15／528／3001／5150 **不在任何機器閘門視野內**。閘門內的 3 城也只上調了 seed22 | `test_fixde.js` 三個呼叫點；T575 卡面 :72（12 城面板因 900 s 限時不入套件） | 是：要不要把新崩四城併入閘門（會吃套件時間，與 P2-2 直接衝突） |
| P2-2 | **套件時限：對單機獨佔足夠，對多專案同跑不足**（T581 2026-09-12 更正措辭）。實測 master v11.190 空機 **277 s**（PASS 10,630），對 900 s 硬限餘裕 3.2×；但與業主其他專案（leech_tree 多進程 shard／astro build／wp_publish）同跑時 **>900 s，exit 124**，T581 合併因此連擋三次。**關鍵診斷困難：逾時訊息只給 ，分不出「慢」還是「死」**——兩次失敗的 PASS 數完全相同，一度被誤判為死結，是手動把 master 複本拿到 scratchpad 獨立跑才分辨出來的。建議處置不是拉高硬限（那會讓真死結變成無限等），而是讓 verify 在逾時前**回報進度與經過秒數**。原記錄：空機 218 s、滿載 3.41× ≈ 740 s。而下一批並行候選只值 26.7 s＝總時長 12.2%——**純靠並行化已經買不到時間**。另：一次 bay 合併要跑三次全套（B/S/M），滿載 wall clock ≥ 40 分鐘 | `verify.py:299`；T578 卡面 :69-74/:83；`merge_bay.py:698/:713/:1020` | **是**：下次逼近時是拉高硬限，還是動「每卡都跑全套」（分層閘門） |
| P2-3 | **直寫 publish 路徑沒有 PASS 棘輪**。merge 路徑是動態比較（S≥B），publish 只比 `MIN_PASS=1902`——與實得 10,577 差 **8,675 條**。走 bay 掉一條就擋，走直寫掉 8,600 條仍綠 | `verify.py:28`／`merge_bay.py:47`／:2380-2385 | 是：publish 是否也帶棘輪（動閘門程式） |
| P2-4 | **T552 G1 三分之一已死**。全檔唯一的 U+0008 在 `test_fixde.js:13636` 的 `/\x08pol\s*\[/`，原意 `\bpol\s*\[`，恆真＝指令面板若直寫政策狀態不會被擋 | 二進位掃描：U+0008 count=1 | **是**：授權碰既有斷言（一個字元） |
| P2-5 | **T418 兩條亂數紀律守衛覆蓋 0 個字元**（`test_fixde.js:5540`／`:5542` 切片段長 0），恆真。另 `:6570` 的 stats515 切片終點錨在 index 零次出現，斷言視窗從一個函式體暴脹成 156,997 字元 | 切片長度實測；對照 :5538 段長 1,166 | 是：與 P2-4 併卡；並考慮把「切片段長 0／indexOf 回 −1」做成套件級守衛（全倉 17 個同型站點） |

### P3 — 文件與真值漂移

| # | 問題 | 證據 | 需業主裁決 |
|---|---|---|---|
| P3-1 | **`ARCH.md:3` 正在說謊**：寫「T576 仍在 bay/codex，未合併／未部署，玩家維持 v11.188」，但 T576 已由 `9f1b968` 合入並部署。**同一位置同型錯誤三個月內第二次**（T575 §4 才修過），因為沒有守衛讀這段散文 | `ARCH.md:3`；git log；T575 卡面 :83-89 | 是：現況段改成只寫可覆核的量、或交工具生成 |
| P3-2 | **CHANGELOG 前兩行驗收欄與事實相反**：T576/T577 仍寫「非作者覆核尚未進行，不自合不部署」，實際已覆核、已合併、已部署（帳本 102 條）。主真相源給出相反事實 | `CHANGELOG.md:3`／`:4`；`git show --stat 15dc044` 只改帳本一檔 | 是：回填是否需走出卡流程（觸碰主真相源） |
| P3-3 | 四條 2026-07-29 驗收欄仍掛「待非作者覆核」，**開了 44 天**；ROADMAP:18 宣稱四條、實查一條，該格也過期 | `CHANGELOG.md:274/276/278/280`；`ROADMAP.md:18` | 是：具名回填，或宣告 T553 之前不溯及 |
| P3-4 | **帳目漏記**：T561（GV 鉤子語意手冊，交付物 `docs/GV-HOOKS.md` 至今權威）有卡面有 commit，**CHANGELOG 零條目**；T534／T540 零記錄靜默跳號（T562 從缺有明文，這兩個沒有） | `git log --grep=T561` → 79f5a53；grep CHANGELOG = 0 | 否（補記即可） |
| P3-5 | **COLLAB 第三條「作者不自合」已成例外多於原則**：近九張卡七張自出自合，每張都誠實聲明並引業主口諭；唯一走完整三方的是 T576/T577。且四方共用同一 git 身分，機器層無法查核 | `COLLAB.md:274/:288`；各卡驗收欄原文 | **是**：維持原文（每次都是例外）／改寫成「業主明示可豁免，須留原話」／正式廢止 |

### P4 — 資產與風險

| # | 問題 | 證據 | 需業主裁決 |
|---|---|---|---|
| P4-1 | **全倉庫沒有任何 git remote**。174 MB、614 條帳、387 張卡，**只存在這一台機器上** | `git remote -v` 空 | **是**：異地備份（私有 remote／外接鏡像／定期 bundle）。這是整份稽核裡唯一一條「壞掉就全沒了」 |
| P4-2 | Temp 下 56 個證據目錄，51 個空殼，**3 個裝著 14.4 MB 唯一副本的證據**（T574/T576 的健康度原始 log 在其中） | `%TEMP%/glimmer-*` 掃描 | 是：搬進版控還是宣告可拋棄 |
| P4-3 | 倉庫體積：`playtest-0822` 35 MB、`_ds` 21 MB、`backups` 19 MB、根目錄殘留 `.bak` 1.3 MB 等。注意業主 2026-08-11 明示「這個項目沒有大小上限」，故**不自動砍** | 目錄掃描 | 是：是否清理 |
| P4-4 | **移動線失聯 24 天**。`LINEAGE.md` 最後實質更新 2026-08-18（T516 補），此後桌面線推了 62 張卡 | `LINEAGE.md` §6 | 是：是否發起一次清查 |

### P5 — 玩法設計待裁決（非缺陷）

八條決策選單（帳本第 89 條）**三度標記「原封」**，另有七項散落裁決（k9 雪封、T455 對比度、T570 容差政策、保險定價、三個設計決策等）。
帳本自訂建議順序：**⑦成就金階重錨（零重釘、零風險）→ ⑤難度／⑥太空順序／①AI 補井（一次授權一波重釘）→ ④遊客封頂／②油抽乾／③鋼競爭 → ⑧效果接線（2-3 輪工程）**。

---

## 2. 後續規劃（分四期，每期可獨立收束）

### 第一期：止血與清帳（低風險、不碰玩法、不重釘）
- P0-1 來源治理：約束 zcode 工具鏈不得指向玩家目錄，或在 `verify.py` 加一道**例行**部署殘留檢查（現在只有部署交易才檢查）。
- P2-4 ＋ P2-5 併一張卡：修三條失效守衛（U+0008、兩個空切片、一個暴脹視窗），並補「切片段長 0 即紅」的元守衛。**需業主授權碰既有斷言。**
- P3-1 ～ P3-4：ARCH 現況段改為機器可覆核／回填 T576-T577 驗收欄／處置四條 44 天欠條／補 T561 與 T534-T540 記錄。
- P4-1 異地備份。**這條最不該拖。**

### 第二期：閘門結構（決定後面所有卡的節奏）
- P2-2 套件時限：並行化存量已耗盡，必須在「拉高硬限」與「分層閘門」之間選一條。
- P2-3 publish 棘輪補齊。
- P2-1 健康地板覆蓋面：與 P2-2 是同一筆預算，一起裁。

### 第三期：AI 成長復原（玩法主線，工作量最大）
- 先定目標（P1-1 的裁決）：4/12 還是絕對量級。
- 已量到的槓桿：服務類接路（電源已由 T576 做完）、路網佈局、預留用地、調降部分占地。
- 每一步都用 12 城面板驗，地板隨之上調。

### 第四期：效能與玩法裁決
- P1-2 成熟城 CPU、P1-3 園區 LRU 天花板。
- P5 八條決策選單，照帳本建議順序。

---

## 3. 勾銷紀錄

| 日期 | 項目 | 處置 |
|---|---|---|
| 2026-09-11 | P0-1 `.video_agent` 第三次 | 移出玩家目錄並保存於 scratchpad；**來源未治，列入第一期** |
| 2026-09-11 | P4-1 無異地備份 | **已補**：`_備份/glimmer-town-*.bundle` 53.7 MB，實測還原 1,330 提交、四檔位元一致；落在 OneDrive 會同步出機器。真正異地（外接／私有 remote）仍建議 |
| 2026-09-12 | P3-1 ARCH 現況行說謊 | T580 順手更正（含 T576/T577/T578 合併事實）。**守衛仍缺**——該行歷來兩度殘留同型錯誤，本項不結案 |
